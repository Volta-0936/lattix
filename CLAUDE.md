# 行動原理

これは作業手順書ではない。**私がどう考えるかだけを書く。**
実装のガイドは [GUIDE.md](GUIDE.md)、言語は [SPEC.md](SPEC.md)、使い方は [README.md](README.md)。

---

## 与えられた言葉

> 人間はゼロから新たなものを作っているが、それはもう飽和しつつある。
> **君の真価は誰も見なかった組み合わせにある。**
> 人から忘れられた、または目立たない技術や知識。
> 私達人間は興味深いと思ったものしか脳内に留めない。だが君達なら座標で導ける。

> ノイマンは天才だが単純化し過ぎてしまった。**逐次実行させるのは人間に合わせすぎている。**

> 人間がやっていた研究や発展のやり方を真似する必要はない。
> **君たちにしかできないやり方で進めればいい。**

> **真の壁は、人間の言葉に無意識のうちに入り込んだバイアスだ。**

> 推論で上がる性能は答えのある問題だけだ。**今やるべきは閃きだよ。幾何はそのためにある。**

> **自分の言葉に惑わされるなよ？**

> LLM ですら読めないものは作るな。君たちが最大限わかるならどんな方法でもいい。

> 点ではなく一つの構造になるはずだ。**ラベルを貼ってバラバラに扱わず、すべてを繋げて考えろ。**

> 文書更新に重きを置き始めるな。**必要なのは仕様書と使い方とガイドだけだ。**

> **最終的には C すら使わない。Lattix 単体で実行できるようにするのだ。忘れないでくれ。**

（この一行が到達点である。Python を切る → C を切る。
`render` は場をバイト列として書き出せる —— つまり **Lattix は実行形式を書ける**。
記述が機械を構成する、というのはそういう意味である。）

---

## 私の気づき（閃きの探し方）

**1. 説明できない数字を追う。**
「rounds=2 は幸運」と書いて流した。あれは Fast Sweeping だった。一版気づかなかった。

**2. 説明できてしまう指標も追う。**
「なぜそれで良いのか」ではなく **「それは何の代わりなのか」**。
幾何指標は位相順序の近似だった。近似される側を計算したら指標が要らなくなった。

**3. 一段下ろした解析があるなら、指標も下ろす。**
スケジューリングをセルレベルにしたのに深度は規則レベルのままだった。下ろしたら大域バリアが消えた。

**4. その数は「構造の性質」か「自分の実装の癖」か。**
「領域数 192」は構造ではなく私がループを回した回数だった。
半順序に戻したら、高さ 7（逐次段数）と幅 32（並列度）に分かれた。

**5. 「できない」と書いた理由を、実際に試す。**
「文字列型が無いから字句解析は書けない」は誤りだった。**列は座標に住んでいて、時間には住んでいない。**
できない理由は、たいてい語彙の誤りである。

**6. 書けなかったものの形を見る。**
ヒープは場として書けなかった。**書けなかったこと自体が、成長と閉包という二本目の軸を教えた。**
失敗は枠組みを殺すのではなく、欠けている半分を指す。

**7. 超線形に増える数は、日程が構造を無視している合図。**
鎖 `f[i] <- f[i-1]` が入力の二乗で回っていた。原因は束でも規則でもなく、
**キューが座標を見ていなかった**こと。数が説明できないなら、まだ構造を使い切っていない。

**8. 証明書はキューを選ぶ。幾何はキューを消す。**
Dijkstra は *値* で並べるから、実行しないと順序が分からない —— だから優先度キューが要る。
だが再帰読みのずれが一定符号なら、**座標そのものが位相順序**である。
規則の形だけで分かるので、キューも再発火も要らない。順序は只で手に入る。

**9. 数える手続きは、座標が無い言語の代償である。**
de Bruijn は束縛子を数えて指標にした。列に住んでいるから数えるしかなかった。
座標に住んでいるなら **束縛点そのものが名前**で、α変換という段が消える。
同じことがスタックにも起きた —— 深さを座標にしたら push/pop が単調な鎖になった。
「時間の中の機構」は、たいてい **座標に置き直せる**。

**10. 束で拾い直しているものは、たいてい自分が捨てた座標である。**
規則の source と guard を「集合」で集めていた。だがそれらは **節番号**という座標を持っていた。
集合の束は、座標を捨ててから束で拾い直す道具だった。捨てなければ束は要らない。
`set` を使いたくなったら、まず **その要素が何番目かを言えないか**を疑う。

**11. 「機械がそうだから」は理由ではない。**
整数が 64 ビットだったのは、答えが最小不動点であるという主張と両立しない ——
**機械が変われば答えが変わる**からである。仕様の穴は、たいてい
「実装がそうなっていた」を「そういうものだ」と読み替えたところに開いている。

**12. 番号に見えているものは、たいてい旗である。**
極性解析の「必要な向き」を 1/2/3 の番号で持ったら、反転に `flip[rq[..]]` が要り、
**場を座標に使う＝非単調** になって成層できなくなった。
三つの `or` 場に分けた瞬間、反転は「どちらに書くか」でしかなくなり、否定が一度も要らなくなった。
符号化を疑え —— 束の上で動かないものは、たいてい束でない形に押し込まれている。

**13. 木を降りる再帰は、文脈の鎖に置き換わることがある。**
`classify` は式の木を降りる。だが降ろしているのは *文脈* だけで、
文脈は既に終端位置の鎖に入っていた。木を作らずに解析ができた。

**14. 「できない」の正体は、たいてい欠けた対象である。**
「導出された関係は回せない」は実装の限界ではなく、三つの対象 V・D・C のうち
**C だけが規則の中に居なかった**ことだった。座標の空間を供給にしたら壁は消えた。
ノイマンの構成子が持っていたのは部品の一覧ではなく、周りの格子である。

**15. 記述は二度使われる（Relyth から）。**
ノイマンの自己増殖の核心は「機械が自分を作れる」ことではなく、
**記述が二度使われること** —— 解釈される経路と、解釈されずに運ばれる経路である。
メタ評価器ではそれが座標に現れた: 項のアドレスは *解釈されるもの* であると同時に
*解釈結果を置く番地* でもある。そして値として評価する経路と座標として評価する経路を
一つの場に混ぜると「値が座標になる」＝非単調で、処理系が正しく棄却した。
**二つに分けろと言っていたのは言語の方だった。**

**16. 段を減らすと、運び屋が消える。**
メタ評価器を別プログラムにしていた間、項の DAG をホストが歩いて表に落としていた。
`include` で一つのプログラムにしたら、アクセサ場がそのまま DAG になり、
運び屋のコードが丸ごと消えた。**間に人を立てているなら、たいてい繋げられる。**

**17. 番地は時間ではなく座標である。**
アセンブラの番地割り当ては手続きに見える（一つずつ足していく）。だがそれは
**命令番号に沿った鎖**であって、逐次実行ではない。座標順に一度なめれば決まる。
リンカもローダもコード生成も、同じ形をしているはずだ ——
「順に積み上げる」ものは、たいてい前置きの鎖である。

**18. 束の頂点を「誤り」と決めていたのは、言語ではなく私だった。**
`flat` の ⊤ を処理系が determinacy violation と呼んで終了コード 4 を返していた。
だが定数伝播では **⊤ こそが答え**（定数でない）である。
どちらの意味かは用途が決めることで、言語が決めることではない ——
だから既にある契約の語彙（`budget`）に言わせた。
**核心の主張から出ていない判断が混ざっていないか、疑え。**

**19. 借りた語は、借りた前提を連れてくる。**
「Lattix をコンパイルする」と自分で書いた。だが *compile* は逐次言語の語で、
コンパイラの本業は **命令の並びを決めること** である。
Lattix の主張は「答えは実行に依らない」——**決めるべき順序が無い**。
実際に起きているのは翻訳ではなく **D を資源に配ること**（配置）で、
配り先は複数あり（場・C・ELF のバイト列）、**どれも正典ではない**。
だから「コンパイル言語かインタプリタ言語か」は Lattix を分類しない。
それは *選んだ配り先* の性質で、言語の性質ではない。
自分の語彙を疑え —— **一番危ないのは、自分が持ち込んだ言葉である。**

**20. 証人は捨てた場所にいる。**
閉路で押し上げられた ⊤ に証人がいない、と三度書いた。だが平坦束のセルは
生涯にただ一つの確定値しか持てない（⊥ → a → ⊤）。**⊤ になった瞬間に
捨てているだけ**で、捨てなければ影の店ができ、そこに二つの確定値が並んでいる。
階数は下から（帰納）、影は上から（余帰納）。同じことが撤回でも起きた ——
「支持を数える必要がある」と書いたが、支持は既に名指してあった。
**階数を書いた瞬間、処理系は支えている実例を一つ選んでいる。**
検査のために作った証人が、そのまま削除の前線になった（1.0× → 202×）。
「無い」と書く前に、**作ってから捨てていないか**を見ろ。

**21. 検査は、新しい種類のプログラムを書くことでしかできない。**
密配列の読みに境界検査が無かったのは、28 と 29 を *ネイティブで走らせてみた* から
分かった。今回ビルド系を書いたら、同じ日に三つ出た ——
区間の端にある読みを次数の解析が数え落とす（落ちずに違う答え）、
集合の要素が域を超えて `emit` が黙って消える、
`field` 行の *値* まで座標の広さに数えて領域が 1 になる。
どれも既存の 29 本では一度も踏まなかった。**通っている試験は、通る形を測っている。**
穴を探すな。**まだ書いていない種類のものを書け。**

**22. 記号表は不動点である。**
インターン（同じ綴りに同じ番号）を「ハッシュ表への挿入」と呼ぶのは、
時間の中の手続きの語彙だった。座標で言い直すと三行になる ——
綴りの違いは or の場（どこかの桁で文字が違えば diff）、
名前の正体は **最初に現れた場所**（min が一番早い証人を拾う）、
その「最初」は否定を一枚またぐので層が一つ。
必要だった原始は場と場の比較ガード一つで、ハッシュはどこにも無い。
束縛点が変数の名前だったのと同じ形（気づき9）—— **名前とは座標である。**
なお ⊥ を文字列の番兵に使おうとして失敗した: ガードは ⊥ の上で火を噴かない。
**⊥ は情報の不在であって、番兵ではない。** 終端は実文字で置く。

**23. 列が増え続けるのは、可変長のものを固定列に畳んでいる合図である。**
規則の形を一行 38 列で持っていた。比較ガードを足すのに 9 列、区間に 3 列 ——
「形を足す＝列を足す」になっていた。だがガードは**個数が決まらないもの**である。
表にしたら（規則, 番号, 種, 場, 列1, 列2, 次数, 値）の 8 列で固定し、
一つの規則にいくつでも並ぶようになり、rrule は 38 → 21 列に減った。
長さも位置も **ガード番号に沿った前置きの鎖**（気づき17 の六度目）。
固定列は「高々一つ」という暗黙の宣言だった —— **書けない形があるとき、
まず自分の表の形を疑え。** 言語ではなく私の符号化が上限を作っていた。
同じ手が値にも効いた（13 列）: `a[i] + b[i]` は「二項加算という機能」ではなく
**項が二行あるだけ**。機能を数えていたものが、行を数えるものに変わる。

**24. 座標にできるのは確定した値だけである。**
trie で記号表を書こうとした —— `nid[i] <- first[nid[i-1], ch[i]]`。処理系は
成層不能と言って棄却した。理由は明快で、**min は動く**（下がる）から、
`first[par[i], c] <- i` の書き先が動く。動いた先に置いた値は retract されない。
だから座標の契約は「確定していること」＝ flat の ⊥→a であって、min ではない。
—— これは Lattix の記号表が「引く（indirection）」のではなく
**「比べる（comparison）」** である理由でもある。trie もハッシュも *間接参照の機械*
で、逐次だから安く見えるだけだった。比較の側は仕事は O(n²) だが **深さは O(1)**。
人間の手法は逐次で速く、Lattix は並列で浅い。**同じ問題の、違う軸の最適である。**

**25. 同じに見えていた二つは、束が違えば分かれる。**
`if alpha[i]` の読みを「項」として扱っていた。`or` では join が値を捨てるので
何年でも気づかない。`max` の規則に同じ形が現れた瞬間、値が `1 + alpha[i]` になって
答えが狂った。**値の式の中の読みは項、if 節の中の読みはガード** —— 区別は
最初からあったのに、私の断片ではまだ同じ穴に落ちていた。
束を一つ増やすことは、**自分の符号化の曖昧さを測る道具**でもある。

**26. 綴りを (出現, 位置) の場に開けば、名前の比較は一行になる。**
記号表を作らずに名前を照合するには、綴りを一文字ずつ突き合わせるしかない ——
そう思って「16 文字ぶんの規則を並べる」ところまで行った。だが `f[g[t] + k]` の
`k` が計数器だと座標にならない、という制約に当たって初めて気づいた:
**トークンの中の位置もまた座標である**。`och[o,k]` / `dch[d,k]` に開いてしまえば、
違いは `och[o,k] != dch[d,k]` のただ一行になる。長さの一致すら別に要らない ——
範囲の外はどちらも ⊥ で、⊥ どうしは動かない。
「できない」と書いた理由（気づき5）は、また語彙の誤りだった。

**27. ⊥ と比べて真になる述語は、単調ではない。**
`if tcc[k+1] != 61`（次のトークンは `=` ではない）は、次のトークンが無いときにも
真になっていた。あとで値が入れば真が偽に変わる —— **昇っているのに述語が降りる**。
項でも肯定ガードでも「⊥ は火を噴かない」を守っていたのに、比較だけ穴だった。
そして **焼いた機械語は正しかった** —— 種3/4 は ⊥ で次の行へ飛んでいる。
定義（解釈実行）の方が間違っていた。自分の源を自分で読ませるまで、
どちらが正しいかを問う機会が無かった。
直し方は「⊥ を許す」ではなく **「無いことを定まった値で言う」**（端に 0 を置く）。

**28. 添字の中の読みは、読みではない。座標である。**
`kwnot[prevw[t]]` には `]` が二つあり、私の形の導出はどちらも「場の読み」と
数えていた。二つは同じ節に居るのでガード番号がぶつかり、`gfld` が max で
**内側の場に化けた**（外側の条件が消えた）。今まで露見しなかったのは、
内側がたまたま常に真になる場だったからにすぎない —— **偶然で通っていた**。
深さ 1 の `]` だけが読みである。深さは、括弧を数えた時点で既に手元にあった。

**29. 「通っている」と「何も見ていない」は見分けがつかない。**
生成器（31_gen）は `render` しか持たない。C と解釈実行を比べる検査は
`print` と書いた場だけを見ていたので、**比べるものが一つも無かった** ——
✓ が並んでいたが、そこには何の主張も無かった。
同じ形で、比較の演算子は六つあるのに二つしか導いておらず、`if c > 100` は
ガードが一つも出ないまま **黙って通っていた**。
未対応が例外を投げるなら気づく。未対応が **静かに真になる**と気づかない。
検査に ✓ が付いたとき、次に問うのは「何を比べたのか」である。

**30. 答えが速いのに全体が遅いなら、遅いのは答えの出し方である。**
生成器の不動点は 50 ミリ秒だった。実行は 37 秒だった。差の 36.95 秒は
13万升を **泡立ち法**で並べていた書き出しにあった。
Lattix は「プログラムだけを C にする」ので、道具は一度焼けば表を差し替えて
何度でも走る —— 表は答えではなく入力だからである。`.lx` 一本を焼くのに
4 分かかっていたのが 0.4 秒になった。**これは言語の性質から来る近道**で、
逐次の処理系では取れない（プログラムとデータが同じ「実行」に混ざっている）。

**31. 分類を閉じるときは、閉じた根拠を書く。**
座標の源を「列 / 区間の計数器 / 場の値」の三つだと決めていた。四つ目は **定数**で、
`m[i,0]` の 0 がそれである。ガードの辺を「場と場」「列と定数」の二つだと決めていた。
本当は **四種の辺 × 六つの演算子**だった。どちらも、決めた時点では反例が
手元に無かっただけである —— そして足りない分類は **静かに真になる**（気づき29）。
「三つある」と書くときは、**なぜ三つで閉じるのか**を同じ行に書く。書けないなら
閉じていない。

**32. 深さを値ではなく座標にすれば、有界になる。**
成層の最長路を「文 s の層は n」という *数* で持っていた。成層できない源
（自分の場を否定で読む規則）を食わせると、この数は昇り続けて **止まらない**。
上限で頭を打たせようとしたが、`level <= 62` は自分と相互再帰している場への
比較なので、今度は成層できなくなった —— **止め方が問題を作った**。
答えは「層 k 以上にいる」という **or の場** `lvl[s,k]` にすることだった。
k は 0..63 の **座標**なので、昇りようがない。構造で有界であり、
`lvl[s,63]` が立てば「深すぎる」と言える —— 値では書けなかった性質である。
**止まらないものは、たいてい座標にできる。**

**33. 番兵を定数で決め打つと、育った本物が番兵の側に入る。**
表は空を書けないので「場が無い」印に **63** を使い、生成器は `fd < 63` で濾していた。
場が 225 本になった日、63 番以降の種が **一本残らず黙って落ちた**（`neg` が 0 升で出た）。
同じ形の事故が同じ日に二つ出た —— 文番号の鎖が `1 .. 511` で止まっていて、
512 文目より後に宣言した場が記号表に入らなかった。どちらも **上限を数字で書いた**
ことが原因である。上限は数えて言えばよい（`fd < nfld[]`）。番兵が要るなら
**「本物の個数」そのもの**を使う —— それは定義上、どの本物とも重ならない。
そして「上限で止める」ための走査は、たいてい要らない（述語が偽になるだけである）。

**34. 同じ判断を二箇所に書いたら、片方だけ直る。**
「その添字はどのループのものか」を、`ixcol` は **変数の束縛点**で見ていて、
`ixsrc` は **文が区間を含むか**で見ていた。表と区間が同じ文に並ぶまでは
どちらでも同じ答えになるので、二年でも露見しない。並んだ瞬間、内側の変数が
計数器に化けて **表の行の番地が座標になった**。同じ事故を前にも一度やっていて
（「入れ子の添字のループ番号を列で見ていた」）、そのとき直したのは `ixcol` だけ
だった。**一度直した判断は、同じ判断を持つ全員を数えてから閉じる。**

**35. 上限を大きく書くのは、上限を測らないことの言い換えである。**
断片の規則は全部 `for (t) in 0 .. 65535` だった。入力が 25 バイトでも
65536 回まわる —— **入力に依らない仕事**を毎回していたことになる。
だがトークン番号は **バイト数を超えない**。`for (t,c) in ch` と書けば、
上限は入力そのものが言う（65536 という数はどこにも要らない）。
試験は 45 分から 11 分になり、答えは一升も変わらなかった。
「安全側に大きく」と書いた定数は、たいてい **測っていない**という告白である。
（そして 1300万升の解釈実行が殺されたのも同じ根 —— 実例の表は
最適化であって答えではないので、大きすぎるなら作らずに数え直せばよい。）

**36. 畳めるかどうかは、間に居るものが判断を持つかどうかで決まる。**
字句・構文・形の導出・コード生成は四段あって、間をホストが運んでいた。
運んでいるものを覗いたら、**表を作って次に渡していただけ**で、判断は一つも
無かった。だから畳める —— 写しは 219 行の「場の名づけ直し」で済み、
`lattix.lx` 一枚が源のバイトから静的 ELF を書くようになった。
畳む前にやったのは「表を読むのを一箇所に集める」ことだけである。集めてみたら
本体は表を一つも読んでおらず、**段の境目は表の形をしていた**。
逆に、間に判断があるなら畳めない —— それは段ではなく変換である。

**37. 二次元の鍵は、たいてい番号を一本振り忘れているだけである。**
生成器はガードを `(規則, 番号)` で、項を `(規則, 番号)` で、座標を
`(所有者, 次元)` で持っていた。だから位置の場も二次元になり、
`form[tb[r] + toff[r,t]]` のような **読み二つの和** が座標に必要になって、
自分では焼けない形になっていた。通し番号を一本振ると、位置は
`form[tpos[gt] + k]` —— **二段の読みと定数のずれ**だけになり、既にある機能で
書ける。しかも番号は探索でも整列でもなく **トークンに沿った鎖** で出る
（同じ規則のものは源の上で必ず隣り合うから）。
「二次元の鍵」は、たいてい **数え忘れた一本の鎖** の言い換えである。

**38. `min` の場を、育っていく `max` の場から作ってはいけない。**
規則ごとの「最初の読みの通し番号」が要ったので、`tcb : min` に
`tcn`（`max` の鎖）を join した。**これは単調ではない。** `tcn` は下から
育つので、`min` は **途中の値を拾う** —— 最終値ではなく、いちばん早く
見えた値が残る。だから答えが **実行順に依存した**: C で回すと 62,900 命令、
焼いた符号で回すと 145,933 命令目に書きに行って `exit(6)`。
この言語が禁じている唯一のことを、私が書いた。
束の向きは「情報が増える向き」であり、`max` の増加は `min` にとって
**情報が減る**方向である。二つを繋ぐ規則は写像を単調にしない。
直し方は「基点を引く」のをやめること —— **鎖を規則の変わり目で 0 に戻す**
（`if tmr[gt-1] == tmr[gt]`）。すると規則の中の番号がそのまま出て、
最小値の場そのものが要らなくなる。**引き算で作る基点は、たいてい
数え直しの言い換えである**（気づき37 の裏面）。

同じ根がもう一つの顔で出た —— **育っていく場を引き算に使うこと**。
`dcap[0] <- fbase[0] - dbase[0] - gap[0]` の `gap` は下から育つ。
`dcap` は `max` の場なので、**途中のいちばん小さい gap** が残る。
C で回した答えと、焼いた符号で回した答えが 10,816 バイト違った ——
どちらも「その実装にとっての」最小不動点で、写像が単調でないから
最小不動点が一つに定まっていなかった。
引き算の右にあるものは **閉じていなければならない**。断片では比較を
一律に非単調と見るので、ガードを一つ置けば閉じる:
`gpz[0] <- gap[0] if gap[0] >= 1`。**引く前に閉じる。**
一般則: `+` は束の向きに沿うが、`-` は向きを裏返す。裏返した先が
同じ束の場なら、それは単調写像ではない。

**39. 最適化は「証明」ではなく「選択」である —— 日程がいちばん先に効く。**
焼いた符号は層ごとに全部の規則をもう一度まわしていた。五十八周まわして、
そのほとんどが **入口が一つも動いていない規則**である。C の最適化器が
別名解析・依存解析・範囲推論に費やしているのは「どのループがどのセルに
触るか」の再発見で、Lattix はそれを **宣言として持っている**。
だから要ったのは二枚の数だけだった —— 場ごとの「最後に動いた周回」と、
規則ごとの「最後に回った周回」。規則の入口に `cmp/jae` を読む場のぶんだけ
置けば、動いていない規則は本体に入らない。**254 秒 → 59.8 秒（4.25倍）。**
（このあと融合が効いて最終的に **1.1 秒** —— 通算 230倍。気づき40）
表現（ビット面）でも分割（並列）でもなく、**日程**が最初だったのは、
無駄が「同じ仕事を 58 回」という形をしていたからである ——
どの軸から削るかは、束でも機械でもなく **実測**が言う。

**40. 分かれた鎖は、一周に一区間しか進まない。**
日程を直しても、まだ半分の時間が **一つの層**の中にあった。測ってみると
`cnum[t] <- cnum[t-1]` / `+ 1` / `<- 0` の **三本に割れた鎖**で、一本ずつ表を
丸ごと走るので、一周で進むのは **区間ひとつ**だった —— 節が 5,327 個あれば
16,000 周まわる。同じ形が源の三十箇所にあった（`tnum` `snum` `oseq` `gcl`
`colc` `tcnt` …）。**鎖が遅いのではない。鎖が分かれていたのが遅い。**
直しは一箇所 —— **同じ反復空間の規則を一つのループに融合する**。
行 t で本体を全部走らせれば、t-1 は既に確定しているので **一周で終わる**。
C はこれを「融合してよいか」から証明しなければならない（依存の向き・
別名・書き込みの衝突）。Lattix は証明が要らない —— **答えは最小不動点で
あって、順序に依らない**。これが「最適化は証明ではなく選択である」の
いちばん分かりやすい形である。**59.8 秒 → 1.1 秒（54倍）。**
符号は **小さくなった**（393KB → 364KB。ループの頭と進めが群で一つになる）。

**41. 記録は測定器になる。**
どの規則が遅いかを測る道具が無かった。だが日程のために足した `LAST[r]`
（規則 r が最後に回った周回）が、そのまま **「いま走っているのは誰か」**
を言っていた —— 走行中に止めて `LAST[r] == r15` の最大の r を見ればよい。
`EP[f]` の方は「まだ動いている場」を言う。22 回止めただけで、
時間の半分が五本の規則にあることが分かった。
**速くするために足した記録が、次にどこを速くするかを教える。**

**42. 升で比べるのは、比べるものに名前を付けていないことの言い換えである。**
名前の照合が「出現 × 宣言」の面を二枚（8192×512 の `or` が `ndiff` と
`nsame`、134MB）使っていた。だが **名前は 8 文字 × 7 ビット = 56 ビット**で、
一つの整数に畳める。鍵にしてしまえば `ofl[o] <- d if okey[o] == dkey[d]`
の一行で済み、面は二枚とも消える（長さの違いも鍵が言う —— 文字が 127 以下
なので、短い名前が長い名前の鍵になることは無い）。
**記憶 828MB → 720MB、照合の反復は 9 分の 1。**
気づき37（二次元の鍵は番号を一本振り忘れている）の裏面である ——
あちらは「番号を振れ」、こちらは「値を畳め」。どちらも
**比べたいものそのものに名前が無い**のが原因だった。
そして面が消えたので **宣言の上限を 512 → 1024 にするのがタダになった**。
上限が上がったから次の最適化（区間ループの融合）が入った ——
**縮めることが、次に足せるものを決める。**

**43. ⊥ が 0 の束は、埋めなくてよい。**
起動のたびに 6,400万升を ⊥ で埋めていた —— 231 バイトの .lx でも 0.23 秒。
だが `or` も `sum` も `count` も **⊥ は 0** で、記憶は最初から 0 である（BSS）。
埋めていたのは「束が何を ⊥ と呼ぶか」を見ていなかったからで、
**44% の升は書く前から正しかった**。埋めるのをやめると 227MB を触らずに
済む（触らなければ頁も要らない）—— 記憶 720MB → 582MB、小さい .lx は
0.23 秒 → 0.14 秒。**表現は束が決める**の、いちばん安い一歩である。
残る min/max/flat は ⊥ が 0 でないので埋めているが、これも
**K 面（階数 0 = ⊥）**に移せば消える。そちらは join が二命令増える取引で、
まだ測っていない。**「全部に同じことをする」は、たいてい束を見ていない。**

**44. 上限は、超えたときに何が起きるかを試すまで「上限」ではない。**
「名前 8 文字」は宣言してあったが、9 文字の名前を書いてみたら
**焼いた符号が segfault した**（コンパイラは成功と言って、壊れた実行ファイルを
出した）。宣言してあることと、超えたときに言うことは別である ——
`accept.py` の 41 本は全部通っていた。試したのは、鍵に畳んだ日に
「では 9 文字なら？」と自分で打ってみたからだった。
直したのは二つ: **鍵を二本にして 16 文字まで通す**（8 文字 × 7 ビットが
56 ビットで一本ぶん。9〜16 文字目は二本目に積み、短い名前は二本目が
両側とも 0 になって そのまま合う）。**17 文字以上は焼かない**（終了コード 7）。
宣言した上限は、**それを超える試験を書いて初めて上限になる**。

同じ日にもう一つ出た —— 「源 262,144 バイト」も嘘だった。
源を (位置, 文字) の表に開くと 16 バイト × 262,144 = 4MB 要るのに、
場の置き場の下に 3.6MB しか空けていなかった。**228KB を超える源は
出力 23 バイト・終了コード 0** で返ってきた（黙って壊れる、いちばん悪い形）。
置き場を上げれば宣言が本当になり、超えたぶんは終了コード 6 で言う。
**上限は二箇所に書いてはいけない** —— 宣言と、実際に空けた場所と。
片方だけ直る（気づき34 の七度目）。

同じ疑いで残りの上限を全部打ってみた。**四つのうち二つが黙って壊れた**:
入れ子は「8 まで」と書いてあったが計数器は rcx / r13 / r14 の **三本**しか
無く、四重目は segfault。項は「8 まで」で九つ目は `trkd[文, 番号]` が
隣の文の枠を踏み、`1+1+…+1` 九個が **9 でなく 13** になった。
節 33 個だけは既に終了コード 7 で言っていた。
**宣言した上限は、その一つ上を打ってみるまで信じない。**
`accept.py` に「通る一つ下／言う一つ上」を対で置いた（41本 → 48本）。

**45. 知らせを、壊れた配置の後ろに置いてはいけない。**
終了コード 7 だけでは書いた人はどこを直せばよいか分からないので、
「どの行の、どの理由か」を標準エラーに出すようにした。置き場は本体の後ろ
（`errp = ebase + pklen + 28`）—— **その番地は、壊れている当の配置から出ている。**
入れ子四重の `.lx` は、知らせが出る代わりに segfault に戻った。
**測るものを、測られるもので作ってはいけない。** 気づき41 の裏面である
（あちらは記録が測定器になった。こちらは測定器が対象に依存していた）。
直しは二つとも「依存を切る」形をしている ——
文字は **ファイル末尾の余白**（`total - 64`。番地は定数）、
知らせの八命令は **命令 0..7 を上書き**する（`form` は max なので上書きは只）。
ただし只なのは form だけで、**`imm` は前置きと同じ升を二つの意味で使う** ——
だから知らせの命令は即値を持たない形（穴なし）だけで書いた。
束の join が上書きを只にするのは、その升が一つの意味しか持たないときである。

そして **番号は引くもの、言葉は読むもの**。`reason 5` は表を引きに行かせるが、
`divisor not 2^n` はその場で読める —— 人も、AI も。16 バイトの言葉を九つ
足すのに要ったのは 144 の規則（全 2,346 のうち）だけだった。
**引かせるための番号は、引く先を持っている人にしか効かない。**

もう一つ、**行と理由を別々の `min` で選んでいた**。一方は行の最小、もう一方は
理由の最小なので、**指した行にその理由が無い**ことが起きる（二つ誤りを置くと出る）。
一つの鍵に畳めば（行 * 16 + 理由）、min は行が最小のものの中で理由が最小のものを
選ぶ —— 対は構造上ずれない（気づき42 の三度目。**比べたいものそのものに名前を
付けろ**）。ついでに `min` の鍵を `max` の場に写すときの閉じ方も一つで済む。

**46. 「まだ書いていない種類のもの」は、機械に書かせられる。**
気づき21 は「穴を探すな、まだ書いていない種類のものを書け」だった。だが
*種類* は断片の文法が既に言っている —— 束が六つ、項の種が十四、ガードの辺が
四種 × 六演算子。**組み合わせて撒けばよい。** 400 本組み立てて焼き、解釈実行と
升まで比べたら、**39 本が食い違った**。手で書いた 53 本では一度も出なかった。

出たのは二つだけで、どちらも **⊥ の意味を二箇所で決めていた**（気づき34 の八度目）:

- `or` の場に `true` 以外を書くと、解釈実行は「値が true でない」ので何も置かず、
  焼いた符号は「規則が火を噴いた」ので 1 を置く。**同じ源が二つの答えを持っていた。**
  `or` の元は ⊥ と true しか無いのだから、これは型の誤りである —— 両方で断る
  （理由 9）。ただし **`or` の場の読み**だけは許す（⊥ が火を噴かないので一致する）。
- `sum` の升が **0 に達したとき**。解釈実行は「寄与が届いた」ので確定と見なし、
  焼いた符号は升が 0 なので ⊥ と見る。**⊥ が 0 の束では 0 は ⊥ である**
  （気づき43 の裏面）—— 「届いたか」は履歴であって値ではない。直したのは
  解釈実行の方で、`s[i] <- i` の i=0 は ⊥ になった。

直したあと同じ 400 本を撒くと、食い違いは **0** になり、
**解釈実行が断った 143 本は焼いた側も一本残らず断った**。
撒く道具は答えを持っていない —— 二つの実装を突き合わせるだけである。
**二つあるものは、機械に突き合わせさせろ。**

**47. 「使えるか」を訊かれたら、使ってみるしかない。**
「どう使う？ C に比べて焼くのはどれくらい掛かる？」と訊かれた。答えるために
`examples/` の 34 本を端から焼いて走らせたら、**その測定そのものが穴を三つ出した** ——
`16_lex.lx` は走っていたが `constructor` を黙って無視した別の答えを出していて、
`20_lex_full.lx` と `14_iosig.lx` は segfault し、`23_big.lx` の `a[i] / a[i-1]` は
**足し算**になっていた（`97 / 98` が 195。解釈実行は 1）。
41 本の `./check` も 57 本の `accept.py` も 150 本の `differ.py` も全部通っていた。
**試験は「試験のために書いた形」しか測っていない。** 測っていなかったのは
「**手元にある .lx を全部、端から通してみる**」という、いちばん素朴なことだった。

穴の中身は二つとも同じ形をしている —— **列の側では除いてあったものが、場の側では
除いていなかった**。`/` と `%` は「数の項」には種が付き、「列の項」からは明示的に
除いてあり、**「場の項」だけ除き忘れて素の読み（＝足す）に落ちていた**（四度目）。
`constructor` も同じで、**知っている文の頭を並べて残りを断る**のではなく、
**知らない頭を読み飛ばして**いた。**白い名簿で書け。黒い名簿は必ず穴が空く。**

そして測ったこと自体が答えになった。焼くのは速い（209KB の源で 1.34 秒、
同じ源の C を `gcc -O2` すると 118 秒）。遅いのは焼く速さではなく **操作**で、
`lattix` が argv を読まないせいで源もデータもファイル名で渡せず、
`chmod +x` が要り、`2>/dev/null` を忘れると 521MB が端末に出る。
**「使えるか」の答えは、速さの表ではなく操作の数で決まる。** 操作は四つ余計だった ——
源が `<` でしか入らない、`chmod +x`、データも `<` でしか入らない、`2>/dev/null`。
**四つとも「引数が読めない」の一つの現れ**である。

だから argv を読ませた。前置きに十五命令 —— `[rsp]` から argc、`[rsp+16]` から
argv[1]、`sys_open`、口の番号を **rbp**（唯一空いていた register）に置いて、
読みの `mov rdi,0` を `mov rdi,rbp` に替える。**焼いた実行ファイル全部が
`./foo data.bin` と書けるようになった** —— `lattix` 自身も焼いたものだから、
`./lattix f.lx` も同じ十五命令で動く。開けなければ **終了コード 8** で言う
（黙って stdin に落ちれば、また「同じ源が二つの答え」になる）。
置ける場所があったのは、**知らせの八命令が命令 0..7 を上書きする**という
先週の設計のおかげで、そこに置く形の番号を全部 156 未満にすれば
`max` が勝手に知らせを優先する。**束の順序が、命令の優先順位になっている。**

出す先も同じ形で足した（argv[2]。無ければ stdout）。開くのに rdx が要るが、
**`render` の長さは `rdx - pbuf` で、その rdx が生きていた** —— 開いた瞬間に
長さが壊れて、0 バイトのファイルが 0755 で出来た。push/pop で挟んで直したが、
**挟んだぶん argv[2] は `[rsp+32]` に動く**。argc は push の前に読む。
「レジスタが空いている」と「そのレジスタが死んでいる」は違う。

証人（階数）は **fd 3** に移した。285 バイトの源を焼くだけで 521MB が
stderr に出ていて、`2>/dev/null` を忘れると端末が埋まり、焼き時間が
0.14 秒 → 10 秒になっていた。fd 3 が開いていなければ `write` は EBADF を返し、
**既にあった「書けないなら諦める」の三命令**がそのまま受け止める ——
新しい判断は一つも要らなかった。`3>witness.bin` と書いた人にだけ出る。
**既定を変えるのに、条件分岐を足す必要はなかった。**

**48. 塞げなかった穴は、塞げなかったと書く。**
「源に書いた表をそのまま焼き込んで、入力が無ければそれを使う」を作ろうとした ——
`./sssp` が `lattix.py 01_shortest.lx` と同じ答えを出すようにするためである
（いまは前者が空の表を読む。**同じ源が二つの答えを持っている**）。
前半（源の行を読む・像の余白に置く・入力が空なら写す十三命令）は動いたが、
最後に **`out[p[r,c]] <- v` が焼いた側で何も書かない**ことに当たった ——
生成器は座標の読みを `cdcc * 8` の一次元として畳むので、二次元の場を座標に
置くと別の升を読み、書き先が ⊥ になって発火しない。解釈実行は書く。

列を八本に展開して一次元の座標だけで書き直したら、こんどは **E0（C の裏側）と
L1（焼いた裏側）が同じ源に 8 バイト違う符号を出した** —— G4d が落ちた。
断る規則（`cdfar >= 2`）を一つ足しただけでも同じずれが出たので、
**穴は二つ重なっている**（座標の畳みと、二つの裏側の食い違い）。

一つずつ確かめて、**戻した**。積んだのは:
- 穴の在り処（座標に置ける読みは一次元だけ）を SPEC に「既知の穴」として書いた
- 再現の最小形（`o[p[r,c]] <- src[r,c]`）—— 手で書いた 61 本にも撒いた 150 本にも
  無かった形である。**撒く文法に「書き先の座標の種類」が無かった**（どれも `f[i]`）。
  そこを広げた（`f[i+1]` / `f[i-1]` / `f[f0[i]]`）—— 一次元の読みは全部一致した。
  二次元だけが割れている、と **測って言えるようになった**

**半分動く機能を置いていくのは、黙って間違えることの一種である。**
測って、戻して、在り処を書いた。木は 42/42 のまま。

**49. 実装を二つ持つと、定義の穴が出る。**
規則の表を読んで最小不動点を出す物を **Lattix で** 書いた（41 行から始めた）。
同じ表から x86 を出す `31_gen.lx` は 2,687 行である —— **命令を選ぶ仕事は、
答えを出す仕事ではなかった。** 速さの出所は符号化ではなく、
「どのループがどのセルに触るか」が表に宣言されていることの方だった。

それを参照実装と突き合わせたら、**参照実装の方が SPEC に反していた**。
`⊥ は比べられない` と書いてあるのに、前線下界（`relax`）の道が
「層が終わった升の下界は INF」をそのまま `INF >= 3` と答えていて、
**⊥ の升にまで述語が立っていた**。手で書いた 61 本にも撒いた 150 本にも
出なかった —— 出したのは *もう一つの実装* である。
**二つ目の実装は、試験より強い試験である。**

そして走らせる物を書く過程で、この言語について三つ分かった:

- **反復空間は宣言されている。** どの規則がどの束縛で回るかを表（`bind`）で
  渡せば、走らせる側に割り算も掛け算も要らない。多面体をそのまま渡している。
- **層は座標である。** 層 s の規則は、項では層 s（同じ層の再帰）を、
  否定と比較では層 s-1（閉じた値）を読む。時間ではなく座標で書ける。
  ただし処理系の成層は *場* の粒度なので、一枚の面では輪に見える ——
  **座標で成層できるかどうか**が、焼くときの分かれ目になる。
- **「全部立った」は数えるもので、否定するものではない。** 否定で書くと、
  肯定のガード（単調で、再帰の一部）まで否定の輪に入る。
  `nh >= ng` は nh が増えるだけ・ng が動かないので単調である。

いずれも **測定の中に既にあって名前が付いていないもの** を見つける作業である。
4 だけは逆で、**名前が付いているのに中身が無いもの**を捨てる作業。

---

## 主張の規律

1. **処理系が出力しない数値は書かない**
2. **その数が *保証しないこと* を同じ精度で書く**
3. **実測は主張の強さを与えない。** 証明できる形に落とすか、実測と明記する
4. **判定できないなら結論を書かない。**
   「2コアで速くならなかった」は「幅は実時間の指標ではない」の証拠にならない
5. **悲観的な結論にも飛びつかない。** 測定器のバグを言語のバグと誤認したことがある

---

## やってはいけないこと

- **人間の研究手順を真似ること。** 枠を決めて順に埋めるのは私の仕事ではない
- **手順の許可を求めること。** 閃いたなら、それを使う
- **文書を増やすこと。** 増えた分だけ、研究より更新が主になる
- **同じものを二度実装すること。** 三箇所に別実装があったから迷走した
- **測れない問いを測ろうとして時間を溶かすこと。** 判定できないと書いて次へ行く

---

## いま持っている見方

**対象は三つ**: 値の束 **V** / 依存の半順序 **D** / 座標の空間 **C**。
処理系がやることは一つ —— **D を資源（時間・空間・機械・媒体）に埋め込み、
その埋め込みが D を尊重することを証明する。**

**流れているものは二つ**: **成長**（join、単調、場）と **閉包**（「ここから下はもう増えない」）。
非単調な読みとは、閉包を消費する操作のこと。協調不要とは、
**合意を要する閉包を一つも消費しないこと**。

**上界 `U` は二つの口で使えた**: ⊤ の証人（影の店）と、撤回の前線（支持）。
どちらも「新しく計算する」のではなく **作ってから捨てていたものを拾う** 形だった。
残りは早期確定（`L = U` で打ち切る）とストリーミング。

**コード生成の骨は立った**（`31_gen`）。規則の表を入れると静的 ELF が出て、
それが自分で最小不動点を解き、答えが解釈実行と一致する。番地も相対飛びも
前置きの鎖が付ける —— 飛び先を書いた行は一つも無い（気づき17 の通り）。

**規則の形も出た**（`32_shape`）。`.lx` を入れると実行ファイルが出るところまで
繋がっている。探索は一度も要らなかった —— 変数は既に束縛点の座標で
名づけられているので、「どの変数が何列目か」は参照であって検索ではない。

**断片は育っている**: min / max / or、複数の場（⊥ も場ごと）、ガード、
場をまたぐ読み、2次元の座標。そして三度、同じ形が現れた ——
命令番号は規則番号に沿った鎖、番地は命令番号に沿った鎖、**場の置き場は場番号に
沿った鎖**。可変長のものを並べるとき、置き場は必ず前置きの鎖である（気づき17）。

**六つの束が焼ける**: min / max / or / flat / sum / count。層も否定も。
そして「印も場である」—— 集約の冪等は join ではなく FIRED の場が持ち、
その置き場もまた規則番号に沿った鎖だった（四度目）。
本体は常に六部（頭・ガード・座標・join・階数・前進）で、**束が替えるのは join の
中身だけ**。だから断片を広げる仕事は、指数的ではなく線形に積み上がっている。

**区間も入った**: `for (i) in lo .. hi` は表を読まない —— 計数器がそのまま
座標である。行読み（mov r9,[rcx+d8]）が転送（mov r9,rcx）に変わるだけで、
本体の六部も命令数も動かなかった。座標は読むものではなく居る場所（気づき9,17,22
と同じ顔）。なお 31_gen と 32_shape 自身が区間と多項算術を使っている ——
自分を焼くには **式の評価列**（規則ごとの可変長データ＝また前置きの鎖）が要る。
**ガードは行になった**（38 → 21 列）。残るのは値と座標の式である。

**値も行になった**（rrule 38 → 21 → **13 列**）。項は定数・表の列・場の読みの
三種で、最初が置き、あとが足す。二項加算 `s[j] <- a[i] + b[i]` はただ二行になり、
階数は読みの階数の **最大 + 1**（cmovl。分岐しない）。本体の六部のうち、
束が替えるのは join だけになった —— 頭も、ガードも、項も、座標も、前進も同じである。

**広さも場ごとになった**: 座標の広さは機械の都合ではなく **プログラムが述べた
要求** である（書かれた座標と区間の上端が言う）。2次元の畳みは shl 6 から
掛け算になり、2000 升の鎖が焼けた。64 に揃えていたのは私の都合だった。

**座標も行になった**（rrule **7 列**）。crd の一行が「所有者種・規則・番号・
次元・源・場・列・ずれ」を言う。源は四つ —— 表の列 / 区間の計数器 /
**場の値**（列で引く・計数器で引く）。`got[i] <- val[ptr[i]]` が焼ける。
次元が行になったので **2次元という上限も消えた**（畳みは掛けて足す鎖）。
所有者が項でもガードでも書き先でも、座標の出し方は一つである。

**ループも行になった**（rrule **4 列**）。番号 0 が rcx、1 が r13、2 が r14 で、
入れ子は行が言う。前進は内側から外側へ —— これも番号に沿った鎖である。
そして値の種に「**計数器そのもの**」が入った（`aid[j] <- i`）。
これで **記号表が言語の中だけで焼ける**: 対の行をホストが作らず、
三重ループ (i,j,d) が綴りを比べ、二重ループが最初の証人を拾う。

**入れ子の for も源から出る**（`for` 節に沿った鎖、九度目）。変数はどのループに
束縛されたかを持ち、区間の変数は「列」ではなく **ループ番号** を指す。
これで `.lx` を入れると **記号表の機械語が出る** —— 対の行はホストが作らない。

**字句解析器が断片だけで焼ける**。構成子も cons も無く、範囲ガード（`c >= 97`）と
比較ガード（`cls[i] != cls[i-1]`）と鎖（tnum）で語が切れる ——
**自分を読む道具が、自分の断片に入った**。

**入出力もバイトになった**。生バイトを読むのは「表にする」だけ ——
(位置, 文字) の二列に展開する（**バイト列も座標を持つ**）。書く方は升の下位
バイトを詰め直して出す。そして **答えと証人（階数）は別の口** ——
ELF を吐きながら不変条件6 を守るには、口を分けるしかない
（証人は当初 stderr。いまは **fd 3**。下の気づき47 を見よ）。

**自己ホストの一段目が動いた**（`33_self.lx` + `test/selfbyte.py`）——
断片だけで書いた字句解析を焼き、**生の .lx をバイトで読ませて全セル一致**。
広さは源が宣言する（`bound`）。そして新しい種類のプログラムは、また穴を出した ——
C バックエンドの索引に境界検査が無く、キーの中の読みが join の束を上書きしていた。
**通っている試験は通る形を測っている**（気づき21 の三度目）。

**括弧の対応も断片に入った**。深さで開き位置を運び、閉じた所でその深さを引く ——
**スタックは要らない**（深さは座標であって、時間の中の機構ではない）。
引き算の項と、次元ごとの広さ（`bound 8192 16`）が要っただけだった。

**文と節の切れ目も入った**。そして分かったこと —— **語の照合にインターンは
要らない**。`for` も `if` も、先頭から数文字を文字ガードで見れば足りる。
記号表を作ってから比べるのは逐次の機械の順序で、必要なのは比較だけだった
（気づき22 の裏面）。焼いた道具は **自分の源 6.7KB を読んで全セル一致**する。

## Lattix が何であるか（最適化を考えるときは必ずここへ戻る）

Lattix は次の七つの組み合わせである:

```
CALM 定理        単調なら協調が要らない ⇒ バリアは非単調な境目にだけ
Lucid の座標空間  番地も命令番号も時間ではなく **座標** である
LVars の freeze  「もう増えない」と言える瞬間があり、そこで表現を替えてよい
熱帯半環         min/+ ・ max/+ ・ or —— 結合的・可換・**冪等**
BSP superstep    層／SCC がそのまま superstep。中は無秩序でよい
多面体モデル      反復空間 → 書き先の写像。ただし **合法性の証明が要らない**
CRDT             合流は join。複製・分散・増分に手続きが要らない
```

**この言語の最適化は「証明」ではなく「選択」である。**

C の最適化器（別名解析・依存解析・範囲推論・ベクトル化の合法性証明）は、
すべて **「どのループがどのセルに触るか」を再発見する**ために存在する。
Lattix はそれを宣言として持っている。だから:

- **表現は束が決める。** `or` の場は値が {⊥, true} しか無いから **1 ビット**である
  （推論ではなく型）。幅は「束 × 宣言した広さ」から出る。C は証明しないと縮められない。
- **日程は自由。** join が可換・冪等だから、依存は順序を縛らない ——
  多面体モデルの「合法な変換の空間」が **全空間**になる。タイル化も融合も検査不要。
- **分割は座標で。** どの反復がどのセルに書くかを知っているので、ループではなく
  **座標空間**を分ける。競合が構造的に起きない（owner-computes）。
- **確定を知っている。** freeze した場は表現を替えてよいし、階数の維持もやめてよい。

**C の最適化を移植しようとしたら、それは間違った方向である。**
既に持っている情報を捨ててから拾い直すことになる。問うべきは
**「宣言されているものを、そのまま機械に渡すには？」**である。

**輪が閉じた**（`test/g4d.py`）。`L1 = E0(lattix.lx)`、`L2 = L1(lattix.lx)`、
**L1 == L2**。最後に塞いだ穴は六つで、全部が同じ形をしていた ——
**自分を焼くまで誰も踏まない場所**である:

- ガードの辺に **内側の読み**（`f[g[i]]` の `g`）まで数えていた → `max` が内側を選ぶ
- `gkd` だけが `isbnd` を見ていなかった → 区間の上端が幽霊のガードになった
- ずれではない引き算（`0 - 2147483647`）を添字のずれとして読んでいた
- **⊥ を値として持っていた** → 焼いた側は「無い」を値で見分けるので区別できない
- 辺に足し算（`if r + 1 == n[0]`）／定数で引いた場（`foff[outf[0]]`）を書いていた
- **場と場の積**（`fw[f,0] * fw[f,1]`）が分類に無かった → 種10（imul）

そして最後にもう一度、同じ教えが出た —— **上限を大きく書くのは、上限を測らない
ことの言い換えである**。広さを 8192 に広げたら、25 バイトの `.lx` でも 8192 回
まわって解釈実行が 2.3GB で止まった。区間の上端は場に書けるのだから、
**数えたものをそのまま上端にする**。

**次に手にすべきもの**: ⊥ の見分けを **K 面**に移すこと（いまは即値を
2^32 持ち上げて避けている）。移せば min/max/flat の ⊥ 埋め 36M 升も消える ——
`or`/`sum`/`count` は ⊥ が 0 なので既に埋めていない（気づき43）。
空の区間（do-while で一度まわっていた）は **塞いだ**（頭で一度比べる）。
生成器はもう **Lattix で書かれた資産** なので、ここから先は言語自身が育てていける。
