# 実装ガイド

`lattix.py` / `native.py` / `attest.py` を触る前に読む。考え方は [CLAUDE.md](CLAUDE.md)。

## ファイル

```
lattix       **焼いた処理系**（静的 ELF 524,288バイト）。`lattix.lx` の像であって、
             手で作るものではない —— `test/g4d.py` が焼いて置き、バイト一致を確かめる
lattix.lx    その源。三行の include（14y）: examples/33_self.lx + lib/fold.lx + lib/gen.lx
             （lib/gen.lx は examples/31_gen.lx の畳み目から下）。焼き手の口が開く
unfold.lx    部品を開く段（14z）。include: 33_self + lib/unfold.lx
lower.lx     下ろし。include: 33_self + lib/fold.lx + lib/lower.lx（14z から Python で組まない。
             型紙を変えたときだけ work/mklower.py が lib/lower.lx の種を書き直す）
run.lx       **走らせる物。配る一枚**。寄与の関係（`eg`/`cd`/`ix` の三つの表）を
             閉じる。プログラムごとに変わるのは表だけで、この一枚は変わらない。
             `work/mkrun.py` が形の一覧から書き出す（手で書かない）
lattix.py    処理系。構文解析・成層・極性解析・証明書・解釈実行
native.py    C バックエンド。単相化・索引導出・スケジューラ選択・領域実行・再番号付け
runtime.py   C バックエンド。**プログラムだけを焼き、データは実行時に読む**（Python 依存を切る道）
attest.py    答えの検査器。**信頼base はここだけ**
check        全検証。何かを変えたら必ず通す
lib/ examples/ test/ bench/ demo/
```

## 処理の順序（固定。崩さない）

```
parse → check → stratify → io_rounds → check_budgets → certify → run
```

`stratify` の後でなければ `io_rounds` / `certify` は呼べない。

## 不変条件（破ると壊れる）

1. **寄与は join である。** `_contribute` は必ず join を通す。直接代入しない
2. **⊥ も ⊤ も座標になれない。** キーに None / TOP が混ざったら寄与しない
3. **索引はキー式の依存も見る。** `r.keys + [r.value] + r.guards` の全部から取る
4. **階数は「最短の」導出深さ。** 勝者総取り束では min、集約束では max。
   **支持は「等しいとき」だけ** —— 吸収されただけの寄与は正当化ではない
5. **D の計算は `lattix.poset` ひとつ。** 解釈系もネイティブも同じものを呼ぶ
6. **証人はどのスケジューラも同じ規則で出す。** 出さないモードを作らない
7. **検査器は生成物の作り方を参照しない。** `attest.py` が native.py を import してよいのは
   `__main__` のドライバ部分だけ
8. **native は未対応を黙って間違えない。** 対応外は必ず `Unsupported` を投げる
9. **参照実装でしか動かない機能は、機能ではない。** 言語に足したものは
   解釈実行と `runtime.py` の両方で動くまで完成ではない。`Unsupported` を投げれば
   正直ではあるが、正直な穴も穴である（区間を三ターン放置して露見した）
10. **焼いた側は「無い」を *値* で見分ける。** だから ⊥ と同じ値を場に置いてはいけない。
   置く必要のある場（⊥ の定数そのものを運ぶ `fbot` / `imm`）は 2^32 だけ持ち上げる
   —— 下げてはいけない（`max` の場は自分の ⊥ より小さい値を join で撥ねる）
11. **直すのは三つの源**（`lattix.lx` は三行の include）。`./lattix lattix.lx lattix.new` で焼き直し、
   もう一度焼いてバイト一致を確かめる。生成器を直すのは `lib/gen.lx`（`examples/31_gen.lx` は表を読む
   上半分と `include "lib/gen.lx"` だけ）。口の取り込みの機械語は `work/inc/expand.s`（`work/inc/mkrows.py` が
   種 `xv` に起こす —— 貼るのは lib/gen.lx）

## 過去の事故

| 事故 | 教訓 |
|---|---|
| `repr(frozenset)` で決定性ハッシュが壊れていた | 値は正規化してから比較する |
| 「明らかに健全な基準実装」と比べて不健全を見逃した | 性質そのものを直接測る |
| flat 束の比較を単調と誤判定 | 束の図を描いてから結論する |
| 実験が2度空振りした（証人が無かった） | 「反例が出ない」を「反例が無い」と読まない |
| BSP が2スレッドで遅かった（前線再構築が全セル走査） | 言語の性質と実装の欠陥を分ける |
| 半順序を全順序に潰した | その数は構造の性質か、自分のループの回数か |
| 「吸収された」を「支持している」と同一視した | 新しいスケジューラは古い機構の測定器である |
| 祖先閉性をガードの後ろで見ていた | いま偽のガードは、祖先が育てば真になる |
| 試験がハッシュ順で揺らいだ | 揺らいだら、揺らいでいるのは試験か検査器 |
| 記憶配置を配列の置換でやろうとした | 生成 C は添字を座標から算術で作る。埋め込みは座標の側でやる |
| 鎖（`f[i] <- f[i-1]`）が二乗で回っていた | **升数が入力に対して超線形なら、日程が構造を無視している** |
| 構成子の `bound` は **全体の最小**が効く | 一つ小さく書くと全部が拒否される。揃える |
| 括弧の深さ表が 8 段で、深い入れ子が黙って壊れた | 有限の資源は宣言し、**超えたら言う**（`toodeep`） |
| 融合したら `continue` が後続の規則まで飛ばした | 外側のループを共有する規則は、一本ずつ `do{}while(0)` で囲う |
| 源の無い規則の ⊥ 検査が関数直下の `continue` になっていた | ループを開かない規則にも囲いが要る |
| 頭部の `f[...]` を「読み」と数えていた | 書き先は読みではない。層が一つずれる |
| 等値 `==` を順序の比較と同じに扱った | **等値はどちらの向きにも単調でない**（育てば真、さらに育てば偽） |
| 起きなかった寄与（⊥・ガード偽）が階数を下げた | `support` を毎回消す。**早い return は状態を残す** |
| まだ階数の無い証人を 0 と読んで階数が浅く出た | 証人が揃うまで書かない。古い階数も捨てる |
| 符号付き整数の引き算で帯を判定していた | **符号付きの溢れは未定義動作。** -O2 が消す。帯の判定は符号なしで |
| 取り込んだ側の `table tok`（置き場所の一行）が本物を上書きした | include は先に展開してから差し替える |
| 継続行が `+` で始まっていて文が切れた | 継続は「末尾がコンマ」か「次が for / if」だけ |
| **密配列の *読み* に境界検査が無く `off[-1]` が配列の手前を読んでいた** | 範囲の外は ⊥。解釈実行では寄与が消えるところが、C では他人のメモリになる |
| 表の一つの列に原子と整数が混ざり、最後の行の型で上書きされていた | 機械の上では両方とも整数。**混ざった列は戻せない** —— 検出して落ちる |
| ⊤ を C から戻すとき復号し忘れていた | ⊤ を誤りだと思っていた間は、誰も戻さなかった |
| `run` が直前の `build` の残り香（module global）を読んでいた | 焼いたときの情報は焼いた物に付ける |
| 集合の束で source/guard を集めていた | **それは節番号という座標を持っていた。**捨ててから束で拾い直していた |
| **区間の端にある場の読みを、次数の解析が数え落としていた** | 読みが居るのは キー・値・ガード・**区間の端** の四つ。落ちずに *違う答え* が出た |
| 集合の要素が域を超えて、`emit` が黙って消えた（ビット集合の外を書いていた） | **集合の要素も座標である。** 超えたら `exit(6)` |
| 生成した ELF が segfault（`p_filesz` を 80 と数えて `p_vaddr` を潰した） | プログラムヘッダは +32 が filesz、+40 が memsz。**構造体の番地は数えるな、確かめろ** |
| `out : max` に 0 を敷いたら、ヘッダの 64 が勝って値が戻らなかった | 敷き詰めと patch を同じ束でやるなら、**patch する場所は敷く前に 0 にしておく** |
| 地上データの並べ方に列数 3 を焼き込んでいて、次数2の表で行が重なった | **列の数はデータが言う**（`ncol[] <- c + 1`）。規則の次数と表の列を別々に持つな |
| `str.replace` が一致せず **黙って何もしなかった**（`(24,184)` と `(24,0,184)`） | 置換は件数を数えて `assert`。**沈黙する編集は編集ではない** |
| `table want` を消したら、それを錨にした試験の re.sub が全部空振りした | 同じ事故が試験側でも起きる。`re.subn` + assert。**弱い試験（終了コード）は空振りを見逃し、強い試験（全セル）は即座に捕まえた** |
| 生成した機械語が証人を出していなかった | 不変条件6 は生成物にも適用される。**階数を継ぐのは 3 命令**（読みの階数 +1 を書き先へ） |
| 形 44 の長さを 13 と数えた（実際 12）。1 バイトずれて以後が全部壊れた | 命令の長さは as/objdump で**確かめてから**表に書く。数えるな |
| 本体を書き直したとき、頭の `mov rcx, ROWS` を書き忘れた | 本体は頭・座標・比較・格納・階数・前進の六部。**部を数えてから消せ** |
| 否定ガードの長さを 5+2g と書いた（実際 4+2g）。空スロットが**番地の鎖に穴**を開け、以後が全部沈黙 | 可変長ブロックの長さは列挙して数え、**穴は鎖を殺す**と覚えておく |
| 22列への移行で `w2` を食って一列ずれた（手書きの表） | 列が 10 を超えたら手で並べない。**名前つきで組み立てて機械に並べさせる** |
| 比較ガードが **⊥ と比べて真になっていた**（解釈実行だけ） | 焼いた符号は種3/4 で ⊥ から飛んでいた。**定義の方が間違っていた。** 「無い」を ⊥ ではなく **端に置いた 0** で言う（`ptcc`/`ntcc`/`ptl`/`parw`） |
| `field a` の次のトークンは `a` ではなく **空白** だった | トークンには空白も入る。「一つ前の語」は位置ではなく **鎖**（`lastw`/`prevw`）で取る |
| `strata[0] <- level[s] + 1 for (s) in …` を **種**と読み、値が定数でなくて落ちた | 種か規則かを決めるのは添字ではなく **`for` があるか**。`hasfor` を足した |
| p_memsz を **三バイトしか書いていなかった** | 16 MB を超えるまで誰も気づかない類の穴。場が 11 MB になった日に、⊥ を敷く最初のループが地図の外へ出た。**幅は境界を越えてから確かめられる** |
| コード面 65536 が足りなくなった（110 規則） | 4096 → 65536 → 131072。**踏んだことは黙って起きる**（生成器は最後まで何も言わない）。気づけたのは焼いた実行ファイルが落ちたからである |
| 頭の一次元目が入れ子の読み（`och[ocof[i], cpos[i]]`）だと次数が 1 のままだった | 区切り `,` は `hidx+1` ではなく **括弧の向こう側**にある。座標は2次元で出るのに場の広さが1次元 —— 掛け数が ⊥ になり、`imul` の即値が空いた |
| 同じ節の二つの `]` がガード番号を共有し、`gfld` が max で **内側の場に化けた** | **添字の中の読みは読みではない、座標である**（深さ 1 の `]` だけが読み）。今まで通っていたのは内側がたまたま常に真だったからにすぎない |
| 成層できない源を食わせると **断片が止まらない**（宿主は「不成層」と言うのに） | 最長路を数で持っていた。**深さを座標にする**と構造で有界になり、「深すぎる」と言える（気づき32） |
| 止めるために `level <= 62` を足したら、**今度は成層できなくなった** | 自分と相互再帰している場への比較は非単調である。**止め方が問題を作る**ことがある |
| `gcc -O1` が 17k 行の単一関数で何分もかかった（走るのは 2 秒） | 掃き取りは一つの巨大な関数である。**道具は最適化しない**（`-O0`） |
| `pkill -f "h4.py"` が **自分のシェルの命令行**に当たって、書きかけのファイルごと消えた | パターンは自分にも当たる。消す前に、何に当たるかを見る |
| 座標の源に **定数が無く**、`m[i,0]` の次元1 が黙って落ちた | 源は 列 / 計数器 / 場の値 / **定数** の四つ。三つだと思い込んでいた分だけ、答えが化けた |
| 源4 を足したら `src >= 2` の条件が拾って、余計な場の読みが一命令挟まった | **範囲で書いた条件は、種を足すと勝手に広がる**。`>= 2` は `2..3` の意味だった |
| 入れ子の添字（`snum[t]`）のループ番号を **列**で見ていた | 内側の計数器が rcx（0番）に化けた。区間なら列ではなく **ループ番号**である —— 外側では直してあったのに、内側で忘れた |
| 座標の導出のループが `for (i) in 0 .. 7` で、**8本目以降のガードの座標が出なかった** | ガードが 8 本を超えた日に、`glen` が ⊥ になって番地の鎖が切れた。**上限は数えてから書く** |
| `runtime.py` が宣言した広さと D の max を取り、`bound 1024 256` が 32768² になって落ちた | **宣言があるならそれが広さ**である。二つの実装が違う広さを取れば、同じ答えにならない |
| 頭の添字が定数の規則（`ncol[0] <- … for …`）は **書き先の座標が出ない** | ⊥ が `blen` に伝わって番地の鎖が切れ、最後は segfault。焼く前に名前を言うようにした |
| 升の数だけ Python の整数を作って 500MB 使った | 86万升の読み戻しで落ちた。**答えではなく、答えの受け取り方**が重かった（`array` で持つ） |
| 比較の演算子を **二つしか導いていなかった**（== と != だけ） | `if c > 100` も `if i > lim[i]` も、ガードが一つも出ないまま **黙って通っていた**。未対応が例外を投げるなら気づくが、**静かに真になる**と気づかない |
| `cmp rsi,rax` を「右 - 左」と読み違えた | 答えが綺麗に **反転**して出た（`c >= 97` が小さい方を拾った）。ModRM は reg と rm のどちらが宛先か —— **符号は数えるな、確かめろ**（形44 の一件と同じ） |
| 比較の辺が場の読みでも **階数を継いでいなかった** | 全セルは一致するのに SOUND が落ちた。**読みは読みである** —— 種9 だけの仕事ではない |
| 生成器の書き出しが **泡立ち法**で、13万升を並べるのに 37 秒（不動点そのものは 50 ミリ秒） | 遅いのは答えではなく **答えの出し方**だった。`NATIVE_MS` が 50 と言っているのに全体が 37 秒なら、見る場所は不動点の外である |
| `runtimecheck` が **`print` と書いた場だけ**を比べていた | 生成器は `render` しか持たないので比べるものが無く、C の答えは一度も検査されていなかった。**通っていたのではなく、何も見ていなかった** |
| C の `render` が末尾に改行を足していた（解釈実行は足さない） | render はバイト列であって行ではない。**足した瞬間に「同じ答え」でなくなる** |
| 焼いた道具を取っておいて走らせたら、復号が別のプログラムの残り香で狂った | 大域変数は「直前の一本」しか覚えない。**実行ファイルごとに文脈を持つ**（`CTX`） |
| 新しいループで `lat` を使い回し、**join の束を潰した**（`or` の場が min で畳まれた） | 二度目である（`klat` の一件と同じ）。**外側の名前を内側で書かない** —— C の生成器では特に、束の名前は一つしかないと思い込みやすい |
| 計数器どうしの比較（`if i >= j`）が **黙って落ちる** | ガードの種は「列と定数」「場と定数」「場と場」しかない。**未対応は静かに真になる** —— まだ塞いでいない穴として記録する |
| 節の書き直しで、その節に居た **他の束の行** も消した（flat の ⊥検査） | 節は束で切っていない。**消す前に節の中の束を数える** |
| ⊥ を文字列の番兵にしようとした | ガードは ⊥ の上で火を噴かない。**⊥ は情報の不在、番兵は実文字** |
| **一括改名で他人の場を潰した**（`tc2` は 22_parse の「記号の二文字目」だった） | 改名は名前ではなく **その名前が指すもの** を数えてから。54 件の置換のうち 18 件が他人のものだった |
| 束ごとに書いていた `mov r8d,1` を消して、無条件版を足し忘れた | **穴は鎖を殺す**（二度目）。節を消したら、その節が出していた命令を数え直す |
| 肯定の読みを「項」として扱っていた（`or` では join が値を捨てるので見えなかった） | **束を増やすと符号化の曖昧さが出る**。max で `1 + alpha[i]` になって初めて分かれた |
| 階数の初期化 `xor r11,r11` をガードの **後ろ** に置いた | ガードが継いだ階数を消していた。**初期化は使う者すべてより前** |
| **索引の counting sort に境界検査が無く `at[-1]` が配列の手前を書いていた** | 値の読みには入れたのに、**索引を作る側**は素の添字だった。鎖の端は範囲の外＝⊥ で、寄与しない行である |
| キーの中の場の読みが join の束を上書きしていた（`lat` の使い回し） | `tstart[tnum[i]]` のように **座標に場を使う規則でしか出ない**。min が max として焼かれ、答えが空になった |
| 出力の `sys_write` を一度きりで書いていた（読みは回していたのに） | パイプは一度で全部受け取らない。**読みが回るなら書きも回る** |
| コード面を 4096 バイトと決め打ちしていた | 命令が 862 本になった日に、**コードがデータを踏んだ**。区画の大きさは中身が言う |
| 次元の区切りを「`]` から +3」と決め打ちしていた | `opos[i-1, d]` のようにずれが入ると位置が動く。**囲む括弧から引く**（22_parse の opos が既にその形） |
| `bound` の無い場で、**次の文の整数**を広さと読んだ | 同じ文であることを要求する（`if not sbrk[k+4]`）。トークンの位置は文をまたぐ |
| 焼いた機械語に **書き座標の境界検査が無かった** | 升を超えると黙って隣の場を踏む。宣言した広さを超えたら **終了コード 6 で止まる**（超えたら言う） |
| C 側の `SZ_f` が data 由来の `D` を掛けていた（宣言した `DOM_f` ではなく） | `bound` は両方の実装に効かなければ機能ではない（不変条件9）。区間はデータに現れない |
| `field` 行の *値* の列まで座標の広さに数えていた | mtime のような大きな数で `int` が切り詰められ、領域が 1 になった |
| 種の場番号を `fd < 63` で濾していた（63 は「場が無い」番兵のつもり） | 場が 63 を超えた日に、**本物の種が黙って落ちた**（`neg` が 0 升になった）。番兵を定数で決め打つと、本物が番兵の側に入る。上限は数えて言う（`fd < nfld[]`）、番兵は「場の数」にする |
| `fno` の鎖が `for (s) in 1 .. 511` で止まっていた | 文が 511 を超えた源で、**それ以降に宣言した場が一つも記号表に入らなかった**。`0 .. 511` は一括置換で直したのに `1 .. 511` は残った —— **置換は書き方を数えろ、意味を数えろ** |
| その鎖は上限を測るために **表を丸ごと巡っていた**（2048×|tok|） | 最後の文より先は述語が偽なので値は動かない。**止める必要が無いなら止めない** —— 上限のための走査が本体より重かった |
| 種の座標の畳みを **次元0 の広さ**で掛けていた（次元1 ではなく） | `bound 8 4` の種 `q[1,1]` が升 9 ではなく 65 に入った。畳みの掛け数は **内側の次元の広さ**である |
| 既定の広さ 64 を宣言と `max` で混ぜていた | `bound 4` と書いた場が 64 升になり、畳みの掛け数まで狂った。**宣言があるならそれが広さ**（runtime.py で同じ事故を直したのに、導出側には入れていなかった）|
| ずれを一種類だと思っていた（`g[x] + 2` だけ） | `g[x+2]`（前置）は **読む前に添字を動かす**別物なのに同じ印に畳んでいて、`vlpz[j+2]` が `vlpz[j]` として焼かれた。**黙って落ちる**（気づき29）|
| 座標の三段目（`lkd[s, vlpz[oppv[t]]]`）を書いていた | 座標は二段まで。三段目は静かに落ちる。中間の場に名前を付ければ二段の読みが二本になる |
| `tstart` は min なので ⊥ が ∞ で、`chv[tstart[t] + 1]` が **⊥ から座標を作っていた** | 桁が溢れて「座標が機械語に収まらない」で停止。**「無い」を座標の材料にしない**（気づき27 の座標側）|
| 入れ子の添字の源を **文が区間を含むか**で決めていた（`isrng[snum[k]]`） | `for (t,c) in ch for (k) in 0 .. 7` のように表と区間が並ぶと、内側の `t` が計数器に化けて **rcx（表の行の番地）** が座標になった。ixcol は既に「その変数がどのループか」で見ていたのに、ixsrc だけ文で見ていた —— **同じ判断を二箇所に書いたら、片方だけ直る** |
| 表のループを 1 番目に置いた（`for (z) in 0 .. 0 for (t,c) in ch`）| 生成器は rcx を **0 番のループの行**に使う。表が内側にいると列を別の計数器から引き、境界検査で落ちた。焼く前に名前を言うようにした |
| 解釈実行が 1300万升の断片で **メモリを使い切って殺された** | worklist / priority / sweep は実例を一本ずつ表に置く。**実例の表は最適化であって答えではない** —— 上限を超えたら表を作らない日程（naive）に落ちる |
| 断片の規則が全部 `for (t) in 0 .. 65535` だった | 入力が 25 バイトでも 65536 回まわる。**トークン番号はバイト数を超えない**ので `for (t,c) in ch` で足りる —— 試験が 45 分から 11 分になった（答えは同じ）|
| 座標の所有者を（種, 規則, 番号）の三つ組で引いていた | 場が 4次元になる。**焼ける座標は 2次元まで**なので、生成器が自分を焼けない —— 所有者に通し番号を一本振ったら 1次元になった（断片の側では所有者はそのまま出現である）|
| `table X = .*?\n(?=table)` の先読みで表を差し替えていた | 表の直後に新しい塊（表→場の写し）を置いた日に、**先読みが塊を丸ごと飲み込んだ**。区切りを「次の表」で決めると、間に何か置いた瞬間に壊れる |
| `table row` を消すのに「次の `table` まで」で切った | 間に挟まっていた写しの塊まで消えた。**二度**やった（一度目で気づかず、同じ切り方をもう一度書いた）|
| 畳んだら三段と **バイトが違った**（一例だけ） | 違いは成層だけだった。断片は比較を一律に非単調と見る（安全側）が、22_parse は極性を見る（`max` の `>= 定数` は単調）。**答えは同じ、並び順が違う** —— 通す条件は答えの一致に置いた |
| ガードの辺に **内側の読み**まで数えていた（`f[g[i]]` の `g`）| 内も外も辺にすると `max` が **内側を選ぶ**（番号が大きいから）。辺になるのは深さ 0 の読みだけ —— 項は `aftarw` が既にそう言っていた（**同じ判断を二箇所に書いたら、片方だけ直る**の三度目）|
| `gkd` だけが `isocc` を見ていた（`isgrd` は `isbnd` を除いていたのに）| `for (r) in 0 .. nrlz[0]` の **上端の読み**が幽霊のガードになり、本物のガードと同じ番号に座った。`glen` が ⊥ になり、二本のガードが同じ位置に出た |
| `ixend` は出現でないトークンでは **自分自身**なので、`0 - 2147483647` のような「ずれではない引き算」も添字のずれとして読まれた | `neg[2147483647]` を読んで地図の外へ出た（C の側では黙って ⊥ が返る）。ずれは 255 まで —— **上限は数えて言う** |
| `imm` に **⊥ そのもの**（max なら -2147483647）を値として置いていた | 焼いた機械語は「無い」を **値で** 見分けるので、⊥ と区別できない。自分を焼くときだけ起きる（生成器の値は ⊥ の定数そのものだから）。2^32 だけ持ち上げて持つ —— 256 で割った余りも商の余りも 2^32 は素通りするので **バイトは一つも変わらない**。下げてはいけない: max の場は自分の ⊥ より小さい値を持てない（join が撥ねる）|
| ガードの辺に **足し算**を書いていた（`if r + 1 == nrule[0]`）| 辺は「計数器」か「場の読み」か「定数」。焼くと `r` だけを見て黙って外れる —— 一つ手前の場（`nrlz`）で言い換えれば、ただの計数器と場になる |
| `foff[outf[0]]` と書いていた | 添字が「**定数で引いた場**」で、焼ける五つの源のどれでもない。名前を一つ置けば「計数器で引いた場」になる |
| `8 * f[x]` と書いていた | 項の列は左からの畳み込みで、掛けられるのは定数だけだった。`f[x] * 8` なら書ける —— そして `fw[f,0] * fw[f,1]`（二次元の畳みの掛け数）は **場と場の積**で、種10（imul）が要った。**掛けられるのに掛けられないのは分類の穴** |
| 種（`for` の無い文）に **計算した値**を書いていた（`wb[0] <- ebase[0] + pklen[0]`）| 種は「場・座標・値」の三つ組で、値も座標も定数でなければならない。`for (z) in 0 .. 0` を付ければただの規則になる —— 自分を焼くまで誰も気づかない |
| 広さを大きくしたら、**上限も大きく書いた**（`for (o) in 0 .. 8191`）| 25 バイトの .lx でも 8192 回まわる。解釈実行が 2.3GB で止まった —— 区間の上端は場に書けるのだから、**数えたものをそのまま上端にする**（気づき35 の再演）|
| `i * i` と書いたら `i + i` が焼けた（**黙って**）| 変数（列・計数器）は「足す」しか無かった。場の側（種9/10）を直したときに、同じ穴が変数の側に空いたままだった —— **分類の穴は対で開く**。種11〜14（列/計数器を引く・掛ける）を足した |
| その穴を見つけたのは試験ではなく **使ってみたこと**だった | `./check` は 40 本とも通っていた。試験は「通る形」を測っていて、**使う側の形**を測っていなかった（気づき21 の四度目）。`test/accept.py` を足した —— 試験のために書かれていない .lx を焼いて走らせ、解釈実行と升まで比べる |
| 焼けない形を **黙って落としていた** | `i / j`（変数で割る）・`if r + 1 == n[0]`（式の辺）・三段の読み・表が二つ —— どれも「導けなかった項が消える」だけで、答えは出るが間違っていた。**導けた数と、あるべき数を比べる**（項・ガード・座標・ループ・規則・種）。違えば入口を出口に向けて **終了コード 7** で言う |
| 「無い」の数え方に集約を使った（`count`）| 区間の集約は runtime.py がまだ焼けない（C が `_r0` を知らないと言う）。数えるのは **前置きの鎖**でよかった —— 鎖は既にどこにでもある |
| 番兵の行（ホストが置く空の一行）まで「焼けない」と数えた | 三段の道では表を空にできないので `(0,63,0,…)` を置く。**自分の都合で置いたものを、他人の間違いとして数えた** —— 種 0 は番兵と読む |
| `render` が **⊥ の升まで書いていた** | 宣言した広さぶん（4096升）を丸ごと詰めて出すので、後ろに ⊥ の下位バイト（0x01）が並んだ。解釈実行の `render` は ⊥ を飛ばす —— **同じ言葉が二つの意味を持っていた**。飛ばすと長さが走ってみないと分からなくなるが、詰め終わった位置から置き場を引けばそれが長さである |
| `.lx` ですらないものを食わせたら **segfault した** | 何も導けなければ命令が一本も出ない —— 出口を後ろに置いても、そこへ飛ぶ番地すら無い。**前置きの三命令をそのまま出口にする**（`form` は max で選ぶので上書きは書けば済む）。命令の数にも下限が要った（三本ぶん） |
| その ELF は `p_memsz < p_filesz` だった | 像より小さい記憶を宣言すると、カーネルは載せる前に殺す。**p_memsz は像より小さくできない** —— 下限を `total + 65536` にした |
| `render` の場を `kwrnd[prevw[t]]` だけで決めていた | `render` の**次の空白**も「出現」になり、番号が一つずれた。語であることを言えばよい（`tkind[t] == 1`）—— 出現の定義が二箇所にあり、片方だけ直っていた（気づき34 の五度目）|
| 走っている `lattix` を上書きしようとした | `Text file busy`。**置き換える**（別名に書いて `os.replace`）—— 焼いた道具を自分で置き換えるなら、置き換え方も道具の一部である |
| 生成器が 0次元の頭（`x[] <- …`）を 40 個持っていた | **焼ける頭は座標を持つ**。定数の頭（`x[0]`）は種、回る文は `x[z] <- … for (z) in 0 .. 0`。自分を焼くには、自分が焼ける形で書かれていなければならない |
| 割り算と余りを機械語に落とすのに idiv が要ると思った | 生成器が使う除数は全部 **二のべき**（256 / 65536 / 16777216）なので、算術右シフトと and で足りる（負でも Python の `//` と同じ床に落ちる）。ただし右シフト量は **定数の対数**なので、二のべきの鎖で引く |
| 生成器の位置の場が二次元だった（`toff[r,t]` / `goff[r,g]` / `coff[oo,d]`）| 座標に **読み二つの和** が要るので自分では焼けない。ガード・項・ループ・座標に **通し番号を一本**振ったら、位置は `form[tpos[gt] + k]`（二段の読み＋定数のずれ）になった。番号は探索ではなく **トークンに沿った鎖**で出る |
| 通し番号を足すのに、規則の順に並べ直そうとした | 要るのは「同じ規則のものが隣り合うこと」だけで、**規則の順である必要は無かった**。源の上で隣り合っているので、トークンの鎖がそのまま使える |
| `f[] <-` を `f[0]` に直したら、規則（`for` のある文）で書き先の座標が出なくなった | 定数の頭は **種**であって規則ではない。回る文は `f[z] … for (z) in 0 .. 0` と書く（過去に一度記録した事故の再来）|
| 規則ごとの「最初の読みの通し番号」を `min` の場で取った | `tcn`（`max` の鎖）を `min` に join すると、**育っていく途中の値**を拾う。単調でなくなり、答えが実行順に依存した —— C は 62,900 命令、焼いた符号は 145,933 命令目に書きに行って `exit(6)`。**この言語が禁じている唯一のことを、私が書いた**。直しは「基点を引く」のをやめること: **鎖を規則の変わり目で 0 に戻す**（`if tmr[gt-1] == tmr[gt]`）と、規則の中の番号がそのまま出る（気づき38）|
| 同じ根の二つ目 —— `dcap[0] <- fbase[0] - dbase[0] - gap[0]` | `gap` は下から育つので、`max` の場は **途中のいちばん小さい gap** を残す。L1 と L2 が 10,816 バイト違った（どちらも「その実装にとっての」不動点）。**引く前に閉じる** —— 比較のガードを一つ置けば層が切れて閉じる（`gpz[0] <- gap[0] if gap[0] >= 1`）。`+` は束の向きに沿うが `-` は裏返す |
| SPEC に「項の列は左からの畳み込み」と書いてあったので、位置の式を `a - b + c * 2` と書いた | **掛け算は直前の項にしか掛からない**（`1 + 2 * 3` は解釈実行で 7）。焼いた符号は左から畳んで 9 —— **同じ言葉が二つの答えを持っていた**。位置の式は「鎖を二つ刻みで数える」ことで掛け算ごと消し、食い違う形（足した後に掛ける）は **焼かないことにした**（終了コード 7、`accept.py` に一本）|
| 試したら「直っていない」と出た | 走っていたのは `/tmp/lattix`（前の版）だった。`cd /tmp` した同じ呼びの中で `./lattix` と書いたため。**道具の場所は測ってから言う** |
| 宣言した上限を全部打ってみたら、**四つのうち二つが黙って壊れた** | 入れ子は「8 まで」と書いてあったが計数器は三本（rcx/r13/r14）しか無く四重目は segfault。項は「8 まで」で九つ目は `trkd[文,番号]` が隣の文を踏み、`1+1+…+1` 九個が **9 でなく 13** になった。節 33 個だけは既に言えていた。**宣言した上限は、その一つ上を打ってみるまで信じない** —— `accept.py` に「通る一つ下／言う一つ上」を対で置いた（41本 → 48本）|
| 「源 262,144 バイト」と宣言してあったが、**228KB を超える源は出力 23 バイト・終了コード 0** で返ってきた | 源を (位置, 文字) の表に開くと 16 バイト × 262,144 = 4MB 要るのに、地上に 3.6MB しか空けていなかった。**上限を二箇所に書いた**（宣言と、実際に空けた場所）ので片方だけ直っていた（気づき34 の七度目）。置き場を上げて宣言を本当にし、超えたぶんは終了コード 6 で言う。`accept.py` に 260KB の源を一本置いた |
| 「名前 8 文字」と宣言してあったが、**9 文字の名前を書いたら焼いた符号が segfault した** | コンパイラは成功と言い、壊れた実行ファイルを出した。**宣言してあることと、超えたときに言うことは別**である（`accept.py` 41本は全部通っていた）。鍵を二本にして **16 文字まで通し**、17 文字以上は終了コード 7 で言うようにした（気づき44）|
| 起動のたびに 6,400万升を ⊥ で埋めていた（231 バイトの .lx でも 0.23 秒）| `or` も `sum` も `count` も **⊥ は 0** で、記憶は最初から 0 である。埋めていたのは束を見ていなかったから —— **44% の升は書く前から正しかった**。記憶 720MB → 582MB、小さい .lx は 0.14 秒（気づき43）|
| 焼いた側のループが do-while で、**空の区間が一度まわっていた** | `for (k) in 1 .. 0` が本体を一度実行し、解釈実行と食い違っていた（`accept.py` に「既知の穴」として置いてあった）。頭で一度比べれば済む —— ループごとに二命令、入口で一度だけ。上端が ⊥ の場でも正しく回らなくなった |
| 名前の照合が「出現 × 宣言」の面を二枚（134MB）使っていた | 名前は 8 文字 × 7 ビット = **56 ビット**で一つの整数に畳める。鍵にすれば `ofl[o] <- d if okey[o] == dkey[d]` の一行で済み、面は二枚とも消える（長さの違いも鍵が言う）。**升で比べるのは、比べるものに名前を付けていないことの言い換え**（気づき42）—— そして面が消えたので宣言の上限 512 → 1024 がタダになり、次の最適化が入った |
| 日程を直しても半分の時間が残った。どこかは **測るまで分からなかった** | 道具が無かったが、日程のために足した `LAST[r]` が **そのまま測定器**だった —— 走行中に止めて `LAST[r] == r15` の最大の r を見れば「いま走っている規則」が出る。22 回止めただけで、時間の半分が五本の規則にあると分かった（気づき41）|
| その五本は `cnum[t] <- cnum[t-1]` の **三本に割れた鎖**だった | 一本ずつ表を丸ごと走るので、一周で進むのは **区間ひとつ**。節が 5,327 個あれば 16,000 周。**鎖が遅いのではない、分かれていたのが遅い** —— 同じ反復空間の規則を一つのループに融合したら **50倍**（気づき40）|
| 融合を入れたら、自分の生成器が **終了コード 7** で拒まれた | `r * 8 + nfl[0] * 8 + 8` —— 私自身が入れた「足した後に掛ける」の拒否に引っかかった。**拒否は正しく働いた**（この式は解釈実行と食い違う）。中間の場を一つ置けば済む |
| 規則を飛ばす条件に、読む場を **四つの口**（項・ガード・座標・ループの上端）から集めた | 一つでも漏らすと「回るべき規則が回らない」で **黙って間違う**。`foff[…]` を値として読む場所を全部数えて突き合わせた —— ループの上端（`for (o) in 0 .. nocz[0]`）が四つ目で、これを落とすと 33_self のほとんどの規則が壊れる |

## 現在地（実測。憶測で書かない）

```
Lattix   6,881行               examples 34本 + lib 4本
lattix.lx 3,854行 216KB        **一枚**（33_self + fold + 31_gen）
                               場 539 / 規則 2,346 / 層 27
lattix    524,288バイト        **それを焼いたもの**（静的 ELF、依存なし）
                               自分を焼き直して **1.17 秒 / 625MB**（最初は 254 秒 / 829MB）
                               78 バイトの .lx なら **0.14 秒**
                               （知らせ —— 行番号・理由・言葉 —— を足して
                                 1.1 → 1.29 秒。**診断は只ではない。**
                                 証人を fd 3 に移して 1.17 秒に戻った ——
                                 521MB を書かないぶんが効いている）
Python   6,329行               処理系 4ファイル（もう再現には要らない）

ネイティブに落ちる : 33本中 29本（32_shape は C 29,215行、33_self は 36,245行）
落ちない           : 07 不成層 / 11 署名（どちらも設計通りの棄却）
                     08 fourv / 12 集約が場を読む（実装の穴）
```

**30_build は実ディレクトリで動く。** 走査も、何が古いかも、段も、全部 .lx 側。
ホストがやるのは stat と、ファイルを書くことだけである。

**31_gen は Lattix のプログラムから機械語を書く。** 出てくるのは静的 ELF
（`not a dynamic executable`、openat 0 回）で、それが自分で最小不動点を解く。
規則を増やせば本体が伸び、番地も相対飛びも前置きの鎖が付け直す。
**そして一枚になった**（`lattix.lx`）。源のバイトを入れると静的 ELF が出る ——
段の間を運んでいたホストは、表を作って渡していただけで判断を持っていなかった。
写しは 275 行（`lib/fold.lx`）で済み、焼けた実行ファイルは三段のものと
**同じ答えを解く**（`test/fold.py`）。

**そして輪が閉じた**（`test/g4d.py`）。C で焼いた起動用 E0 が `lattix.lx` から
L1 を作り、L1 が `lattix.lx` から L2 を作る —— **L1 == L2**（sha256 一致、
524,288 バイト）。起動用の C は再現にも要らない。焼けた `lattix` は
`examples/01_shortest.lx` も実行ファイルにする（表は実行時に stdin から）。

**日程は自由である —— 4.25倍。** 焼いた符号は層ごとに「何か動いたか」だけを見て
**全部の規則をもう一度まわして**いた。Lattix は「どの規則がどの場を読むか」を
**宣言として持っている**（C の別名解析・依存解析・範囲推論は、すべてこれを
再発見するために存在する）。だから要ったのは二枚の数だけだった:

```
EP[f]    場 f が最後に動いた周回      規則の join が動いたときに書く（1命令）
LAST[r]  規則 r が最後に回った周回    規則の頭で書く（1命令）
r15      周回番号                    層の尻尾で 1 増える
```

規則 r の入口はこうなる —— **自分が読む場のどれかが、自分が回ってから
動いたか**（読む場ごとに `cmp [EP[f]],rax / jae`、二命令）。単調だから
飛ばしても答えは変わらない: 入口が動いていないなら導けるものも増えていない。
輪は「何も動かなかった」で閉じるので、健全性は元のままである。

読む場は四つの口から出る —— **項・ガード・座標（`f[g[i]]` の g）・
ループの上端**（`for (o) in 0 .. nocz[0]` の nocz）。一つでも数え漏らすと
黙って間違う（規則が回るべきときに回らない）ので、`foff[…]` を読む場所を
全部数えた。

```
自分を焼き直す           254 秒 → 59.8 秒      4.25×
同じ源を C（-O2, 半素朴）  37.9 秒             焼いた符号はその 1.58 倍（前は 3.3 倍）
符号面（ELF が自分で言う） 289,201 → 393,406 バイト   増えたぶんが検査である
L1 == L2                sha256 一致           G4d は保たれている
```

**そして融合 —— 59.8 秒 → 1.1 秒。** 日程を直しても、まだ半分の時間が
**一つの層**の中にあった。`LAST[r]` を走行中に読むと（記録がそのまま
測定器になる）、`cnum[t] <- cnum[t-1]` / `+ 1` / `<- 0` の **三本に割れた鎖**
だった。一本ずつ表を丸ごと走るので、**一周で進むのは区間ひとつ** ——
節が 5,327 個あれば 16,000 周まわる。同じ形が源の三十箇所にある。

**鎖が遅いのではない。鎖が分かれていたのが遅い。** 直しは生成器の一箇所:

```
群 = 隣り合う / 同じ層 / 一重の表ループ / 同じ列数
[検査（群ぜんぶの読み）][ループの頭][本体1][本体2]…[本体k][進め]
```

行 t で本体を全部走らせれば t-1 は既に確定しているので **一周で終わる**。
C はこれを「融合してよいか」から証明しなければならない（依存の向き・
別名・書き込みの衝突）。**Lattix は証明が要らない** —— 答えは最小不動点で
あって、順序に依らないからである。

```
自分を焼き直す           59.8 秒 → 1.1 秒      54×（通算 254 秒から 230×）
周回の数                34,443 → 364          分かれた鎖が一周で閉じるようになった
同じ源を C（-O2, 半素朴）  46 秒               **焼いた符号が C の 40 倍速い**
符号面                  393,406 → 363,627 バイト  **小さくなった**（頭と進めが群で一つ）
記憶                    829MB → 582MB         照合の面を二枚捨て（気づき42）、
                                              ⊥ が 0 の束は埋めるのをやめた（気づき43）
固定費                  0.23 秒 → 0.14 秒     231 バイトの .lx でも払っていたぶん
L1 == L2                sha256 一致           G4d は保たれている（./check 42/42）
```

**融合は表でも区間でも同じ判定でよい** —— 種・列数・下端・上端・上端の場と
座標が全部同じなら、二つの規則は同じ行を同じ順に歩く。それだけが条件である。

**これは C から移した最適化ではない。** C の最適化器が持てないのは
「このループが触るセルの集合」で、Lattix はそれを宣言として持っている。
持っているものを機械にそのまま渡しただけである（気づき39）。

**32_shape は規則の形を導く。** `for (w,j,i) in edge` と列を入れ替えても
正しい機械語が出る —— 変数は束縛点の座標で名づけられているので、
「どの変数が何列目か」は探索ではなく参照である（記号表も α変換も無い）。

**束が命令を選ぶ。** `min` は cmp/jge、`or` は test/cmpq —— 本体の長さも 14 と 13
で違うので、命令番号は掛け算では出ない。**規則番号に沿った前置きの鎖**で出る
（番地と同じ形。気づき17 がもう一段上でも成り立った）。

**扱える断片**（＝ `lattix` が受け取る形）は [SPEC.md §12](SPEC.md) にまとめた ——
六つの束・複数の場・ガード・層・2次元の座標・区間・集約まで入っていて、
`lattix.lx` 自身（519場 1,977規則 27層）がその中に収まっている。
まだ入っていないのは、構成子・集合・文字列原子・成分（component）・撤回である。

## 目標（この順で。終わりの条件つき）

**完成の定義（ユーザーの言葉）**: Rust や C と同じように、
**Lattix をインストールさえすれば .lx がそのまま実行できる**。C も Python も無しで。
.lx を使いたい人が他の言語を書かされるのはおかしい。
配布物は自前で焼いた `lattix` 一つ。焼き直せること（バイト一致）がその証明である。

```
G0  座標の区間をネイティブに      → 24 25 28 29 が runtimecheck で ✓     済
G1  「なぜ」を問える              → 任意のセルの導出木が出る。⊤ は二つの寄与を名指す 済
G2  上界 U                        → 閉路の ⊤ が影の店で witness できる          済
                                     密グラフの撤回が削減を出す（1.0× → 202×）  済
G3  駆動プログラムをもう一本      → ビルド系が実ディレクトリで動く（30_build）済
G4  コード生成を Lattix で        → 四段に割る。終わりは **バイト一致** だけ
  G4a 骨                          → 規則から機械語が出て、答えが解釈実行と一致  済
  G4b 前段と繋ぐ                  → `.lx` を入れると実行ファイルが出る（selfgen）済
                                     規則の形も 32_shape.lx が導く（手置き 0）
  G4c 断片を広げる                → min / max / or、**場は複数**（⊥ も場ごと）  済
                                     ガード / 場をまたぐ読み / **2次元の座標**
                                     **層**（前の層が閉じてから次。成層がそのまま入る）
                                     **否定ガード** `if not f[…]` / **読み無し規則**
                                     出力は場と階数、**attest が機械語の答えを別証**
                                     **flat**（⊥→a→⊤ の join が三分岐の機械語に）
                                     **集約 sum/count**（一度だけ撃つ印も場になる）
                                     **鎖** `off[i] <- off[i-1] + w`（座標のずれ±定数）
                                     **データは実行時**（表は stdin。焼き直し無しで
                                     同じ実行ファイルが違うデータを解く＝配布形）
                                     **場と場の比較ガード** `if f[A] != g[B]`
                                     → **記号表が不動点として焼けた**（intern。
                                       aid[t] = 同じ綴りが最初に現れた場所。ハッシュ無し）
                                     32_shape が比較ガードの形まで源から導く ——
                                     **intern の三行が .lx のまま機械語になる**
                                     種の無いプログラムは番兵の種 (0,63,0,0)
                                     **区間** `for (i) in lo .. hi` —— 計数器が
                                     そのまま座標。行読みが転送 (mov r9,rcx) になり、
                                     表の無いプログラムも焼ける（`..` は 21_lex の
                                     二文字記号、形は 32_shape が源から導く）
                                     **ガードは表**（規則,番号,種,場,列1,列2,次数,値）——
                                     一つの規則にガードが何個でも並ぶ。rrule は
                                     38 → 21 列。番号は if 節に沿った前置きの鎖で、
                                     32_shape が源から導く（比較は左右で二行）
                                     **項も表**（規則,番号,種,場,列1,列2,次数,ずれ,値）——
                                     定数・列・場の読みが並び、最初が置き後が足す。
                                     rrule は **13 列**。二項加算 `a[i]+b[i]` は
                                     ただ二行（階数は読みの最大+1、cmovl）
                                     **座標の広さは場ごと**（fdim 表。畳みは掛け算）——
                                     広さは源が言う（書かれた座標と区間の上端）。
                                     2000 升の鎖が焼ける。64 の壁は私の都合だった
                                     **座標も表**（所有者種,規則,番号,次元,源,場,列,ずれ）
                                     —— 源は 列 / 計数器 / **場の値** の四つ。
                                     `got[i] <- val[ptr[i]]` が焼け、次元が行に
                                     なったので 2次元の上限も消えた。rrule は **7 列**
                                     **ループも表**（規則,番号,種,下端,上端,次数）——
                                     入れ子は行（rcx / r13 / r14）、前進は内側から。
                                     値の種に **計数器そのもの**（`aid[j] <- i`）。
                                     → **記号表が言語の中だけで焼ける**（対の行を
                                       ホストが作らない。三重ループが綴りを比べる）
                                     **入れ子の for も源から**（ループ番号は節の鎖）——
                                     `.lx` を入れると記号表の機械語が出る
                                     **範囲ガード** `if c >= 97` / `if f[C] <= v`、
                                     **肯定の読みはガード**（種9。階数もここで継ぐ）
                                     → **字句解析器が断片だけで焼ける**（cons 無し。
                                       範囲と比較と鎖だけで語が切れる）
                                     **バイトの口**（`inp`/`outp` の表）—— 生バイトを
                                     (位置,文字) に展開して読み、升の下位バイトを
                                     詰め直して書く。**答えは stdout、証人は stderr**
                                     **場の広さは宣言できる** `field f : max bound 8192`
                                     → **33_self.lx**（断片だけの字句解析）を焼き、
                                       **生の .lx をバイトで読ませて全セル一致**
                                       （test/selfbyte.py。自己ホストの一段目）
                                     **引く項**（`pre[i-1] - 1`）と **括弧の深さ**
                                     **括弧の対応** `match[i] <- opos[i, pre[i]]`
                                     （場の値が座標。スタックは要らない）と
                                     **次元ごとの広さ** `bound 8192 16`
                                     **文と節の切れ目**（行頭＋継続の規則）と
                                     **語の照合**（`for`/`if` は文字ガードで見る ——
                                     インターンは要らなかった）
                                     → **焼いた道具が自分の源（6.7KB）を読んで一致**
                                     **注釈**（`#` から行末。`[` も `for` も語でない）
                                     **名前の比較**（参照ではなく比較。気づき24）——
                                     綴りを **(出現, 位置)** の場に開けば、違いは
                                     `och[o,k] != dch[d,k]` の一行になる。
                                     木も引き算も要らず、長さの一致すら要らない
                                     **成層の導出**（非単調な辺 = `not` の直後 /
                                     比較のある節 / **添字の中**）—— 場の番号を
                                     座標にして書き手と読み手を繋ぎ、重み付き
                                     最長路がそのまま層になる
                                     → **自分の源（15KB）を読んで層 12 と答え、
                                       処理系の成層器と一致した**（67 場すべて）
                                     **道具は一度焼いて取っておく** —— `runtime.py`
                                     は *プログラムだけ* を C にするので、字句解析器も
                                     形の導出も生成器も一本ずつ焼けば、あとは表を
                                     差し替えて走らせるだけでよい（表は答えではなく
                                     入力である）。`.lx` 一本を焼く時間が
                                     **4 分 → 0.4 秒**、selfgen 全体が 35 分 → 4 秒。
                                     `LATTIX_SLOW=1` で解釈実行に戻る
                                     **比較は演算子が言う** —— 辺は四種
                                     （場の読み / 表の列 / 区間の計数器 / 定数）、
                                     演算子は六つ（== != >= <= > <）。
                                     以前は「場と場」「列と定数（== >= <= だけ）」
                                     しか導けず、`if i > lim[i]` も `if c > 100` も
                                     **黙って落ちていた**（ガードが一つも出ない）。
                                     形の導出が要る種は 2 / 3 / 4 / 9 の四つに減った
                                     **三段目の土台**が断片に入った ——
                                     語の照合（束の名前・`table`/`print`/`bound`）、
                                     **数を読む**（`n[i-1] * 10 + d`。桁は掛け算）、
                                     宣言の中身（束・次数・広さ）、表の行と列（鎖）、
                                     種、そして **変数がどの列か**（束縛点の綴りを
                                     比べて引く。位置ごとに開くのは座標が二段までだから）
                                     → **32_shape と同じ答えが出る**（flat_ / fwide /
                                       far_ / trow / seed / vcol を突き合わせて一致）
                                     規則の行・ループ・項・座標・ガードの表も出た
                                     （＝ 31_gen に渡す形そのもの）—— 断片は
                                     **自分自身を含む .lx の形を導く**ところまで来た
                                     （既に **座標の上の関係** である ——
                                       `match[k] <- opos[k, pre[k]]` がその形）
                                     ※ trie は書けない: **動く min は座標になれない**
                                       （座標の契約は確定。だから比べる、引かない）
                                     29_dataflow の残りは kill の三次元だけ
  G4d 自分自身                    → `lattix` が自分を作り直して **バイト一致**  済
                                     E0 = C で焼いた起動用 / L1 = E0(lattix.lx) /
                                     L2 = L1(lattix.lx) —— L1 == L2（sha256 一致）。
                                     要ったのは: 広さを実寸に合わせる・上端を数えて
                                     言う・種と規則の区別・辺の分類（内側の読み /
                                     足し算 / 定数で引いた場）・**⊥ を値として持たない**
                                     （即値を 2^32 持ち上げる）・場と場の積（種10）
                                     そのうえで **日程を選んだ**（気づき39〜41）——
                                     入口が動いていない規則は本体に入らない／
                                     同じ反復空間の規則は一つのループで回す。
                                     自分を焼き直す時間 254秒 → **1.2秒**（210×）、
                                     同じ源を焼く C の 37 倍。L1 == L2 は保たれている
```

G0〜G3 が汎用言語としての仕事、G4 が依存を切る仕事。**順序を入れ替えない。**

## いま開いている穴


- **Python から離れる**: `runtime.py` は min/max/or/and・整数座標・素朴ラウンドまで。
  残りは (1) スケジューラ（索引と日程を実行時に組む） (2) set/flat/sum/count
  (3) 構成子＝ハッシュ表 (4) `render`＝バイト出力。**(3)(4) は Lattix で書いた処理系が使う**
- **上界 `U`**: 二つの使いどころは塞いだ（⊤ の証人 / 撤回の前線）。
  残りは **ストリーミング**と**早期確定**（`L = U` になったセルを打ち切る）。
  `retract` は上界を拾っているがまだ打ち切りには使っていない
- 31_gen には旧い専用のガード種（1/5/6/7/8/10/11）が残っている。形の導出は
  もう出さないが、`test/gen.py` の手置き表がまだ通している。**消すのは別の仕事**
- **33_self の広さ**は宣言であって上限である（源 262,144 バイト / 文 4,096 /
  出現 8,192 / 宣言 1,024 / 名前 16 文字 / 層 128 / ずれ 255 /
  **入れ子 3 / 項 8 / 節 32**）。超えたら焼いた符号は
  `exit(6)` で言う。**回る回数の方は宣言ではなく実測**である（区間の上端が場）
- **`i / j`（変数で割る）は焼けない。** 二のべきの定数でしか割れない ——
  idiv を入れるか、`f[i] / g[i]` を諦めるか。いまは終了コード 7 で言う
- **⊥ の見分けが値である**: 焼いた符号は「無い」を値（`fbot`）で判定する。
  だから ⊥ と同じ値を場に置けない —— 生成器は即値を 2^32 持ち上げて避けている。
  **K 面（階数）を見れば傾けは要らない**（`cmp q[K+r9*8+off],0`。命令数も同じ）し、
  **⊥ 埋めそのものが要らなくなる**（min/max/flat の 36M 升）。
  join が二命令増えるので、時間と記憶の取引である —— まだ測っていない。
  `or` / `sum` / `count` は ⊥ が 0 なので **既に埋めていない**（BSS が正しい）
- **融合は一重のループだけ**（入れ子は融合しない）。入れ子も同じ判定で
  できるはずだが、段ごとに比べる場が要る（いまは段0 だけを見ている）
- **群の検査は粗い**: 群の中の誰かの入口が動けば、群ぜんぶが回る。
  規則ごとに分けるには行の中で分岐が要り、行ごとの費用になる —— 測っていない
- **日程の次**（実測が言った順。憶測で入れ替えない）:
  (1) **表現を束から決める** —— `or` の場は値が {⊥, true} しか無いから 1 ビット。
      `bts [rbx+d32], r9` は join と「変わったか」を一命令で言う。
      いまの場は 8 バイト固定で、`or` が全升の 51.3% を占めている（記憶の話）
  (2) **座標で分割して並列** —— owner-computes。競合が構造的に起きない
  (3) 入れ子の融合と、群の検査の細分
- **規則を飛ばす条件は保守的である**: 同じ場を二度読めば検査も二つ出るし、
  場ごとの粒度なので「その升は動いていない」までは見ていない。
  升の粒度（本当の半素朴）は差分の置き場が要る —— まだ測っていない
- 幅が実時間になるかは未決（2コアでは判定できない。8コア以上の機械が要る）
- 構成子のネイティブ対応（ハッシュ表。**密配列の方が特殊ケース**である）
- 検査器の独立性（構文解析・成層を処理系と共有している）
- ストリーミング（`U` に還元できるはず）
- **メタ評価器（25_eval）の範囲**: 深度1・`min` と `or`・一次の場・source ひとつ。
  ガードは入った（地上の比較と `or` 場の読み）。経路は三本 ——
  値 `ev` / 座標 `evk` / 真理値 `evb`。**どの経路を通るかは記述が言う。**
  残り: `max`・`set`・集約（`evx` を足すだけ。形は同じ）、source 二つ以上、
  二次以上の場、そして深度2以上（対象の `level` を層座標に持ち上げる）
- **前段の深度**: 文ごとの層は Python と一致するが、深度の *数* は 1 ずれる。
  Python は構成子から作る `_size` / アクセサ規則も数えるが、こちらはまだ生成していない
- **前段の未対応**: `component` / `use` は展開しない（10_library の場が出ない）。
  `source` / `emit` の宣言もまだ読まない
- 字句解析器では **コメントが文字列に勝つ**。どちらが先かは循環しているので
  選ぶしかなく、コメント内の引用符（ありふれている）を優先した。
  だから文字列の中の `#` は落ちる（稀）
- **座標順の一掃は `lattix.py` と `runtime.py` に入った。`native.py` にはまだ無い**
- 正準形: 同じ表を二度回す規則は束縛点で変数を区別できない（同型判定になる）
- **ℤ**: 並列の原子 join（`amin`/`amax`）は大整数を扱えない。黙って間違えず `exit(5)`。
  内容アドレス表（`zmk`）が排他制御を持っていないのが理由
- 撤回のネイティブ化（前線と索引はあるので、過剰削除の歩きを C に写すだけ）
