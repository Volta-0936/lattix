# Lattix —— 進捗の詳しい報告と、§0 までの計画（2026-09-05）

> **仮の文書。** `PLAN.md` §6「いまここ」と `HANDOFF.md` を置き換えるために書いた。
> 採るなら §4 を `PLAN.md` §3 に写し、この文書と `HANDOFF.md` は消す（文書は四つに戻す）。
> 数は全部 **実測**（処理系が出した数）か、**「見積り」と明記した数** のどちらかである。混ぜない。

---

## 0. 的（写す。言い換え禁止）

> **自由に書かれた `.lx` を、Windows / Linux にインストールされた Lattix だけで
> 実行できるようにする。Python も C も要らない。最終形は配れる package である。**

物差しは一つ —— *参照実装 `lattix.py` と同じ答えを出す `.lx` の範囲*。

`PLAN.md` §2 が形を決めている: `lattix` ＝ `front.lx`（源のバイト → 規則の表）＋
`run.lx`（規則の表 → 最小不動点）＋ `show.lx`（答え → 字面）。三つとも Lattix で書き、
焼くのは `31_gen.lx`（Lattix で書かれた x86 生成器）である。gcc は道ではない。

---

## 1. 報告 —— 何が動いたか（実測）

### 1.1 一週間の縦の列

git の履歴は 08-25 に始まり 90 commit。`front.lx` の一枚目は **08-29** である。
以下は「動く物が増えた」ことだけを数えた列（説明の言い換えは数えない）:

| 日 | 増えた物 | 測った値 |
|---|---|---|
| 08-28 | 多面体の道（`dat/spc/ssz/mp/fg/fc/fp`）—— 反復空間を点に潰さずに機械へ渡す | `a_min` の辺 4 本 → 1 本 |
| 08-29 | 層は座標である（軸に沿った成層）。`run.lx` を s 軸の圧縮形に | 1,270,165 → 89,949 バイト |
| 08-29 | 三面鏡（`trinity`）—— 言語に自分のバグを探させ、形の一覧を定義に刈らせる | |
| 08-29 | **`front.lx`** 一〜三枚目（軸と空間 / 置き場と写像 / 広さを測る）、noema・16/20/21_lex・29_dataflow・17_render | 20 → 39 本一致 |
| 08-30〜31 | 割り算の種族・`is`・一元の集合・間接の畳みの一般化・構成子の入れ子（深さも座標） | 43 → 48 本 |
| 08-31 | **二段の通し運転**（源 → `front.lx` → 表 → `run.lx` → 答え）—— 表の経路に Python が居ない | 48 本全問一致 |
| 08-31 | 閉じる（口 `orc` の値）のも C の測定器で | |
| 09-02 | 22_parse（883 規則）・25_eval（913 規則）完全一致、依存する区間 ＝ 矩形 × ガード、PLAN 2.5（include / use / emit / source / budget）、24_space、33_self の自己適用（408 規則） | 54 → 59 → 66 本 |
| 09-03 | **`show.lx`**（第3段）。三段の通し運転が `python3 lattix.py` の標準出力と **バイト一致** | 37 本 |
| 09-03 | 04_aggregate（生きた sum/count の比較）・誤りの報告（07/11 は前段も同じ行で断る） | BOOKS 66 本 |
| 09-03〜04 | **自己適用**: 前段が自分の源を読む。疎の場を外す → 層の混ざり（閉じた max と生きた min）→ 前段を「自分が家になる形」に → **完全一致** | 1,761 規則・534 枠 |
| 09-04 | 学びの輪（自己適用の一巡） | 3.5 時間 → 約 30 分 |
| 09-05 | **機械が前段を走らせた** —— `run.lx`（層 48）が `front.lx` 自身の家の表を走らせる | 899 場すべて一致・31 秒 |
| 09-05 | **前段が機械の上で本を読んだ** —— 前段を機械の形（閉じの器）で最後まで走らせ、`a_min.lx` を読ませる | 899 場すべて一致 |

### 1.2 `PLAN.md` §3 の段に照らした現在地

| 段 | 出口条件（PLAN） | 状態 | 測った値 | 出口に足りない物 |
|---|---|---|---|---|
| 第1段 `run.lx` | 全 34 本 + noema + 撒いた 150 本で升まで一致 | **ほぼ ✓** | 十束・259 規則・340 行・層の上限 48。BOOKS 66 本で一致か正しく断る。通し運転 63/63（07/11 は断る本） | 31_gen / 32_shape は未（前段が読めない、§2.3）。撒いた 150 本は家の `run.lx` で撒き直していない（**未測**）。層 48・空間 128・写像 32768 は宣言した定数（§3.4） |
| 第2段 `front.lx` | 表が Python の表と完全一致。**Python は表作りから外れる** | 一致は ✓、外れるは **未** | 4,469 行（畳むと 5,801 行・2,355 規則・899 場・38 層）。66 本一致。自己適用 完全一致 | 字面の前処理・口 `orc/ext`・`fat`・結線が Python（§2.1）。31_gen（2,687 行）を読めていない |
| 第3段 `show.lx` | 標準出力がバイト一致 | ✓ | 430 行・177 規則。37 本一致（print の無い本・render の本は外） | `fat` を前段が出す。深さ 8 の打ち切り。render の本 |
| 第4段 焼く | `./lattix noema.lx` が参照と同じ答え | **未着手** | 4.1「読んで数える」の初版がこの文書 §2.3 | 全部 |
| 第5段 包む | まっさらな箱で走る | 未着手 | | 全部 |
| 第6段 速さ | | 測定器の上でだけ | | |

### 1.3 三つの一致 —— 自己適用が何を言ったか

自己適用は **三つの別々の等式**として成り立った。それぞれが別のことを言っている。

1. **front(front) ＝ flat(front)** —— 焼いた前段が自分の源（`work/self_front.lx` ＝ 33_self + fold + front.lx）を読んで出す家の表（`dat/spc/ssz/mp/fg/fc/fp`: 1,761 規則・534 枠）が、参照 `work/flat.py` の表と **中身まで一致**。→ 前段の文法は、自分を書けるだけ広い。
2. **run(front の表) ＝ 参照** —— 機械 `run.lx`（層 48）が前段の家（1,761 規則・16,395 写像・4,772 枠）を走らせ、**899 場すべて**が参照実装の答えと一致（31 秒）。→ 機械は前段を走らせられる。
3. **front の上で本を読む ＝ 参照** —— 前段のプログラムを機械の形（閉じの器 ＝ `run.lx` と同じ形の C）で最後まで走らせ、`a_min.lx` を読ませた表が **899 場すべて**一致。→ 「前段を機械で走らせて本を読む」は成り立つ。ただし flat 側で 4 規則が家にならず点の道に落ちた（未特定）、flatten に 1.9 時間。

**測ったこと（09-06）**: 上の三つは `zfat` の三本を足しても保たれている ——
自己適用 1764 規則 完全一致 / BOOKS 66/66 / 通し運転 6 本バイト一致。

まだ無い **四つ目**: 前段（機械の上）→ `run.lx` → `show.lx` の通し運転が、**Python の結線なしに一枚の本として**回ること。これが §4 第A段の出口である。

### 1.4 速さ（実測）

| 何 | 前 | 後 |
|---|---|---|
| 学びの輪（自己適用の一巡） | 3.5 時間 | 約 30 分（flat 側 17 分 + front 側 10 分） |
| 閉じの器の焼き | 層ごとに gcc（36 層 × 二周、毎回） | 走行内で一度。表は焼いた後に差し替え、二周目以降は焼かない |
| 41MB の C（閉じの器、層 38 に展開）を焼く | gcc 5.7GB・10 分（8GB の箱で落ちる） | clang 2.1GB・61 秒（出力は同じ —— 比べた） |
| 前段の C の走行 | 30 分（層 18 が 424 回、層 21 が 735 回 掃いていた） | 10 分弱（SCC ごとの不動点、位相順） |
| 前段の全場の読み出し | Python 6GB で死ぬ | 突き合わせが読む 58 場だけ print |
| `run.lx`（層 48）の焼き / 走行 | — | 焼き 465 秒（参照の成層 15k 規則の Python が 9 割）/ 前段の表を 31 秒。C は 24.6MB（階数なし） |
| 二段の通し運転 | — | 22_parse 5.1 秒、33_self 3.4 秒 |
| 前段が機械の上で `a_min` を読む | — | flatten 6,793 秒（閉じの器を規則ごとに呼ぶ）。答え 3 秒 |

箱は 2 コア・8GB。cc1 は 5.7GB で殺される。これは **箱の広さ**であって正しさではないが、「焼ける大きさも資源」（HANDOFF）である。

### 1.5 言語が自分で見つけた穴（このセッション）

全部「突き合わせで食い違った」から出た。読んで見つけた物は一つも無い（気づき49: 二つ目の実装は試験より強い試験）。

| 穴 | 出た所 | 前は | 直し |
|---|---|---|---|
| 閉じた max と生きた min の比較・引き算（層が混ざる） | 自己適用 `zfsz0 <- zexth - zextl + 1` | **黙って違う答え** | 種11 に束1（min）と `<=`/`<`、値は面から直に読む vf 6、front は zbxl/zoppl |
| 点の道の `tolat` が生きた他束を閉じた面から写す | 同上 | 黙って違う答え | 声に出して断る |
| ⊤ の番兵 2^62 が 23_big の `pow2[62]` と衝突 | 通し運転 63 本 | ⊤ と本物の値が同じ字面 | 番兵を INT64_MAX に |
| `run.lx` の層の上限 16 ＜ 前段の 38 層 | 機械が前段を走らせた | **18 場が黙って空** | 層 48（形は増えていない）。上限は §3.4 で値にする |
| 一文のガードが 8 を超えると前段が黙って落とす | 自己適用 | 黙って落ちる | 文を割る + `zerr` 種2 で断る |
| 前段の疎の場 `ztdk`（名前の鍵を座標にした二次元） | 自己適用の一巡目 | 一巡目の広さが 2^126 → 閉じが解釈に落ちて終わらない | 表ごとに引く。**前段に疎の場を置かない** |
| 正規化の regex が `b * 24 + k` を `b * k + 24` に | BOOKS 66 本 | 黙って違う表 | 戻して個別に |
| 引数の数を `hasw2`（注釈の宣言でも真）で見ていた | 自己適用 | 黙って違う次数 | 使われ方の次数 zfusd/zfvol |

---

## 2. §0 に足りない物（数えた）

### 2.1 Python が輪の中に居る場所（六つ）

| # | 場所 | 何をしている | 消し方（§4 の番号） |
|---|---|---|---|
| 1 | `test/front.py` の `expand_uses` / `desugar_emit`、`lattix._expand_includes` | `include` / `component`+`use` / `emit` の糖衣を **字面**で剥く（参照実装と同じ判断） | 前々段（bytes → bytes）を Lattix で。33_self の道具で書ける **[A.2]** |
| 2 | `work/flat.py` + `work/engine2.close_upto`（C の測定器） | 口 `orc`（閉じた値）と `ext`（値で決まる座標の次元の広さ）を前段に渡す | 機械の面から読む —— **pass を座標に** **[A.3]** |
| 3 | ~~`test/show.py`~~ | ~~`fat`（原子か数か）を **答えから**作っている~~ → **済**（d7f30e2 記述から / a035276 前段 zfat の三本 / 367dc76 通し運転が前段の物を使う）。残るのは写しの伝播（コーパスに一本も無い） | — |
| 4 | `test/pipeline.py` | 結線: 写像の番号詰め（compact）、番兵、表の受け渡し、前段 ↔ 機械の交互（BSP の継ぎ目） | 一枚の本にする **[A.4]** |
| 5 | `work/mkrun.py` + `work/shapes.json` | `run.lx` を形の一覧から書く（開発時に一度、~20 分） | 残してよい（配布物には入らない）。ただし形の表を `gen.lx` と共有する **[C.1]** |
| 6 | `lattix.py`（参照実装） | 審判 | 残す。配らない（PLAN §4-1） |

### 2.2 C が輪の中に居る場所

`front.lx` / `run.lx` / 閉じの器は `runtime.py`（C の測定器）で焼いて走らせている。§0 の道では
**`31_gen.lx` の後継が焼く**。測定器は差し合わせ（気づき49）のために残す。

### 2.3 焼く物の数え上げ（PLAN 4.1「読んで数える」の初版）

三つの本を **読んで数えた**（`work/self_front.lx` ＝ 畳んだ前段、`run.lx`、`show.lx`）:

| | `front.lx`（畳んだ源） | `run.lx` | `show.lx` |
|---|---|---|---|
| 行 / 規則 / 場 | 5,801 / 2,355 / 899 | 340 / 259 / 42 | 430 / 177 / 84 |
| 束 | max 587・or 253・min 33・flat 17・count 5・sum 4 | **十束ぜんぶ**（max 21・flat 7・count 5・or 3・min/sum/and/fourv/set/bag 各 1） | max 66・or 17・count 1 |
| 場の次元 | 1: 703、2: 196 | 1: 19、2: 21、**3: 2** | 0: 18、1: 36、2: 26、**3: 3、4: 1** |
| 層 | 38 | s は座標（0..47、`for (s)` が 236 箇所） | 少 |
| 表 | `ch` / `orc` / `ext` | `dat spc ssz mp fg fc fp ate ix eg cd`（点の道 `eg/cd` も同居） | 12 枚（`fld fnm atm pv prn ab fat ps ctr cac topc emp`） |
| 演算 | + 883・− 533・* 415・% 24（法は M1/M2 の二つ。**2^n の除数は 0**） | + − * / % | / % 2^32（34_decimal の技） |
| ガード | cmp 2,849・肯定 1,946・not 552 | == 756・>= 81・<= 45・< 13・> 10・!= 6・not 14 | == 19・<= 41・>= 40・not 56 |
| 値が上端の区間 | 1,520 規則 | | |
| 書き先が二次元で座標に読みを持つ規則 | **347**（一次元だけに読み 218・両方 129。二次元の書き先は 766） | 多数（`v[s, rz[..]]`） | |
| 入れ子（`for` の本数） | 最大 3（19 規則） | 最大 2 | |
| ガード（一文あたり） | 最大 8（6 規則） | | |
| 項（一文あたり、概算） | 最大 5 | | |
| 名前の長さ | 最大 7 | | |
| 源の大きさ | **308,172 バイト（注釈を除く）/ 353,942** | 108,385 | 26,978 |

これを **いまの断片**（`33_self.lx` + `31_gen.lx` が読める形。上限は気づき44 で「一つ上を打って」確かめた物）に当てると:

| 断片の上限 / 性質 | 三つの本の実測 | 判定 |
|---|---|---|
| 束は六つ（min / max / or / flat / sum / count） | front・show は六つの中。**`run.lx` は十** | `run.lx` に set / bag / and / fourv が要る（§3.2） |
| 入れ子 3（rcx / r13 / r14） | 3 | 通る（縁） |
| 項 8 / ガード 8 | 5 / 8 | 通る（ガードは縁。超過は `zerr` 種2 で断る） |
| 名前 16 文字 / 宣言 1,024 | 7 / 908 | 通る（宣言の余りは 116） |
| 源 262,144 バイト | **308,172** | **超える**。宣言と置き場を測った値に（気づき44: 上限は二箇所に書くな） |
| 座標に置ける読みは一次元だけ（SPEC 既知の穴。PLAN 4.2） | **347 規則が踏む** | **塞ぐ必要**。最小形 `o[p[r,c]] <- src[r,c]` は既にある |
| `mul after add` | 7 箇所 | 書き換えで消える |
| 三次元の場（次元は行になったので上限は消えたはず —— 打って確かめる） | `run.lx` に 2、`show.lx` に 3（四次元 1） | 要る。気づき44 の流儀で「一つ上を打つ」 |
| 升の番号 4.4×10^17（内容番地を座標にする場）| `run.lx` の値の面 11 枚 | 密配列にできない → §3.3（intern）か hash |
| ⊤ の規律（比較は ⊤ で偽・⊤ は座標になれない） | front の flat 17 場・機械の flat 面 | `31_gen` に無い（既知） |
| 値が上端の区間 | 1,520 規則 | 断片にある |
| 層・否定 | 38 層 | 断片にある |

**読み**: 前段の *構文* は断片からそれほど遠くない（束・入れ子・項・ガード・名前は全部通る）。
遠いのは三つ —— **二次元の座標書き**（347 規則）、**源の大きさ**、そして **`run.lx` の側**
（十束・三次元・内容番地の広さ・s 軸）。§3.2 の道を採ると、数える対象は「断片の構文」から
「家の表の形」（`run.lx` の 259 規則 ＝ 有限）に変わる。

### 2.4 その先

Windows（PE/COFF の容器と kernel32 の口）、package（tar / deb / zip）、速さ（第6段）は未着手。
既知のことは PLAN §5.3 の通りで、ここに足す物は §3.6 だけ。

---

## 3. 革新の方向 —— Lattix にしかできない形で的へ近づく

七つの組み合わせ（CALM / Lucid の座標 / LVars の freeze / 熱帯半環 / BSP / 多面体 / CRDT）に戻る。
以下は「新しい機能」ではなく、**既に宣言されているものを捨てずに機械へ渡す**ための六つである。
どれも **まだ測っていない**（この節の数は全部見積り）。

### 3.1 一つの不動点 —— front ∪ run ∪ show を一枚に。pass は座標である

いまの通し運転は Python が **BSP の継ぎ目を運んでいる**: 前段の一巡目（仮の広さで数える）
→ 閉じの器（閉じた値）→ 前段の二巡目（測った広さ）→ 機械 → 見せる物。
運んでいる物を覗くと、表を作って次に渡しているだけで判断は無い（気づき36: 判断が無いなら畳める）。

畳み方は一つで足りる —— **pass を座標にする**。前段の場を `zdat[p, …]`（p ∈ {0, 1}）にし、
機械の面を `v[p, s, …]` にする。すると

- 機械 p=0 は前段 p=0 の表を読み、前段 p=1 は機械 p=0 の面を読み、機械 p=1 は前段 p=1 の表を読む。
  輪は無い（p が層である）。**一巡目・二巡目は別の走行ではなく、同じ不動点の層 0 と層 1** である。
- 口 `orc`（閉じた値）は **層 s−1 の読み**そのものになる。閉じの器（`engine2.close_upto` 601 行 +
  hints + キャッシュ、いちばん遅い所 1.9 時間）は **消える**。閉じるのは機械の仕事である。
- 口 `ext`（値で決まる座標の次元の広さ）も同じ —— 機械 p=0 が触った升の最大は、機械の面の
  `max` 一つで出る。
- 結線（compact・番兵・受け渡し）は、同じ本の中の場と場なので **要らない**。

出口は測れる: `python3 lattix.py all.lx`（一枚の本 + 表 `ch`）の標準出力が
`python3 lattix.py f.lx` と **バイト一致**する。これは第A段の出口であり、
**§0 の「Lattix だけで」の意味を、参照実装の下で先に成り立たせる**手である。

### 3.2 形の表は一つ、配り先は二つ —— `run.lx` は走らせ、`gen.lx` は符号にする

`run.lx` は「言語の形の数 × 層」で書かれた一枚で、プログラムの大きさに依らない（259 規則）。
`31_gen.lx` は旧断片の表（`rrule/grd/trm/crd`）から x86 を出す 2,687 行で、家の表を知らない。

気づき15（記述は二度使われる）を **機械そのものに**当てる:

> 家の表（`fg/fc/fp/mp/spc/ssz/dat`）を読む物は二つあってよい。一つは **join する**（`run.lx`）、
> もう一つは **join する命令を書く**（`gen.lx`）。二つは同じ形の表（`work/shapes.json`）から生まれ、
> `differ` が「走らせた答え ＝ 焼いた答え」を測る。

これで三つが同時に起きる:

- **断片が消える。** 「31_gen が読める構文」という第二の文法が要らなくなる。`gen.lx` の的は
  `run.lx` の 259 の形であり、**有限で、読めば数えられる**（§2.3 の表の下半分がそのまま一覧）。
  fp 種 0..11、fc 種 1..6/11、vf 形 0..6、十束 —— 一形ずつ足し、一形ずつ差し合わせる。
- **多面体をそのまま機械に渡す。** `spc` の一行（基・幅・歩幅）は計数ループ一つ、`mp` の一行
  （定数 + Σ係数·軸 + 係数·pa）は `lea/imul/add` の鎖一つ。合法性の証明は要らない（答えは順序に依らない）。
  C の測定器 `engine2.engine_text` が既にこの翻訳の試作である —— C を出す代わりに命令面を出す。
- **`31_gen` の後半は残る。** 命令面 → バイト → ELF（前置きの鎖、argv、終了コード、知らせの八命令）は
  そのまま。書き直すのは前半（規則の表 → 六部）だけである。

`lattix` ＝ `gen(front の家)` ⊕ `gen(run の家)` ⊕ `gen(show の家)`。三つの家の表は **既に手元にある**
（自己適用の一致 1 がそれである）。利用者のプログラムは §2 の通り `run` が走らせる —— `gen` は
三つの本を焼ければよく、速い道として利用者の本を焼くのは「通る形だけ」の選択である。

### 3.3 疎な座標は intern の不動点で密になる —— **測った**（2026-09-06）

内容番地を座標にする場がどれだけ疎かを測った（`fl.lay` の帯と、参照の答えが
使った升）:

| 本 | 場 | 帯 | 使った升 |
|---|---|---|---|
| 35_noema | `_size` | 64,376,966,080,669,152 | **20** |
| 35_noema | `because_binary_left` ほか 7 枚 | 約 5×10^16 ずつ | 4〜8 |
| 15_parse | `_size` | 68,895,855,717,684,665 | **9** |
| 15_parse | `node_left` / `node_right` / `node_sym` | 34,833,350,933,040,860 | 4 |
| z_twoctor | `_size` / `pair_x` / `pair_y` | 61,481,115,305,955,602 | **3** |
| 16_lex | `cons_head` / `cons_tail` | 66,867,314,477,203,863 | 4 |

**升の総数は noema で 424,170,182,318,650,658。使っているのは 64 升である。**
`run.lx` が升の上限を 4.4×10^17 と宣言しているのは、この一事による。そしてこれが
四つの壁の同じ顔である —— A.3 の残り 4 本 / 焼く側が密配列を持てない（C の
測定器は hash 表に落ちる）/ 機械の升の上限が大きい / 第C段で ELF に置けない。

**どこを直せばよいかは一行で言える。** 内容番地は `flat.py` の `pctor` の
最後の一行、**fp 種2 の枠**（`h1·M2 + h2`）である。種2 の枠は内容番地
「だけ」であり、その枠の数は **静的に数えられる**（枠 × 空間の点の数）。

機械の側の形（気づき22「記号表は不動点である」の三行と同じ）:

```
pr[s, p]        種2 が書く生の番地（いまの pa[s, p]）
PU[p]           種2 の枠（産地。pb + t で密である）
RP[s, p] : min  同じ番地の最初の証人   ← pr[s,p] == pr[s,q] の比較
NM[s, p]        「代表が p 以下にいくつあるか」の前置きの鎖
pa[s, p]        = NM[s, RP[s,p]] − 1   ← **密な番号**。写像は今のまま読む
```

こうすると場の広さは **産地の数**になり（noema なら 64 ではなく高々「枠×点」）、
測る必要も消える（静的に数えられるので A.3 の口が要らなくなる）。

**代償を先に書く（塞げていないところ）**:

1. **intern は層を一つ食う。** `RP` は `min`（下る束）で、`NM[s, RP[s,p]]` は
   その値を座標に使う —— 気づき24「座標にできるのは確定した値だけ」に当たる。
   だから番地を作る層と、番地で書く層は分かれる。参照実装では「同じ値は同じ鍵」
   が **定義上の同一性**で、計算ではなかった。計算にすると層が要る ——
   これは LVars の freeze（もう増えないと言えた瞬間に表現を替える）そのものである。
2. **比較は O(P²)、深さは O(1)**（気づき24 の取引）。`for (p) for (q)` の上限は
   `run.lx` の宣言した `npa`（137,378）ではなく、**その本の産地の数**でなければ
   ならない —— つまり §3.4（上限はデータが言う）が先に要る。二つは錠と鍵である。
3. **番地が値として運ばれる本**（25_eval の「項のアドレスは解釈されるものであると
   同時に置き番地でもある」）では、値から密な番号を引く道も要る。産地と同じ
   比較で引けるが、P が増える。

**やってみて分かったこと（2026-09-06。intern ではなく「法で畳む」に変えた）**:

最初は intern（最初の証人を `min` で拾って番号を振り直す）を考えたが、二つで
折れた —— **層を食う**（min の値を座標に使うのは気づき24）し、**番号が
安定しない**（後から小さい産地が同じ番地を作ると番号がずれる）。番地の同一性が
**局所的に計算できる**ことは hash-consing の値打ちそのものなので、そこは崩さず
**帯だけを畳む**ことにした:

```
座標 = 基 + (番地 mod W)      W は一巡目に測った番地の集合が言う
                               —— 衝突しない最小の 2 の冪
```

同じ番地は同じ剰余なので `cons` の同一性は保たれる。衝突は許さない（許せば
別の値が同じ升に落ちる）ので、W は測って決める。**機械は一行も変わらない**
—— fp 種3（`pa % mo`）が既にある。

`work/flat.py` に入れた（`LATTIX_ADDRMOD=1`。既定は off）。測った値:

| 本 | 升の総数（前） | 升の総数（後） |
|---|---|---|
| z_twoctor | 184,443,345,917,867,022 | **240** |
| 16_lex | 200,601,943,431,611,647 | **106** |
| 35_noema | 424,170,182,318,650,658 | **1,655** |

焼いた一枚（`test/baked.py`）で **食い違い 0**（z_twoctor / 16_lex / 15_parse /
35_noema / z_atomarg / a_min）。22_parse は辺 6634 > 6136（宣言した上限。法の枠が
一本増えるぶん）、25_eval は 400 秒で切った —— どちらも正しさではない。

**前段の側は戻した。** 枠 26（種3）を足して noema と z_atomarg は両側で一致
したが、22_parse が割れた。原因は測って分かった ——

> **枠が立つかどうかは、浅いところで決めなければならない。**
> 枠の使用 `zfpu[r, 26]` の書き手に深い場（`zsyf[r]`）を置くと、枠の置き場
> `zfpb`（枠の使用に沿った前置きの鎖）の層が上がり、それを読む `zmib` が
> 置いていかれる。鎖は「全部の枠が決まってから」しか正しい基を出せない。

否定で選ぼうとした二度の失敗も同じ根である（`not zfpu[r,26]` は層を上げ、
`not zamu[zsyf[r]]` は `zsyf` が ⊥ の規則で火を噴かなかった）。

**次の形**（決まった。次はここから）: 枠 26 は `zissy[r]` **だけ**で立てる
（浅い）。法は `zfpmo[r,26]` に置き、口が言わなければ 2^62（番地より大きい
＝畳まない）を既定にする —— `zfpmo` は鎖に入らないので層を上げない。
`flat.py` も同じく「常に種3 の枠を置き、法は W か 2^62」にすれば両側が揃う。
出口条件は一つ: **答えが参照と一致したまま、`fl.ncell()` が桁で落ちること**
（flat 側では既に落ちている）。

### 3.4 上限はデータが言う —— `bound` を値に

`run.lx` の層 48・空間 128・写像 32,768・辺 6,136 は **宣言した定数**であり、「上限を大きく書くのは
測らないことの言い換え」（気づき35）に当たる。18 場が黙って空になった事故（§1.5）はここから出た。

前段は既に `ssz`（空間の大きさ）を表で渡している。同じことを **場の広さ**にも許す:
`field v : max bound nz[]` —— 上端が場の値である区間（1,520 規則が使っている）と同じ形を、
宣言に置くだけである。焼いた側では、場の置き場は場番号に沿った前置きの鎖（気づき17）なので、
**同じ鎖を走行の頭で一度計算する**（`off[f] <- off[f-1] + size[f-1]`）。超えたら終了コード 6。
「一枚の `run.lx` がどの本にも合う」は、これで初めて本当になる。

### 3.5 s は座標であって、ループである —— 符号は形の数に比例させる

層は座標である（`for (s) in 0 .. NS-1`）。C の測定器はこれを **48 枚に展開**し、24.6MB の C になる。
焼いた側で同じことをすれば `gen` の出力も 48 倍になる。

気づき8（座標そのものが位相順序）を焼く側に入れる: 層 s の規則は項では s（同じ層）を、否定と
比較では s−1（閉じた値）を読む。**s 方向のずれは一定符号**なので、s は外側の計数ループで、
内側が一層ぶんの不動点 —— これが BSP の superstep そのものである。参照実装が `_axis_unroll` で
やっている展開を、`gen` は **ループとして出す**。符号は 259 形 × 1 で、NS という数はどこにも要らない
（層の数は前段の表が言う。§3.4）。

### 3.6 一つの不動点、二つの容器 —— ELF と PE は同じ命令面の二つの `render`

答えは配り先に依らない（気づき19）。命令のバイトは x86-64 で同じで、違うのは容器（ELF / PE）と
口（syscall / kernel32）だけである。命令面を一枚の場に置き、**`render` を二つ書く**:
ELF ヘッダの前置きの鎖と、PE ヘッダ + 取り込み表（RVA と thunk —— これも番地に沿った鎖）。
口は四つ（open / read / write / exit）で、前置きの十数命令が替わる。Windows は PLAN §5.3 の
通り最後だが、**設計は最初から「render が二つ」**にしておく —— そうすれば命令の側に
if が一つも入らない。

### 3.7 検査 —— 三つの一致を門にし、家の文法で撒く

- **門は三つの等式**（§1.3）。どれかが崩れたら進まない。`./check` に入れる。
- **撒く文法を「家の形」にする。** `differ` / `trinity` は源の文法で撒く。`gen` の的は形の表なので、
  **259 の形が一つずつ踏まれたか**を数える（例が何本かではなく、形が何個か —— PLAN §5-7）。
- **輪を閉じる等式**（g4d の後継）: `L1 = gen(all.lx)`、`L2 = L1(all.lx, -o)`、**L1 == L2** バイト一致。
  Python も C も居ない輪である。

### 3.8 速さは日程から（第6段。順序だけ書く）

断片の焼いた符号で通った道をそのまま使う: 日程（動いていない規則に入らない —— 4.25 倍）→
融合（同じ反復空間を一つのループに —— 54 倍）→ 表現（`or` は 1 ビット、⊥ が 0 の面は埋めない）→
分割（owner-computes）。**移植するのではなく、家の表がその情報を持っているので、`gen` が読むだけ**である。

---

## 4. 段取り（出口条件つき）

各段に出口条件を付ける。満たすまで次へ行かない。満たせなかったら「満たせなかった」と書いて戻す。
**作業単位は 3 時間**（一回のセッション）。見積りは実測ではない —— 目安として、`front.lx` は
08-29 から 09-05 の 8 日で 66 本まで来た。

### 第A段 —— 一枚にする（Python を輪から外す。参照実装の下で）

| 番号 | やること | 状態（2026-09-06 実測） |
|---|---|---|
| A.1 | `fat`（原子か数か）を前段が出す | **済**（d7f30e2 / a035276 / 367dc76）。前段は `zacl` を既に持っていたので三本で足りた —— 座標・値の項・構成子の引数場。三段の通し運転が原子の本 6 本でバイト一致、BOOKS 66/66、自己適用 1764 規則 完全一致 |
| A.2 | 前々段（bytes → bytes）: `include` / `component`+`use` / `emit` の糖衣を Lattix で | **未着手**。効く本は 8 本（10/11/12/13/14/25/30/32）。`include` は他のファイルを読む＝入出力なので前々段の仕事ではない（配る形では実行ファイルが繋いで渡す）。残るのは `emit`（小さい字面の書き換え）と `use`/`component`（付け替えつきの取り込み）。出す物は「新しい `ch` の表」でよい —— 前段の `for (t,zc) in ch` を書き換えずに済む |
| A.3 | **pass を座標に**: 口 `orc` / `ext` を機械の面の読みに | **骨は済**（b4c72c0 / 13e23fd。`test/twopass.py`）。閉じの器（engine2.close_upto）を使わずに **60/66 が参照と一致**、断る本 2、食い違い 4。置き場を戻すのも前段自身の置き場の場で行う（flat.py の `name_of` を使わない）。残り 4 本の理由は下記 |
| A.4 | 結線を消す: 一枚の本 `all.lx`（front ∪ run ∪ show） | **走った**（2026-09-17。`work/onebook.py` / `test/one.py`）。`run.lx` の表の読みを場の読みに機械的に書き換え、参照実装の下で小さい本が一致。**表を運ぶ Python が一行も無い。** 残りは `show` を継ぐことと、下の「払った代償」 |
| A.5 | 大きな本へ: `self_amin` の nofam 4 規則 → 22_parse → `31_gen.lx` | **未着手**（A.3 の実測が先に一部を答えた: 22_parse / 25_eval は二巡の輪で 600 秒で切れる） |

**A.4 で測って分かった言語の事実（2026-09-17）** —— 表を場にすると何が変わるか:

- **⊥ を繰り返しの上限に置くと、繰り返しは空になる。** だから「行が無い」は
  場でもそのまま「火が消える」になる。番兵の層 999 を場の側に作らずに済んだ。
- **無いことは `not` では測れない —— 0 も偽だからである。** `m[1] <- 0` と
  書いた升に `not m[1]` は真を返す。既定が 0 の列ならそれでよい（0 を重ねる
  だけ）が、既定が 0 でない列（fg の `am`/`wm`、fc の `wm`）では嘘になる。
  **在ることを `>= 0` で立て、それを否定する。**
- **行が在ることを、場では立てて言わねばならない。** 表は「行のある物だけ」を
  なめるので、存在の判断が繰り返しそのものに入っていた。場は宣言した箱を
  なめるので `if Fgu[e]` / `if Fcu[e,i]` / `if zfpu[e,u]` が要る。入れ忘れると
  門の数 `fq` が 1 のはずが 9 になり、**家の寄与が一つも書かれずに静かに空**に
  なった（zov も鳴らない。数えているのは「立った門」であって「在る門」では
  ないから）。
- **列は束ねた変数であって、場ではない。** だから `[` が続く綴りは列ではない。
  面の名前 `vf` と `eg` の列 `vf` を潰して `Egvf[e][s-1, …]` にした事故が
  ここから出た。
- **口は継ぎ目ではなく層である —— ただし素直に繋ぐと輪になる。** 一枚に
  すれば前段の `zorc` を機械の面から読めるはずだが、場の粒度では
  置き場 → 面 → `zorc` → 置き場 の輪になって成層器が棄却する。正しい形は
  **pass を座標にする**こと（§3.1 が自分で書いていた）—— 面と置き場に
  pass 軸 P を足し、pass P+1 の口が pass P の面を読む。巡の数は測ってあり
  （2〜3 巡）、一枚の本ではそれが層になるだけである。
- **表の列は地上の値である。場になると「この形は起きない」を処理系が
  数えられなくなる。** `run.lx` が成層を通っていたのは、`if lat == 8 if vf == 0`
  が **地上のガード**だったからで、`_fires` が「どの行とも合わない」と言えた
  →`_sign_sense` が `sum` の符号を UP に保てた。場になると `Fglat[e] == 8` は
  場の読みなので何も言えず、`sum`/`bag` の面は符号不明（NONE）になり、
  生きた読みが非単調になる。**これが一枚の本で払っている唯一の代償である**
  （下記）。
- **成層器は同じ規則の同じ場を、書き手の数だけ歩き直していた。** 座標の集合は
  (規則, 場) だけで決まるので覚えれば済む（`lattix.py:_stratify_core`）。
  判断は変えていない。一枚の本（前段 2371 規則 ∪ 走らせる物 366 規則）では
  成層だけで数十分掛かっていたのが数秒になった。成層を見る五つの門
  （verify / selfhost / sabotage / certify_all / axisstrat）は通ったまま。

**A.4 が払った代償（数えてある）**: 一枚にすると `sum` / `bag` の面を
**生きたまま読む**寄与三形ずつと、その面を生きたまま問う門 4 形、合わせて
**10 形**が刈られる（`work/onebook.py:prune` が形で声に出す）。刈る判断は
`work/mkrun.py` が表の形でやっているのと同じものである —— **同じ判断が
二箇所にある**（気づき34）。

**ここでも一度、読む前に書いた。訂正する。** 「家の道の `fg` に `la` が無い
のが穴で、`la` を足せば塞がる」と書いたが、`flat.py` を読むと違った ——
**束の違う源は、もう `pa` を通して閉じた面から写している**（flat.py 1293 行:
「束の違う源を、先と同じ束の升に写す。閉じた面から写すので、育つ値を別の束で
読むことにはならない」）。家の道の `am` が同じ束・生きたままなのは、**設計**
であって抜けではない。

**本当のところ**: 刈られた 10 形は「`sum`/`bag` の場が、同じ束の場を
**生きたまま**読んで足す」形である。そして `work/mkrun.py` は `pa` の側では
**同じ理由で既に除外していた** ——

    生きた写しは昇る束だけ（2 max / 3 or / 5 flat / 6 fourv / 9 count）。
    符号の要る束（sum / bag）を生きたまま pa へ写す規則は、
    run.lx 自身が成層できない。

つまり **同じ判断が二箇所にあり、片方（直の読み `am`）だけが緩かった**
（気づき34 の形。四度目）。表の形でそれが通っていたのは、番兵の表のおかげで
`_fires` が「この形は起きない」と言えたからで、**本物の表を入れれば同じ
run.lx も成層できない**。だから一枚にして刈るのは、失うことではなく
**もともと緩かった判断を揃えること**である。

**そして数えた（64 本、`work/t` ∪ `examples`）**:

| | 刈った形を使う本 |
|---|---|
| 寄与 6 形（`sum`/`bag` の vf=1 / vf=4） | **一冊も無い** |
| 門 4 形 | **`04_aggregate.lx` の `(kk=11, lat=8)` 一つだけ** |

つまり一枚の本で払っている代償は、**64 本のうち一冊の、門ひとつ**である。
数え上げた 10 形のうち 9 形は誰も使っていなかった —— 「数え上げた形であって
測った形ではない」がそのまま出た。

（`fl.fsh` は古く、表の中身と食い違う —— x_sumstrata の `fg` には `lat=8` の
行があるのに `fl.fsh` は `[(2,0,0)]` としか言わない。上の数えは `fl.tables()`
から取り直したものである。**宿題**: `fl.fsh` を表から作り直す。`mkrun` と
`baked.coverage` がこれを読んでいる。）

**一枚の本に残っている穴は、口だけである**（2026-09-18。`work/t` の 34 本を
数えた）:

| | 本 |
|---|---|
| 口が要らない | **24** |
| `orc` だけ（上端の値） | 2（x_sumstrata / z_depint） |
| `ext` だけ（合成の場の広さ・法） | 7（w_indir / z_accread / z_atomarg / z_dim2rd / z_nestwm / z_twoctor / z_twoind） |
| 両方 | 1（y_paseam） |

要るからといって落ちるとは限らない —— z_dim2rd と z_nestwm は口を渡さなくても
バイト一致した（その値が答えを動かさない）。**実測でバイト一致 25 本**
（上の 20 本＋ z_clmin / z_cont3 / z_dim2rd / z_nestwm / z_prod）。

**口の `ext` のうち二つは、前段が自分で数えられた**（front.lx に追加）:

- **合成の場の広さ**は「構成する場所 × その点の数」より多くならない（`zcsn`）。
  口が無いと `zfsz0` が ⊥ のままで **箱が立たない** —— 番地は 2^56 に散らばった
  まま書かれ、自分の箱の外へ出て隣を踏む。z_twoctor では `pa` が 507 升中
  **504 升 ⊤** になっていた。箱の広さで畳めば必ず中に入る。狭ければ衝突するが、
  衝突は ⊤ で **声に出る**（気づき44: 黙って隣を踏むより、そちらが正しい）。
- **値の位置の構成（`zcrr`）の枠 24 に法が無かった。** 合成規則（`zissy`）の
  枠 24 には入っていたのに、値の位置には入っていない —— **同じ判断が二箇所に
  あって、片方だけが抜けていた**（気づき34、五度目）。畳まないと番地が生で
  出る（`p[0] = 59870496655506589`）。

それでも `p[0] = pair(, )` —— 引数が空だった。**広さは数えられるが、法は
測るものだった。** 参照実装は「一巡目に触れた番地の集合が衝突しない最小の
2 の冪」を取る（flat.py）—— *走らせてから* でないと分からない。前段にできるのは
**詰め込み率を決めること**だけである: 升の数の 4 倍以上の、最小の 2 の冪
（最低 8）。これは測った値ではなく **設計の定数**であり、外したときは衝突して
⊤ が声に出る。法を升の数そのもの（3）にしていたときは、3 升に 3 つ入れて
衝突していた —— ハッシュ表に詰め込み率が要るのと同じ話である。

**これで構成子の本が通った**: `z_twoctor` ✓（字面 61 バイト）、
`z_atomarg` ✓（62 バイト）。道中でもう一つ: **綴りのトークンに引用符は
入っていない** —— `tag(alpha, 0)` が `tag(lpha, 0)` と出て分かった
（頭を一字落としていた）。**実測でバイト一致 27 本。**

**集合は場にできない。** 面に置かれた集合から元を取り出す言葉が Lattix に無い
（作れるが、数えられない）。機械は arena に整列して置いて番号で引くが、それは
面の外の構造であって規則からは見えない。02_strata が
`unreachable_from_0: ⊥ everywhere` と出るのはそれである。**言語の穴として
数えておく** —— 塞ぐには「面の集合を数える言葉」を足すか、走らせる物が元を
点ごとの側の面に控えるかのどちらかである。

**A.4 の実測（2026-09-17）**: `work/t/*.lx` の小さい 17 本
（a_min / b_3term / c_ge / d_eq / e_ne / f_two / g_sub / k_max / l_or /
m_orguard / n_range / o_ctr / p_nest / q_ctrguard / r_fguard / s_not / u_cross）
＋ z_unit0 / z_mixlat / z_chain が **一致 20 / 食い違い 0**、`zov` は一本も
鳴っていない。一本 17〜69 秒（層 3〜4）。

**ここで一度、私は測らずに書いた。訂正する。** `x_sumstrata.lx` が `total` で
食い違うのを見て「刈った 10 形の代償がそのまま出た」と書いた。**違った。**
刈った 10 形を字面ではなく **形**で言わせると、全部が「同じ面を生きたまま読む
寄与」だった:

    fn  kk=3 lat=8 / kk=3 lat=10 / kk=11 lat=8 op=3 n=0 / kk=11 lat=8 op=5 n=0
    vm  lat=8  vf=1 n=1 / vf=1 n=2 / vf=4        （vb は lat=10 で同じ三つ）

`vf=0`（定数）と `vf=3`（一元の集合）は刈られていない。そして x_sumstrata の
`total` の家の行は `(lat=8, vf=0, n=0)` —— **刈られていない形**である。

**本当の理由は口だった。** `total[] <- i for (i) in 0 .. n[]` の上端 `n[]` は
**閉じた升の値**であり、前段はそれを口（`orc`）から受け取る。一枚の本は口を
番兵のまま渡しているので、空間の点が 521 ではなく 1 になり、`sum` の面が
一升も立たない（`zssz = {0:1, 1:1}`、`vm` が空）。つまり A.4 の食い違いは
**A.3 の残りが顔を出したもの**であって、刈りの代償ではない。

**口が要る本は 34 本中 3 本**（実測）: x_sumstrata（`n[]`=520）、
y_paseam（`(2,0,7)`）、z_depint（`(0,0,3)` と `(0,63,3)`）。残り 31 本は
口が要らない —— 測った 20 本は全部そちら側である。

**刈った 10 形を使う本が在るかどうかは、まだ測っていない。**（`vf=1`/`vf=4` は
「同じ束の場を生きたまま読んで足す」形なので、在りうる。数え上げた形であって
測った形ではないので、数えるまで代償の大きさは分からない。）

v_poly / w_indir は参照実装の解釈実行が 300 秒で切れる ——
**一枚の本の遅さは、参照実装の遅さである**（焼けば別の数になる。第C段）。

**一枚の本の面は、参照実装の標準出力を**バイトまで**作れる**（2026-09-17）:
`LATTIX_SHOW=1 python3 test/one.py` で、測った 20 本すべてが
`python3 lattix.py f.lx` と **バイト一致**（18〜74 バイト）。見せる物
（`show.lx`）はまだ表で食っているので、この段は「面が揃っているか」の測定
である —— 揃っていた。**源のバイトから字面まで、答えの側に Python は居ない。**

**そして見せる物も一枚に入った**（2026-09-18。実測 20/20、字面 18〜74 バイト、
一本 38〜120 秒）。`work/onebook.py` が
`show.lx` の十枚の表も場の読みに書き換え、`LATTIX_SHOW=1 python3 test/one.py`
が **一枚の本が出したバイト列**を `python3 lattix.py f.lx` と直に比べる ——
源のバイトから字面まで、Lattix 一枚。Python は審判だけである。

道中で三つ、やり方の誤りを測って直した:

- **規則は行ではない。** `show.lx` は手書きなので続きの行がある。行で切ると
  規則が割れる —— `isset` の `for (…) in pv` だけ落として `if item[t] == c`
  を残し、`c` が宙に浮いた。**論理規則で切る。**
- **書き手の消えた場は、読んでも正しい。** `if not isnv[t]` は書き手が
  居なければ真で、それがこの段の意味（構成子の値は無い）そのものである。
  書き手が消えたからと読む規則まで落としたら、`num[t,3] <- ival[t] …
  if not isnv[t]` が道連れになって **値が一つも出なくなった**
  （`d[0] = ` だけが出た）。**宣言は残す。**
- **名前は座標である。** `farr` / `nch` / `neg` の三つが前段と綴りで
  ぶつかっていた（同じ綴りは同じ場になる）。見せる物の側を改名した。

字面の表（`topc` / `emp`）は種の場に直したので、**表はもう `ch` 一枚**である
（`orc` / `ext` は番兵一行のまま —— 口が要る 3 本のために残してある）。

`show.lx` を一枚に入れるには前段に一つ足りなかった: **`print` の文を誰も
読んでいなかった。** 33_self は `print` の語（`kwprn`）を知っているのに、
使う規則が無い。**足した**（front.lx、2026-09-17）——

`print f` の `f` は **出現ではない**（出現は `f[` の f）。だから綴りの鍵を
宣言と同じ畳み方で作って宣言と突き合わせる。道中で一つ測って分かった:
**`tch` は 8 文字しか持っていない**（宣言の `dch` は 16）。8 文字で切ると
長い名前（`unreachable_from_0`）が二本目の鍵で食い違うので、綴りは
**文字の流れから直に**拾う（33_self が `och` を作るのと同じ判断）。
実測: `work/t` の 18 本と `16_lex`/`20_lex_full`/`02_strata` で
参照実装の `p.prints` と一致。

残りの表も前段が持っている: `fld`（置き場）は `zlatf`/`zfna`/`zfb`/`zflo*`/
`zfsz*`/`zfs*`、`fnm`（名前の綴り）は 33_self の `dch[d,k]`、`fat` は `zfat`、
`ab` は `zatb`。**show.lx 側の書き換えだけが残っている** —— `pv` は疎なので、
場にすると宣言した升を全部なめる形になる（`for (f) for (c) in 0 .. fvz[f]`。
構成子の規則は `if nof[f] >= 0` で絞れる）。

**A.4 の測り方**: `python3 test/one.py`（`work/t/*.lx`）。層の上限は本ごとに
`fl.strata() + 2` を渡す —— 観測であって経路ではない。足りなければ面が空に
なるので、静かに間違えるのではなく **見えて**違う。

**A.3 が残した 4 本 —— 測って分かったこと**（§3.3 の予想が実測になった）:
構成子の引数場と `_size` は **内容番地が座標**である。一巡目に広さを渡さないと
番地の升が立たず、**立たないものは測れない**ので広さの鎖が伸びない。かといって
「広いが有限」の帯も敷けない —— 番地は 2^56 に散らばるので、noema の 8 枚に帯を
敷くと機械の升の上限（4.4×10^17）を超える。**疎な番地を intern して密にする**
（§3.3: 産地の最初の証人を `min` で拾い、前置きの鎖で番号を振る）まで、
この 4 本は閉じの器なしでは測れない。**他の 60 本には要らない。**

**A.3 で開いたもの（三つとも「黙って落ちる」の顔）**:
- **巡の数は二ではない。広さの鎖の高さである。** 参照は一巡目に 2^62 の帯を
  敷けるので二巡で足りるが、機械の升には宣言した上限がある。だから帯は
  「広いが有限」にして **測って広げる** —— 単調なので止まるところが不動点
  （実測: ほとんど 2 巡、育つ本が 3 巡）。
- **添字なしの場（`n[]`）は前段では一次元の升 0 に居る。** 口を `()` で引いて
  いたので x_sumstrata の `total` が丸ごと落ちていた。
- **静的な広さで一巡目を走らせると、値で決まる座標が黙って落ちる**
  （y_paseam の `form[5..7]`）。落ちたものは測れないので鎖が伸びない ——
  仮の帯はそのためにある。

**第A段の出口**（変わらず）: 参照実装の下で一枚の本が動き、Python は審判以外に
居ない。いま残っているのは A.2 / A.4 / A.5 と、A.3 の 4 本である。

### 第B段 —— 数えて塞ぐ（PLAN 4.1 / 4.2）

| 番号 | やること | 出口条件 |
|---|---|---|
| B.1 | §2.3 の一覧を `all.lx` に対して取り直す（形の表 259 を単位に） | 一覧に「数」と「塞ぎ方」が付いている |
| B.2 | 既知の穴: 二次元の座標書き（347 規則）/ ⊤ の規律 / 源 262,144 → 測った値 | 最小形 `o[p[r,c]] <- src[r,c]` が焼いて一致し、g4d バイト一致のまま |
| B.3 | `run.lx` に要る形の設計を書いて測る: set/bag/and/fourv（測定器の arena 方式を Lattix で）/ 三次元 / 番地の密化（§3.3、22_parse の P を測る）/ `bound` を値に（§3.4）/ s をループに（§3.5） | 各形に「参照実装の下で一致する試験」が一本ずつある |

見積り: 4〜6 単位。

### 第C段 —— `gen.lx`（`31_gen` の前半を家の表に書き直す。§3.2）

| 番号 | やること | 出口条件 |
|---|---|---|
| C.1 | 形の表を `run.lx` と共有（`shapes.json` → `run.lx` と `gen.lx` の両方） | 一形ごとに「走らせた答え ＝ 焼いた答え」（`differ`） |
| C.2 | 六部（頭・ガード・座標・join・階数・前進）を `fg/fc/fp/mp/spc/ssz` から出す。`31_gen` の後半（命令面 → ELF）はそのまま | `a_min` の家 → 実行ファイル → 参照と一致 |
| C.3 | s のループ・データ上限・intern・十束 | `run.lx` の 259 形が全部踏まれ、examples 34 本 + noema + 撒いた 150 本で一致 |
| C.4 | 輪を閉じる: `L1 = gen(all.lx)`、`L2 = L1(all.lx)` | **L1 == L2 バイト一致** |

見積り: 12〜18 単位。いちばん大きく、いちばん危ない（§5-1）。

### 第D段 —— 焼いて閉じる（PLAN 4.4）

| 番号 | やること | 出口条件 |
|---|---|---|
| D.1 | `lattix` ＝ `gen(all.lx)`。`./lattix f.lx` が表を読んで答えを出し、`show` の字面で出す | Linux で `./lattix noema.lx` ≡ `python3 lattix.py noema.lx`（examples 34 本 + noema + 150 本、バイト一致） |
| D.2 | Python も C も無い箱で測る | 走る |

見積り: 3〜5 単位。**ここで §0 の Linux 半分が閉じる。**

### 第E段 —— 包む（PLAN §5）

5.1 木を整える → 5.2 Linux（tar / deb）→ 5.4 使い方（argv・終了コード）→ 5.5 まっさらな箱 → 5.3 Windows（PE + kernel32、§3.6 の二つ目の `render`）。
見積り: Linux 3〜5 単位、Windows 6〜10 単位。

### 第F段 —— 速さ（§3.8）

順序だけ決めてある。数は測ってから。

**合計の見積り: 35〜55 単位（3 時間 × 35〜55）。実測ではない。**

---

## 5. 危なさ（先に書く）

1. **`gen.lx` の大きさ。** 前半の書き直しは 2,687 行の半分に当たる。全部書いてから合わせるのではなく、
   **一形ずつ**足して一形ずつ `differ` で測る（PLAN 4.3 の「一つ足すごとに」）。順序は `a_min` に要る形から。
2. **intern の O(P²)。** 22_parse の産地の数 P を測ってから桶の幅を決める。測る前に設計しない。
3. **宣言した定数の上限**（層 48・空間 128・写像 32,768）は §1.5 の事故をもう一度起こす。
   第B/C段で値にするまでは、超えたら **断る**（黙って空にしない）ことだけ先に入れる ——
   **入れた**（05a634c。`zov` の八つの口。層を上限に押し上げた表で立つことを確かめた）。
   ただし空の表の番兵（層 999）が四箇所に書いてある（気づき34 の形）。番兵を
   「本物の個数」にする直しは宿題である。
4. **8GB の箱。** C の測定器は 5.7GB で落ちた。`gen.lx` は Lattix なので C を経ない（断片の焼きは
   209KB の源で 1.34 秒だった —— 気づき47）。測定器を使うのは差し合わせだけにする。
5. **前段の走行時間。** 22_parse 以上の本と `31_gen` 自身を前段に読ませる時間は **未測**。
   A.5 で測る。時間で切れるなら原因は上限（zdat/zdrt/zdsr の bound）か日程で、答えではない。
6. **文書。** この文書と `HANDOFF.md` は消す対象である。§4 を `PLAN.md` §3 に写して一つに戻す。

---

## 6. 次のセッションの最初の三手

1. **A.5 の頭**: `self_amin` の nofam 4 規則を家にする（`/tmp/machine_check.py` が nofam を出す）。
   一致 3 を「全部家」にする。
2. **A.1**: `fat` を前段から。小さく、一本で閉じる。
3. **A.3 の最小形**: `a_min` で前段 p=0/1 と機械 p=0/1 を一枚にし、`orc` が `v[p=0, s, ·]` の読みで
   置き換わることを **一本の本で**確かめる。ここが通れば閉じの器は要らなくなり、第A段の残りは写しである。
