# Lattix —— 進捗の詳しい報告と、§0 までの計画（2026-09-05）

> **仮の文書。** `PLAN.md` §6「いまここ」と `HANDOFF.md` を置き換えるために書いた。
> 採るなら §4 を `PLAN.md` §3 に写し、この文書と `HANDOFF.md` は消す（文書は四つに戻す）。
> 数は全部 **実測**（処理系が出した数）か、**「見積り」と明記した数** のどちらかである。混ぜない。

---

## 0. 的（写す。言い換え禁止）

> **自由に書かれた `.lx` を、Windows / Linux にインストールされた Lattix だけで
> 実行できるようにする。Python も C も要らない。最終形は配れる package である。**

物差しは一つ —— *参照実装 `lattix.py` と同じ答えを出す `.lx` の範囲*。

`PLAN.md` §2 が形を決めている: `lattix` ＝ `front.lx`（源のバイト → 規則の表）＋
`run.lx`（規則の表 → 最小不動点）＋ `show.lx`（答え → 字面）。三つとも Lattix で書き、
焼くのは `31_gen.lx`（Lattix で書かれた x86 生成器）である。gcc は道ではない。

---

## 1. 報告 —— 何が動いたか（実測）

### 1.1 一週間の縦の列

git の履歴は 08-25 に始まり 90 commit。`front.lx` の一枚目は **08-29** である。
以下は「動く物が増えた」ことだけを数えた列（説明の言い換えは数えない）:

| 日 | 増えた物 | 測った値 |
|---|---|---|
| 08-28 | 多面体の道（`dat/spc/ssz/mp/fg/fc/fp`）—— 反復空間を点に潰さずに機械へ渡す | `a_min` の辺 4 本 → 1 本 |
| 08-29 | 層は座標である（軸に沿った成層）。`run.lx` を s 軸の圧縮形に | 1,270,165 → 89,949 バイト |
| 08-29 | 三面鏡（`trinity`）—— 言語に自分のバグを探させ、形の一覧を定義に刈らせる | |
| 08-29 | **`front.lx`** 一〜三枚目（軸と空間 / 置き場と写像 / 広さを測る）、noema・16/20/21_lex・29_dataflow・17_render | 20 → 39 本一致 |
| 08-30〜31 | 割り算の種族・`is`・一元の集合・間接の畳みの一般化・構成子の入れ子（深さも座標） | 43 → 48 本 |
| 08-31 | **二段の通し運転**（源 → `front.lx` → 表 → `run.lx` → 答え）—— 表の経路に Python が居ない | 48 本全問一致 |
| 08-31 | 閉じる（口 `orc` の値）のも C の測定器で | |
| 09-02 | 22_parse（883 規則）・25_eval（913 規則）完全一致、依存する区間 ＝ 矩形 × ガード、PLAN 2.5（include / use / emit / source / budget）、24_space、33_self の自己適用（408 規則） | 54 → 59 → 66 本 |
| 09-03 | **`show.lx`**（第3段）。三段の通し運転が `python3 lattix.py` の標準出力と **バイト一致** | 37 本 |
| 09-03 | 04_aggregate（生きた sum/count の比較）・誤りの報告（07/11 は前段も同じ行で断る） | BOOKS 66 本 |
| 09-03〜04 | **自己適用**: 前段が自分の源を読む。疎の場を外す → 層の混ざり（閉じた max と生きた min）→ 前段を「自分が家になる形」に → **完全一致** | 1,761 規則・534 枠 |
| 09-04 | 学びの輪（自己適用の一巡） | 3.5 時間 → 約 30 分 |
| 09-05 | **機械が前段を走らせた** —— `run.lx`（層 48）が `front.lx` 自身の家の表を走らせる | 899 場すべて一致・31 秒 |
| 09-05 | **前段が機械の上で本を読んだ** —— 前段を機械の形（閉じの器）で最後まで走らせ、`a_min.lx` を読ませる | 899 場すべて一致 |

### 1.2 `PLAN.md` §3 の段に照らした現在地

| 段 | 出口条件（PLAN） | 状態 | 測った値 | 出口に足りない物 |
|---|---|---|---|---|
| 第1段 `run.lx` | 全 34 本 + noema + 撒いた 150 本で升まで一致 | **ほぼ ✓** | 十束・259 規則・340 行・層の上限 48。BOOKS 66 本で一致か正しく断る。通し運転 63/63（07/11 は断る本） | 31_gen / 32_shape は未（前段が読めない、§2.3）。撒いた 150 本は家の `run.lx` で撒き直していない（**未測**）。層 48・空間 128・写像 32768 は宣言した定数（§3.4） |
| 第2段 `front.lx` | 表が Python の表と完全一致。**Python は表作りから外れる** | 一致は ✓、外れるは **未** | 4,469 行（畳むと 5,801 行・2,355 規則・899 場・38 層）。66 本一致。自己適用 完全一致 | 字面の前処理・口 `orc/ext`・`fat`・結線が Python（§2.1）。31_gen（2,687 行）を読めていない |
| 第3段 `show.lx` | 標準出力がバイト一致 | ✓ | 430 行・177 規則。37 本一致（print の無い本・render の本は外） | `fat` を前段が出す。深さ 8 の打ち切り。render の本 |
| 第4段 焼く | `./lattix noema.lx` が参照と同じ答え | **未着手** | 4.1「読んで数える」の初版がこの文書 §2.3 | 全部 |
| 第5段 包む | まっさらな箱で走る | 未着手 | | 全部 |
| 第6段 速さ | | 測定器の上でだけ | | |

### 1.3 三つの一致 —— 自己適用が何を言ったか

自己適用は **三つの別々の等式**として成り立った。それぞれが別のことを言っている。

1. **front(front) ＝ flat(front)** —— 焼いた前段が自分の源（`work/self_front.lx` ＝ 33_self + fold + front.lx）を読んで出す家の表（`dat/spc/ssz/mp/fg/fc/fp`: 1,761 規則・534 枠）が、参照 `work/flat.py` の表と **中身まで一致**。→ 前段の文法は、自分を書けるだけ広い。
2. **run(front の表) ＝ 参照** —— 機械 `run.lx`（層 48）が前段の家（1,761 規則・16,395 写像・4,772 枠）を走らせ、**899 場すべて**が参照実装の答えと一致（31 秒）。→ 機械は前段を走らせられる。
3. **front の上で本を読む ＝ 参照** —— 前段のプログラムを機械の形（閉じの器 ＝ `run.lx` と同じ形の C）で最後まで走らせ、`a_min.lx` を読ませた表が **899 場すべて**一致。→ 「前段を機械で走らせて本を読む」は成り立つ。ただし flat 側で 4 規則が家にならず点の道に落ちた（未特定）、flatten に 1.9 時間。

**測ったこと（09-06）**: 上の三つは `zfat` の三本を足しても保たれている ——
自己適用 1764 規則 完全一致 / BOOKS 66/66 / 通し運転 6 本バイト一致。

まだ無い **四つ目**: 前段（機械の上）→ `run.lx` → `show.lx` の通し運転が、**Python の結線なしに一枚の本として**回ること。これが §4 第A段の出口である。

### 1.4 速さ（実測）

| 何 | 前 | 後 |
|---|---|---|
| 学びの輪（自己適用の一巡） | 3.5 時間 | 約 30 分（flat 側 17 分 + front 側 10 分） |
| 閉じの器の焼き | 層ごとに gcc（36 層 × 二周、毎回） | 走行内で一度。表は焼いた後に差し替え、二周目以降は焼かない |
| 41MB の C（閉じの器、層 38 に展開）を焼く | gcc 5.7GB・10 分（8GB の箱で落ちる） | clang 2.1GB・61 秒（出力は同じ —— 比べた） |
| 前段の C の走行 | 30 分（層 18 が 424 回、層 21 が 735 回 掃いていた） | 10 分弱（SCC ごとの不動点、位相順） |
| 前段の全場の読み出し | Python 6GB で死ぬ | 突き合わせが読む 58 場だけ print |
| `run.lx`（層 48）の焼き / 走行 | — | 焼き 465 秒（参照の成層 15k 規則の Python が 9 割）/ 前段の表を 31 秒。C は 24.6MB（階数なし） |
| 二段の通し運転 | — | 22_parse 5.1 秒、33_self 3.4 秒 |
| 前段が機械の上で `a_min` を読む | — | flatten 6,793 秒（閉じの器を規則ごとに呼ぶ）。答え 3 秒 |

箱は 2 コア・8GB。cc1 は 5.7GB で殺される。これは **箱の広さ**であって正しさではないが、「焼ける大きさも資源」（HANDOFF）である。

### 1.5 言語が自分で見つけた穴（このセッション）

全部「突き合わせで食い違った」から出た。読んで見つけた物は一つも無い（気づき49: 二つ目の実装は試験より強い試験）。

| 穴 | 出た所 | 前は | 直し |
|---|---|---|---|
| 閉じた max と生きた min の比較・引き算（層が混ざる） | 自己適用 `zfsz0 <- zexth - zextl + 1` | **黙って違う答え** | 種11 に束1（min）と `<=`/`<`、値は面から直に読む vf 6、front は zbxl/zoppl |
| 点の道の `tolat` が生きた他束を閉じた面から写す | 同上 | 黙って違う答え | 声に出して断る |
| ⊤ の番兵 2^62 が 23_big の `pow2[62]` と衝突 | 通し運転 63 本 | ⊤ と本物の値が同じ字面 | 番兵を INT64_MAX に |
| `run.lx` の層の上限 16 ＜ 前段の 38 層 | 機械が前段を走らせた | **18 場が黙って空** | 層 48（形は増えていない）。上限は §3.4 で値にする |
| 一文のガードが 8 を超えると前段が黙って落とす | 自己適用 | 黙って落ちる | 文を割る + `zerr` 種2 で断る |
| 前段の疎の場 `ztdk`（名前の鍵を座標にした二次元） | 自己適用の一巡目 | 一巡目の広さが 2^126 → 閉じが解釈に落ちて終わらない | 表ごとに引く。**前段に疎の場を置かない** |
| 正規化の regex が `b * 24 + k` を `b * k + 24` に | BOOKS 66 本 | 黙って違う表 | 戻して個別に |
| 引数の数を `hasw2`（注釈の宣言でも真）で見ていた | 自己適用 | 黙って違う次数 | 使われ方の次数 zfusd/zfvol |

---

## 2. §0 に足りない物（数えた）

### 2.1 Python が輪の中に居る場所（六つ）

| # | 場所 | 何をしている | 消し方（§4 の番号） |
|---|---|---|---|
| 1 | `test/front.py` の `expand_uses` / `desugar_emit`、`lattix._expand_includes` | `include` / `component`+`use` / `emit` の糖衣を **字面**で剥く（参照実装と同じ判断） | 前々段（bytes → bytes）を Lattix で。33_self の道具で書ける **[A.2]** |
| 2 | `work/flat.py` + `work/engine2.close_upto`（C の測定器） | 口 `orc`（閉じた値）と `ext`（値で決まる座標の次元の広さ）を前段に渡す | 機械の面から読む —— **pass を座標に** **[A.3]** |
| 3 | ~~`test/show.py`~~ | ~~`fat`（原子か数か）を **答えから**作っている~~ → **済**（d7f30e2 記述から / a035276 前段 zfat の三本 / 367dc76 通し運転が前段の物を使う）。残るのは写しの伝播（コーパスに一本も無い） | — |
| 4 | `test/pipeline.py` | 結線: 写像の番号詰め（compact）、番兵、表の受け渡し、前段 ↔ 機械の交互（BSP の継ぎ目） | 一枚の本にする **[A.4]** |
| 5 | `work/mkrun.py` + `work/shapes.json` | `run.lx` を形の一覧から書く（開発時に一度、~20 分） | 残してよい（配布物には入らない）。ただし形の表を `gen.lx` と共有する **[C.1]** |
| 6 | `lattix.py`（参照実装） | 審判 | 残す。配らない（PLAN §4-1） |

### 2.2 C が輪の中に居る場所

`front.lx` / `run.lx` / 閉じの器は `runtime.py`（C の測定器）で焼いて走らせている。§0 の道では
**`31_gen.lx` の後継が焼く**。測定器は差し合わせ（気づき49）のために残す。

### 2.3 焼く物の数え上げ（PLAN 4.1「読んで数える」の初版）

三つの本を **読んで数えた**（`work/self_front.lx` ＝ 畳んだ前段、`run.lx`、`show.lx`）:

| | `front.lx`（畳んだ源） | `run.lx` | `show.lx` |
|---|---|---|---|
| 行 / 規則 / 場 | 5,801 / 2,355 / 899 | 340 / 259 / 42 | 430 / 177 / 84 |
| 束 | max 587・or 253・min 33・flat 17・count 5・sum 4 | **十束ぜんぶ**（max 21・flat 7・count 5・or 3・min/sum/and/fourv/set/bag 各 1） | max 66・or 17・count 1 |
| 場の次元 | 1: 703、2: 196 | 1: 19、2: 21、**3: 2** | 0: 18、1: 36、2: 26、**3: 3、4: 1** |
| 層 | 38 | s は座標（0..47、`for (s)` が 236 箇所） | 少 |
| 表 | `ch` / `orc` / `ext` | `dat spc ssz mp fg fc fp ate ix eg cd`（点の道 `eg/cd` も同居） | 12 枚（`fld fnm atm pv prn ab fat ps ctr cac topc emp`） |
| 演算 | + 883・− 533・* 415・% 24（法は M1/M2 の二つ。**2^n の除数は 0**） | + − * / % | / % 2^32（34_decimal の技） |
| ガード | cmp 2,849・肯定 1,946・not 552 | == 756・>= 81・<= 45・< 13・> 10・!= 6・not 14 | == 19・<= 41・>= 40・not 56 |
| 値が上端の区間 | 1,520 規則 | | |
| 書き先が二次元で座標に読みを持つ規則 | **347**（一次元だけに読み 218・両方 129。二次元の書き先は 766） | 多数（`v[s, rz[..]]`） | |
| 入れ子（`for` の本数） | 最大 3（19 規則） | 最大 2 | |
| ガード（一文あたり） | 最大 8（6 規則） | | |
| 項（一文あたり、概算） | 最大 5 | | |
| 名前の長さ | 最大 7 | | |
| 源の大きさ | **308,172 バイト（注釈を除く）/ 353,942** | 108,385 | 26,978 |

これを **いまの断片**（`33_self.lx` + `31_gen.lx` が読める形。上限は気づき44 で「一つ上を打って」確かめた物）に当てると:

| 断片の上限 / 性質 | 三つの本の実測 | 判定 |
|---|---|---|
| 束は六つ（min / max / or / flat / sum / count） | front・show は六つの中。**`run.lx` は十** | `run.lx` に set / bag / and / fourv が要る（§3.2） |
| 入れ子 3（rcx / r13 / r14） | 3 | 通る（縁） |
| 項 8 / ガード 8 | 5 / 8 | 通る（ガードは縁。超過は `zerr` 種2 で断る） |
| 名前 16 文字 / 宣言 1,024 | 7 / 908 | 通る（宣言の余りは 116） |
| 源 262,144 バイト | **308,172** | **超える**。宣言と置き場を測った値に（気づき44: 上限は二箇所に書くな） |
| 座標に置ける読みは一次元だけ（SPEC 既知の穴。PLAN 4.2） | **347 規則が踏む** | **塞ぐ必要**。最小形 `o[p[r,c]] <- src[r,c]` は既にある |
| `mul after add` | 7 箇所 | 書き換えで消える |
| 三次元の場（次元は行になったので上限は消えたはず —— 打って確かめる） | `run.lx` に 2、`show.lx` に 3（四次元 1） | 要る。気づき44 の流儀で「一つ上を打つ」 |
| 升の番号 4.4×10^17（内容番地を座標にする場）| `run.lx` の値の面 11 枚 | 密配列にできない → §3.3（intern）か hash |
| ⊤ の規律（比較は ⊤ で偽・⊤ は座標になれない） | front の flat 17 場・機械の flat 面 | `31_gen` に無い（既知） |
| 値が上端の区間 | 1,520 規則 | 断片にある |
| 層・否定 | 38 層 | 断片にある |

**読み**: 前段の *構文* は断片からそれほど遠くない（束・入れ子・項・ガード・名前は全部通る）。
遠いのは三つ —— **二次元の座標書き**（347 規則）、**源の大きさ**、そして **`run.lx` の側**
（十束・三次元・内容番地の広さ・s 軸）。§3.2 の道を採ると、数える対象は「断片の構文」から
「家の表の形」（`run.lx` の 259 規則 ＝ 有限）に変わる。

### 2.3b 焼く物の数え上げ（**実物**で数え直した。2026-09-18）

§2.3 は front / run / show を別々に読んで数えていた。いまは `all.lx` が在るので、
**焼く物そのもの**を数えた（`work/onebook.py` で組んだ a_min の一枚）:

| | 値 |
|---|---|
| 源 | **489,457 バイト**（33_self の置き場 262,144 の 1.87 倍） |
| 規則 / 場 / 表 | 2,889 / 1,072 / 3（`ch` / `orc` / `ext`） |
| 束 | or 285 / max 712 / min 35 / flat 21 / count 9 / sum 6 / **and 1 / fourv 1 / set 1 / bag 1** |
| 次数 | 0次 3 / 1次 804 / 2次 259 / **3次 5 / 4次 1** |
| 源の種類 | 表 `ch` 712 / 区間 3,291 / `orc` 2 / `ext` 6 |
| ガードの数 | 最大 8（0 が 288 本、2 が 805 本） |
| 繰り返しの深さ | 最大 4 |
| 演算子 | `+` 3265 / `>=` 1926 / `*` 1846 / `==` 1172 / `-` 702 / `%` 646 / `/` 609 / `<=` 397 / `!=` 53 / `<` 12 / `>` 7 |

**数えたら、壁は思っていたより小さかった。**

- **④ 三次元以上は 6 場だけ**だった（`esame` 4次、`fn`/`fo`/`ech`/`eless`/`dgt`
  3次）。そして **後ろの軸の広さを掛けて足せば二次元になる** —— 座標の意味も
  升の数も変わらない（積が同じ）。`work/onebook.py:fold3` で畳んだ:
  **三次元以上の場は 0、字面はバイト一致のまま**。**④ は閉じた。**
- **③ 断片に無い束は 4 場だけ**（`va` / `v4` / `vs` / `vb` の面が一つずつ）。
  集合と bag を外せば残るのは `and` と `fourv` の二束である。
- **⑤ 源の置き場は壁ではなく費用だった。** 33_self は 5,470 万升（8 バイト/升で
  **0.44 GB**）を宣言している。置き場を倍にしても **0.9 GB** 程度である。
  しかも **源の置き場は二つある** —— *配る* 物は 262,144 でよい（人が書く本の
  大きさ）。1M 要るのは *焼く*（bootstrap の）側だけである。

### 2.3c 第C段を **実際に叩いた**（2026-09-18）

数えるだけでなく、`./lattix` に一枚の本を食わせた。分かったことを順に:

1. **焼いた実行ファイルが読める表は一つだけ**である（`./lattix` で焼いた本を
   走らせると `reason 6: need one table`）。番兵のまま残っていた `orc` / `ext` を
   落として、**読む表は `ch` 一枚**にした（`onebook.build(oneport=True)`）。
2. **断片の六束（min/max/or/flat/sum/count）に絞れる。** 断片に無いのは
   `and` / `fourv` / `set` / `bag` の四つで、一枚の本ではその面が一つずつしか
   無い。落としても答えは変わらない（`six=True`。43 行）。
3. **三次元以上は畳めば消える**（上記 ④）。
4. これで一枚の本は **512,952 バイト / 表 1 / 六束 / 二次元**になった。
   33_self の源の置き場を 262,144 → 524,288 にすれば **入る**（97.8%）。
   置き場を倍にした焼き手（`lattix-big`）は自分で焼けて、小さい本も焼ける。

そして **次の壁が出た。31_gen 自身の上限である**:

| 31_gen の箱 | 場の数 | 一枚の本の要求 |
|---|---|---|
| `bound 1024`（場ごとの配列: `fbot` / `fsz` / `foff` …） | 13 | **場 1,068** |
| `bound 2048`（規則ごとの配列） | 58 | **規則 2,889** |
| `bound 131072` | 7 | 命令の数（未測） |

**そして超えても言わない。** `rc=0` で **23 バイトの ELF の頭だけ**が出る ——
黙って空を返す、この企画がいちばん嫌う壊れ方である（気づき44 の形。
`zov` のような見張りが 31_gen には無い）。上限を 16384 に上げると今度は
**segfault**（記憶は 1.07 GB → 2.15 GB。容器は 7 GB なので足りている）。

**そして 31_gen は、もう言っていた。** 符号面の註にこう書いてある ——

> 131072 でも足りなくなった —— 形の導出が断片に入った日に、三度目。
> **踏んだことは黙って起きる**ので、今度は言うようにした ——
> 符号の長さを ELF ヘッダの空き（8..10 バイト目）に書く。読む側が確かめられる。

23 バイトの頭にもその三バイトは入っていた。読んだら **`codelen = 7,864,638`**
——符号面 131,072 バイトの **60 倍**である。一枚の本（規則 2,889）は約 8MB の
機械語になる（1 規則あたり 2,700 バイト。多面体のループ入れ子と座標の式）。
**見張りは要らなかった。書いてあったのに、誰も読んでいなかった。**

**それでも見張りは入れた**（31_gen 末尾、理由 0「箱が足りない」）。`nfl`（場）/
`now`（所有者）/ `nrl`（規則）を `cap*` と比べ、超えたら既にある断りの口
（`bad` / `badk` → 標準エラーに一行）に流す。上限は **一箇所で言う**
（気づき33）—— `cap*` が宣言の `bound` と揃っている。

ただし **符号の長さは、ここでは言えない**。断りは命令を書く（`form[0..7]`）
ので、`codelen` を見て断ると 符号 → 長さ → 断り → 符号 の輪になる ——
処理系が正しく棄却した。だから長さは頭に書いて **読む側が確かめる**。
そして見張りは **符号面が既に溢れているときには勝てない**（断りの命令列と
通常の命令列が同じ `form` を取り合い、`not bad[0]` の抑止が無い）。
これは第C段の宿題である。

**見張りは、最初の問いで自分を捕まえた。** `lattix.lx` は規則 **2,406** 本で、
規則ごとの箱は `bound 2048` だった —— **配っている焼き手が、自分の箱を 358 規則
ぶん超えて走っていた**。g4d はバイト一致していたので、踏んだ先がたまたま
無害だっただけである。箱を 4096 にして、組み直して焼き直したら **g4d は
バイト一致のまま**（`lattix` を入れ替えた）。

**そして見張りは一度、狼を呼んだ。** 所有者の数（`now`）を *場*の箱（1024）と
比べていた —— 所有者の配列は `bound 8192` である。正しい本まで断った。
**見張る箱は、推測せずに読む。**

**そしてもう一つ、bootstrap の輪が出た。** 面を大きくした焼き手を焼こうとすると、
**その焼き手自身の符号が 415,878 バイト**になり、いまの焼き手の 131,072 に
収まらない。**大きい焼き手は、大きい焼き手でないと焼けない。** 出口は
C の測定器である（気づき49: 二つ目の実装は試験より強い試験）—— 測定器は
符号面を持たないので、大きい焼き手をそのまま走らせて「一枚の本は焼けるのか」
を測れる。

### 2.3d 一枚の本を、Lattix の生成器が焼いた（2026-09-18）

C の測定器で大きい焼き手を走らせ、一枚の本（512,874 バイト / 表 1 / 六束 /
二次元）を食わせた。**走り切った** —— exit 0、2 巡、190 秒、438M の join。
出た面を組み直すと **`\x7fELF` で始まり、`codelen = 90,025`**。

（前に読んだ `codelen = 7,864,638` は **溢れた焼き手が書いた頭**だったので
数字自体が壊れていた。箱を足りるようにしたら、一枚の本の機械語は
**90KB** である —— 2,889 規則で 31 バイト/規則。）

**面の箱は詰めなければならなかった。** `run.lx` は面を
`bound 48 437671048959211563` と宣言している（内容番地が座標だから。コーパス
全体で測った値）。断片は hash 表を持たないので、4.4×10^17 升は敷けない ——
焼いた物は segfault した。だが **一枚の本では番地は法で畳んである**（`zamw`）
ので、升の番号は前段の置き場に収まる。`nc=65536` に詰めても **字面は
バイト一致のまま**だった。*内容番地を畳む仕事が、焼けるかどうかを決めていた。*

**まだ走らない。** 焼けた ELF の `filesz` が 0 だった —— 31_gen はそれを
**三バイト**で書くので、`total = 16,777,216`（2^24）は切り捨てられて 0 になる。
2^23 にして焼き直しているところである。

### 2.4 その先

Windows（PE/COFF の容器と kernel32 の口）、package（tar / deb / zip）、速さ（第6段）は未着手。
既知のことは PLAN §5.3 の通りで、ここに足す物は §3.6 だけ。

---

## 3. 革新の方向 —— Lattix にしかできない形で的へ近づく

七つの組み合わせ（CALM / Lucid の座標 / LVars の freeze / 熱帯半環 / BSP / 多面体 / CRDT）に戻る。
以下は「新しい機能」ではなく、**既に宣言されているものを捨てずに機械へ渡す**ための六つである。
どれも **まだ測っていない**（この節の数は全部見積り）。

### 3.1 一つの不動点 —— front ∪ run ∪ show を一枚に。pass は座標である

いまの通し運転は Python が **BSP の継ぎ目を運んでいる**: 前段の一巡目（仮の広さで数える）
→ 閉じの器（閉じた値）→ 前段の二巡目（測った広さ）→ 機械 → 見せる物。
運んでいる物を覗くと、表を作って次に渡しているだけで判断は無い（気づき36: 判断が無いなら畳める）。

畳み方は一つで足りる —— **pass を座標にする**。前段の場を `zdat[p, …]`（p ∈ {0, 1}）にし、
機械の面を `v[p, s, …]` にする。すると

- 機械 p=0 は前段 p=0 の表を読み、前段 p=1 は機械 p=0 の面を読み、機械 p=1 は前段 p=1 の表を読む。
  輪は無い（p が層である）。**一巡目・二巡目は別の走行ではなく、同じ不動点の層 0 と層 1** である。
- 口 `orc`（閉じた値）は **層 s−1 の読み**そのものになる。閉じの器（`engine2.close_upto` 601 行 +
  hints + キャッシュ、いちばん遅い所 1.9 時間）は **消える**。閉じるのは機械の仕事である。
- 口 `ext`（値で決まる座標の次元の広さ）も同じ —— 機械 p=0 が触った升の最大は、機械の面の
  `max` 一つで出る。
- 結線（compact・番兵・受け渡し）は、同じ本の中の場と場なので **要らない**。

出口は測れる: `python3 lattix.py all.lx`（一枚の本 + 表 `ch`）の標準出力が
`python3 lattix.py f.lx` と **バイト一致**する。これは第A段の出口であり、
**§0 の「Lattix だけで」の意味を、参照実装の下で先に成り立たせる**手である。

### 3.2 形の表は一つ、配り先は二つ —— `run.lx` は走らせ、`gen.lx` は符号にする

`run.lx` は「言語の形の数 × 層」で書かれた一枚で、プログラムの大きさに依らない（259 規則）。
`31_gen.lx` は旧断片の表（`rrule/grd/trm/crd`）から x86 を出す 2,687 行で、家の表を知らない。

気づき15（記述は二度使われる）を **機械そのものに**当てる:

> 家の表（`fg/fc/fp/mp/spc/ssz/dat`）を読む物は二つあってよい。一つは **join する**（`run.lx`）、
> もう一つは **join する命令を書く**（`gen.lx`）。二つは同じ形の表（`work/shapes.json`）から生まれ、
> `differ` が「走らせた答え ＝ 焼いた答え」を測る。

これで三つが同時に起きる:

- **断片が消える。** 「31_gen が読める構文」という第二の文法が要らなくなる。`gen.lx` の的は
  `run.lx` の 259 の形であり、**有限で、読めば数えられる**（§2.3 の表の下半分がそのまま一覧）。
  fp 種 0..11、fc 種 1..6/11、vf 形 0..6、十束 —— 一形ずつ足し、一形ずつ差し合わせる。
- **多面体をそのまま機械に渡す。** `spc` の一行（基・幅・歩幅）は計数ループ一つ、`mp` の一行
  （定数 + Σ係数·軸 + 係数·pa）は `lea/imul/add` の鎖一つ。合法性の証明は要らない（答えは順序に依らない）。
  C の測定器 `engine2.engine_text` が既にこの翻訳の試作である —— C を出す代わりに命令面を出す。
- **`31_gen` の後半は残る。** 命令面 → バイト → ELF（前置きの鎖、argv、終了コード、知らせの八命令）は
  そのまま。書き直すのは前半（規則の表 → 六部）だけである。

`lattix` ＝ `gen(front の家)` ⊕ `gen(run の家)` ⊕ `gen(show の家)`。三つの家の表は **既に手元にある**
（自己適用の一致 1 がそれである）。利用者のプログラムは §2 の通り `run` が走らせる —— `gen` は
三つの本を焼ければよく、速い道として利用者の本を焼くのは「通る形だけ」の選択である。

### 3.3 疎な座標は intern の不動点で密になる —— **測った**（2026-09-06）

内容番地を座標にする場がどれだけ疎かを測った（`fl.lay` の帯と、参照の答えが
使った升）:

| 本 | 場 | 帯 | 使った升 |
|---|---|---|---|
| 35_noema | `_size` | 64,376,966,080,669,152 | **20** |
| 35_noema | `because_binary_left` ほか 7 枚 | 約 5×10^16 ずつ | 4〜8 |
| 15_parse | `_size` | 68,895,855,717,684,665 | **9** |
| 15_parse | `node_left` / `node_right` / `node_sym` | 34,833,350,933,040,860 | 4 |
| z_twoctor | `_size` / `pair_x` / `pair_y` | 61,481,115,305,955,602 | **3** |
| 16_lex | `cons_head` / `cons_tail` | 66,867,314,477,203,863 | 4 |

**升の総数は noema で 424,170,182,318,650,658。使っているのは 64 升である。**
`run.lx` が升の上限を 4.4×10^17 と宣言しているのは、この一事による。そしてこれが
四つの壁の同じ顔である —— A.3 の残り 4 本 / 焼く側が密配列を持てない（C の
測定器は hash 表に落ちる）/ 機械の升の上限が大きい / 第C段で ELF に置けない。

**どこを直せばよいかは一行で言える。** 内容番地は `flat.py` の `pctor` の
最後の一行、**fp 種2 の枠**（`h1·M2 + h2`）である。種2 の枠は内容番地
「だけ」であり、その枠の数は **静的に数えられる**（枠 × 空間の点の数）。

機械の側の形（気づき22「記号表は不動点である」の三行と同じ）:

```
pr[s, p]        種2 が書く生の番地（いまの pa[s, p]）
PU[p]           種2 の枠（産地。pb + t で密である）
RP[s, p] : min  同じ番地の最初の証人   ← pr[s,p] == pr[s,q] の比較
NM[s, p]        「代表が p 以下にいくつあるか」の前置きの鎖
pa[s, p]        = NM[s, RP[s,p]] − 1   ← **密な番号**。写像は今のまま読む
```

こうすると場の広さは **産地の数**になり（noema なら 64 ではなく高々「枠×点」）、
測る必要も消える（静的に数えられるので A.3 の口が要らなくなる）。

**代償を先に書く（塞げていないところ）**:

1. **intern は層を一つ食う。** `RP` は `min`（下る束）で、`NM[s, RP[s,p]]` は
   その値を座標に使う —— 気づき24「座標にできるのは確定した値だけ」に当たる。
   だから番地を作る層と、番地で書く層は分かれる。参照実装では「同じ値は同じ鍵」
   が **定義上の同一性**で、計算ではなかった。計算にすると層が要る ——
   これは LVars の freeze（もう増えないと言えた瞬間に表現を替える）そのものである。
2. **比較は O(P²)、深さは O(1)**（気づき24 の取引）。`for (p) for (q)` の上限は
   `run.lx` の宣言した `npa`（137,378）ではなく、**その本の産地の数**でなければ
   ならない —— つまり §3.4（上限はデータが言う）が先に要る。二つは錠と鍵である。
3. **番地が値として運ばれる本**（25_eval の「項のアドレスは解釈されるものであると
   同時に置き番地でもある」）では、値から密な番号を引く道も要る。産地と同じ
   比較で引けるが、P が増える。

**やってみて分かったこと（2026-09-06。intern ではなく「法で畳む」に変えた）**:

最初は intern（最初の証人を `min` で拾って番号を振り直す）を考えたが、二つで
折れた —— **層を食う**（min の値を座標に使うのは気づき24）し、**番号が
安定しない**（後から小さい産地が同じ番地を作ると番号がずれる）。番地の同一性が
**局所的に計算できる**ことは hash-consing の値打ちそのものなので、そこは崩さず
**帯だけを畳む**ことにした:

```
座標 = 基 + (番地 mod W)      W は一巡目に測った番地の集合が言う
                               —— 衝突しない最小の 2 の冪
```

同じ番地は同じ剰余なので `cons` の同一性は保たれる。衝突は許さない（許せば
別の値が同じ升に落ちる）ので、W は測って決める。**機械は一行も変わらない**
—— fp 種3（`pa % mo`）が既にある。

`work/flat.py` に入れた（`LATTIX_ADDRMOD=1`。既定は off）。測った値:

| 本 | 升の総数（前） | 升の総数（後） |
|---|---|---|
| z_twoctor | 184,443,345,917,867,022 | **240** |
| 16_lex | 200,601,943,431,611,647 | **106** |
| 35_noema | 424,170,182,318,650,658 | **1,655** |

焼いた一枚（`test/baked.py`）で **食い違い 0**（z_twoctor / 16_lex / 15_parse /
35_noema / z_atomarg / a_min）。22_parse は辺 6634 > 6136（宣言した上限。法の枠が
一本増えるぶん）、25_eval は 400 秒で切った —— どちらも正しさではない。

**前段の側は戻した。** 枠 26（種3）を足して noema と z_atomarg は両側で一致
したが、22_parse が割れた。原因は測って分かった ——

> **枠が立つかどうかは、浅いところで決めなければならない。**
> 枠の使用 `zfpu[r, 26]` の書き手に深い場（`zsyf[r]`）を置くと、枠の置き場
> `zfpb`（枠の使用に沿った前置きの鎖）の層が上がり、それを読む `zmib` が
> 置いていかれる。鎖は「全部の枠が決まってから」しか正しい基を出せない。

否定で選ぼうとした二度の失敗も同じ根である（`not zfpu[r,26]` は層を上げ、
`not zamu[zsyf[r]]` は `zsyf` が ⊥ の規則で火を噴かなかった）。

**次の形**（決まった。次はここから）: 枠 26 は `zissy[r]` **だけ**で立てる
（浅い）。法は `zfpmo[r,26]` に置き、口が言わなければ 2^62（番地より大きい
＝畳まない）を既定にする —— `zfpmo` は鎖に入らないので層を上げない。
`flat.py` も同じく「常に種3 の枠を置き、法は W か 2^62」にすれば両側が揃う。
出口条件は一つ: **答えが参照と一致したまま、`fl.ncell()` が桁で落ちること**
（flat 側では既に落ちている）。

### 3.4 上限はデータが言う —— `bound` を値に

`run.lx` の層 48・空間 128・写像 32,768・辺 6,136 は **宣言した定数**であり、「上限を大きく書くのは
測らないことの言い換え」（気づき35）に当たる。18 場が黙って空になった事故（§1.5）はここから出た。

前段は既に `ssz`（空間の大きさ）を表で渡している。同じことを **場の広さ**にも許す:
`field v : max bound nz[]` —— 上端が場の値である区間（1,520 規則が使っている）と同じ形を、
宣言に置くだけである。焼いた側では、場の置き場は場番号に沿った前置きの鎖（気づき17）なので、
**同じ鎖を走行の頭で一度計算する**（`off[f] <- off[f-1] + size[f-1]`）。超えたら終了コード 6。
「一枚の `run.lx` がどの本にも合う」は、これで初めて本当になる。

### 3.5 s は座標であって、ループである —— 符号は形の数に比例させる

層は座標である（`for (s) in 0 .. NS-1`）。C の測定器はこれを **48 枚に展開**し、24.6MB の C になる。
焼いた側で同じことをすれば `gen` の出力も 48 倍になる。

気づき8（座標そのものが位相順序）を焼く側に入れる: 層 s の規則は項では s（同じ層）を、否定と
比較では s−1（閉じた値）を読む。**s 方向のずれは一定符号**なので、s は外側の計数ループで、
内側が一層ぶんの不動点 —— これが BSP の superstep そのものである。参照実装が `_axis_unroll` で
やっている展開を、`gen` は **ループとして出す**。符号は 259 形 × 1 で、NS という数はどこにも要らない
（層の数は前段の表が言う。§3.4）。

### 3.6 一つの不動点、二つの容器 —— ELF と PE は同じ命令面の二つの `render`

答えは配り先に依らない（気づき19）。命令のバイトは x86-64 で同じで、違うのは容器（ELF / PE）と
口（syscall / kernel32）だけである。命令面を一枚の場に置き、**`render` を二つ書く**:
ELF ヘッダの前置きの鎖と、PE ヘッダ + 取り込み表（RVA と thunk —— これも番地に沿った鎖）。
口は四つ（open / read / write / exit）で、前置きの十数命令が替わる。Windows は PLAN §5.3 の
通り最後だが、**設計は最初から「render が二つ」**にしておく —— そうすれば命令の側に
if が一つも入らない。

### 3.7 検査 —— 三つの一致を門にし、家の文法で撒く

- **門は三つの等式**（§1.3）。どれかが崩れたら進まない。`./check` に入れる。
- **撒く文法を「家の形」にする。** `differ` / `trinity` は源の文法で撒く。`gen` の的は形の表なので、
  **259 の形が一つずつ踏まれたか**を数える（例が何本かではなく、形が何個か —— PLAN §5-7）。
- **輪を閉じる等式**（g4d の後継）: `L1 = gen(all.lx)`、`L2 = L1(all.lx, -o)`、**L1 == L2** バイト一致。
  Python も C も居ない輪である。

### 3.8 速さは日程から（第6段。順序だけ書く）

断片の焼いた符号で通った道をそのまま使う: 日程（動いていない規則に入らない —— 4.25 倍）→
融合（同じ反復空間を一つのループに —— 54 倍）→ 表現（`or` は 1 ビット、⊥ が 0 の面は埋めない）→
分割（owner-computes）。**移植するのではなく、家の表がその情報を持っているので、`gen` が読むだけ**である。

---

## 4. 段取り（出口条件つき）

各段に出口条件を付ける。満たすまで次へ行かない。満たせなかったら「満たせなかった」と書いて戻す。
**作業単位は 3 時間**（一回のセッション）。見積りは実測ではない —— 目安として、`front.lx` は
08-29 から 09-05 の 8 日で 66 本まで来た。

### 第A段 —— 一枚にする（Python を輪から外す。参照実装の下で）

| 番号 | やること | 状態（2026-09-06 実測） |
|---|---|---|
| A.1 | `fat`（原子か数か）を前段が出す | **済**（d7f30e2 / a035276 / 367dc76）。前段は `zacl` を既に持っていたので三本で足りた —— 座標・値の項・構成子の引数場。三段の通し運転が原子の本 6 本でバイト一致、BOOKS 66/66、自己適用 1764 規則 完全一致 |
| A.2 | 前々段（bytes → bytes）: `include` / `component`+`use` / `emit` の糖衣を Lattix で | **未着手**。効く本は 8 本（10/11/12/13/14/25/30/32）。`include` は他のファイルを読む＝入出力なので前々段の仕事ではない（配る形では実行ファイルが繋いで渡す）。残るのは `emit`（小さい字面の書き換え）と `use`/`component`（付け替えつきの取り込み）。出す物は「新しい `ch` の表」でよい —— 前段の `for (t,zc) in ch` を書き換えずに済む |
| A.3 | **pass を座標に**: 口 `orc` / `ext` を機械の面の読みに | **骨は済**（b4c72c0 / 13e23fd。`test/twopass.py`）。閉じの器（engine2.close_upto）を使わずに **60/66 が参照と一致**、断る本 2、食い違い 4。置き場を戻すのも前段自身の置き場の場で行う（flat.py の `name_of` を使わない）。残り 4 本の理由は下記 |
| A.4 | 結線を消す: 一枚の本 `all.lx`（front ∪ run ∪ show） | **走った**（2026-09-17。`work/onebook.py` / `test/one.py`）。`run.lx` の表の読みを場の読みに機械的に書き換え、参照実装の下で小さい本が一致。**表を運ぶ Python が一行も無い。** 残りは `show` を継ぐことと、下の「払った代償」 |
| A.5 | 大きな本へ: `self_amin` の nofam 4 規則 → 22_parse → `31_gen.lx` | **未着手**（A.3 の実測が先に一部を答えた: 22_parse / 25_eval は二巡の輪で 600 秒で切れる） |

**A.4 で測って分かった言語の事実（2026-09-17）** —— 表を場にすると何が変わるか:

- **⊥ を繰り返しの上限に置くと、繰り返しは空になる。** だから「行が無い」は
  場でもそのまま「火が消える」になる。番兵の層 999 を場の側に作らずに済んだ。
- **無いことは `not` では測れない —— 0 も偽だからである。** `m[1] <- 0` と
  書いた升に `not m[1]` は真を返す。既定が 0 の列ならそれでよい（0 を重ねる
  だけ）が、既定が 0 でない列（fg の `am`/`wm`、fc の `wm`）では嘘になる。
  **在ることを `>= 0` で立て、それを否定する。**
- **行が在ることを、場では立てて言わねばならない。** 表は「行のある物だけ」を
  なめるので、存在の判断が繰り返しそのものに入っていた。場は宣言した箱を
  なめるので `if Fgu[e]` / `if Fcu[e,i]` / `if zfpu[e,u]` が要る。入れ忘れると
  門の数 `fq` が 1 のはずが 9 になり、**家の寄与が一つも書かれずに静かに空**に
  なった（zov も鳴らない。数えているのは「立った門」であって「在る門」では
  ないから）。
- **列は束ねた変数であって、場ではない。** だから `[` が続く綴りは列ではない。
  面の名前 `vf` と `eg` の列 `vf` を潰して `Egvf[e][s-1, …]` にした事故が
  ここから出た。
- **口は継ぎ目ではなく層である —— ただし素直に繋ぐと輪になる。** 一枚に
  すれば前段の `zorc` を機械の面から読めるはずだが、場の粒度では
  置き場 → 面 → `zorc` → 置き場 の輪になって成層器が棄却する。正しい形は
  **pass を座標にする**こと（§3.1 が自分で書いていた）—— 面と置き場に
  pass 軸 P を足し、pass P+1 の口が pass P の面を読む。巡の数は測ってあり
  （2〜3 巡）、一枚の本ではそれが層になるだけである。
- **表の列は地上の値である。場になると「この形は起きない」を処理系が
  数えられなくなる。** `run.lx` が成層を通っていたのは、`if lat == 8 if vf == 0`
  が **地上のガード**だったからで、`_fires` が「どの行とも合わない」と言えた
  →`_sign_sense` が `sum` の符号を UP に保てた。場になると `Fglat[e] == 8` は
  場の読みなので何も言えず、`sum`/`bag` の面は符号不明（NONE）になり、
  生きた読みが非単調になる。**これが一枚の本で払っている唯一の代償である**
  （下記）。
- **成層器は同じ規則の同じ場を、書き手の数だけ歩き直していた。** 座標の集合は
  (規則, 場) だけで決まるので覚えれば済む（`lattix.py:_stratify_core`）。
  判断は変えていない。一枚の本（前段 2371 規則 ∪ 走らせる物 366 規則）では
  成層だけで数十分掛かっていたのが数秒になった。成層を見る五つの門
  （verify / selfhost / sabotage / certify_all / axisstrat）は通ったまま。

**A.4 が払った代償（数えてある）**: 一枚にすると `sum` / `bag` の面を
**生きたまま読む**寄与三形ずつと、その面を生きたまま問う門 4 形、合わせて
**10 形**が刈られる（`work/onebook.py:prune` が形で声に出す）。刈る判断は
`work/mkrun.py` が表の形でやっているのと同じものである —— **同じ判断が
二箇所にある**（気づき34）。

**ここでも一度、読む前に書いた。訂正する。** 「家の道の `fg` に `la` が無い
のが穴で、`la` を足せば塞がる」と書いたが、`flat.py` を読むと違った ——
**束の違う源は、もう `pa` を通して閉じた面から写している**（flat.py 1293 行:
「束の違う源を、先と同じ束の升に写す。閉じた面から写すので、育つ値を別の束で
読むことにはならない」）。家の道の `am` が同じ束・生きたままなのは、**設計**
であって抜けではない。

**本当のところ**: 刈られた 10 形は「`sum`/`bag` の場が、同じ束の場を
**生きたまま**読んで足す」形である。そして `work/mkrun.py` は `pa` の側では
**同じ理由で既に除外していた** ——

    生きた写しは昇る束だけ（2 max / 3 or / 5 flat / 6 fourv / 9 count）。
    符号の要る束（sum / bag）を生きたまま pa へ写す規則は、
    run.lx 自身が成層できない。

つまり **同じ判断が二箇所にあり、片方（直の読み `am`）だけが緩かった**
（気づき34 の形。四度目）。表の形でそれが通っていたのは、番兵の表のおかげで
`_fires` が「この形は起きない」と言えたからで、**本物の表を入れれば同じ
run.lx も成層できない**。だから一枚にして刈るのは、失うことではなく
**もともと緩かった判断を揃えること**である。

**そして数えた（64 本、`work/t` ∪ `examples`）**:

| | 刈った形を使う本 |
|---|---|
| 寄与 6 形（`sum`/`bag` の vf=1 / vf=4） | **一冊も無い** |
| 門 4 形 | **`04_aggregate.lx` の `(kk=11, lat=8)` 一つだけ** |

つまり一枚の本で払っている代償は、**64 本のうち一冊の、門ひとつ**である。
数え上げた 10 形のうち 9 形は誰も使っていなかった —— 「数え上げた形であって
測った形ではない」がそのまま出た。

（`fl.fsh` は古く、表の中身と食い違う —— x_sumstrata の `fg` には `lat=8` の
行があるのに `fl.fsh` は `[(2,0,0)]` としか言わない。上の数えは `fl.tables()`
から取り直したものである。**宿題**: `fl.fsh` を表から作り直す。`mkrun` と
`baked.coverage` がこれを読んでいる。）

**一枚の本に残っている穴は、口だけである**（2026-09-18。`work/t` の 34 本を
数えた）:

| | 本 |
|---|---|
| 口が要らない | **24** |
| `orc` だけ（上端の値） | 2（x_sumstrata / z_depint） |
| `ext` だけ（合成の場の広さ・法） | 7（w_indir / z_accread / z_atomarg / z_dim2rd / z_nestwm / z_twoctor / z_twoind） |
| 両方 | 1（y_paseam） |

要るからといって落ちるとは限らない —— z_dim2rd と z_nestwm は口を渡さなくても
バイト一致した（その値が答えを動かさない）。**実測でバイト一致 25 本**
（上の 20 本＋ z_clmin / z_cont3 / z_dim2rd / z_nestwm / z_prod）。

**口の `ext` のうち二つは、前段が自分で数えられた**（front.lx に追加）:

- **合成の場の広さ**は「構成する場所 × その点の数」より多くならない（`zcsn`）。
  口が無いと `zfsz0` が ⊥ のままで **箱が立たない** —— 番地は 2^56 に散らばった
  まま書かれ、自分の箱の外へ出て隣を踏む。z_twoctor では `pa` が 507 升中
  **504 升 ⊤** になっていた。箱の広さで畳めば必ず中に入る。狭ければ衝突するが、
  衝突は ⊤ で **声に出る**（気づき44: 黙って隣を踏むより、そちらが正しい）。
- **値の位置の構成（`zcrr`）の枠 24 に法が無かった。** 合成規則（`zissy`）の
  枠 24 には入っていたのに、値の位置には入っていない —— **同じ判断が二箇所に
  あって、片方だけが抜けていた**（気づき34、五度目）。畳まないと番地が生で
  出る（`p[0] = 59870496655506589`）。

それでも `p[0] = pair(, )` —— 引数が空だった。**広さは数えられるが、法は
測るものだった。** 参照実装は「一巡目に触れた番地の集合が衝突しない最小の
2 の冪」を取る（flat.py）—— *走らせてから* でないと分からない。前段にできるのは
**詰め込み率を決めること**だけである: 升の数の 4 倍以上の、最小の 2 の冪
（最低 8）。これは測った値ではなく **設計の定数**であり、外したときは衝突して
⊤ が声に出る。法を升の数そのもの（3）にしていたときは、3 升に 3 つ入れて
衝突していた —— ハッシュ表に詰め込み率が要るのと同じ話である。

**これで構成子の本が通った**: `z_twoctor` ✓（字面 61 バイト）、
`z_atomarg` ✓（62 バイト）。道中でもう一つ: **綴りのトークンに引用符は
入っていない** —— `tag(alpha, 0)` が `tag(lpha, 0)` と出て分かった
（頭を一字落としていた）。**実測でバイト一致 27 本。**

**集合は場にできない。** 面に置かれた集合から元を取り出す言葉が Lattix に無い
（作れるが、数えられない）。機械は arena に整列して置いて番号で引くが、それは
面の外の構造であって規則からは見えない。02_strata が
`unreachable_from_0: ⊥ everywhere` と出るのはそれである。**言語の穴として
数えておく** —— 塞ぐには「面の集合を数える言葉」を足すか、走らせる物が元を
点ごとの側の面に控えるかのどちらかである。

**A.4 の実測（2026-09-17）**: `work/t/*.lx` の小さい 17 本
（a_min / b_3term / c_ge / d_eq / e_ne / f_two / g_sub / k_max / l_or /
m_orguard / n_range / o_ctr / p_nest / q_ctrguard / r_fguard / s_not / u_cross）
＋ z_unit0 / z_mixlat / z_chain が **一致 20 / 食い違い 0**、`zov` は一本も
鳴っていない。一本 17〜69 秒（層 3〜4）。

**ここで一度、私は測らずに書いた。訂正する。** `x_sumstrata.lx` が `total` で
食い違うのを見て「刈った 10 形の代償がそのまま出た」と書いた。**違った。**
刈った 10 形を字面ではなく **形**で言わせると、全部が「同じ面を生きたまま読む
寄与」だった:

    fn  kk=3 lat=8 / kk=3 lat=10 / kk=11 lat=8 op=3 n=0 / kk=11 lat=8 op=5 n=0
    vm  lat=8  vf=1 n=1 / vf=1 n=2 / vf=4        （vb は lat=10 で同じ三つ）

`vf=0`（定数）と `vf=3`（一元の集合）は刈られていない。そして x_sumstrata の
`total` の家の行は `(lat=8, vf=0, n=0)` —— **刈られていない形**である。

**本当の理由は口だった。** `total[] <- i for (i) in 0 .. n[]` の上端 `n[]` は
**閉じた升の値**であり、前段はそれを口（`orc`）から受け取る。一枚の本は口を
番兵のまま渡しているので、空間の点が 521 ではなく 1 になり、`sum` の面が
一升も立たない（`zssz = {0:1, 1:1}`、`vm` が空）。つまり A.4 の食い違いは
**A.3 の残りが顔を出したもの**であって、刈りの代償ではない。

**口が要る本は 34 本中 3 本**（実測）: x_sumstrata（`n[]`=520）、
y_paseam（`(2,0,7)`）、z_depint（`(0,0,3)` と `(0,63,3)`）。残り 31 本は
口が要らない —— 測った 20 本は全部そちら側である。

**刈った 10 形を使う本が在るかどうかは、まだ測っていない。**（`vf=1`/`vf=4` は
「同じ束の場を生きたまま読んで足す」形なので、在りうる。数え上げた形であって
測った形ではないので、数えるまで代償の大きさは分からない。）

v_poly / w_indir は参照実装の解釈実行が 300 秒で切れる ——
**一枚の本の遅さは、参照実装の遅さである**（焼けば別の数になる。第C段）。

**一枚の本の面は、参照実装の標準出力を**バイトまで**作れる**（2026-09-17）:
`LATTIX_SHOW=1 python3 test/one.py` で、測った 20 本すべてが
`python3 lattix.py f.lx` と **バイト一致**（18〜74 バイト）。見せる物
（`show.lx`）はまだ表で食っているので、この段は「面が揃っているか」の測定
である —— 揃っていた。**源のバイトから字面まで、答えの側に Python は居ない。**

**そして見せる物も一枚に入った**（2026-09-18。実測 20/20、字面 18〜74 バイト、
一本 38〜120 秒）。`work/onebook.py` が
`show.lx` の十枚の表も場の読みに書き換え、`LATTIX_SHOW=1 python3 test/one.py`
が **一枚の本が出したバイト列**を `python3 lattix.py f.lx` と直に比べる ——
源のバイトから字面まで、Lattix 一枚。Python は審判だけである。

道中で三つ、やり方の誤りを測って直した:

- **規則は行ではない。** `show.lx` は手書きなので続きの行がある。行で切ると
  規則が割れる —— `isset` の `for (…) in pv` だけ落として `if item[t] == c`
  を残し、`c` が宙に浮いた。**論理規則で切る。**
- **書き手の消えた場は、読んでも正しい。** `if not isnv[t]` は書き手が
  居なければ真で、それがこの段の意味（構成子の値は無い）そのものである。
  書き手が消えたからと読む規則まで落としたら、`num[t,3] <- ival[t] …
  if not isnv[t]` が道連れになって **値が一つも出なくなった**
  （`d[0] = ` だけが出た）。**宣言は残す。**
- **名前は座標である。** `farr` / `nch` / `neg` の三つが前段と綴りで
  ぶつかっていた（同じ綴りは同じ場になる）。見せる物の側を改名した。

字面の表（`topc` / `emp`）は種の場に直したので、**表はもう `ch` 一枚**である
（`orc` / `ext` は番兵一行のまま —— 口が要る 3 本のために残してある）。

`show.lx` を一枚に入れるには前段に一つ足りなかった: **`print` の文を誰も
読んでいなかった。** 33_self は `print` の語（`kwprn`）を知っているのに、
使う規則が無い。**足した**（front.lx、2026-09-17）——

`print f` の `f` は **出現ではない**（出現は `f[` の f）。だから綴りの鍵を
宣言と同じ畳み方で作って宣言と突き合わせる。道中で一つ測って分かった:
**`tch` は 8 文字しか持っていない**（宣言の `dch` は 16）。8 文字で切ると
長い名前（`unreachable_from_0`）が二本目の鍵で食い違うので、綴りは
**文字の流れから直に**拾う（33_self が `och` を作るのと同じ判断）。
実測: `work/t` の 18 本と `16_lex`/`20_lex_full`/`02_strata` で
参照実装の `p.prints` と一致。

残りの表も前段が持っている: `fld`（置き場）は `zlatf`/`zfna`/`zfb`/`zflo*`/
`zfsz*`/`zfs*`、`fnm`（名前の綴り）は 33_self の `dch[d,k]`、`fat` は `zfat`、
`ab` は `zatb`。**show.lx 側の書き換えだけが残っている** —— `pv` は疎なので、
場にすると宣言した升を全部なめる形になる（`for (f) for (c) in 0 .. fvz[f]`。
構成子の規則は `if nof[f] >= 0` で絞れる）。

**A.4 の測り方**: `python3 test/one.py`（`work/t/*.lx`）。層の上限は本ごとに
`fl.strata() + 2` を渡す —— 観測であって経路ではない。足りなければ面が空に
なるので、静かに間違えるのではなく **見えて**違う。

**A.3 が残した 4 本 —— 測って分かったこと**（§3.3 の予想が実測になった）:
構成子の引数場と `_size` は **内容番地が座標**である。一巡目に広さを渡さないと
番地の升が立たず、**立たないものは測れない**ので広さの鎖が伸びない。かといって
「広いが有限」の帯も敷けない —— 番地は 2^56 に散らばるので、noema の 8 枚に帯を
敷くと機械の升の上限（4.4×10^17）を超える。**疎な番地を intern して密にする**
（§3.3: 産地の最初の証人を `min` で拾い、前置きの鎖で番号を振る）まで、
この 4 本は閉じの器なしでは測れない。**他の 60 本には要らない。**

**A.3 で開いたもの（三つとも「黙って落ちる」の顔）**:
- **巡の数は二ではない。広さの鎖の高さである。** 参照は一巡目に 2^62 の帯を
  敷けるので二巡で足りるが、機械の升には宣言した上限がある。だから帯は
  「広いが有限」にして **測って広げる** —— 単調なので止まるところが不動点
  （実測: ほとんど 2 巡、育つ本が 3 巡）。
- **添字なしの場（`n[]`）は前段では一次元の升 0 に居る。** 口を `()` で引いて
  いたので x_sumstrata の `total` が丸ごと落ちていた。
- **静的な広さで一巡目を走らせると、値で決まる座標が黙って落ちる**
  （y_paseam の `form[5..7]`）。落ちたものは測れないので鎖が伸びない ——
  仮の帯はそのためにある。

**第A段の出口**（変わらず）: 参照実装の下で一枚の本が動き、Python は審判以外に
居ない。いま残っているのは A.2 / A.4 / A.5 と、A.3 の 4 本である。

### 第B段 —— 数えて塞ぐ（PLAN 4.1 / 4.2）

| 番号 | やること | 出口条件 |
|---|---|---|
| B.1 | §2.3 の一覧を `all.lx` に対して取り直す（形の表 259 を単位に） | 一覧に「数」と「塞ぎ方」が付いている |
| B.2 | 既知の穴: 二次元の座標書き（347 規則）/ ⊤ の規律 / 源 262,144 → 測った値 | 最小形 `o[p[r,c]] <- src[r,c]` が焼いて一致し、g4d バイト一致のまま |
| B.3 | `run.lx` に要る形の設計を書いて測る: set/bag/and/fourv（測定器の arena 方式を Lattix で）/ 三次元 / 番地の密化（§3.3、22_parse の P を測る）/ `bound` を値に（§3.4）/ s をループに（§3.5） | 各形に「参照実装の下で一致する試験」が一本ずつある |

見積り: 4〜6 単位。

### 第C段 —— `gen.lx`（`31_gen` の前半を家の表に書き直す。§3.2）

| 番号 | やること | 出口条件 |
|---|---|---|
| C.1 | 形の表を `run.lx` と共有（`shapes.json` → `run.lx` と `gen.lx` の両方） | 一形ごとに「走らせた答え ＝ 焼いた答え」（`differ`） |
| C.2 | 六部（頭・ガード・座標・join・階数・前進）を `fg/fc/fp/mp/spc/ssz` から出す。`31_gen` の後半（命令面 → ELF）はそのまま | `a_min` の家 → 実行ファイル → 参照と一致 |
| C.3 | s のループ・データ上限・intern・十束 | `run.lx` の 259 形が全部踏まれ、examples 34 本 + noema + 撒いた 150 本で一致 |
| C.4 | 輪を閉じる: `L1 = gen(all.lx)`、`L2 = L1(all.lx)` | **L1 == L2 バイト一致** |

見積り: 12〜18 単位。いちばん大きく、いちばん危ない（§5-1）。

### 第D段 —— 焼いて閉じる（PLAN 4.4）

| 番号 | やること | 出口条件 |
|---|---|---|
| D.1 | `lattix` ＝ `gen(all.lx)`。`./lattix f.lx` が表を読んで答えを出し、`show` の字面で出す | Linux で `./lattix noema.lx` ≡ `python3 lattix.py noema.lx`（examples 34 本 + noema + 150 本、バイト一致） |
| D.2 | Python も C も無い箱で測る | 走る |

見積り: 3〜5 単位。**ここで §0 の Linux 半分が閉じる。**

### 第E段 —— 包む（PLAN §5）

5.1 木を整える → 5.2 Linux（tar / deb）→ 5.4 使い方（argv・終了コード）→ 5.5 まっさらな箱 → 5.3 Windows（PE + kernel32、§3.6 の二つ目の `render`）。
見積り: Linux 3〜5 単位、Windows 6〜10 単位。

### 第F段 —— 速さ（§3.8）

順序だけ決めてある。数は測ってから。

**合計の見積り: 35〜55 単位（3 時間 × 35〜55）。実測ではない。**

---

## 4.9 上限の日（2026-09-18）—— 同じ判断を二箇所に書いた跡を七つ数えた

一日かけて追ったのは一本の糸である。**焼けた一枚の本が `filesz = 0` で落ちた。**
そこから出てきたものは全部、同じ形をしていた —— *一つの判断が二箇所に書いてあって、
片方だけが直っている*（気づき34）。数えたら七つあった。

| | 二箇所 | どちらが古かったか |
|---|---|---|
| 1 | `p_filesz`（3 バイト）と `p_memsz`（4 バイト） | filesz。memsz の側には「**四バイト目を書き忘れていた**」という註まで付いていた |
| 2 | ELF ヘッダの `codelen`（8..10） | 三バイト。EI_PAD はまだ一バイト空いていた |
| 3 | 知らせの番地 `encb[167]` = 4718528 | `4194304 + total - 64` の生き写し。四バイト目も無い |
| 4 | `dbase` = 4718592 | `total + 4194304` の生き写し。`total` を上げると **地上データが像の中を指す** |
| 5 | `fbase` = 12582912 | `dbase + 7864320` の生き写し |
| 6 | 源の上限: 場は 262,144 升／読み口 `rcap` は 524,288 バイト | （後述 —— これは穴ではなかった） |
| 7 | 種の置き場 1024 と種の番号 `sdno` 4096 | 置き場。同じ段落の `cok`（規則）も `okt`（項）も 4096 だった |

註が付いていたのが一番こたえる（1 番）。**書いた人は穴を見ていて、言葉にして、
それでも隣の同じ穴は直さなかった。** 見えていないから直らないのではない。
*同じものだと見えていない* から直らない。

### 測った上限（気づき44 —— 超えたときを試すまで上限ではない）

数字は全部、**一つ上を打って**出した。

| 何 | 境目 | どう落ちたか |
|---|---|---|
| 源の長さ | 262,144 文字ちょうど（262,145 で落ちる） | 終了コード 6、**知らせ無し** |
| 種（輪の無い規則 `x[3] <- 1`） | 1,024 本ちょうど | 同上 |
| 命令 | 131,072 本 | 同上。`lattix.lx` は 417,000 バイト ≒ 99% 埋まっていた |

種の境目の出し方が効いた。`lattix.lx` に**中身の無い規則を一本ずつ足して**、
何本目で落ちるかを二分探索する。四つの形を試したら分かれた ——

    輪なし・別座標  あと 58 本      輪なし・同座標  あと 58 本
    輪なし・ガード  あと 58 本      **輪 `for (z) in 0 .. 0` を付けると 399 本でも平気**

輪を一つ付けるだけで壁が消える。壁は「規則」でも「命令」でもなく **種**
（輪の無い規則）だった。968 + 58 = 1026 —— `bound 1024` である。

### 直した形

**(a) 数字を一箇所に。** 3〜5 は `total` から導いた（`mad`, `dbase`, `fbase`）。
7 は `sdno` の 4096 に揃えた（`sdf` `sdc` `sdv` `sdp` `s3` `sdc8` `oks` `cos`）。
1・2 は四バイト目を足した。

**(b) はみ出しが口をきくようになった。** 終了コード 6 は長いあいだ三命令だった
—— `mov eax,60 / mov rdi,6 / syscall`。**数字の 6 だけが残り、何が狭かったのかは
誰にも分からない。** 焼けなかったとき（コード 7）は一行出すのに、走って落ちた
ときは出さない。同じ判断が二箇所にあって、片方だけ実装されていた ——
**八つ目**である。知らせの八命令は即値を持たない形だけで書けるので、前置きの
即値とは混ざらない（`bad` と同じ手を使った）。像の末尾 64 バイトが `bad` の一行、
その手前 64 バイトがこちら。二つは決して同時に出ない。

    $ ./lattix < あふれる.lx
    lattix: field bound exceeded - input too big for this .lx

焼く側でも、焼けたプログラムの側でも、同じ一行が出る。**この系のどの沈黙も、
いま声になった。**

**(c) 見張りに二つ足した。** `capi`（命令）と `caps`（種）。`capi` は一度書いて
外している —— `codelen`（バイト数）で見ようとして循環した（符号 → 長さ → 断り →
符号）からである。だが **命令の数 `nins` は循環しない**: 規則と輪の数だけで決まり、
命令を一本も置く前に分かっている。ここが見えていなかった。

### 6 番は穴ではなかった（直しかけて、やめた）

場が 262,144 升で読み口が 524,288 バイト —— 「一つの判断が二箇所」に見えたので、
揃えようとした。揃える先は 524,288（読み口が既に約束している値）で、
**場が 223 枚あるので +640 MB**（`p_memsz` 1.09 GB → 1.73 GB）。焼くたびに
1.7 GB に ⊥ を敷く道具になる。誰もが払う税である。

払う前に、**なぜ二つが違うのか**を考えた。読み口を場と同じ 262,144 にすると、
読みの輪はそこで止まる —— つまり **長すぎる源は黙って切り落とされる**。
読み口が場より広いからこそ、262,145 文字目が場からはみ出し、**見つかる**。

> **二つの数が違うのが穴とは限らない。片方が黙っているのが穴である。**

黙りは今日直した（一行出る）。だから 6 番は揃えない。**この不揃いは検出器**
である。一枚の本（512,874 バイト）を焼くときは、**その場限りの広い焼き手**を
一枚作る（`bound` の付け替えだけなので、いまの焼き手で焼ける）—— 配る道具に
640 MB を積むのではなく。

### 数えたついでに（宿題）

262,144 の場 223 枚は、**全部が本当に文字ごと**だった（書き座標を静的に分類した:
224 枚ぶんが `for (i,c) in ch`、30 枚ぶんが文字＋他、66 枚ぶんは書き手が見つからず）。
つまり源の一文字あたり **約 2 KB** の状態を持っている。ここが道具の重さの正体である。

削る筋は一つ見えている: **`or` の場は一升 1 ビットで足りる**（升は今どれも 8 バイト）。
223 枚の多くは真偽値なので、詰めれば二桁縮む。ただし生成器の中で「座標 × 8」が
番地の作り方そのものなので、直すなら深い所を触る。**測ってから**にする。

### 天井を三つ、数字で測った（これが一番の収穫かもしれない）

一枚の本（512,874 バイト）を焼こうとして、**焼き手を広げる** ことを繰り返したら、
広げられない所に当たった。全部、打って確かめた数字である。

| 天井 | 値 | 何が効いているか |
|---|---|---|
| **場の置き場ぜんぶ** | **2 GB**（2^31） | 場の読みは `mov rax, [rbx + imm32]`。**符号つき** 32 ビットのずれなので、場の領域が 2 GB を越えた瞬間に番地が負へ回る。1,973 MB の焼き手は動き、2,061 MB（100.6%）は **tiny な .lx でも segfault** |
| **出す像の大きさ** | `total`（いま 524,288） | 焼いた符号は像に入らなければならない。`lattix.lx` 自身が **421,919 バイト＝像の 80%**。ガードを 1,017 本足すと codelen は 524,073 になって止まる |
| **源の長さ** | 文字面の `bound`（いま 262,144） | 223 枚の場が一文字ごとに立つ |

三つ目が一つ目を食っている。文字面を 524,288 にすると **223 × 524,288 × 8 =
935 MB** —— **2 GB の予算の 46% が、字句解析の一文字ごとの状態**である。

一枚の本の勘定（測った）:

| | 一枚の本 | `lattix.lx` |
|---|---|---|
| バイト | 512,874 | 236,689 |
| 場 | 1,068 | 559 |
| 規則 | 2,845 | 2,494 |
| 輪 | 3,877 | 1,576 |
| ガード | 6,403 | 1,892 |
| 項 | 5,475 | 3,205 |

必要な焼き手: 文字面 524,288（+640 MB）・場 2,048・輪 4,096・規則 8,192・
像 1 MB（+46 MB）。積むと **2,061 MB = 天井の 100.6%**。

> **一枚の本は、いまの生成器の天井に 0.6% 足りない。**

削る所を探した（文字面を 516,096 に、読みの面を戻す…）と 19 MB ほど出るが、
99.7% で立てるのは「立っている」とは言わない。**数字を削るのではなく、
天井を上げる筋**を書いておく。

### 天井を上げる筋（次にやるなら、これ）

**升はどれも 8 バイトである。** 束が `or`（真偽）の場も 8 バイト使っている。
文字面 223 枚の多くは `or` で、`alpha` `digit` `space` `nl` `brk` … 全部
一ビットで足りる。**束ごとに升の幅を変える**と、935 MB は二桁縮む。

生成器の側では番地が `foff[f] + 座標 * 8` で、書きは `mov [K + rdx*8], r11`
（x86 の SIB の倍率 8）である。倍率は 1/2/4/8 が選べるので、**`or` の場を
倍率 1 にする**のは形の上では通る。`fsz8`（バイト数）という場が既にあるので、
幅は一箇所で言える所まで来ている —— *幅を束から出す* だけである。

これは「速くする」直しではない。**2 GB の天井の下で、どれだけ大きな .lx を
焼けるか**を決めている一本である。いまは一文字あたり約 2 KB 使っている。

**そしてもっと安い筋が、その隣にあった。** 場の置き場は `ftot2 = ftot * 2` ——
**升ごとに 16 バイト**使っている。8 バイトが値（F）で、8 バイトが **階数**（K、
どの周回で決まったかの証人）である。階数は周回の番号で、層の数より小さい ——
**4 バイトどころか 2 バイトで足りる**。8 を 4 にするだけで置き場は 25% 減り、
2,061 MB → 約 1,590 MB。**一枚の本はそれだけで天井の下に入る。**

しかも束ごとの幅（上の筋）と違って、階数の幅は **一律**である。分岐が要らない。
触る所は数えられる（読んだ）:

- 形 92 `mov [K+rdx*8],r11` —— 階数を書く（`jpos[r]-1`）
- 形 64 `mov q[r10+rdx*8+d32],1` —— 読み無し規則の階数（種と同じ 1）
- 種の三命令（`sdp[s]+1`, `+2`）—— 値と階数を並べて置く
- 階数の最大を溜める `cmovl` の読み（`bb[r]` の段）
- `kbase[0] <- fbase[0] + ftot[0]` と、出口で階数を **stderr へ** 書く二命令

最後の一つが効く: **階数は出力である**（不変条件6 の証明書）。幅を変えると
`certify` / `runtimeattest` が読む形も変わる —— 小さな直しだが、
**忘れると「答えは合っているのに証明書が読めない」**という、いちばん質の悪い
壊れ方をする。二箇所に同じ判断がある形そのものなので、先に書いておく。

**次の一手はこれである。**

### 階数を 4 バイトにした（やった。25% 減った）

上で「次の一手」と書いたものを、その場でやった。

升は値（F）と階数（K）の二枚で、どちらも 8 バイトだった。**階数は周回の番号**で、
層の数より小さい —— 8 バイトは要らない。x86 の SIB は倍率 1/2/4/8 を持っているので、
`r10`（K の基）を使う七つの形だけ **倍率 8 → 4、REX.W を落として 32 ビット**にする。

    45 mov r9,[r10+r9*8+d32]   → mov r9d,[r10+r9*4+d32]     4F→47  CA→8A
    46 mov [r10+rdx*8+d32],r9  → mov [r10+rdx*4+d32],r9d    4D→45  D2→92
    52 mov q[r10+d32],1        → mov d[r10+d32],1           49→41
    64 mov q[r10+rdx*8+d32],1  → mov d[r10+rdx*4+d32],1     49→41  D2→92
    88 cmp r11,[r10+r9*8+d32]  → cmp r11d,[r10+r9*4+d32]    4F→47  CA→8A
    89 cmovl r11,[...]         → cmovl r11d,[...]           4F→47  CA→8A
    92 mov [r10+rdx*8+d32],r11 → mov [r10+rdx*4+d32],r11d   4D→45  D2→92

**七つとも長さが一バイトも変わらない。** 変わるのは REX と SIB の一バイトずつだけ
なので、形の表（`shp`）も番地の鎖も一切動かない。あとは階数を指すずれを
`koff[f] = foff[f] / 2` にし（十一箇所）、`ftot2 = ftot * 2` を `ftot + ktot` にし、
出口で証人に書き出す長さを `ktot` にする。

**階数は出力である**（不変条件6 の証明書）ので、読む側も同じ turn で直した ——
`test/gen.py` と `test/selfgen.py` の `'q'` を `'i'` に。忘れると「答えは合って
いるのに証明書が読めない」という、いちばん質の悪い壊れ方をする。

結果: **`p_memsz` 1,041 MB → 786 MB（−24.5%）。** 不動点は二回で来て、
三段目・四段目までバイト一致。selfgen は全部 SOUND/COMPLETE。

### そして「註は見張りではない」を、もう一度

`total` を 1 MB に上げたら、焼いた道具が tiny な .lx でも segfault した。
原因は `rcap` の所に **書いてあった**:

> `out` の升数が rcap + 65536 を超えると、詰め直しが地図の外へ出る。

註だけで、検査は無かった。見張りに `pcap` を足した。今日これで **九つ目**である。

さらに測って分かったこと: **焼き手が出せる像の大きさは、焼き手自身の
`rcap + 65536` で決まる**（詰め直しの置き場がそこだから）。だから大きい像を
出す焼き手を作るには **二段**要る ——

    lattix（rcap 524k）→ _bigbuf（rcap を広げただけ。像は 524k のまま）
                        → _wide（像 1.3 MB。_bigbuf が 1.3 MB まで出せる）

### 一枚の本は、まだ焼けない —— どこまで行ったかを数字で

面を順に広げて、壁に当たるたびに広げ直した（種 → 場 → 輪 → 規則 → 項 →
命令 → バイト位置 → 像）。**文字面以外を全部広げた焼き手**で 1,924 MB
（天井の 94%）まで来て、それでもまだ `field bound exceeded` が出る。

原因は割り算で出る。文字ごとの面を数えた:

| 束 | 枚 | 升 | 割合 |
|---|---|---|---|
| `or` | 86 | 101 | 46.8% |
| `max` | 85 | 110 | 50.9% |
| `min` | 4 | 4 | 1.9% |
| `count` | 1 | 1 | 0.5% |

**一文字あたり 216 升 × 12 バイト = 2,592 バイト。** 源 524,288 文字で
**1,296 MB** —— 2 GB の天井の 63% が、字句解析の一文字ごとの状態である。

そして **その 46.8% は `or`、つまり真偽値**である。`or` の ⊥ は 0 で（`nofill`）、
取る値は 0 か 1 しかない —— 8 バイトは 8 ビットでも多い。

**ただし階数は一緒には縮まない。** 階数は「読みの階数の最大 + 1」（31_gen の
規則そのもの）なので、`tnum[i] <- tnum[i-1] + 1` のような鎖では
**鎖の長さまで伸びる** —— 50 万文字の源なら 50 万。1 バイトや 2 バイトでは
足りず、4 バイトが下限である。
**測った**（気づき44 —— 推論で止めない）。`c[i] <- c[i-1] + 1 for (i,v) in ch`
を焼いて、源の長さを変えながら証人（fd 3）を読む:

    源  16 バイト → 階数の最大    16      源 1024 → 階数の最大 1024
    源  64 バイト → 階数の最大    64      源 4000 → 階数の最大 4000
    源 256 バイト → 階数の最大   256

**階数 = 鎖の長さ、ぴったり。** 一枚の本（512,874 バイト）なら階数は
50 万に届く —— 2 バイト（65,535）なら **その日に黙って回る**。4 バイトで
正しい。そして この測りは、今日の 8→4 が端から端まで通っていることの
確かめにもなっている（証人が 4 バイトで書かれ、4 バイトで読めている）。
だから `or` の升は **値 1 + 階数 4 = 5 バイト**になる:

    216 升 × 12 = 2,592 バイト/文字  →  115 × 12 + 101 × 5 = 1,885 バイト/文字
    源 524,288 文字で 1,296 MB  →  943 MB   （**−353 MB**）

1,924 − 353 = **1,571 MB**。天井まで 470 MB 余る。**一枚の本はそれで焼ける。**

（最初 −505 MB と書いた。階数も 1 バイトにできると思ったからで、**間違い**
である。上に残しておく —— 「値と階数は別の物差しを持つ」ことを忘れると
こうなる。）

**もう一つ、ただし書き。** いまは `koff[f] = foff[f] / 2` で階数のずれを
値のずれから出している。これは **升の幅が一律**だから成り立つ式で、`or` だけ
幅が変わると成り立たない —— `koff` も自分の前置きの鎖を持つ必要がある
（`koff[f] <- koff[f-1] + fsz[f-1] * 4`、`foff` と同じ形）。
**今日 8→4 が簡単だったのは一律だったからで、次は一律ではない。**

**長さは変わる（調べた）。** 階数のときは REX と SIB の一バイトずつで済んだが、
値の側はそうはいかない:

    いま  40 mov rax,[rbx+r9*8+d32]        4A 8B 84 CB d32        8 バイト
    or    movzx eax,byte [rbx+r9*1+d32]    42 0F B6 84 0B d32     9 バイト
    （`mov al,[...]` なら 8 バイトだが上位が残る。`test rax,rax` が壊れる）

書きの側は逆に縮む（`mov q[rbx+rdx*8+d32],1` 12 → `mov byte [...],1` 8）。
**長さが動くので、番地の鎖も形の表も動く** —— 生成器はそれを `shln` から
出しているので破綻はしないが、「差分が小さいから安全」という今日の楽さは無い。
試験を全部回すつもりで入ること。

**ついでに一つ、思い違いを正す。** 2 GB の天井は **番地の広さ**であって
使うメモリではない。`or` / `sum` / `count` の面は `nofill` で ⊥ を敷かない
（BSS の 0 がそのまま ⊥）ので、書かれない升は **物理ページを一枚も使わない**。
それでも落ちるのは、`[rbx + 符号つき imm32]` が届かなくなるからである ——
**足りないのは場所ではなく、番地の桁**である。

> **次の一手: `or` の場の升を 1 バイトにする。**
> 階数を 4 バイトにしたのと同じ手（倍率と REX を変えるだけ、長さは変わらない）が
> 使えるかを先に確かめること —— `or` は読みが `test rax,rax` なので、
> `movzx eax, byte [rbx+rdx*1+d32]` になる。長さは変わる（倍率 1 は SIB が要らない
> 形もある）ので、今回のように「形の表が動かない」とはいかないかもしれない。

### 今日の試験（4 バイト階数の版）

| | |
|---|---|
| `test/selfgen.py` | ✓（全部 SOUND/COMPLETE） |
| `test/gen.py` | ✓ |
| `test/front.py` | 53 本 ✓ |
| `test/accept.py` | **61 / 61 一致** |
| `test/fold.py` | 一致（畳んだ一枚と三段が同じ答え） |
| `test/g4d.py` | **バイト一致** —— L1 = L2 = `a86491f5…` |

g4d の `a86491f5…` は、C の起動用を経ずに `lattix` が自分を四段焼いて
出した不動点と **同じ値**である。二つの実装（C の測定器と、焼けた ELF）が
同じバイト列に落ちた（気づき49: 二つ目の実装は試験より強い試験）。

### 升の幅を束から出した（2026-09-18 夜）—— 599 MB

朝に「次の一手」と書いたものをやった。ただし **やる前に一段掘った**。

素朴にやると、形の表が倍になる。`mov rax,[rbx+rdx*8+d32]` と
`…*1+d32` は別の形で、幅を一つ足すたびに全部の読み書きが二重になる。
四つの幅を許せば四倍である。**これは現象であって基ではない。**

x86 の SIB バイトは上二ビットに倍率を持っている —— **倍率は最初から
命令の中の穴**で、形の表がそれを定数で埋めていただけだった。番地の d32 を
穴にしたのと同じことを倍率にもすれば、形は一つで四つの幅を差せる。

    field mrm    mrm[f] <- encb[f, shho[f] - 2]      modrm は穴の二つ手前
    field mrmr   mrmr[f] <- mrm[f] % 8               rm が 4 なら次が SIB（x86 の決まり）
    field hassib hassib[f] <- true if mrmr[f] == 4
    field shsb   shsb[f] <- shho[f] - 1 if hassib[f]  位置は必ず穴の一つ手前
    field sibb   sibb[f] <- encb[f, shsb[f]] % 64     倍率を抜いた SIB
    field fmsc   fmsc[f] <- encb[f, shsb[f]] - sibb[f]  いまの倍率（既定）

**表は一行も書き換えていない。** SIB を持つ形（数えたら 17）も、その位置も、
倍率を抜いた値も、**表から出る**。書く側は一行:

    out[i] <- sibb[bfm[i]] + scb[bown[i]]   for (i) in 0 .. bmax[0] if issib[i]

この段だけを入れて測ったら、`work/t` の 18 本と `lattix.lx` 自身、
**19 本すべてで生む機械語がバイト一致**した —— 機構は入ったが何も変えていない。

### そして束が幅を言う

    field fwb    fwb[f] <- 1 if fdlat[f] == 3        or の升は 0 か 1 しか取らない
                 fwb[f] <- 8 （ほかの束）
    field fwbz   fwbz[f] <- fwb[f] if fwb[f] >= 1    **閉じてから座標に使う**（気づき24/38）

読みだけは形が要る —— x86 では `mov r64,m64` が 8B、`movzx r32,m8` が 0F B6 で、
**オペランド幅は opcode が決める**から倍率のようには穴にできない。だが
場所が形を選ぶのではなく **幅が形を選ぶ**ようにすれば、場所の書き方は変わらない:

    field fmrd   fmrd[1] <- 175   fmrd[8] <- 40      幅 → rax に読む形
    field frd    frd[f] <- fmrd[fwbz[f]]             場 f を読む形
    form[gc[gg]] <- frd[gdf[gg]]                     場所は「この場を読め」としか言わない

足した形は **五つ**（読み二つ・比べ一つ・入れる一つ・種の書き一つ）。
幅を増やしても、次からは表に一行足すだけである。

### 断る、広げない

最初は「輪の上端や座標に読まれる `or` の場は 8 バイトのままにする」と書いた
（`wide8`）。動いたが、**間違いだった** —— 幅が生成器の内部都合で決まると、
場の吐き出し（答えの形）を読む側が `wide8` を知らないと解けない。
**同じ判断が二箇所**である。今日ずっと潰してきたものを自分で作っていた。

幅は束だけが言う。束と合わない使われ方は **理由8 で断る**。
`work/t` 33 本と `lattix.lx` を通したら、**断られたものは一つも無かった**。

（`wide8` を入れたまま最初に測ったとき、`or` の場が狭まらなかった。
ガードを忘れて `wide8[cdcf[gc]]` を全 gc に書いていたので、場を読まない
座標では `cdcf` が 0 になり、**場 0 が 8 バイトに固定されていた**。
⊥ ではなく 0 が入るので、規則は黙って発火する。）

### 測った

| | |
|---|---|
| 機構だけ（倍率を穴に） | 19 本すべて **バイト一致** |
| `or` を 1 バイトに | `or` の無い本 24 本は **バイト一致**、`or` のある本 9 本だけが変わる |
| 一本を実測 | `big : or bound 256` が 2,048 → **256 バイト**、中身 `01 01 01 00 00 00`（正しい） |
| 自分を焼く | 不動点、三段バイト一致 |
| **置き場** | 795 MB → **599 MB**（−24.6%） |

`lattix.lx` は `or` の場を 86 枚持っている。読みか書きが一つでも狂えば
自分を焼けない —— **自己ホストがそのまま試験になる**（気づき49）。

一日で: 1,041 MB → 795 MB（階数 4 バイト）→ **599 MB**（`or` 1 バイト）。
**42.5% 減った。** 2 GB の天井の下で焼ける .lx は、その分だけ大きくなった。

### 一枚の本は 97% まで来た（まだ焼けない）

幅が縮んだので、朝に 1,924 MB（天井の 94%）で止まっていた研究用の焼き手は
**1,223 MB（59.8%）**になった。700 MB 空いた。それでもまだ焼けない。

**数えられる寸法は全部足りている**（`_wide` に中身の無い規則を形ごとに
一本ずつ足す二分探索で測った）:

| | 上限 | 一枚の本 | |
|---|---|---|---|
| ガード | 8,537 | 6,403 | 足りている |
| 項 | 14,165 | 5,475 | 足りている |
| 輪 | 8,122 | 3,877 | 足りている |
| 場 | 4,065 | 1,068 | 足りている |
| 文字 | 519,794 | 512,874 | 足りている |

それでも落ちる。**本そのものを二分探索した**（宣言は全部残し、論理規則を
先頭から k 本だけ残す。継続行は規則に属させる）:

    2,752 本（503,820 バイト）→ 焼ける
    2,753 本（503,914 バイト）→ 終了コード 6

**2,845 本中 2,752 本 —— 97%。** 焼けた分の `p_memsz` は 1,763 MB で、
codelen は 89,770 バイト（像の 6.8%）しかない。**符号は小さく、場が重い。**

一度、道を間違えた。`rcap` を 1,310,720 まで上げていたので
`dcap = rcap*16 + 1MB = 22 MB` になり、集約規則ごとに置く「一度だけ撃つ印」
（`fsz2[r] <- dcap[0]`）が一本あたり 22 MB を食っていた。本の符号は 90 KB
しか要らないので `rcap` を 589,824 に戻したら焼き手は 1,223 MB に減った ——
**天井は保存量で、どこに使うかは選べる**（`s-conserved-total`）。
だが本は依然として焼けない。印の面は主因ではなかった。

### 道具を作ったら、一手で片が付いた

上に「次に要るのは推測ではなく道具」と書いて、そのまま作った。

はみ出しの出口は全規則で共有しているので、飛ぶ前に場番号を渡せない。
規則ごとに **二命令の踏み台**を置く:

    stub[r]:  mov r15d, <場番号>     （形162。既にあった）
              jmp  はみ出しの出口     （形78。既にあった）

出口は r15 を像の末尾の枠に落として四バイト小端で吐く。足した形は三つ、
どれも **穴を持たない**（枠の番地は `total` から焼く）—— SIB の検出を
「穴の二つ手前が modrm」で導いているので、穴が即値の形を足すと
即値の中のバイトが modrm に見えることがあり、**値によって当たり外れが出る**。
そこは踏まないようにした。ついでに `mod` も見るようにした（rm==4 だけでは
絶対番地の SIB を拾いうる）。

道中で一つ踏んだ: 枠へ落とす命令を `4C 89 3C 25`（REX.W）と書いたら
**八バイト書き**、隣の知らせの頭四文字が消えた（`lattix:` が
`\0\0\0\0ix:` になった）。`44`（REX.W なし）で四バイト。

そして一枚の本に当てたら:

    lattix: field bound exceeded - input too big for this .lx
    踏んだ場の番号: 443  →  coff（座標の要素の前置きの鎖、bound 32768）

**座標の要素**だった。朝から数えてきた寸法（ガード・項・輪・場・文字）の
どれでもない —— 撒いた試験が一度も触っていない量である。一枚の本は
`fold3` で二次元の場が多いので、座標の要素が規則の数より速く増える。
`bound 8192` の族を 131,072 に上げたら:

    焼き手 1,328 MB（天井の 64.8%）
    **一枚の本が焼けた** —— 655,360 バイト、codelen 121,601、memsz 1,100 MB

> **見張りは、踏んだ場所を言えなければ半分しか立っていない。**
> 言えるようにした瞬間、朝から半日探していたものが一回で出た。

### そして次の壁は、寸法ではなく言葉だった

焼けた一枚の本を走らせたら、こう言った:

    lattix: cannot bake - line 001415 reason 8: unsupported form

つまり **生成器は断った**（焼けた実行ファイルは断りの知らせだった）。
1415 行目:

    zsnr[hfz[osn[o]]] <- true for (o) in 0 .. nocz[0] ...

**書き座標の読みが二段**である。断片が許すのは一段（`tstart[tnum[i]] <- i`）まで。
数えたら **一枚の本に 78 箇所、うち 48 箇所は `front.lx` にある**。
`front.lx` は解釈実行に向けて書かれたので、書き座標に好きなだけ入れ子を
書けた —— 断片はそれを読めない。

直し方は今日もう一度やった手である —— **引いてから配る**:

    field zsnro : max bound <出現の上限>
    zsnro[o] <- hfz[osn[o]]   for (o) in 0 .. nocz[0] if oin[o] if osn[o] >= 0
    zsnr[zsnro[o]] <- true    for (o) in 0 .. nocz[0] if ... if zsnro[o] >= 0

意味は変わらない（中間の場を一枚置くだけ）。78 箇所は機械的に写せる ——
`work/onebook.py` が既に場化の書き換えをしているので、そこに足すのが自然。

**一枚の本に残っているのは、もう寸法ではない。** 言葉である。

### 参考: 推測で潰した半日

道具を作る前は、面を一つずつ広げて試していた。記録として残す ——
`rcap` を 1,310,720 まで上げたせいで `dcap = rcap*16 + 1MB = 22 MB` になり、
集約規則ごとに置く「一度だけ撃つ印」が一本 22 MB を食っていた。本の符号は
90 KB しか要らないので `rcap` を 589,824 に戻したら焼き手は 1,223 MB に減った
—— **天井は保存量で、どこに使うかは選べる**。だがそれは主因ではなかった。
主因は `coff` で、それは道具が一回で言った。



### 見張りが勝てない場所（これは直っていない）

種を 4,200 本にすると、`caps`（4,096）が声を出す前に **生成器が自分の種の置き場を
踏んで** コード 6 で落ちる。数える場（`sdno`, `sdcnt`）が数えられるもの（種）と
**同じ広さ**だからで、数え終わる前に数える場があふれる。

> **数える場は、数えられるものと同じ広さであってはならない。**
> 上限を超えたことは、*超えた分を置かずに* 数えられなければならない ——
> 数は一升（`sum` / `max` の単一セル）に溜め、配列に溜めてはいけない。

今日はここで止めた。いまは「コード 6 ＋ 一行」なので黙ってはいない。

### 起動の三角（bound だけの中継ぎ）

広い焼き手は、狭い焼き手では焼けない。だが **bound の付け替えは規則を増やさない**
ので、`HEAD` の源に bound だけ当てた中継ぎを一枚作れば、いまの焼き手で焼ける。

    lattix(旧) → 中継ぎ(広い面, 規則は旧) → L6(新しい規則, 旧の配置)
                                          → L7 = L6(源)   ← ここが不動点
                                          → L8 = L7(源) = L7 ✓  L9 = L8 ✓

不動点は **二回** 回さないと来ない。L6 は旧の配置で置かれているからで、
L7 以降が自分の配置になる。`L7 == L8 == L9`、バイト一致。

### 機械の話（測った）

`lattix` を一回走らせるたびに **約 1 GB** 触る（`p_memsz` = 1,088,275,504）。
宣言した升に ⊥ を敷くのが最初の仕事なので、ページは全部実在化する。
これを C の測定器（clang が 2.2 GB）と同時に何本も走らせたら、7 GB の箱は
負荷 30 で固まり、`python3 -c "print(1)"` すら起動できなくなった
（メモリ圧の full が 89%）。**重い仕事は一度に一つ。**

---

## 5. 危なさ（先に書く）

1. **`gen.lx` の大きさ。** 前半の書き直しは 2,687 行の半分に当たる。全部書いてから合わせるのではなく、
   **一形ずつ**足して一形ずつ `differ` で測る（PLAN 4.3 の「一つ足すごとに」）。順序は `a_min` に要る形から。
2. **intern の O(P²)。** 22_parse の産地の数 P を測ってから桶の幅を決める。測る前に設計しない。
3. **宣言した定数の上限**（層 48・空間 128・写像 32,768）は §1.5 の事故をもう一度起こす。
   第B/C段で値にするまでは、超えたら **断る**（黙って空にしない）ことだけ先に入れる ——
   **入れた**（05a634c。`zov` の八つの口。層を上限に押し上げた表で立つことを確かめた）。
   ただし空の表の番兵（層 999）が四箇所に書いてある（気づき34 の形）。番兵を
   「本物の個数」にする直しは宿題である。
4. **8GB の箱。** C の測定器は 5.7GB で落ちた。`gen.lx` は Lattix なので C を経ない（断片の焼きは
   209KB の源で 1.34 秒だった —— 気づき47）。測定器を使うのは差し合わせだけにする。
5. **前段の走行時間。** 22_parse 以上の本と `31_gen` 自身を前段に読ませる時間は **未測**。
   A.5 で測る。時間で切れるなら原因は上限（zdat/zdrt/zdsr の bound）か日程で、答えではない。
6. **文書。** この文書と `HANDOFF.md` は消す対象である。§4 を `PLAN.md` §3 に写して一つに戻す。

---

## 6. 次のセッションの最初の三手

1. **A.5 の頭**: `self_amin` の nofam 4 規則を家にする（`/tmp/machine_check.py` が nofam を出す）。
   一致 3 を「全部家」にする。
2. **A.1**: `fat` を前段から。小さく、一本で閉じる。
3. **A.3 の最小形**: `a_min` で前段 p=0/1 と機械 p=0/1 を一枚にし、`orc` が `v[p=0, s, ·]` の読みで
   置き換わることを **一本の本で**確かめる。ここが通れば閉じの器は要らなくなり、第A段の残りは写しである。

---

## 断片の際を測った —— そして測定器が嘘をついていた

### 際は三つあって、三つとも違う

「入れ子は一段まで」と覚えていた。測ったら、**場所によって三つとも違った**。
`./lattix` に十二枚の小さい `.lx` を食わせて焼き、走らせて数えた結果である
（組は `test/accept.py` に置いた —— 覚えていられないものは試験にする）。

| 置き場所 | `x[i]` | `a[b[i]]` | `a[b[p[i]]]` |
|---|---|---|---|
| 書き座標 | 通る | **焼けない** | 焼けない |
| 値 | 通る | 通る | **焼けない** |
| ガード | 通る | 通る | **焼けない** |

書き座標だけが一段で、値とガードは二段入る。`work/flatten.py` は書き座標しか
見ていなかったので、値とガードの深すぎる読みは残っていた（本の中に 282 本）。

### 「焼けない」と言えていなかった —— 番兵より大きい番兵

もっと悪いものが出た。**`if a[i] + 1 >= 2` は焼けて、走らせると segfault した。**

源（`33_self.lx`）は正しく判じていた。「辺は式ではない」と書いてあり、式の辺には
**知らない種 15** を立てていた。立てた 15 は生成器に届いていた。届いた先がこうだった:

    glen[gg] <- ctot[gdoo[gg]] + 5   for (gg) in 0 .. nggz[0] if gdk[gg] >= 10
    form[gc[gg]] <- frd[gdf[gg]]     for (gg) in 0 .. nggz[0] if gdk[gg] >= 7

種は 1 から 11 までしか無い。だが振り分けが `>= 10` と `>= 7` で **開いていた**ので、
番兵の 15 を「知っている種」として拾ってしまう。拾われた 15 は、場の番号が ⊥ の
まま `mov rsi,[rbx+r9*8+d32]` になり、走らせると落ちる。

> **番兵を「知らない種」として大きい数に置いたのに、読む側が `>=` で開いていた。**
> 番兵は上限の外に居るつもりだったが、**上限が無いのだから外も無い**。
> 上限は超えたときを試すまで上限ではない（気づき44）—— ここでは「超えたとき」が
> 番兵そのものだった。

直し方は、閉じる数を三箇所に書かないことである（気づき34）。焼ける種を言う場を
一枚置いて、三つの振り分けがそれを読む:

    field gknown : or bound 4096                   # この種は生成器が出せる
    gknown[gg] <- true  for (gg) in 0 .. nggz[0] if gdk[gg] >= 1 if gdk[gg] <= 11

### 数え上げは漏れる —— 六本を一本にした

源の側も直した。以前はここに六本並んでいた（左辺の手前が `+ - * / %` か五本、
右辺の後ろに `± 数` が続くか一本）。数え上げなので、**数えていない形は漏れる** ——

    if 1 + a[i] >= 2     左辺の手前は `[` なので当たらない  → 黙って `1 +` を落とす
    if not a[i] + 1      比較ではないので当たらない          → 黙って `+ 1` を落とす

言いたいことは一つしか無い。**if 節の中に、括弧の外の演算子があってはならない。**
括弧の中（`f[i-1]` のずれ）は座標の話なので当たらない。一本にしたら、測った十二枚の
うち式の辺を持つ四枚が全部断られるようになった（前は二枚が黙って走っていた）。

### 引いてから配るには **値段** がある

`work/flatten.py` を三つの際すべてに広げた。`front.lx` は
書き座標 63 / 値 77 / ガード 331 本が平らになり、中間の場は 188 枚、
**断片の外に出る規則は 0 本**になった（`test/front.py` で答えは変わらない）。

一つ、意味を変えた所があって直した。引いた場に `>= 0` を付けるのは
**書き座標に引いたときだけ**である。値やガードに付けると答えが変わる ——

    if not a[b[p[i]]]        中が ⊥ のとき **撃つ**（`not ⊥` は真）
    if not a[zh[i]] if zh[i] >= 0    撃たない                    ← 違う

座標で要るのは、**書き座標の ⊥ は 0 に落ちて 0 番の升を汚す**からである。
読みの座標やガードでは ⊥ は ⊥ のまま伝わるので、足してはいけない。

そして本体の壁に当たった。一枚の本（前段 + `run.lx`）には、こういう規則が 105 本ある:

    pa[s, Fppb[e,u] + t] <- vc[s, MC[Fpam[e,u]] + MK0[Fpam[e,u]] * zdat[zspb[Fgsp[e],
      MA0[Fpam[e,u]]] + (t / zsps[…]) % zspw[…], ML0[…]] + … ]   for (s) … for (e) …
      for (u) … for (t) in 0 .. zssz[Fgsp[e]] - 1

項は一つ（深さ0 の `+` は無い）。深いのは **座標**で、読みが五段ある。
内側を引けば浅くなる —— ただし引いた場は、その式が依る **輪の変数すべて**で
添字を張らねばならない。この式は一番内側の `t`（升の番号）に依るので、

    [e, u, t] = 4096 × 128 × 137,378 = 720 億升 = **576 GB**

> **引いてから配るの値段は、輪の上限の積である。**
> 外側の小さい輪にしか依らない式は安い。一番内側の大きい輪に依る式は、
> 機械が **レジスタ一本**で済ませているものを、こちらは配列に広げることになる。
> だから深い座標は「書き換えで平らにする」ものではない —— **生成器が鎖にする**
> ものである。（気づき50）

### 座標を鎖にした —— 見立ては当たり、場所は違った

鎖の仕掛けは既に在った（源5「内側の所有者が座標を作り終えた所で読む」）。
だが広げる場所は `lib/fold.lx` の `innr0` ではなかった —— そこを広げても
何も変わらなかった。落ちていたのは一段手前、`examples/33_self.lx` である:

    ixsr[j] <- 2 … if isocc[j] if lkd[snum[j], vlpz[j+2]] == 0
    ixsr[j] <- 3 … if isocc[j] if lkd[snum[j], vlpz[j+2]] >= 1

添字トークン `j` が読みのとき、その **添字** `j+2` の輪番号 `vlpz` を見ている。
`a[b[i]]` なら `i` は変数だから当たる。`a[b[h[i]]]` なら `h` は **読み**なので
`vlpz` が ⊥ になり、どちらの規則も撃たず、**源が出ないまま座標が消えていた**。

> 座標の源は「列・計数器・場の値・定数」の四つだと思い込んでいた。
> **読みも源である。** 四つ数え上げて五つ目を数え忘れたのではない ——
> 五つ目は「他の四つのどれかを内側でやったもの」なので、数え上げの外にある。

二行足すだけで届いた:

    ixsr[j] <- 5 for (j,zc) in ch if isocc[j] if isocc[j+2]
    ixcc[j] <- oseq[j] for (j,zc) in ch if isocc[j] if isocc[j+2]

測った（解釈実行と升まで突き合わせ）—— 値・ガードは **六段**、書き座標は
**五段**、二次元の座標の中でも三段。原理的な上限は無い（鎖は一段ずつ内側に
開くだけで、機械語としては `mov r9,[…]` が一本増える）。上限は超えたときを
試すまで上限ではないので、**源が実際に使う深さまで**測って試験に置いた
（`front.lx` は書き座標 5 / 値 6）。

これで `front.lx` に要る書き換えは **ガードの辺だけ**になった ——
書き座標 63 本・値 77 本の引きが不要になり、中間の場は 188 → 59 枚。

### 壁は次の一枚へ動いた（同じ形をしている）

一枚の本は **焼けるところまで来た**（1,048,576 バイトの像が出る。広い焼き手は
RCAP 983,040 / TOTAL 1,048,576 / CHARS 589,824）。走らせるとまだ断る。
理由は二つ残っていて、どちらも「断片が言語より狭い」同じ形である:

1. **ガードの辺に式が置けない**（本の中に 41 本）。`run.lx` の評価器の芯は
   `if (MC[…] + MK0[…] * zdat[…] + …) == …` という形で、辺が **項の列**である。
   引いて平らにすると、また一番内側の `t` に依るので値段が払えない。
   生成器は **値の側では**項の列を評価できる（`tmk` の種、八項まで）。
   同じ仕掛けを辺に繋げばよい —— 座標のときと同じ話である。

2. **足してから掛ける形**（本の中に 58 本）。`a + b * c` は焼いた符号が左から
   畳むので `(a+b)*c`、解釈実行は `a+(b*c)`。生成器は正しく断っている
   （黙って違う答えを出すよりよい）。直し方は二つ —— 源の側で掛ける項を先に
   出す（`b * c + a`。ただし掛ける項が二つ以上ある規則には足りない）、または
   生成器が **項ごとに一時レジスタで掛けてから足す**（本物の計算機がやって
   いること）。後者が筋である。

どちらも「回避できる狭さ」ではなく「生成器が言語に追いついていない所」である。
座標の鎖がそうだったように、**仕掛けは既にあって、繋がっていないだけ**という
形をしている。

> **気づき51: 数え上げの外にある五つ目は、いつも「他の四つを内側でやったもの」である。**
> 座標の源を四つ数えたとき、五つ目（内側でもう一度座標を作る）は数の外に居た。
> ガードの辺を三つ数えたとき（計数器・場の読み・定数）、四つ目（項の列）も
> 同じ所に居る。**数え上げた種は、再帰の一段目しか数えていない。**


---

## 項を群に分けた —— 「足してから掛ける」は焼ける

`a + b * c` は長いあいだ **焼けない形**だった。焼いた符号が `rax` を累算器に
した左からの畳みで `(a+b)*c` になり、解釈実行の `a+(b*c)` と食い違うので、
生成器は断っていた。断るのは正しかった（黙って違う答えを出すよりよい）。
だが断られる形は `run.lx` の評価器の芯そのものだった ——
`mu * pa[…] + mo * pa[…]`。

**群（積の連なり）の中では左畳みは正しい。** 直すのは群の境目だけである:

    群0 を rax に作る  →  mov [和], rax
    群k を rax に作る  →  add [和], rax   （群の符号が `-` なら sub）
    最後に            →  mov rax, [和]

だから「最初の項か」で mov / add を選んでいた所が **「群の先頭か」** になり、
引く項（種5/9/11/13）は「引く命令」ではなく **群の符号**になった。

### 和を置く所が無かった —— register は一つも空いていない

r8 変化印 / r9 座標 / r10 階数の面 / r11 階数の最大 / r12・r15 枠 /
rcx・r13・r14 計数器 / rbx 場の面 / rsi 読み。rbp すら fd で、源に
「唯一空いていた register」と書いてある。

stack は使えない。**場の読みが ⊥ のとき `je av` で次の行へ飛ぶ**ので、
push したまま抜ける道があり、周回ごとに漏れる。

答えは升だった。場の下（`gap`）には 512 バイトの余りがあり、使われているのは
`[rbx-8]`（表の終わり）だけである。`[rbx-16]` を和にした —— 升なら
**途中で飛んでも壊れない**（次の周回で群0 が上書きする）。

形は三つ足りただけだった: `add [rbx+d32],rax` / `sub [rbx+d32],rax` /
`mov rax,[rbx+d32]`。**載せる形（mov）は全部すでに在った** ——
定数 2 / 列 10 / 読み 85 / 計数器 109・111・113。「引く」種の先頭も
それを使う。

測った: 十の組（`a + b * c`、`a - b * c`、`a * b + c * a`、`a + b / 2`、
計数器・定数・列…）が升まで解釈実行と一致。「足してから掛ける（焼けない）」は
**「通る」に書き換えた** —— 直したのに断りを残すと、試験が嘘になる。

## 自分の源を読めなくなる 226 バイト手前に居た

上を足したら `_bigbuf` が「場の広さを超えた（場 7 = `incom`）」と言った。
`incom` は文字の面で、広さは 262,144。**`lattix.lx` は 261,918 バイトだった。**

生バイトの置き場 `rcap` は 524,288 と宣言してある。位置で引く場が
その半分しか無ければ「524,288 読める」は嘘である —— `dneed`/`dcap` と
同じ形の、**二つの数が一つの判断**であるところだった。

面を `rcap` に合わせた（33_self の 162 枚と fold の 9 枚。31_gen の
262,144 は命令の面なので触らない）。測った ——

    文字の面    memsz      自分を焼く
    262,144     631 MB     2.02 秒
    393,216     881 MB     —
    524,288   1,097 MB     2.15 秒

一割の時間で倍の源が読める。`test/accept.py` の「宣言の内側」の組を
260KB から **520KB** に上げた —— 二つの数が同じであることは、
覚えておくのではなく **測る**。

広い面は狭い焼き手では焼けないので、起動は三角を通る（bound だけ替えた
中継ぎ → 新しい規則 → 不動点）。`work/mkwide.py` と同じ手である。

## 残る壁は一つ、そして形が分かっている

一枚の本の外は **35 箇所**、全部これである:

    if vn[s, …] is MC[…] + MK0[…] * zdat[…] + MK1[…] * zdat[…] + …

二つ足りない ——

1. **`is`**（`a ⊒ c`。どの束でも単調な順序の検査）は、焼く側に形が一つも無い。
2. **ガードの辺が項の列**。生成器は値の側では項の列を評価できる（群になった
   いま、掛け算も正しい）。辺は「計数器・場の読み・定数」の三つしか取らない。

二つ目の直し方は座標の鎖と同じ形をしている —— **仕掛けは在って、繋がって
いない**。ただし繋ぎ方は座標より重い: 項は `trm` の行で、持ち主は
「規則の値」しか無い。ガードの辺を項の列にするには、
(a) 33_self が `if` 節の辺のトークンを **そのガードの項**として印し、
(b) fold が持ち主つきで `trm` に流し、
(c) 31_gen が持ち主ごとに項の鎖（位置・長さ）を回す、
の三段が要る。座標の `crd` が既に「持ち主が誰でも同じ形」で書かれているので、
**項も同じ形に直せる**はずである —— 座標は所有者種 0 項 / 1 ガード / 2 書き先
を持っているのに、項は持っていない。そこが継ぎ目である。

---

## 「完璧になってからリリースしたい」に答えるために作ったもの

願いは叶う。ただし **「完璧」を「欠陥が無いこと」から「嘘をつかないこと」へ
置き換える**必要がある。欠陥が無いことは **原理的に観測できない**からである。

その根拠は今日の記録そのものにある。今日見つけた誤りは五つ、**全部**
「触ったら出てきた」もので、読んで見つけたものは一つも無い。この報告書には
それ以前から同じ文が書いてある ——「全部『突き合わせで食い違った』から出た。
読んで見つけた物は一つも無い」。しかも今日は **測定器の方が二度嘘をついた**
（`accept.py` の括弧 regex、番兵を飲み込む `>= 10`）。上限は予告なく来た ——
焼き手は自分の源を読めなくなる **226 バイト手前**に居た。

観測できるのは一つだけである。**知らない形を黙って焼いたか。**
限界は許される（使う人は形を覚えて避けられる）。嘘は許されない
（使う人は戻ってこない）。だからリリースの条件はこう置くべきだ ——

> **嘘をつかないことが、覚えていることではなく、測られていること。**

### 作ったもの ①: `test/meta.py` —— 言語自身の法則で試す

`differ.py` は二つの実装を突き合わせる。強いが、**撒く文法が言語より狭ければ
狭い所の嘘は永久に生き延びる**。実際 `if a[i] + 1 >= 2` は焼けて segfault して
いたのに、`differ` は終了コードを見ているのに「食い違いなし」と言い続けた ——
その形を撒いていなかっただけである。

Lattix には他の言語に無い足場がある。**答えは最小不動点なので、規則の順序に
依らない。** そこから神託の要らない試験が四つ出る:

    順序      規則の並びを替えても答えは同じ
    平ら      引いてから配っても（中間の場を一枚置いても）同じ
    広げ      宣言を広げても同じ（増えるのは届かない升だけ）
    死んだ場  読まれない場を足しても同じ（動くのは配置だけ）

どれも「意味は変えず **配置だけを変える**」形をしている。今日の難しい誤りは
全部そこに居た —— 位置の鎖、飛び先、群の境目、番兵。答えを知らなくてよいので、
撒く文法をいくらでも深くできる。

**試験が落ちられることを先に確かめた。** 和の升をわざと `[rbx-8]`（表の終わり）に
ぶつけたら破れ 5 件を捕まえ、正しい生成器に戻すと破れ無しと言った。

そして本番 250 本で **破れ 5 件**（順序 2 / 平ら 1 / 広げ 1 / 死んだ場→segfault 1）。
accept 85/85・gen・selfgen・differ・fold・g4d が全部通っている状態で、である。

### 見つけたもの: **座標に濾しが無い**（まだ直っていない）

    field big : max bound 8
    big[i] <- 100 …
    field b : max bound 8
    b[i] <- a[big[i]] …

    解釈実行   b: ⊥ everywhere   （100 番の升は a に無い）
    焼いた側   b[0] = 0          ← **隣の場の升を読んで、それを答えにした**

鎖にすると読んだごみが次の座標になって segfault する（`f0[f1[f1[i]]]`）。
表の列を座標にしても同じ（`a[j]`）。

**基**: 濾し（`cmp r9, 広さ` / `jae`）は在るのに、`cdhf == 1`（ずれがあるとき）に
しか出ない。検査が **「座標」という概念ではなく「ずれ」という機能**に
紐づいていた —— 気づき34 の裏面である:

> **一つの判断を機能の側に置くと、その機能を通らない道から漏れる。**

直しも測った。スロットの終わりに無条件で濾しを置けば三つとも直り、
自分を焼いてバイト一致も保つ。だが **書き先の座標には既に別の検査があり**
（`wcp + 2`。はみ出したら場の番号を言って止まる）、手前に濾しを置くと
それを黙らせる。書きだけ外そうとすると二次元の書き座標（`r[q[i,k]]`）が
壊れた。そこで止めた —— **直せていないものを直したことにはしない。**

道中でもう一つ出た。**焼き手自身がこの穴に寄りかかっていた** ——
九項目の `trkd[s, 8]` を席の外へ書いており、濾しを入れたら自分が止まった
（源の側で `if tcnt[t] <= 7` と止めれば直る。これは入れた）。

### 作ったもの ②: `differ.py` の文法を言語に追いつかせた

直した形は必ず撒く —— 鎖の座標（`f0[f0[f0[i]]]`）、群（`a + b * c`）、
辺の式（断るはず）。広げた途端、200 本で **食い違い 6 本**（うち segfault 1）。
昨日までゼロだった数である。**試験は通っていたのではなく、見ていなかった。**

### だからリリースの条件はこう書ける

Lattix は他の言語より強い主張ができる立場に居る。三つとも既に手元にある ——

1. **答えが検査できる**（`attest.py`）。プログラムが仕様そのものなので、
   最小不動点であることは規則実例を一度なめるだけで局所的に確かめられる
   （安定性と有基性）。C にも Rust にもこれは無い。
2. **実装が二つあって、バイトまで一致している**（`g4d`）。C 測定器と Lattix 自身。
   これは Wheeler の diverse double-compiling そのもので、Thompson の
   trusting trust に対する実務上の答えである。
3. **限界が測られている**（accept 86 / differ / meta / fits）。覚えているのではない。

足りないのは一つだけ —— **検査器が Python にある**。§0 は「Python も C も
要らない」である。`attest` を `.lx` で書けば、使う人は `lattix` と `attest` の
二枚だけで **自分のプログラムの答えを自分で確かめられる**。そのとき初めて
「完璧」は私が保証するものではなく、**使う人が確かめられるもの**になる。

それがリリース条件だと思う。「バグが無い」ではなく、
**「間違っていたら、使う人が自分で気づける」**。前者は永遠に来ない。
後者はあと一枚で届く。
