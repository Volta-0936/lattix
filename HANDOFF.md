# 引き継ぎ — 次の担当へ

> **この文書も仮である。** 読んで頭に入れたら消してよい（文書は少なく保つ）。
> 中身は「的」「前任者が外した外し方」「いまの実測値」「次の一手」の四つだけ。

---

## 0. 最初に読む順（省略禁止）

1. `CLAUDE.md` —— 行動原理。とくに「Lattix が何であるか」の節と
   「やってはいけないこと」。**セッションの最初に、言われる前に読む。**
2. `PLAN.md` §0（目的）/ §2（作る物の形）/ §5（やってはいけないこと）。
   §6 は「いまここ」なので最後でよい。
3. この文書。

`PLAN.md` §0 を写す（言い換え禁止）:

> **自由に書かれた `.lx` を、Windows / Linux にインストールされた Lattix だけで
> 実行できるようにする。Python も C も要らない。最終形は配れる package である。**

`PLAN.md` §2 が形を決めている:

```
lattix（一つのネイティブ実行ファイル）
  = front.lx   源のバイト  → 規則の表
  + run.lx     規則の表    → 最小不動点
  + show.lx    答え        → 人が読める文字
三つとも Lattix で書く。焼くのは 31_gen.lx（Lattix で書かれた x86 生成器）である。
```

**焼くのは gcc ではない。** `lattix`（木の直下にある静的 ELF 512KB）は
`31_gen.lx` が書いた実行ファイルで、`test/g4d.py` が「自分を焼いてバイト一致」を
示している。**「Lattix が実行形式を書く」はもう閉じている。**
閉じていないのは、31_gen が読める範囲（断片）が狭いことだけである。

---

## 1. 前任者が外した外し方（同じ穴に落ちないために）

私は `run.lx` を **gcc で焼いた**。8.6MB の実行ファイルが出て、コーパス 52 本中
49 本が参照実装と一致した。それ自体は正しく測れている。**だが道が違う。**

- `PLAN.md` §2 は「焼くのは `31_gen.lx`」と書いてある。gcc は出てこない。
- `PLAN.md` §5-2「言い換えを進捗と呼ぶな」に真正面から当たっている。
  私は「ネイティブの実行ファイルが一個できた」を進捗と呼んだ。的は
  「**Lattix だけで**実行できる」であって「ネイティブになった」ではない。
- 原因ははっきりしている。**私は `PLAN.md` を読まずに §6.8 だけを書き換えた。**
  §0 と §2 を読んでいれば、gcc に渡した時点で気づけた。

**教訓（次の担当への具体的な指示）**:
- 手を動かす前に `CLAUDE.md` と `PLAN.md` §0/§2/§5 を読む。毎セッション。
- 「これは §0 のどの部分を前に進めるか」を一行で言えないなら、着手しない。
- C バックエンド（`native.py` / `runtime.py`）は **測定器**である。
  二つ目の実装は試験より強い試験なので（気づき49）穴を出すのに使ってよいが、
  **配布物の道ではない**。`test/nativecheck.py` / `test/runtimecheck.py` /
  `test/baked.py` はどれもその category である。
  - `test/baked.py` は私が足した（`run.lx` を gcc で焼いて表を差し替える試験）。
    測定器として残すか消すかは判断してよい。**道ではない**とだけ憶えておくこと。

---

## 1.5 第2段の現在地（front.lx —— このセッションで立てた縦の列）

`front.lx` が動き始めた。**33_self.lx + lib/fold.lx の後ろに繋ぐ算術の断片**で、
源のバイトから家の表（dat/spc/ssz/mp/fg/fc/fp）を出す。突き合わせは
`test/front.py` —— 番号（軸・空間・写像・eid・面の基）はラベルなので、
**中身で正規化してから** flat.py の表と比べる。走らせ方は C の測定器で一度
焼いて表（ch/orc/ext）だけ差し替える（selfgen.cgo。焼き直しは front.lx を
書き換えたときだけ、約 60 秒）。

通っている: **31 本が完全一致**（work/t のほぼ全部 + 01/03/08/18/26/27 ほか）。
入っている物: 軸と空間の intern（min と前置きの鎖）/ 場の置き場（名前の辞書順、
左詰めの鍵）/ 測った広さ（区間算術で静的に出る —— flat の「一巡目」の正体）/
表は何枚でも（行は全表を通した鎖）/ range(a,b) / 添字なしの場 n[] /
値が上端の区間（閉じた値の口 table orc —— BSP の継ぎ目）/ 2次元の頭と読み /
読みの中の読み（zrk : flat の同層再帰 —— LVar の座標読みは単調）/ 値の鎖
（*/÷/% —— fp 種3/4）/ 束ちがいの読み（fp 種0、生きた flat は lt −5）/
精密な成層 zlvl（比較を束の向きで見る。fc 種11 = 生きた単調な比較）/
fourv（Belnap の否定は門の種5）/ **構成子**（脱糖・Horner の折り込み・
番地の鎖 —— z_twoctor 完全一致。_fold は +1 つきに注意）。

まだ: 構成子の 原子/一次式/入れ子 の引数（16_lex, z_atomarg, 21/22）、
文字列原子（33_self の字句が引用符を知らない）、ガードの辺の式（gkd 15）、
`is`（種4）、budget/source/emit/component/include、誤りの報告。
参照側の断りは別軸: 集合の一元 {v}（02/05/06/12/13/14/30）、i−f[i] 座標、
23_big の生きた pa。

**このセッションで参照実装の穴も一つ出た**（気づき49 の再演）: flat.py の
_frefs は添字の中まで降りるので `_size[a[i]] + 1` の内側の a[i] が重みに
足し込まれていた → _tops に直した。再現 work/t/z_nestwm.lx。同じ本は
**点の道の焼いた側では s が空**（ix の間接が静かに落ちる）—— 未修理。

## 2. いまの実測値（処理系が出した数字だけ）

### 2.1 走らせる物（`run.lx`）—— PLAN 第1段

| 測り方 | 結果 |
|---|---|
| `test/relation.py`（本ごとにエンジンを起こす） | 一致 49 / 未対応 3 / 食い違い 0 |
| `test/onerun.py`（**配る一枚** `run.lx` 一枚で全部） | 一致 45 / 未対応 5 / 食い違い 0 |
| `sh work/run_all2.sh`（小さい 19 本） | 一致 19 / 食い違い 0 |
| `test/baked.py`（gcc で焼いた `run.lx`。**測定器**） | 一致 49 / 未対応 3 / 食い違い 0 |

未対応の中身: `07_illposed` と `11_badsig` は **断られるべき本**。
残るのは `04_aggregate`（「層 0 で閉じた値を問うている」）と、
`onerun` の二本（`15_parse` / `29_dataflow` が 400 秒で切れる＝速さの宿題）。

→ **第1段（走らせる物を全意味論へ）は、出口条件にほぼ届いている。**
   残っているのは `04_aggregate` 一本と速さ。

### 2.2 まだ通していない検査（重要）

**`./check` を、最後の変更（集合・袋・集約・原子）のあとに通していない。**
その一つ前の状態では 45/45 だった。次の担当は**最初に `./check` を通し、
落ちた物を直すところから始めること**。

### 2.3 木の状態

- `run.lx` 2,399 行（形 37 / 条件 32 / 束 10 / 層の上限 16 / 升 2.14×10^17）
- `work/flat.py` `.lx` → 表（**ここがまだ Python。第2段の的**）
- `work/engine2.py` 表 → `run.lx` の本文を書く（形の一覧から規則を並べる）
- `work/mkrun.py` コーパスを測って `run.lx` を書き直す（形と上限を**測る**）
- `check` 45 本

---

## 3. 前任セッションで入れた物（信用の可否を判断できるように）

### 3.1 意味論を直した（参照実装・C 二種を揃えた）

- **flat / fourv の升は LVar である。** ⊥ → v で止まるので、読みは値でも
  座標でも述語でも単調。単調でなくなる出口は ⊤ だけ。そこを二つ塞いだ:
  - **⊤ は「少なくとも真」**（比較が ⊤ で偽に潰れていた）
  - **⊤ は座標になれない**（黙って飛ばすと「起きたか」が日程で変わる）
  - 決め方は推測ではない。`test/discover.py` の乱択反証器が座標の読み
    （`f[g[x]]`）を一度も撒いていなかったので撒くようにしたら反例が出た。
- **双子（`examples/22_parse.lx`）にも同じ判断を教えた。** `test/selfparse.py`
  が全 16 本で一致する。
- 副作用として層が浅くなった（16_lex 3→2、20_lex_full 5→4、21_lex 7→6）。

### 3.2 多面体の道を広げた（`PLAN.md` §6.8-2）

- **ガードも写像で書ける**（`fc` の表）。「全部立った」は (辺, 点) で数える。
- **閉じた層だけで決まる問いは表に載せない**（層ごとに一度だけ閉じる）。
- **値で決まる座標も一次式に戻せる**（`fp` の表 / 番地の面 `pa`）。
  `f[g[i]]` の `g[i]` は点ごとに違うが、その値の置き場もまた空間である
  （点 t につき一升）。使わない写像は `pa[0] = 0` を読む —— 場合分けを
  作らないための番兵。
- 効き方: `out[i] <- val[idx[i]]`（点 1000）が **辺 0 / 多面体の辺 3 /
  番地の写し 1**。`32_shape` の 147,456 点の規則が 49万本の辺に潰れなくなった。

**これが第2段（`front.lx`）に効く理由**: 前段の出力が
**プログラムの字数に比例する**（データの量ではない）。実例を数え上げる仕事が
消えたので、`front.lx` が現実的な大きさになる。

### 3.3 内容アドレスを 56 ビットに戻した

番地を座標にする場は番地の幅ぶんの帯を一本占める。31+31=62 ビットだと帯四本で
2^64 を超え、升の番号が i64 に入らない。**SPEC が最初から 56 と書いていた。**
`lattix.py` の `CTOR_M1/M2` を 2^28 近傍の素数にした（`test/ctorhash.py` が
C と Python の一致を測る）。升の上限 1.64×10^19 → 2.14×10^17。

### 3.4 測定器（C 側）に入れた直し —— 道ではないが、穴は本物である

焼いてみて初めて出た穴。**Lattix 側で焼くときにも必ず踏む**ので記録する。

- 集約の「一度だけ撃つ」印が、源の一本目だけで番号を作っていた。表と区間が
  同じ規則に並ぶと、行ひとつにつき一度しか撃たない（気づき34 の九度目）。
  **寄与元キーは束縛の組そのもの**である（`lattix._prov` と同じ同値）。
- `fourv` は「値が二つしか無く否定が入れ替えるだけの flat 束」として置ける。
- 密配列の広さを、いま手元にある表で測っていた（実行時読込では表が差し替わる）。
  **宣言した `bound` を優先する**（気づき44）。
- 集合と袋は要素の域を宣言できない（面に入る要素は升の値だから）。
  **整列した並びを arena に置いて番号で指す** —— 合流は併合、写しは番号の共有。

---

## 4. 次の一手（`PLAN.md` の段取りのまま）

### 第1段の残り（小さい。先に片付ける）

1. `./check` を通す。落ちた物を直す。
2. `04_aggregate` —— 「層 0 で閉じた値を問うている」。`work/flat.py` の
   `cond()` が、寄与元キー付きの束（sum/count/bag）を生きた面で比べるのを
   禁じているのが原因。層 0 で閉じた面が無いので断っている。

### 第2段 —— `front.lx`（**ここが本丸**）

`examples/32_shape.lx` が既に「規則・項・ガード・座標・ループ」の表を
源のバイトから出している。多面体の道が入ったので、そこから先は

```
規則     → fg の一行 ＋ 写像（mp）
ガード   → fc の一行 ＋ 写像
源       → 軸（dat / spc / ssz）
座標     → 写像（一次式。値で決まるものは fp と番地の面）
```

という**算術だけ**の仕事になる。実例の数え上げは要らない。
`work/flat.py` が Python でやっていることの、多面体の側だけを写せばよい。

出口条件（`PLAN.md` §第2段）: 全 34 本 + `noema.lx` について、
`front.lx` が出す表が Python が出す表と**完全に一致**する。

### そのあと

第3段 `show.lx` → 第4段 **`31_gen.lx` に足りない形を足して三つを焼く** →
第5段 包む（Linux tar.gz / .deb、そのあと Windows は PE 容器 + kernel32 の口）。

**第4段が「C も Python も要らなくなる」場所である。** そこまで gcc は測定器。

---

## 5. 検査の回し方（時間の使い方）

前任者は `./check` （一周 3 時間）を五回まわして時間を溶かした。

- **速い部分集合**で回す: `sh work/run_all2.sh`（3 分）/ `python3 test/nativecheck.py`
  （3 分）/ `python3 test/runtimecheck.py`（4 分）/ `python3 test/relation.py`（20 分）。
- **`./check` は一連の変更をまとめて最後に一度**。
- 報告は区切りごとではなく、**実際に一段進んだときだけ**。
