# 引き継ぎ — 次の担当へ

> **この文書も仮である。** 読んで頭に入れたら消してよい（文書は少なく保つ）。
> 中身は「的」「前任者が外した外し方」「いまの実測値」「次の一手」の四つだけ。

---

## 0. 最初に読む順（省略禁止）

1. `CLAUDE.md` —— 行動原理。とくに「Lattix が何であるか」の節と
   「やってはいけないこと」。**セッションの最初に、言われる前に読む。**
2. `PLAN.md` §0（目的）/ §2（作る物の形）/ §5（やってはいけないこと）。
   §6 は「いまここ」なので最後でよい。
3. この文書。

`PLAN.md` §0 を写す（言い換え禁止）:

> **自由に書かれた `.lx` を、Windows / Linux にインストールされた Lattix だけで
> 実行できるようにする。Python も C も要らない。最終形は配れる package である。**

`PLAN.md` §2 が形を決めている:

```
lattix（一つのネイティブ実行ファイル）
  = front.lx   源のバイト  → 規則の表
  + run.lx     規則の表    → 最小不動点
  + show.lx    答え        → 人が読める文字
三つとも Lattix で書く。焼くのは 31_gen.lx（Lattix で書かれた x86 生成器）である。
```

**焼くのは gcc ではない。** `lattix`（木の直下にある静的 ELF 512KB）は
`31_gen.lx` が書いた実行ファイルで、`test/g4d.py` が「自分を焼いてバイト一致」を
示している。**「Lattix が実行形式を書く」はもう閉じている。**
閉じていないのは、31_gen が読める範囲（断片）が狭いことだけである。

---

---

## 2. 第A段の現在地（2026-09-06。詳しくは `REPORT.md`）

**A.1 原子の型 —— 済。** 見せる物の `fat` は参照の答えを見て作っていた。
原子性は (場, 次元) を升にした `or` の場の不動点である（表の綴りの列 zacl に
束ねた変数を座標／値に書いたか）。`flat.fat_of` に置き、**前段にも三本**で
持たせた（座標・値の項・構成子の引数場）。三段の通し運転が原子の本 6 本で
バイト一致、BOOKS 66/66、自己適用 1764 規則 完全一致。

**A.3 閉じの器を外す —— 骨は済（60/66）。** `test/twopass.py`:

    一巡目 front（仮の帯） → 表 → run.lx → 面
    口 ＝ その面から読む（閉じた値・触れた升の広さ）
    次の巡 front（測った口） → 表 → run.lx → …

置き場を戻すのも前段自身の置き場の場（zfb/zfna/zfs0/zfs1/zflo*/zfsz*）で行う。
**一致 60 / 断る本 2 / 食い違い 4**。開いたもの三つ:
- **巡の数は二ではない。広さの鎖の高さである**（実測 2〜3 巡）。参照は 2^62 の
  帯を敷けるが機械の升には上限があるので、「広いが有限」の帯を敷いて測って広げる。
- 添字なしの場（`n[]`）は前段では一次元の升 0 に居る（口を () で引くと落ちる）。
- 静的な広さで一巡目を走らせると、値で決まる座標が **黙って落ちて**鎖が伸びない。

食い違い 4 本（noema / 15_parse / 22_parse / 25_eval）は全部 **構成子の重い本**で、
理由は測って分かっている —— 内容番地が座標なので、帯を敷かないと升が立たず、
敷こうにも 2^56 に散らばるので機械の升の上限を超える。**疎な番地を intern して
密にする**（REPORT §3.3）が要る。他の 60 本には要らない。

**A.2 前々段（include / use / emit）—— 未着手。** 効く本は 8 本。`include` は
入出力なので前々段の仕事ではない（実行ファイルが繋いで渡す）。残るのは `emit`
（小さい字面の書き換え）と `use`/`component`（付け替えつきの取り込み）。
出す物は「新しい `ch` の表」でよい —— 場を print して結線が渡せば、
前段の `for (t,zc) in ch` を一行も書き換えずに済む。

**A.4 一枚の本 —— 走った（2026-09-17）。** `work/onebook.py` が `run.lx` の
表の読みを **機械的に**場の読みへ書き換え、前段と同じ一冊にする。読む表は
`ch` 一枚（`orc` / `ext` は番兵一行のまま＝口の要らない本では実質使わない）。
測り方は `python3 test/one.py`。**表を運ぶ Python が一行も無い。**

書き換えは四つの判断でできている（どれも測って分かった。REPORT の A.4 の節）:

1. 表の行 → 鍵つきの場の読み（`for (e,sp,st,…) in fg` → `for (e) in 0 .. znr2m[0]
   if Fgu[e]`、列は `Fgsp[e]` …）。**番号は前段のものをそのまま使う** ——
   `compact`（pipeline.py）が要ったのは機械の配列が小さい番号しか引けないから。
2. **行が在ることを立てて言う**（`if Fgu[e]` / `if Fcu[e,i]` / `if zfpu[e,u]`）。
   入れ忘れると門の数 `fq` が 9 になり、家の寄与が **静かに** 一つも書かれない。
3. **無いことは `not` では測れない**（0 も偽）。既定が 0 でない列は
   `>= 0` で在ることを立ててから否定する。
4. **列は場ではない** —— `[` が続く綴りは列ではない（面 `vf` と eg の列 `vf`）。

**払った代償は 10 形**（`sum`/`bag` の面を **生きたまま**読む寄与と、その面を
生きたまま問う門）。表の列は地上の値なので `_fires` が「この形は起きない」と
数えられたが、場になると数えられず `sum` の符号が落ちる。

**ここでも一度、読む前に書いて外した。** 「`fg` に `la` が無いのが穴」と
書いたが、`flat.py` を読むと束の違う源は **もう `pa` を通して閉じた面から
写している**（flat.py 1293 行）。家の道の `am` が同じ束・生きたままなのは
設計であって抜けではない。

**本当のところ**: 刈られた 10 形は「`sum`/`bag` の場が同じ束の場を
**生きたまま**読んで足す」形で、`work/mkrun.py` は `pa` の側では
**同じ理由で既に除外していた**（「符号の要る束を生きたまま pa へ写す規則は
run.lx 自身が成層できない」）。**同じ判断が二箇所にあり、片方（直の読み
`am`）だけが緩かった**（気づき34、四度目）。表の形で通っていたのは番兵の表の
おかげで、本物の表を入れれば同じ run.lx も成層できない。だから刈るのは
失うことではなく、**もともと緩かった判断を揃えること**である。

**宿題**: `fl.fsh` が表の中身と食い違う（x_sumstrata の `fg` には `lat=8` の
行があるのに `fl.fsh` は `[(2,0,0)]` としか言わない）。mkrun と
baked.coverage がこれを読んでいる —— 表から作り直すこと。

**口を一枚に入れる形も分かった**: 素直に `zorc` を面から読むと
置き場 → 面 → `zorc` → 置き場 の輪になって棄却される。正しいのは
**pass を座標にする**こと（REPORT §3.1）—— 面と置き場に pass 軸を足し、
pass P+1 の口が pass P の面を読む。巡の数は測ってある（2〜3 巡）。

**実測（2026-09-17）**: `work/t` の小さい 20 本が一致、食い違い 1 ——
`x_sumstrata.lx` の `total`。

**この食い違いの理由を、私は一度測らずに書いて外した。** 「刈った 10 形の
代償」と書いたが違う。刈った形を **形**で言わせると全部
「同じ面を生きたまま読む寄与」（`vf=1`/`vf=4`）で、x_sumstrata の `total` は
`(lat=8, vf=0, n=0)` ——刈られていない。**本当の理由は口である。**
`for (i) in 0 .. n[]` の上端は閉じた升の値で、前段は口（`orc`）から受け取る。
一枚の本は番兵のまま渡すので空間の点が 521 でなく 1 になり、`sum` の面が
一升も立たない。つまり A.4 の食い違いは **A.3 の残りが顔を出したもの**である。

**口が要る本は 34 本中 3 本**（実測）: x_sumstrata / y_paseam / z_depint。
残り 31 本は要らない —— 測った 20 本は全部そちら側。

v_poly から先は参照実装の解釈実行が 300 秒で切れる ——
一枚の本の遅さは参照実装の遅さである。

**面は字面までバイト一致する**（2026-09-17）。`LATTIX_SHOW=1 python3
test/one.py` で、測った 20 本すべてが `python3 lattix.py f.lx` と
**バイト一致**（18〜74 バイト）。見せる物はまだ表で食っているので、この段は
「一枚の本の面が字面を作れるだけ揃っているか」の測定 —— 揃っていた。
**源のバイトから字面まで、答えの側に Python は居ない。**

**前段が `print` の文を読むようになった**（front.lx、2026-09-17）。
`print f` の `f` は出現ではない（出現は `f[` の f）ので、綴りの鍵を宣言と
同じ畳み方で作って突き合わせる。道中で一つ測って分かった: **`tch` は 8 文字
しか持っていない**（宣言の `dch` は 16）。8 文字で切ると長い名前
（`unreachable_from_0`）が二本目の鍵で食い違うので、綴りは **文字の流れから
直に**拾う。実測: `work/t` の 18 本と 16_lex / 20_lex_full / 02_strata で
参照実装の `p.prints` と一致。

残りの表も前段が持っている: `fld` は zlatf / zfna / zfb / zflo* / zfsz* /
zfs*、`fnm` は 33_self の `dch[d,k]`、`fat` は `zfat`、`ab` は `zatb`。
**show.lx 側の書き換えだけが残る** —— run.lx と同じ機械的な仕事だが、`pv` は
疎なので場にすると宣言した升を全部なめる形になる
（`for (f) for (c) in 0 .. fvz[f]`。構成子の規則は `if nof[f] >= 0` で絞れる）。

**刈った 10 形を使う本を数えた（64 本）**: 寄与 6 形は **一冊も無い**、
門 4 形は **04_aggregate.lx の (kk=11, lat=8) 一つだけ**。数え上げた 10 形の
うち 9 形は誰も使っていなかった —— 「数え上げた形であって測った形ではない」が
そのまま出た。

**見せる物も一枚に入った**（2026-09-18）。`show.lx` の十枚の表も場の読みに
書き換え、`LATTIX_SHOW=1 python3 test/one.py` が **一枚の本が出したバイト
列**を `python3 lattix.py f.lx` と直に比べる。字面の表（topc / emp）は種の場に
直したので、**表はもう `ch` 一枚**である（orc / ext は番兵一行のまま）。

道中で三つ、やり方の誤りを測って直した ——

- **規則は行ではない。** show.lx は手書きなので続きの行がある。行で切ると
  規則が割れる（`isset` の `c` が宙に浮いた）。論理規則で切ること。
- **書き手の消えた場は、読んでも正しい。** `if not isnv[t]` は書き手が
  居なければ真で、それがこの段の意味である。読む規則まで落とすと
  `num[t,3]` が道連れになり、値が一つも出なくなる（`d[0] = ` だけが出た）。
  **宣言は残す。**
- **名前は座標である。** `farr` / `nch` / `neg` が前段と綴りでぶつかる。

**一枚の本に残っている穴は、口だけである**（work/t 34 本を数えた）——
口が要らない **24 本**、orc だけ 2 本、ext だけ 7 本、両方 1 本。
要るからといって落ちるとは限らない（z_dim2rd / z_nestwm は渡さなくても
バイト一致）。**実測でバイト一致 25 本。**

**口の ext のうち二つは前段が自分で数えられた**（front.lx に追加済み）:
① 合成の場の広さ = 構成する場所 × その点の数（`zcsn`）。口が無いと `zfsz0` が
⊥ で箱が立たず、番地が 2^56 のまま書かれて隣を踏む（z_twoctor は `pa` が
507 升中 504 升 ⊤ だった）。② 値の位置の構成（`zcrr`）の枠 24 に法が無かった
（合成規則 `zissy` の枠 24 には入っていた ——同じ判断が二箇所にあって片方だけ
抜けていた）。これで構成子の本は `p[0] = pair(, )` まで来た。

**構成子の本は通った**（z_twoctor ✓ 61 バイト / z_atomarg ✓ 62 バイト）。
決め手は三つ目 —— **広さは数えられるが、法は測るものだった。** 参照実装は
「一巡目に触れた番地の集合が衝突しない最小の 2 の冪」を取る（flat.py）。前段に
できるのは **詰め込み率を決めること**だけ: 升の数の 4 倍以上の最小の 2 の冪
（最低 8）。測った値ではなく設計の定数であり、外せば衝突して ⊤ が声に出る。
法を升の数そのもの（3）にしたときは 3 升に 3 つ入れて衝突していた。

もう一つ: **綴りのトークンに引用符は入っていない**（`tag(alpha,0)` が
`tag(lpha,0)` と出て分かった）。**実測でバイト一致 27 本。**

**集合は場にできない。** 面に置かれた集合から元を取り出す言葉が Lattix に
無い。機械は arena に整列して置いて番号で引くが、それは面の外の構造であって
規則からは見えない（02_strata が `⊥ everywhere` と出るのはそれ）。塞ぐには
言葉を足すか、走らせる物が元を点ごとの面に控えるか。

**焼く物を実物で数え直した**（2026-09-18。REPORT §2.3b）。`all.lx`（a_min の
一枚）は 源 489,457 バイト / 規則 2,889 / 場 1,072 / 表 3。数えたら **壁は
思っていたより小さかった**:

- **④ 三次元以上は 6 場だけ**（esame 4次、fn/fo/ech/eless/dgt 3次）。後ろの軸の
  広さを掛けて足せば二次元になる —— 座標の意味も升の数も変わらない（積が同じ）。
  `work/onebook.py:fold3` で畳んで **三次元以上は 0、字面はバイト一致のまま**。
  **④ は閉じた。**
- **③ 断片に無い束は 4 場だけ**（va / v4 / vs / vb の面が一つずつ）。集合と
  bag を外せば残るのは `and` と `fourv` の二束である。
- **⑤ 源の置き場は壁ではなく費用だった。** 33_self は 5,470 万升（0.44 GB）を
  宣言している。倍にしても 0.9 GB 程度。しかも **源の置き場は二つある** ——
  *配る*物は 262,144 でよく、1M 要るのは *焼く*（bootstrap）側だけである。

**第C段を実際に叩いた**（REPORT §2.3c）。`./lattix` に一枚の本を食わせて:

1. **焼いた実行ファイルが読める表は一つだけ**（`reason 6: need one table`）。
   番兵の `orc` / `ext` を落として **読む表は `ch` 一枚**にした（`oneport=True`）。
2. **断片の六束に絞れる**（`six=True`。落ちるのは va / v4 / vs / vb の 43 行）。
3. 三次元以上は畳めば消える（`fold=True`）。
4. 一枚の本は **512,952 バイト / 表 1 / 六束 / 二次元**。33_self の源の置き場を
   262,144 → 524,288 にすれば **入る**（97.8%）。置き場を倍にした焼き手
   （`/tmp/lattix-big`）は自分で焼けて小さい本も焼ける。

**次の壁は 31_gen 自身の上限である**: 場ごとの配列が `bound 1024`（一枚の本は
**場 1,068**）、規則ごとの配列が `bound 2048`（**規則 2,889**）。**そして超えても
言わない** —— rc=0 で 23 バイトの ELF の頭だけが出る。黙って空を返す、この企画が
いちばん嫌う壊れ方である（31_gen には `zov` のような見張りが無い）。上限を
16384 に上げると今度は **segfault**（記憶 1.07 GB → 2.15 GB。容器は 7 GB）。

**そして 31_gen は、もう言っていた。** 符号面の註に「踏んだことは黙って起きる
ので、今度は言うようにした —— 符号の長さを ELF ヘッダの空き（8..10 バイト目）に
書く。読む側が確かめられる」と書いてある。23 バイトの頭にもその三バイトは
入っていて、読んだら **`codelen = 7,864,638`** —— 符号面 131,072 バイトの
**60 倍**だった。一枚の本（規則 2,889）は約 8MB の機械語になる。
**見張りは要らなかった。書いてあったのに、誰も読んでいなかった。**

それでも見張りは入れた（31_gen 末尾、理由 0「箱が足りない」）。`nfl` / `now` /
`nrl` を `cap*` と比べ、既にある断りの口に流す。上限は一箇所で言う。
ただし **符号の長さはここでは言えない**（断りは命令を書くので 符号 → 長さ →
断り → 符号 の輪になる。処理系が正しく棄却した）。見張りは符号面が既に
溢れているときは勝てない（`not bad[0]` の抑止が無い）—— 第C段の宿題。

**bootstrap の輪**: 面を大きくした焼き手を焼こうとすると、その焼き手自身の
符号が 415,878 バイトになり、いまの焼き手の 131,072 に収まらない。
**大きい焼き手は、大きい焼き手でないと焼けない。** 出口は C の測定器
（気づき49）—— 符号面を持たないので、大きい焼き手をそのまま走らせて
「一枚の本は焼けるのか」を測れる。

**残り**: 上の見張り、③（and / fourv）、⑥（⊤ の規律）、口の本（pass を座標に）、
集合、大きい本（解釈実行が遅い）、刈った形（64 本中 04_aggregate の門ひとつ）。

**成層器を速くした**（`lattix.py:_stratify_core`）。座標の集合は (規則, 場) だけで
決まるのに、書き手の数だけ歩き直していた。一枚の本では成層だけで数十分
掛かっていたのが数秒になる。**判断は変えていない** —— 成層を見る五つの門
（verify / selfhost / sabotage / certify_all / axisstrat）は通ったまま。

**A.5 大きな本 —— 未着手。** A.3 の実測が一部を先に答えた（22_parse / 25_eval は
二巡の輪で 600 秒で切れる）。

**run.lx は上限を超えたら言うようになった**（`zov`。層・空間・写像・辺・
指し番号・行列・点・番地の八つ）。層を宣言の上限に押し上げた表で立ち、
正常な表では立たないことを確かめてある。空の表の番兵（層 999）が
flat.tables / mkrun.pad / pipeline の三箇所に書かれている —— 番兵を
「本物の個数」にする直しは宿題（気づき34 の形）。

---

---

## 3. 第C段（焼く）の現在地（2026-09-17）

**測って分かった順序の訂正。** 「31_gen を広げて run.lx を焼く」は道ではない ——
焼いた実行ファイルが読める表は **標準入力の一つだけ**で、run.lx は 11 枚読む。
`./lattix run.lx` は終了コード 6、C の測定器（E0）は 512KB を出すが、その
実行ファイルに 11 枚の表を渡す口が無い。だから順序は **A.4（一枚の本）が先**で、
そのとき run.lx の表は front が書く **場**になり、読む表は `ch` 一枚だけになる。
第C段はその一枚を焼く。

**① 二次元の読みを座標に置く —— 済**（108c903）。SPEC の「既知の穴」。
`v[s, rz[s,t]]`（run.lx の芯）と `o[p[i,j]]` が焼けて、解釈実行と升まで一致する。
直し方は **列を足すことではなかった** —— 内側の読みはもう所有者の番号を持って
いるので、外側は源5 で指すだけでよい。内側は **r9 だけで畳む**（外側の畳みの
最中に評価されるので r12 に触れない）。g4d バイト一致・accept 61/61。
道中で「形の数 168」が三箇所に書いてあり、形を足した日に命令の並びが黙って
途切れた（気づき33 の再来）——`nfm[0]` 一箇所にした。

**残り（読んで数えた。REPORT §2.3）**:
- ② 表が一枚 —— A.4 で消える（run.lx の表が場になる）
- ③ 束 and / fourv / set / bag（run.lx が十束、断片は六束）
- ④ 三次元・四次元の場（run.lx に 2、show.lx に 3+1）—— **A.4 の `fold3` で閉じた**
- ⑤ 源の置き場 262,144 バイト（前段は 308,172）
- ⑥ ⊤ の規律が x86 生成器に無い（既知。比較は ⊤ で偽・⊤ の座標は誤り）

**⑤ の正体（2026-09-18 に測った）。** 境目は 262,144 文字ちょうど
（262,145 で終了コード 6）。だが **読み口 `rcap` は 524,288 バイト**ある ——
*読める量の半分しか置けない*。この二つは一つの判断で、二箇所に書いてあった。
揃えるなら 524,288（読み口が既に約束している値）で、場が 232 枚あるので
`p_memsz` は +486 MB（いまは 1.09 GB）。一枚の本は 512,874 バイトなので、
揃えた瞬間に **焼ける側に入る**（余裕 2.2%）。次の一手はこれである。

**今日閉じた上限（REPORT §4.9 に七つ数えた）**:
種 1,024 → 4,096（`sdno` は最初から 4,096 だった）、命令 131,072 → 262,144、
`p_filesz` と ELF の `codelen` に四バイト目、知らせの番地・`dbase`・`fbase` を
`total` から導いた。見張りに `capi`（命令）と `caps`（種）を足した。

**そして終了コード 6 が口をきくようになった** —— 焼く側でも焼けた側でも
`lattix: field bound exceeded - input too big for this .lx` の一行が出る。
これまでは数字の 6 だけだった。**この系の沈黙は、いま全部声になっている。**

**階数を 4 バイトにした（同じ日の後半）。** 升は値 8 + 階数 8 だったが、
階数は周回の番号で 8 バイト要らない。`r10` を使う七つの形だけ倍率 8→4 に
落とす（**長さは一バイトも変わらない**ので形の表も番地の鎖も動かない）。
`p_memsz` 1,041 MB → **786 MB**。証明書の読み手（`test/gen.py` /
`test/selfgen.py` の `'q'` → `'i'`）も同じ turn で直した ——
**階数は出力である**（不変条件6）ことを忘れると、
「答えは合っているのに証明書が読めない」という壊れ方をする。

### いま分かっている天井（数字。REPORT §4.9）

| | 値 | 効いているもの |
|---|---|---|
| 場の置き場ぜんぶ | **2 GB** | 場の読みが `[rbx + 符号つき imm32]` |
| 焼き手が出せる像 | 焼き手自身の `rcap + 65536` | 詰め直しの置き場がそこだから（**二段の起動が要る**） |
| 源の長さ | 文字面の `bound` | 一文字あたり **216 升 = 2,592 バイト** |

### 升の幅は束が言う（同じ日の夜。REPORT §4.9）

**倍率を穴にした。** x86 の SIB バイトは上二ビットに倍率を持っている ——
形の表はそれを定数で埋めていただけで、幅ごとに形を二重に持つ理由は無かった。
SIB を持つ形（17）も、位置（穴の一つ手前）も、倍率を抜いた値も、
**表から導く**（`mrm` → `mrmr % 8 == 4` → `hassib` → `shsb` → `sibb` / `fmsc`）。
表は一行も書き換えていない。この段だけ入れて 19 本 **バイト一致**。

**そして `fwb[f]`。** `or` の升は 0 か 1 しか取らないので 1 バイト。
読みだけは形が要る（x86 はオペランド幅が opcode にあるので倍率のように
穴にできない）が、**幅が形を選ぶ**ようにすれば場所の書き方は変わらない ——
`form[gc[gg]] <- frd[gdf[gg]]`。足した形は五つ。

`p_memsz` 795 MB → **599 MB**。一日で 1,041 → 599 MB（**−42.5%**）。

**読む側が五箇所あった。** `test/gen.py` / `test/selfgen.py` / `test/accept.py` /
`test/differ.py` / `test/g4d.py` —— どれも「一升 8 バイト」を前提に書いてあった。
全部「幅は束が言う」に直した。**場の吐き出しは境界面である**ことを忘れると、
「答えは合っているのに読めない」という壊れ方をする（階数のときと同じ）。

**やりかけて、やめたこと。** 最初は「輪の上端や座標に読まれる `or` の場は
8 バイトのままにする」(`wide8`) と書いた。動いたが、幅が生成器の内部都合で
決まると読む側が `wide8` を知らないと解けない —— **同じ判断が二箇所**である。
幅は束だけが言い、束と合わない使われ方は **理由8 で断る**ことにした。
`work/t` 33 本と `lattix.lx` で、断られたものは一つも無かった。

### 一枚の本が焼けた（2026-09-18 深夜）

**見張りに「踏んだ場所」を言わせたら、半日探していたものが一回で出た。**
規則ごとに二命令の踏み台（`mov r15d,<場番号>; jmp ovf`）を置き、出口で
像の末尾の枠に落として四バイト吐く。当ててみたら:

    踏んだ場の番号: 443  →  coff（座標の要素の前置きの鎖、bound 32768）

**座標の要素**だった —— ガード・項・輪・場・文字のどれでもない、撒いた試験が
一度も触っていない量。一枚の本は `fold3` で二次元の場が多いので、座標の要素が
規則より速く増える。`bound 8192` の族を 131,072 にしたら **焼けた**
（655,360 バイト、codelen 121,601、memsz 1,100 MB、焼き手は天井の 64.8%）。

### だが走らせると、次の壁は言葉だった

    lattix: cannot bake - line 001415 reason 8: unsupported form
    zsnr[hfz[osn[o]]] <- true for (o) in 0 .. nocz[0] ...

**書き座標の読みが二段**。断片が許すのは一段（`tstart[tnum[i]] <- i`）まで。
数えたら **一枚の本に 78 箇所、うち 48 箇所が `front.lx`**。`front.lx` は
解釈実行に向けて書かれたので、書き座標に好きなだけ入れ子を書けた。

**直し方は決まっている —— 引いてから配る**（今日二度使った手）:

    field zsnro : max bound <出現の上限>
    zsnro[o] <- hfz[osn[o]]   for (o) in 0 .. nocz[0] if oin[o] if osn[o] >= 0
    zsnr[zsnro[o]] <- true    for (o) in 0 .. nocz[0] if ... if zsnro[o] >= 0

意味は変わらない。中間の場を一枚置くだけである。

### 次の一手（一つだけ）

**78 箇所の「二段の書き座標」を一段に落とす。** `work/onebook.py` が既に
場化の書き換えをしているので、そこに足すのが自然（中間の場の `bound` は
輪の変数の定義域から取る —— 文字なら文字面、出現なら出現の上限）。
落とし切ったら一枚の本は走るはずで、`python3 lattix.py work/t/a_min.lx` と
突き合わせれば A.4 が閉じる。

焼き手の作り方（今日の実測値）:

    RCAP=589824  TOTAL=655360        像は 90〜130 KB しか要らない。上げすぎると
                                      dcap=rcap*16 が集約規則ごとの印を太らせる
    bound 262144 → 524288   （文字面・符号面）
    bound 1024   → 4096     （場）        bound 2048 → 8192   （輪）
    bound 4096   → 16384    （規則・項）  bound 8192 → 131072 （**座標の要素**）
    bound 65536  → 262144   bound 512 → 4096   bound 128 → 1024
    bound 524288 → TOTAL
    cap* も揃える。二段で焼く（まず rcap だけ広い中継ぎ、次に本体）

### 元の「次の一手」（済み）

**一枚の本を焼く。** 幅が縮んだので、朝に 1,924 MB（2 GB の天井の 94%）で
止まっていた「文字面以外を全部広げた焼き手」は、いま **その分だけ軽い**。
文字ごとの面 216 升のうち 101 升が `or` なので、源 524,288 文字で
**−353 MB** の見込み（値 1 + 階数 4 = 5 バイト。階数は縮まない —— 測った）。
1,924 − 353 ≒ **1,571 MB**、天井まで 470 MB。**焼けるはずである。**

手順は REPORT §4.9 にある通り:
1. 焼き手が出せる像は **焼き手自身の `rcap + 65536`** で決まるので、
   まず `rcap` だけ広げた中継ぎ（`_bigbuf`）を焼く。
2. その中継ぎで、面と像を広げた研究用の焼き手を焼く。
3. それで `/tmp/folded.lx`（一枚の本、512,874 バイト）を焼く。
4. 焼けたら走らせて `python3 lattix.py work/t/a_min.lx` と突き合わせる。

**焼けなかったら、どの面かを二分探索で出す**（今日の手が使える）——
中身の無い規則を形ごとに一本ずつ足して、何本目で落ちるかを見る。
輪・ガード・項・種で形を変えると、どの数が効いているかが分かれる。

**`./check` の読み方**（2026-09-17 の実測 41/47）: 自己ホストの門は全部緑
（selfparse / metaeval / elfout / asmout / gen / genparse / selfgen / selfbyte /
self3 / fold / g4d / accept / differ）。赤い 6 本のうち runtime / rtattest /
relation / onerun は **900 秒の予算切れ**（runtimecheck は 30_build に届くまでで
既に 25 分 —— 私の変更と関係なく予算を超えている）、baked は既知の 5 本、
incremental は demo。予算を本ごとに測り直すのは宿題である。

---

## 1. 前任者が外した外し方（同じ穴に落ちないために）

私は `run.lx` を **gcc で焼いた**。8.6MB の実行ファイルが出て、コーパス 52 本中
49 本が参照実装と一致した。それ自体は正しく測れている。**だが道が違う。**

- `PLAN.md` §2 は「焼くのは `31_gen.lx`」と書いてある。gcc は出てこない。
- `PLAN.md` §5-2「言い換えを進捗と呼ぶな」に真正面から当たっている。
  私は「ネイティブの実行ファイルが一個できた」を進捗と呼んだ。的は
  「**Lattix だけで**実行できる」であって「ネイティブになった」ではない。
- 原因ははっきりしている。**私は `PLAN.md` を読まずに §6.8 だけを書き換えた。**
  §0 と §2 を読んでいれば、gcc に渡した時点で気づけた。

**教訓（次の担当への具体的な指示）**:
- 手を動かす前に `CLAUDE.md` と `PLAN.md` §0/§2/§5 を読む。毎セッション。
- 「これは §0 のどの部分を前に進めるか」を一行で言えないなら、着手しない。
- C バックエンド（`native.py` / `runtime.py`）は **測定器**である。
  二つ目の実装は試験より強い試験なので（気づき49）穴を出すのに使ってよいが、
  **配布物の道ではない**。`test/nativecheck.py` / `test/runtimecheck.py` /
  `test/baked.py` はどれもその category である。
  - `test/baked.py` は私が足した（`run.lx` を gcc で焼いて表を差し替える試験）。
    測定器として残すか消すかは判断してよい。**道ではない**とだけ憶えておくこと。

---

## 1.5 第2段の現在地（front.lx —— このセッションで立てた縦の列）

`front.lx` が動き始めた。**33_self.lx + lib/fold.lx の後ろに繋ぐ算術の断片**で、
源のバイトから家の表（dat/spc/ssz/mp/fg/fc/fp）を出す。突き合わせは
`test/front.py` —— 番号（軸・空間・写像・eid・面の基）はラベルなので、
**中身で正規化してから** flat.py の表と比べる。走らせ方は C の測定器で一度
焼いて表（ch/orc/ext）だけ差し替える（selfgen.cgo。焼き直しは front.lx を
書き換えたときだけ、約 60 秒）。

通っている: **48 本が完全一致**（work/t のほぼ全部 + 01/02/03/05/06/08/09/
15/16/17/18/19/20/21/23/26/27/28/29/34 + 35_noema —— 字句解析・CYK 構文
解析・データフロー解析・描画・ELF アセンブラ・割り算の族・一元の集合・
is まで）。
入っている物: 軸と空間の intern（min と前置きの鎖）/ 場の置き場（名前の辞書順、
左詰めの鍵）/ 測った広さ（区間算術で静的に出る —— flat の「一巡目」の正体。
値で決まる座標の次元だけは走らせて測るしかないので **口 table ext から渡す**）/
表は何枚でも（行は全表を通した鎖。原子は引用符なしでもよい）/ range(a,b) /
添字なしの場 n[] / 値が上端の区間（閉じた値の口 table orc —— BSP の継ぎ目）/
**三次元の場**（rd[pc,v,d] —— 写像の三本目の枠 zmj2、次元1 の歩幅 zfs1）/
束縛は八本まで（33_self の照合は六本 —— zvbnd/zvlpz/zvcolz で補う）/
読みの中の読み（zrk : flat の同層再帰）/ 種8 =「変数 ± 読み」の次元
（out[owner[i], i-owner[i]]）/ 負の定数の座標（quotes[-1] の種）/ 値の鎖
（*/÷/% —— fp 種3/4）と **係数つきの読み・変数**（f[i]*10、i*2 —— 間接の m）/
束ちがいの読み（fp 種0。生きた読みは負の束 —— 昇る束だけ。下る束と sum は
run.lx 自身が成層できないので断る）/ ガードの鎖 `f[i]%c==k`（pa に畳んで種1）/
反転の比較 `pc < maxpc[]`（読みを左へ、演算子を裏返す）/ 精密な成層 zlvl
（比較も値の読みも **束の名でなく向き** —— max を flat の頭に読むのは単調）/
fourv / **構成子**（脱糖・Horner・原子は ate 表 —— noema 完全一致）。

さらに入った物: **割り算の種族**（種8 pa÷pa / 種9 pa%pa / 種11 (写像)÷法
—— 種10 は要らなかった。**写像が係数と一次式を運ぶ**）/ front の鎖は四形
（zchn 読みの芯 / zchav 一次式の芯 i/2 / zchpp 括弧の芯 (120+c[])%256、
seed でも / zchdv pa で割る）/ 一元の集合 = fsh 形3（v7 ← {wm}）/
is = 種4（右辺の綴り→原子番号。is の右辺の綴りも原子の候補 zqtka）/
**間接の畳みの一般化**（pcell: 座標の読み何本でも種7 で一本に / pmapcell:
三本目も種7 / pctor: slot 上限撤廃）—— 22_parse は flat 側で断り 0、
焼いて答えも一致 / 次元0 の「定数+読み」（out[120+off[i]+1]）。
**測りの本則**: 測りは区間算術（前段が静的に写せる形）で、値で決まる
座標を持つ規則だけ一巡目を点で測る（flat.py の self.measuring —— 全部を
点にすると家の測りとずれて 10 本割れる。三面鏡が 28_asm の out 146/154 で
教えた）。zrq の潜在バグ（opc2>=5 が < を拾い向きを潰す）もこの差し
合わせが出した。

**二段の通し運転が通った**（test/pipeline.py）: 源のバイト → front.lx
（焼いた Lattix）→ 家の表 → run.lx（焼いた Lattix）→ 最小不動点。
48 本全部で答えが定義と一致 —— **表の経路に Python は居ない**（結線と
審判、升の名前を戻す観測だけ）。§0 の最終形の背骨がここに立った。

さらに（このセッション）: **構成子の入れ子と定数の木**（22_parse の語彙
W[i] <- cons(102,cons(111,…))）——「深さも座標である」。使用の検出は全深さ
（host の結線だけ深さ0）、引数の列は colc でなく **深さ d のコンマの数**
zacd[t,d]、引数の種9 = 入れ子の使用、Horner の鎖は規則ごとでなく
**使用ごと**（zuh1/zuh2。入れ子は enc(番地)=3·(番地%M) で外の鎖に畳まれ、
底から上へ深さの順に閉じる）、合成規則の種9 は値も座標も内の使用の対
（枠 33/34/35 —— 参照実装は定数でも対 (h1,h2) の形を残すので形ごと合わせる。
枠 30/31 は三間接が使う、写像 52/53 が空きだった）。zra の間接は種 7..8
だけに（9 を入れると原子の引数が壊れる —— z_atomarg が教えた）。
**_dynd の広げ**: 座標に読みを持つ規則は一巡目を点で測る（measuring の門と
同じ判断）ので、書き先の**全次元**を口 ext から渡す —— oat の軸0 が静的
区間算術と 1 違い、辞書順で後ろの場の基が全部 +20 ずれていた。
これで 22_parse の家の差は 390/736 → **239/267**。

**学びの輪の速さ**: close_upto は解釈でなく **C の測定器で閉じる**
（sha1 で _gen/close/ に置き、同じ内容なら焼き直さない。-O1）。
表の値が 2^62 を超える一巡目（仮の帯 ATOMB0*2=2^63 が i64 の flat ⊤ と
衝突 —— x_sumstrata の rz が割れた）と、測定器が語れない形（set の
文字通り など —— 28_asm/30_build）は解釈に落ちる。
**器の広さは正しさを縛らない。**
22_parse の学びの輪は /tmp/fam22.py（prep で flat 側を漬け、front 側だけ
回して差分。約 8 分）。

さらに（このセッション、五つの山）: **深い読みの席は前置きの鎖**
（hf[snum[k]] —— 表 zu0k は一段。ztbl に無い読み持ちの出現は文ごとの鎖で
枠 68+2i・写像 76+2i。枠の空間 64→128、種9 の対は 64/65/66・写像 120/121）/
**入れ子の使用が読みを持つとき**（cons(bind(f[..],g[..]),…) —— 内の鎖を
同じ規則の帯 80..93 に建て、外の Horner へ係数 3·K^(n-1-k) で (対%M) を
間接に。zam に拾われた使用引数も鎖が親 zisua）/ **使用の引数は値の写し**
（zonm の「深さ1 は非単調」から除外。昇る束は生きたまま −lat、下る束だけ
閉じる —— vbind の層が flat と揃った）/ **反転比較の生きた flat**
（v == W[6] —— zgfl に lt −5 の種1 が無かった）/ **三次元ぜんぶ読みの畳み**
（trow —— 次元1+2 を fp 種7 で一本に。枠 94/95・写像 127。zd1cm は
角括弧を跨ぐ。zq2 に種7）。

**22_parse は完全一致**（883 規則、0/0。BOOKS 49 本目）。二段の通し運転も
通った（源 → front.lx → 表 → run.lx → 答え一致、5.1 秒 —— Lattix の
構文解析器を Lattix の前段が読み、Lattix の機械が走らせる）。
道中の山: 深い読みの席は前置きの鎖（zu0k の表は一段）/ 読み持ち入れ子の
帯（80..93）/ 種10 =「読み·定数 + 読み」の畳み / ガードの 読み±定数
（cm/wm の定数へ。zgrst は符号つき）/ empty の層のずれが args/grp/issep/
rqC を連鎖で一つ下げていた（左辺 読み±定数 の向きの zonm）/ 定数の
構成子の合成規則は host の値の読みに縛られない（zlevg で独立に成層）。
学びの輪の道具: /tmp/fam22.py（BOOK=… prep で他の本にも）、
/tmp/fam22c.py（A/B 落とし）、/tmp/flatmap2.py（残差→行番号の対応。
_family を monkeypatch して eid→規則を残す —— まず環境変数
LATTIX_FAMILY_MIN=1 を flat の import 前に）。

**依存する区間は通した**（1649136 / e703e09）: `for (rw) in 0 .. nrs[snum[k]]`
は **矩形 × ガード**（多面体 = 箱 ∩ 半空間）。flat 側: 一巡目の点の閉じの
値を engine2.close_upto が `fl.closedany[(場, 鍵)]` に貯め、_bmax がその最大
（+定数の寄せ）を上端にして `0 .. H` の矩形へ書き換え、元の端は fc に残す
（_family の前置き）。front 側: oracle が fref の端に `(fnum, 63, max)` の行を
送り、lkd 種3（`zorc[…,63] + 寄せ` で zlphi2、無ければ H=lo の一点）、合成
ガードは席7（ずれ付き or 閉じ → 種1: cm=r·128+29 / 枠39 が pa / 写像31。
裸の読みで生きた非 flat → 種11: cm=写像31 そのもの。flat.py fcond の判断）。
種3 の軸の鍵は値 (lo, H)（region は書き換え後の repr で intern —— `0 .. 0` は
番兵の一点と同じ軸）。試験 z_depint / z_unit0。

**25_eval は front 側も完全一致**（913 規則、0/0）。道中で開いたもの:
include は字面の取り込み（run_front が lattix._expand_includes と同じ前処理）/
口 orc の場番号は **front の番号**（宣言 → _size → 引数場。oracle が参照の
番号で送っていた —— 構成子のある本で初めて割れた。test/front.py frontnum）/
**合成の場の読み**（`cons_head[efref_args[..]]`, `_size[..]`）は宣言が無いので
33_self の ofl が行き当たらない → 引数場の綴りから okey/okey2 と同形の鍵
（zaok/zaok2）を作って ofl を継ぐ（z_accread）/ **次元2 の読み**
（`trow[stab[..], rw, bind_pos[..]]`）は二本目の間接の席を次元1 と分け合う
（zdrd2/zdrdb/zdrob/zfsb —— 席で引く。z_dim2rd）/ 深い鎖の枠の生きた読みは
5 だけでなく昇る束ぜんぶ −束（zlva）。
前段の速さ: ch × ch の総当たりを二つ潰した（表名は鍵で引く ztdk、囲む使用は
括弧の相手 opos で引く zenu）。それでも 20 分だった真因は **測定器側**:
`zacd[t, d]`（表 × 区間の鎖）が融合の一掃に乗らず、規則の切り替わりぶん
掃き直していた（gprof: mslot/mget 17e9 回、mjoin_zacd 9.6e8）。runtime.py の
fuse_plan を「源0 が表なら二本目以降は行の内側」に緩めて 25_eval の前段は
65 秒（焼き込み）。

**PLAN 2.5 の本も通した**（10_library / 12_world / 13_budget / 14_iosig /
30_build。BOOKS 59 本、二段の通し運転も ✓）。判断:
- include / component+use は参照実装でも **字面の取り込み**（_expand_includes
  / _instantiate は付け替えて末尾に継ぐ）。同じ字面変換を run_front の前に置く
  （test/front.py expand_uses —— 場の宣言は部品の順、規則は use の順に末尾へ）。
- emit も参照実装では糖衣（`emit "ch" <- e` = `emit_ch[] <- {e}`、場 emit_ch は
  最初の emit の位置で宣言）。同じ糖衣を字面で剥く（desugar_emit）。
- source は宣言（33_self に kwsrc: `source x : lat from "ch"` は isdcl）。往復の
  辺（応答を読む規則は emit_ch を書く規則より +1 の層）は front.lx が
  チャネルの語から `emit_ch` の名の鍵を作って宣言に行き当て（zsrcf）、
  zlvl/zlvg に +1 の辺を足す。書き手の無い場は zfws = −1（fstr.get(f,-1)）、
  次数は読まれ方から（zfna）。
- 上端 `n[]` は座標 0。口 orc に無い上端（まだ閉じていない）は種2 でも
  矩形 × ガードへ（zorch の否定で lkd 3 —— flat の gval が int でない道）。
  文に依存する区間が二つあれば席 7−q（zdlr[s,q]、写像 8+3i/9+3i/10+3i、枠 32+i）。
- flat.py: 集合の元に読み（`{profile[u]}`）は pa 経由の間接で wm へ（vf 3）。
- budget は無視（2.7 で誤りとして扱う）。
これらの字面変換（include/use/emit）は今は test/front.py の Python にある。
配る形では Lattix の前々段（bytes → bytes）に移す —— 33_self の道具で書ける。
**33_self.lx が家で完全一致（408 規則）し、通し運転も通った（3.4 秒）** ——
前段の土台が、前段自身に読まれて機械で走った。道中で開いたもの:
- 家のガード番号（zgn/zgkd）: 33_self の gno2 は点の道の cd の行番号（比較は
  左右で二行）なので 8 席に 4 つしか入らなかった → if 節ごとに +1 で数え直す。
- 空の区間（`for (s) in 1 .. nstz[0]` で nstz=0）を持つ文は実例が無い —— flat は
  空間を作らず消し（幅 0 の軸は機械の割り算を壊す）、front は zlemp で家から
  外し、種2 の空の軸は代表にしない。
- 24_space（`comp[i * j]`、`if i * j <= n[]`）: 軸の一次式を枠に落とし（種11、
  法 1）積を種10 で。座標は zdvv（枠108-110、写像63/119）、ガードの左辺は zgvv
  （枠40+i/48+i/56+i、写像32+i/40+i）。flat の pcell/fcond は鎖の項を間接へ。
- run.lx は `LATTIX_FROMJSON=1 python3 work/mkrun.py` で shapes.json から
  書き直せる（測量なし、~20 分）。種11 は昇る束 × (>=, >) を数え上げ。**焼ける
  大きさも資源**: 8GB で cc1 が落ちる（55MB の C）—— 形を増やすときは焼いて確かめる。
- runtime.build: C は焼けてから本名に置く（gcc が落ちた後に古い実行ファイルを
  使い続けていた）。実行ファイルが C より古ければ焼き直す。

**第3段 show.lx が入った**: 面の値 → `name[k1, k2] = v` の字面。三段の通し運転
（源 → front.lx → run.lx → show.lx）が `python3 lattix.py f.lx` の標準出力と
**バイト一致**（`LATTIX_SHOW=1 python3 test/pipeline.py`）。test/show.py は
参照の答え + flat の置き場で 37 本一致（print の無い本・render の本は外）。
原子か数かは番号では分からない（値は座標の上端を越える）ので `fat (f, d)` で
型を渡す —— **今は答えから作っている。前段が列の綴りと写しから出すのが宿題**。
構成した値は引数場の DAG に沿った不動点で開く（深さ 8 の打ち切りは未実装）。

04_aggregate（count の形0、生きた sum/count の比較 = 種11）、07/11（断る本は
前段も同じ行で断る: zerr/zerrl、部品の深さは取り込みが参照の parse に判じさせる）
も通った —— **例の 34 本のうち 31_gen / 32_shape 以外は全部**、家で一致するか
正しく断る（BOOKS 66 本）。

自己適用（work/self_front.lx = 33_self+fold+front.lx）: flat 側が **解釈実行の
閉じ**に落ちて終わらなかった —— 原因は前段の疎の場 `ztdk`（名前の鍵を座標に
した 2 次元の疎の場）。一巡目の仮の広さ（2^63）が二次元で 2^126 になり、
測定器（i64）の門（2^62）で解釈に落ちる。ztdk を「表ごとに引く」（ch × 表の数）
に書き換えて外した。**前段は自分が家になる形で書く** —— 疎の場（bound の無い場）
を前段に置かない。engine2.close_upto は溢れた表を /tmp/close_big.log に書く。
再走中（閉じの C は 14MB、ccache で一度だけ焼く）。

自己適用の続き（5e86430）: 一巡目（二周 × 36 層、閉じは C で約 3.5 時間）が
通り、`zfsz0 <- zexth - zextl + 1 if zexth >= zextl` で「多面体のガードで束が
混ざる」。**混ざっているのは束ではなく層**（閉じた max と生きた min）:
- ガード: 閉じた側は pa に写せるので、生きた読みを左に置いて向きを返す
  （`lo <= hi - c`）—— 種11・束1（min）・`<=`/`<`（run.lx に (11,1,4/6,0) を足した）。
  wm は定数 −c と間接（左の読みの枠 32+i）。front は zbxl、flat は fcond の反転。
  種11 の wm は間接も運ぶ（engine2 が `_map('wm', PS)`）。
- 値: `hi - lo + 1` は下る min を引く形。下るほど値は上がるので単調だが、
  pa（flat）には写せない（下る値は ⊥→v→v' と動く）→ **面から直に読む vf 6**
  （am が逆の面、同じ束の直の読み一本は bm。(1|2, 6, 1|2)）。front は
  zoppl/zopp/zhopp、flat は _family の opp。
- 点の道の tolat は生きた他束を **閉じた面から写していた**（黙って違う答え —— この
  形は自己適用まで誰も踏まなかった）→ 声に出して断る。
- flat.region: 空の区間（1 .. −1）で行を戻さない（dat の重複 → closer が
  「contradictory value used as coordinate」）。
試験 z_clmin（三段で一致）。BOOKS 66/66。
静的な事前検査: /tmp/mixg2.py（生きた読みと閉じた読みが混ざる比較を数える）、
/tmp/reads3.py（直の読みが三本以上の値）。

**学びの輪の速さ（一巡 3.5 時間 → 約 30 分）**。遅さの正体は四つあった:
1. 閉じの器（engine2.close_upto）が **表の行を字面に混ぜて焼いていた** —— 層が
   一つ進むたびに C が変わり、36 層 × 二周を毎回 gcc。→ 広さは 2 の冪に丸め、
   形は本ごとの覚え書き（`_gen/close/hints/<sha(置き場)>.json`、union）で先回りし、
   表は焼いた後に差し替える（前段の cgo と同じ手）。build は走行内で一度
   （parse/stratify が閉じの 9 割だった）。二周目と二度目以降は焼かない。
2. gcc: 41MB の C（閉じの器。層は座標なので 38 層に展開される）に 5.7GB・10 分、
   8GB の箱で死ぬ。**clang は 2.1GB・1 分**（出力は同じ —— 比べた）。runtime.build
   は clang があれば clang。
3. 前段（front.lx）の C が 30 分: 層まるごとの一掃を、一本の長い鎖（成層 zlvl の
   最長路）の回数だけ繰り返し、同じ層の無関係な g×h・ch 走査ごと掃いていた
   （層 18 が 424 回、層 21 が 735 回）。→ **SCC ごとに不動点**（位相順 scc_rank）。
   10 分弱に。素朴な一掃の本体は SCC の道を通る層では書かない（二度書くと C が倍）。
4. 前段の全場を print すると出力を読む Python が 6GB で死ぬ → 突き合わせが読む
   58 場だけ print（test/front.py NEED、FRONT_EXTRA=… で足す）。
一周の道具: /tmp/loop.sh（flat 側を漬ける prep 約 17 分 → front 側 約 10 分）。

**前段は自分が家になる形で書く**（自己適用が教えた三つ）:
- 直の読みは二本まで（am/bm）。三本目は中間の場へ（znut / zsig1 / zq01 / zqs）。
- `(読み − 読み) × 読み` は一次式でない → 「定数 − 下端」を場に置いてから歩幅を掛ける
  （zdl0 / zdl1。読み × 読み は種10）。
- 非単調な鎖（`%` / 読み×読み の自己再帰: zsps / zpwa / zpwb / zcf1 / zcf2）は参照が
  **軸に沿って展開**する（_axis_unroll —— 点ごとに層が違う規則になる）。前段には
  その展開が無い。→ 鎖の場を **flat** にする: flat は ⊥→v なので剰余も積も単調、
  展開が要らない（層は座標である、の別の顔）。
- 実例の無い文（zlemp）の区間は軸にしない（zlive —— 代表にも前例にもしない）。
**自己適用が完全一致した**（621f6e4 → 修正 1 つ）: front.lx（焼いた Lattix）が自分の
源（work/self_front.lx = 33_self + fold + front.lx、入力 ch は一点）を読んで出す家の表
（dat/spc/ssz/mp/fg/fc/fp: 1761 規則・534 枠）が、flat.py の表と **中身まで一致**。
BOOKS 66/66 も保ったまま。二巡目の学びの輪で開いたもの（全部「前段は自分が家に
なる形で書く」の帰結。前段の文法上の制限であって言語の制限ではない —— 使う側の本には
効かない）:
- 書き先の座標は 読み か 一次式: `zrn[s]*128+3` のような 読み×定数 は場（zrw3 …）、
  「深い読み + 変数」は行の場（zdrt / zdsr / zqbg）。次元0 の 読み + 変数 は前段が
  支える（zd0rv: 間接 + 変数の枠）。「定数 − 読み」の座標は場（zfln8）。
- 項の形は 変数/読み が先（`i + 32`、`i * 3`。`N + v`・`N * v` は取りこぼす）。
  正規化を regex でやったら `b * 24 + k` が `b * k + 24` に化けた —— 66 本が教えた。
- 一文のガードは八つまで（席 8）。超えるなら二段に割る（zkwrg / zglvv / zkwcd1）。
  **前段は黙って落としていた** —— 声に出す口はまだ無い（宿題）。
- 直の読みは二本まで。定数どうしの積は畳んで書く。(読み−読み)×読み は場に置く。
- 折り込み（Horner）は 読み×定数+読み と 読み%法 の二段（zcp1 / zqf1p / zaf1p / zaf2p）。
- 書き手の層 zfws は消える規則（zlemp）も数える（flat.py の fstr と同じ）。
- sum / bag の向きはプログラムが言う（寄与に読みがあれば向き無し —— 参照実装
  field_senses と同じ判断。zsnr / zosen = 3）。
- 生きた左・閉じた右の種11（zbxr）、右辺が定数だけ/変数±定数 の符号つき和（zgrst）。
- 使われない宣言の場は升一つ（zfusd。flat の lay は使われ方の次数で並べる）。
**機械が前段を走らせた**（e2aeb0c）: run.lx が front.lx 自身の家の表（1761 規則・
16395 写像・4772 枠）を走らせ、**899 場すべて**が参照実装と一致（走行 31 秒）。
道中で開いたもの:
- run.lx の層の上限は 16 だった（前段は 38 層）。層 16 以降の規則が黙って走らず、
  18 場が空 —— 「食い違う場」の並びが全部 層 ≥ 16 の書き手を持つ場だった。
  上限を 48 に、空間 128、写像 32768（**形は増えていない**、箱の広さだけ）。
- 層 48 に展開した C が焼けるように、階数（順序の証人 rget/rset）は測るときだけ焼く
  （runtime.build(ranks=False) —— 走らせる物と閉じの器）。C は四割痩せる。
- 写像の番号もラベル（pipeline.compact が使う番号だけ詰める。fp の am も）。
- 焼きの時間は clang でなく **参照の成層**（48 層に展開した 15k 規則の Python）が
  食う（465 秒のほぼ全部）。器の build は走行内で一度なので通し運転では一度きり。
- 道具: /tmp/pipe_self.py（`prep` で前段・審判・観測を漬け、引数なしで機械だけ回す
  —— BAKEDIR / N / BOOK）。二つの走行が同じ器を使えるよう、データは pid 名で書く。
**前段が機械の上で本を読んだ**（21958c4）: 前段のソースに `table ch = (0,b0),…`
（a_min.lx のバイト）と口（orc/ext）を埋めた work/self_amin.lx（/tmp/mkself.py が作る）を、
閉じの器（run.lx と同じ形）で最後まで走らせ、**899 場すべて**が参照実装と一致
（/tmp/machine_check.py）。ただし flat 側の家は 4 規則が家にならず点の道に落ちた
（nofam の中身は次に出す —— run.lx の通し運転には全部家であることが要る）。
遅さ: flatten が 1.9 時間。閉じの器を走らせる回数が多い（一回 15 秒）。直したもの:
出す面は層 k だけ（LATTIX_PRINT_S0）、無い値の閉じ直しは書き手が増えたときだけ、
name_of は二分探索。まだ多い —— 閉じの回数を数えて、層ごとに一度に寄せるのが次。
次: (1) self_amin の nofam 4 規則を家に（/tmp/machine_check.py が nofam を出す）、
(2) 前段（焼いた Lattix）に self_amin.lx を読ませて表を出し、run.lx（層 48）で走らせ、
zdat/zfg… が a_min の家の表になることを確かめる（/tmp/pipe_self.py prep/run, BOOK=…）、
(3) 大きな本（22_parse）へ —— zdat/zdrt/zdsr の bound と前段の走行時間。

残る大物: 31_gen（x86 の焼き）は旧断片（rrule/grd/trm/crd）の生成器 —— 家の表
（fg/fc/fp/mp）を焼く形に書き直す必要がある（第4段）。32_shape は旧前段で
front.lx が置き換えた。

---

**断片の際を測り、座標を鎖にした**（ff66967 / a50d1d2）。三つ出た。

① **測定器が嘘をついていた。** `if a[i] + 1 >= 2` は焼けて、走らせると segfault
した。源（33_self）は正しく「知らない種 15」を立てていたのに、生成器の振り分けが
`if gdk >= 10` / `>= 7` と開いていて、**番兵より大きい番兵を拾っていた**。
`gknown`（焼ける種を言う場）を一枚置いて三箇所が読む。源の側の六本（左の手前が
`+ - * / %` か五本 + 右の後ろに `± 数` か一本）は数え上げなので漏れていた
（`if 1 + a[i] >= 2`、`if not a[i] + 1`）—— 一本にした:
**if 節の中に、括弧の外の演算子があってはならない**。

② **際は三つあって三つとも違った。** 覚えていた「入れ子は一段まで」は書き座標
だけの話で、値とガードは二段入っていた。十二枚の `.lx` で測って `test/accept.py`
に置いた（61 → 79 例）。`accept.py` の `widths` も直した —— `[^\]]*` が最初の `]`
で止まるので `r[q[i,k]]` を二次元と読み、**生成器の側が悪いように見えていた**。

③ **座標を鎖にした。** 三段目が落ちるのは `33_self.lx` の `ixsr` で、添字トークン
が読みのとき **その添字の輪番号** `vlpz[j+2]` を見ていたから（`a[b[h[i]]]` では
`h` が読みなので ⊥）。座標の源は「列・計数器・場の値・定数」の四つだと思い込んで
いたが、**読みも源である**。源5（内側の所有者が座標を作り終えた所で読む）は
`f[g[i,j]]` のために既に在ったので、二行足すだけで届いた:

    ixsr[j] <- 5 for (j,zc) in ch if isocc[j] if isocc[j+2]
    ixcc[j] <- oseq[j] for (j,zc) in ch if isocc[j] if isocc[j+2]

値・ガードは六段、書き座標は五段まで測った（升まで一致）。

**なぜ鎖が要ったか**（引く方では払えないから）: 一枚の本の `run.lx` 側の評価器の芯
105 本は、座標が五段で、一番内側の輪 `t`（升の番号）に依る。引くと
`[e,u,t] = 4096 × 128 × 137,378 = **576 GB**`。
**引いてから配るの値段は輪の上限の積である。**

**いまの一枚の本**: `_mkone.py <prog>` → `_folded.lx` →
`work/flatten.py`（ガードの辺だけ）→ `_folded2.lx` → 広い焼き手で **焼ける**
（1,048,576 バイトの像）。広い焼き手は `/tmp/g/mkwide.py`
（RCAP=983040 TOTAL=1048576 CHARS=589824、二段: `lattix` → `_bigbuf` → `_wide`。
bound の写しは **一度に**やること —— 順に置換すると 1024→4096→16384 と二度当たる）。

走らせるとまだ断る。残りは二つ、どちらも「生成器が言語に追いついていない」形:

1. **ガードの辺に式**（41 本）。`if (MC[…] + MK0[…] * zdat[…] + …) == …`。
   生成器は値の側では項の列を評価できる（`tmk`、八項まで）—— 同じ仕掛けを
   辺に繋ぐ。引いて逃げるのは 576 GB と同じ理由で無理。
2. **足してから掛ける**（58 本）。`a + b * c` は焼くと `(a+b)*c`。生成器は
   正しく断っている。筋は **項ごとに一時レジスタで掛けてから足す**こと。

道具: `work/flatten.py`（三つの際すべて。深さは `wlimit`/`vlimit` で外せる。
`>= 0` を足すのは **書き座標に引いたときだけ** —— 値やガードに足すと
`if not a[b[p[i]]]` が中が ⊥ のとき撃たなくなる）。

**項を群に分けた（済）。** 「足してから掛ける」は焼ける。群（積の連なり）の
中では左畳みは正しいので、直したのは境目だけ ——

    群0 を rax に作る → `mov [和],rax` / 群k → `add [和],rax`（符号 `-` なら sub）
    最後に `mov rax,[和]`。「最初の項か」→「**群の先頭か**」、
    引く種（5/9/11/13）は「引く命令」ではなく **群の符号**。

和は register に置けない（**一つも空いていない**。rbp すら fd）。stack も
使えない —— 読みが ⊥ で `je av` に飛ぶ道があり、push したまま抜けて漏れる。
だから **場の下の升 `[rbx-16]`**（`gap` の 512 バイトの余り。使われているのは
`[rbx-8]` だけ）。升なら途中で飛んでも壊れない。形は三つ足すだけで済んだ:
`add [rbx+d32],rax` (183) / `sub …` (184) / `mov rax,[rbx+d32]` (185)。
**載せる形は全部すでに在った**（定数 2 / 列 10 / 読み 85 / 計数器 109・111・113）。

**文字の面を `rcap` に合わせた（済）。** 上を足したら「場の広さを超えた
（`incom`）」と言われた —— `lattix.lx` は 261,918 バイトで、面は 262,144。
**自分の源を読めなくなる 226 バイト手前**に居た。`rcap` は 524,288 と
宣言してあるので、面がその半分なら宣言が嘘である。合わせた:

    文字の面 262,144 → memsz 631 MB / 焼き 2.02 秒
    文字の面 524,288 → memsz 1,097 MB / 焼き 2.15 秒

33_self の 162 枚と fold の 9 枚（31_gen の 262,144 は **命令の面**なので
触らない）。`accept.py` の「宣言の内側」を 260KB → **520KB** に上げた。
広い面は狭い焼き手では焼けないので、起動は三角を通る（`work/mkwide.py` と同じ手）。

**残る壁は一つ、形も分かっている。** 一枚の本の外は 35 箇所、全部これ:

    if vn[s, …] is MC[…] + MK0[…] * zdat[…] + MK1[…] * zdat[…] + …

二つ足りない ——
(1) **`is`**（`a ⊒ c`。どの束でも単調な順序の検査）は焼く側に形が一つも無い。
(2) **ガードの辺が項の列**。値の側は項の列を評価できる（群になったいま掛け算も
    正しい）が、辺は「計数器・場の読み・定数」の三つしか取らない。

(2) の継ぎ目は見えている: **座標 `crd` は「所有者種 0 項 / 1 ガード / 2 書き先」を
持っているのに、項 `trm` は持ち主が「規則の値」しか無い。** 項を座標と同じ形
（持ち主つき）に直せば、ガードの辺も書き先の座標も同じ仕掛けで回る。三段:
(a) 33_self が `if` 節の辺のトークンを **そのガードの項**として印す、
(b) fold が持ち主つきで `trm` に流す、(c) 31_gen が持ち主ごとに項の鎖を回す。

道具: `work/fits.py`（この `.lx` は断片に入るかを測る）、
`work/flatten.py`（入らない所を引いて平らにする。深さの引きはもう要らない ——
`wlimit`/`vlimit` を上げて呼ぶ）、`work/mkwide.py`（広い焼き手を二段で作る）。

**もう一つの道（気づいたので書いておく）。** 一枚の本が詰まっているのは
`run.lx` の評価器の芯で、あれは **どんな項の形でも回せる一般の評価器**である
（`MK0/MK1/MK2` の 0/1 の面で項を選ぶから、一本の規則が 24 項になる）。
だが本を作る時点で **プログラムは埋まっている**（`table ch` がそれである）。
つまり `onebook` は、その program の項の形に **特化した**規則を出せる ——
一般の評価器を畳めば、巨大な式は小さい具体的な式の束になり、
ガードの辺も項の列でなくなる。特化は Python がやる作業だが、
`flatten.py` と同じで **走るときには居ない**（出来上がった `.lx` が配るもの）。

どちらが筋かは決めていない。生成器を広げる方は **言語の力**が増える
（ガードの辺が項の列になるのは、断片が言語に追いつくことである）。
特化する方は **本が早く走る**（しかも器が小さくなる）。
前者は §0 の道、後者は A.4 の道である。§0（`.lx` → 実行ファイル）は
`lattix` が既にやっているので、急ぐのは前者ではない。

---

**言語自身の法則で試す試験を足した**（96c6f33）。`test/meta.py`。
Lattix は答えが最小不動点なので **規則の順序に依らない** —— そこから
神託の要らない試験が四つ出る（順序 / 平ら / 広げ / 死んだ場）。どれも
「意味は変えず配置だけを変える」形で、難しい誤りは全部そこに居る。
使い方: `python3 test/meta.py [本数]`（250 本で約 30 分）。
**先に「落ちられる」ことを確かめること** —— 和の升を `[rbx-8]` にぶつけると
破れ 5 件を捕まえ、戻すと破れ無しと言う。落ちない試験は何も証明しない。

**いま赤い試験が二つある。どちらも本物の穴を指している。**

① `test/meta.py` 250 本 → 破れ 5（順序 2 / 平ら 1 / 広げ 1 / 死んだ場→segfault 1）
② `test/differ.py 200` → 食い違い 6（うち segfault 1）

どちらも根は一つ: **座標に濾しが無い。**

    b[i] <- a[big[i]]   big[i] = 100、a は bound 8
    解釈実行 ⊥ / 焼いた側 b[0] = 0（隣の場の升を読んで答えにする）

濾し（`cmp r9, 広さ` / `jae`）は在るのに `cdhf == 1`（ずれがあるとき）に
しか出ない —— 検査が「座標」という概念ではなく「ずれ」という機能に
紐づいていた。`accept.py` に「既知の穴」として刻んである。

**直し方は測ってある**（が、まだ出していない）:
`form[cend[gc]-2] <- 50 / form[cend[gc]-1] <- 73` をスロットの終わりに
無条件で置き、`clen` を +2。条件は `csd[gc] == 0` **または**
`not isin[cso[gc]]`（内側の所有者でも次元0 は座標として完成している ——
そこを外すと `f0[f1[f1[i]]]` の真ん中の輪が素通りする）。像は 512KB を
超えるので、**文字の面 = rcap = 589,824 / 像 = 655,360** に上げる
（三つは一本に繋がっている。起動は `work/mkwide.py` の三角を通る）。
源の側も一つ要る: 九項目の `trkd[s,8]` を席の外へ書いているので
`if tcnt[t] <= 7` で止める（**焼き手自身がこの穴に寄りかかっていた**）。

**残っている難所はここ**: 書き先の座標には既に別の検査がある（`wcp + 2`。
はみ出したら場の番号を言って止まる ——「広さを超える（言う）」がそれを測る）。
手前に濾しを置くとそれを黙らせるので、書きだけ外す必要がある。だが
`clen` を `iswro` で分岐させると二次元の書き座標（`r[q[i,k]]`）が壊れた
（`accept.py` の「二次元の中間の場」、および `clen` を `clen0 + chkl` に
組み替えると全体が壊れる）。**そこが次に掘る所である。**
`jt` の飛び先だけを `stub[cdr[gc]]` に替える手（長さを変えない）は
accept 84/85 まで行った —— 残ったのは `r[q[i,k]]` の一本だけだった。

**リリースの条件について**（REPORT.md の最後に長く書いた）。
「完璧」は内側から観測できない。観測できるのは「知らない形を黙って焼いたか」
だけで、それは測れる。Lattix は三つ手元にある —— 答えの検査器（`attest.py`）、
二実装のバイト一致（`g4d` = Wheeler の diverse double-compiling）、
測られた限界（accept / differ / meta / fits）。足りないのは **検査器が
Python にあること** だけである。`attest.lx` を書けば、使う人は `lattix` と
`attest` の二枚で自分の答えを自分で確かめられる。読むのは `.lx` と答えと階数
だけなので、**不動点の計算であって、Lattix で書ける**。そこが本丸だと思う。

---

**座標の濾しを直した**（565d4cf）。リリース条件（嘘をつかない）に一番近い所を潰した。

嘘だったもの: `b[i] <- a[big[i]]` で big が a の広さを超えると、**隣の場の升を
読んでその数を答えにしていた**（解釈実行は ⊥）。鎖にすると読んだごみが次の
座標になって segfault。表の列でも同じ。

基: 濾し（`cmp r9, 広さ` / `jae`）は在ったのに `cdhf == 1`（ずれがあるとき）に
しか出なかった —— 検査が「座標」という **概念**ではなく「ずれ」という
**機能**に紐づいていた（気づき34 の裏面）。

直すときに踏んだ三つ（同じ所を触る人へ）——

1. **内側の所有者でも次元0 は濾す。** 外すと `f0[f1[f1[i]]]` の真ん中の輪が
   素通りして segfault が生き残る。外すのは次元1 以降だけ（畳みの途中）。
2. **書き先の座標は `av` ではなく `stub`（場の番号を言って止まる）へ飛ばす。**
   読みのはみ出しは「値が無い」（⊥）、書き先のはみ出しは「宣言が嘘」。
   長さは変えない。`iswro[rroo[r]]` で見分ける。
3. **`clen` は足すだけ。書き換えない。** 源の長さを 1→3 に書き換えると、
   内側の次元1（源 1・畳み 2 で max が要る）で max が 3 を選び **一命令の穴**が
   空いて番地の鎖が切れる。`clen0 + chkl` に組み替えるのも同じ理由で壊れた。
   大きい値を `if cchk[gc]` 付きで **足す**（max が正しく選ぶ）。

道中: 濾しを入れたら **焼き手自身が止まった** —— 九項目の `trkd[s,8]` を席の
外へ書いており、自分が直した穴に寄りかかっていた（`if tcnt[t] <= 7` で止めた）。
像が 512KB を超えたので **文字の面 = rcap = 589,824 / 像 = 655,360** に上げた
（三つは一本。起動は `work/mkwide.py` の三角を通る）。

**いまの試験の色**: accept 90/90（既知の穴 2 を含む）、gen ✓、selfgen ✓、
自分を焼いてバイト一致（Lattix 側 / C 測定器の g4d 側とも）。
`differ` 200 本で食い違い **1**（6 から）、`meta` 250 本で破れ **1**（5 から）。

**残った二つ。どちらも今日より前から在る**（HEAD の焼き手も同じ答えを出す）。
再現は `accept.py` に `known=True` で置いてある ——

① **否定のガードの下の ⊥**。`if not g[f[i]]` で `f[i]` が ⊥ なら `g[⊥]` は ⊥、
  `not ⊥` は真 → **撃つ**のが正しい。焼いた側は座標の ⊥ 検査を一律
  「次の行へ」にしているので撃たない。
  直し方は測ってある: 飛び先を `av` ではなく **ガードの終わり**
  （`gpos[gg] + glen[gg]`）にする。長さは変わらない。所有者が否定のガードか
  （`gdk == 2`）で見分け、源5 の内側へは `cdcc` 経由で継ぐ。
  **入れたら「途中で切れている（言う）」が断らなくなった** —— 断りの数え
  （`nokX != nX`）に何が効いたのかまだ掴めていない。そこが次の掘り所。

② **count の読み × min**。`f2[f0[i]] <- f1[i]`（f1 は count、f2 は min、
  書き座標が読み）で、説明のつかない 1 が入る（正しくは 2）。
  `f2[i] <- f1[i]` と書けば正しい。`f2` を max にしても正しい。
  つまり **min の join と count の読みと「読みの書き座標」が三つ揃ったとき**
  だけ出る。まだ基を掘れていない。

**リリース条件から見た現在地**:
「もっともらしい違う数を返す」嘘は消えた。残っているのは「撃つべき所で撃たない」
「説明のつかない値が一つ入る」の二つで、どちらも **名前と再現と測る道具**が
付いている。隠れているものが減った、というのが正味である。

---

**残った二つを閉じ、同じ基からあと二つ出して閉じた**（2026-09-19。d475c0a / f7b88f5 /
58b7de5 / 0f5f5fe）。詳しくは `REPORT.md` の最後。

① **否定の下の ⊥** —— 間違っていたのは **解釈実行**だった（`ev` の `not` が、座標の不在を
「その升は ⊥」にすり替えて真に裏返していた）。焼いた側の飛び先を動かす直し方は、直す相手が
違っていたので断りを壊した。意図は `classify` / `native.botguards` / C 測定器 / 焼いた符号の
四箇所に書いてあった。

② **count × min** —— 焼く側の成層が **位置**（`not` の直後 / 比較の節 / 添字の中）しか
数えず、**向き**を数えていなかった。読む場の動く向きと読む位置が要る向きが食い違うと、
同じ表を回る規則が一つのループに畳まれて途中の値を掴む。`33_self` に四つ目（`tnm4`）を
足した。`lattix.lx` 自身の中に 116 本あった（鎖ばかりで出なかった）。気づき38 と SPEC の
「言語が禁じている」は同じ基を使い手への禁止として書いたもの —— いまは層が一つ増えるだけ。

③ **書き先の側の向き** —— flat / sum / set / bag / fourv の書き先は動く値を呑めない
（flat は ⊤、sum は **黙って小さい和**）。`classify` 自身が flat の書き先に「上へ」を
要求していた。解釈実行が正しかったのは評価器が依存の順（SCC）に解くからで、**分析ではなく
順序が救っていた**。`classify` に `_target_req`、`33_self` に `sabs`。構成子の引数も「動かないこと」。

④ **`toodeep` を誰も読んでいなかった** —— 成層できない源（`07_illposed.lx`）を焼いた側は
終了コード 0 で答えていた。理由 7（`unstratifiable`。空いていた番号）で言う。直すと fourv の
本が「成層できない」と言われた（Belnap の `not` は単調なのに断片は否定と数える）—— 理由が
嘘なので、断片の知らない束は **宣言の行で**理由 8 と言う。

**道中: 焼き手は自分の出現の面の 2 手前に居た**（8,190 / 8,192）。④を足したら場 53
（`okey2`）で止まった。出現・所有者・スロットの面を **16,384** にした（64 枚と `capw`）。
起動は二段: 面だけ広げた③の源を③の焼き手で焼き、それで④の源を焼く。
**次に近い天井**: 文 3,366 / 4,096（82%）、規則 2,731 / 4,096（67%）。

**測る道具を一つ足した: `test/strat.py`**（30 秒）。断片に探針を足して焼き、**焼く側が
実際に振った層**を文ごとに描かせて、`classify` の印と突き合わせる。層を振れない本は
焼いて走らせて本当に断るかを見る。直す前の断片は `lattix.lx` だけで 400 辺を破っていた。

**いまの試験の色**: accept 99/99、strat 破れ 0（69 本・21,024 辺）、differ 200 食い違い
なし、meta 250 破れ無し、gen ✓、selfgen ✓、fold ✓、g4d バイト一致。

**同じ所を触る人へ（五つ）**:
1. **成層を触ったら `test/strat.py` を回す。** accept / meta は「畳まれたループが途中の値を
   掴む」形を作れたときしか捕まえない（向きの穴は七本とも、手で形を作るまで accept に
   出なかった）。strat は層そのものを見る。
2. **`classify` と `33_self` の `tnm4` / `sabs` は同じ判断を別の道具で書いている**
   （気づき34）。片方を触ったらもう片方も触り、strat で突き合わせる。
3. **余分な層は安い、足りない層は嘘。** 焼く側は変数の符号を証さない（データを見ずに
   焼く）ので、解釈実行より層が多いことがある。それは答えを変えない。
4. **焼き手を作り直すときは二段で焼く**: 古い焼き手で新しい `lattix.lx` を焼き（S1）、
   S1 で焼き直す（S2）、S2 == S3 を見てから置く。g4d は C 測定器の道で同じバイトに
   着くかを見る。C の生成物は `_gen/ccache` に **内容で**引かれるので、`fold.py` と
   `g4d.py` は先に回した方が clang（約 16 分）を払い、後の方は速い。
5. **面を広げるときは、狭い焼き手で焼ける源から広げる**: HEAD の源の `bound` だけを
   書き換えて HEAD の焼き手で焼き（出現の数は変わらないので焼ける）、その焼き手で新しい
   源を焼く。`work/mkwide.py` と同じ三角である。

**次の一手**:
1. **⊤ の規律**（⑥）。SPEC は「⊤ は少なくとも真」（比較は ⊤ で **立つ**）、「⊤ は座標に
   なれない」、算術は ⊤ 厳密と言う。焼いた符号は ⊤ の印 0x7ffffffe をただの数として読み、
   `if g[0] < 5` で解釈実行は立つのに焼いた側は立たない（測った）。**嘘の側である。**
   （前の担当の「比較は ⊤ で偽」という書き方は SPEC と逆だった。）
2. **`attest.lx`** —— リリース条件の最後の一枚。答えの検査器を `.lx` で書けば、使う人は
   `lattix` と `attest` の二枚で自分の答えを確かめられる。

---

**⊤（⑥）と、撒く道具二つで出た穴、口と幅の穴を閉じた**（2026-09-19。df800fe / 9e96b7b /
3d9c16b / 05d645e / b56b69b / b617bcd）。詳しくは `REPORT.md` の最後。

- **⑥ ⊤**: ⊤ が **数として誤読される場**だけ、作る所で止まって終了コード 5（`tdang`）。
  素のガードと flat への素の写しは誤読ではないので止めない（定数伝播は回る）。
- **`test/mutate.py`**（300 本で 1 分）: 通る本を一か所壊して撒く。最初は 125 本で 22 本破れた。
- **`test/coords.py`**（300 本で 1.5 分）: 座標の入れ子を積のまま撒く。一つ前の焼き手で
  150 本のうち 19 本が嘘か segfault（内側の次元1 を濾していない・次元1 の読みが畳みかけを
  潰す・前置のずれの印の付け過ぎ）。
- **値の幅**: 印と等しい値・印を越える値（min は 2147483646 まで、max は -2147483646 から）は
  終了コード 4、種は理由 D。64 ビットの溢れは終了コード 3（imul・群の足し引き・sum の直後に jo）。
- **口**: 置き場より大きい入力は 6（前は黙って切った）、read の失敗と行の途中で切れた語の列は
  8（前は空の入力・0 で埋めた一行）、描く値が 0..255 の外は 2（前は下位八ビット）、書けない
  出す先は 9（前は 0）、render 二つは理由 8（前は片方だけ）。
- **面**: 像 1 MB（rcap 983,040）。文・規則・項・ガード・ループ・種の面を 8,192 に
  （ループ 2,062 本が面 2,048 を超えて、焼き手が自分を焼けなくなった）。符号の面は像の
  末尾 1024 バイト（知らせ）の手前まで —— 届けば焼き手が 6 で止まる。

**いまの試験の色**: accept 156/156、strat 破れ 0、coords 300 × 2・mutate 300 × 2・values 300 × 2 破れ無し、mouths 12/12、differ 200 食い違いなし、meta 250 破れ無し、gen ✓、selfgen ✓、fold ✓、g4d バイト一致（L1 == L2 == 三段で焼いた X2）。

**同じ所を触る人へ（三つ足す）**:
6. **焼き手を触ったら `test/mutate.py`・`test/coords.py`・`test/values.py`・`test/mouths.py`
   を回す。** accept は通る形を、differ は文法の形を測る。mutate は「一か所壊れた形」、coords は
   「深く組んだ座標」、values は「印と 64 ビットの近くを通る値」、mouths は「入口と出口」を測る。
   種を変えて何度か（`python3 test/coords.py 300 <種>`）。
7. **一つの値に二つの意味を持たせた所を探す。** read の 0（終わり / 置き場が無い）、⊥ の印
   （無い / その数）、書きの失敗（頼まれていない証人 / 頼まれた答え）、描く升（バイト / 値）。
   どれも黙る方へ倒れていた。**もう一つの意味で来る入力**を一つ作れば見える。
8. **面を広げる起動は三段**: 像や rcap を広げるときは中継ぎ（rcap だけ広い源を今の焼き手で
   焼き、それでもう一度焼く —— 自分の詰め直しの置き場が広くなる）、面を広げるときは
   HEAD の源の `bound` だけを書き換えて今の焼き手で焼く。そこから本体を焼き、S2 == S3。
   **次に近い天井**: 命令 約 14 万 / 262,144、出現（lattix.lx 自身）/ 16,384。

**次の一手**:
1. **`attest.lx`** —— リリース条件の最後の一枚。答えの検査器（`attest.py`: 安定性と
   有基性）を `.lx` で書く。一般の評価器を断片で書くには、区間の上端に変数の添字、疎な場
   （内容アドレス）、表の複数 —— どれかが要る。何を足すかから決める。
2. **⊤ の規律を焼く**（止めるのをやめる）。比較で ⊤ なら立つ、⊤ を座標にしたら止まる、
   算術は ⊤ 厳密、描画は ⊤ を飛ばす。
3. **撒く道具を足す**: 値の幅（印の近く・64 ビットの近く）を撒く道具、口（大きい入力・
   閉じた出す先）を撒く道具。今日の口の穴は手で打つまで出なかった。

---

**前段が知らない語を黙って読み飛ばしていた —— 語を数えるようにした**（2026-09-19 夜。f228ea2 と
その次の束）。詳しくは `REPORT.md` の最後。

- **見つけ方**: attest.lx の前段（焼き手の前段 + 表を語の列にして描く尻尾）を焼いたら、
  出てきた語が全部 2147483646 だった（`-2147483646` の先頭の `-` が消えた）。手で打つと、
  値・座標・ガード・for 節・頭・種の **知らない語**をどれも黙って読み飛ばしていた
  （`-5` が 5、`c[i*2]` が c[i]、`if not not x` が `not x`、`c[i-200]` が c[i+56]…）。
- **基**: 認識する側（前段）と撒く側（differ / mutate / progs / coords / values）が **同じ語彙**を
  持っていた。語彙の外は両方から見えない。
- **直し**: 形を足すのではなく、**読めた語を数える**（`vok` / `crdok` / `gdok2` / `fok` / `fpok` /
  `hdok` / `sreg`）。読めなかった語が一つでも残れば理由 8。先頭の `-` は群0 の符号として焼く
  （単項。`*` `/` `%` より強い）。ずれは -128..127（符号つき imm8）。2^32 以上の法は shl / shr。
  二のべきの除数は 2^62 まで。20 桁の数は理由 C（前は焼き手が 3 で落ちた）。表は 15 列まで。
- **f228ea2**: 同じ升の種は束で join、⊥ の上端は空の区間、RANGE_CAP 4,194,304（理由 F / 終了コード 10）、
  場の置き場 2 GB（理由 E）、描く置き場を場の升数で取る、`test/progs.py`（本を丸ごと撒く）。

**いまの試験の色**: accept 217/217、mouths 13/13、strat 破れ 0、coords・values・mutate・progs 各 300 × 2
破れ無し、differ 200 食い違いなし、meta 250 破れ無し、gen ✓、selfgen ✓、fold ✓、g4d バイト一致
（sha256 8d4a1629…）。

**同じ所を触る人へ（二つ足す）**:
9. **形を数え上げない。読めた語を数える。** 前段に形を足したら、その形が読む語を被覆の印
   （`vok` など）にも足す。印の無い語は理由 8 で断られるので、足し忘れは「断りすぎ」として
   見える（黙った嘘にはならない）。撒く道具に形を足すときは、**前段が知らない語**を混ぜる
   （`test/mutate.py` の語を撒く六つの壊し方が雛形）。
10. **穴の幅は値の幅ではない。** 機械語の即値に値を置く所は、穴の **符号**で範囲を数える
    （imm8 は -128..127、`and` の imm32 は符号拡張される）。

**次に近い天井**: 焼き手の源の場 749 / 宣言の面 1,024、出現 約 11,200 / 16,384、ループ 2,286 / 8,192。

**次の一手**:
1. **`attest.lx`**（作っている）。表の意味は Python（`attest_ir`）で測り終えた —— accept の答え 100 本と
   壊した証明書 217 本で attest.py と判定が一つも食い違わない。評価器（.lx）は 4,096 実例の版で
   答え 100 本のうち 95 本を ATTESTED と言い、残り 5 本は限界（鎖の深さ 3 以上・印の値）で
   UNSUPPORTED / 止まる。壊した証明書を一度も ATTESTED と言わない。
2. **証人の形を揃える**: fd 3 に値の面と階数の面を続けて出す（render の本でも attest できるように）。
3. **⊤ の規律を焼く** / **焼いた本の昇鎖に上限**（止まらない本はいまも止まらない）。
