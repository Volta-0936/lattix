# Lattix —— インストール可能にするまでの計画

> **この文書は仮のものである。** 計画が終わったら消す（文書は少なく保つ ——
> README / SPEC / GUIDE / CLAUDE の四つに戻す）。
> ここにあるのは「何を作るか」「どこまで来たか」「何をやってはいけないか」で、
> 言語の説明ではない。

---

## 0. 目的（言い換え禁止）

**自由に書かれた `.lx` を、Windows / Linux にインストールされた Lattix **だけ** で
実行できるようにする。Python も C も要らない。最終形は配れる package である。**

この一文が的である。以下は的を狭めた言い方であり、**すべて禁止**する:

- 「断片が焼ければよい」—— 断片は配布物ではない
- 「`noema.lx` が走ればよい」—— noema.lx は自由に書かれたファイルの一例にすぎない
- 「`examples/` が何本焼けるか」—— これはコンパイラ計画の網羅率であって、目的ではない
- 「評価器が焼ければよい」—— 評価器は手段であって的ではない

**測る物差しは一つ**: *参照実装（`lattix.py`）と同じ答えを出す `.lx` の範囲*。
これが「全部」になったとき、目的は達成される。

---

## 1. いまある物（実測。2026-08-22）

### 1.1 動く物

| 物 | 何をする | 大きさ | 状態 |
|---|---|---|---|
| `lattix.py` ほか Python 4 本 | **言語の全部**を解いて答えを出す（参照実装） | 6,418 行 | 全構文が動く。`noema.lx` も動く |
| `lattix`（静的 ELF 512KB） | 断片の `.lx` を焼いて実行ファイルにする | — | 自分を焼いてバイト一致（G4d） |
| `lattix.lx` | 上の実行ファイルの源（Lattix で書かれている） | 3,853 行 | `33_self` + `lib/fold` + `31_gen` を畳んだ物 |
| `examples/33_self.lx` | 源のバイト → 規則の表（**断片の構文だけ**） | 1,064 行 | 前段。全構文には足りない |
| `examples/31_gen.lx` | 規則の表 → x86 の静的 ELF | 2,687 行 | 焼く物。Linux ELF のみ |
| `examples/25_eval.lx` | Lattix で書いた Lattix の評価器 | 158 行 | 深度1・min・ガード無しまで |
| 走らせる物（試作） | 規則の表 → **答え**（最小不動点） | **41 行** | min/max/or・表のループ・加算・種。四つのプログラムで参照実装と一致 |
| 試験 42 本 + 実演 11 本 | 検証 | 7,031 行 | 全部 Python |

### 1.2 いま配れない理由

1. **言語の全部を解く物が Python である。** `lattix` が焼けるのは断片だけで、
   `examples/` 34 本のうち焼けて走るのは 4 本。
2. **木が散らかっている。** 直下に `lattix.py` / `check` / `lattix`（実行ファイル）/
   `lattix.lx` / `examples/` / `test/` / `bench/` / `demo/` / `_gen`（451MB の生成物）
   が同列に並んでいる。展開してすぐ使える形ではない。
3. **Windows が無い。** `31_gen.lx` は Linux の静的 ELF と生の syscall しか出さない。
4. **人が読める出力が焼いた側に無い。** 焼いた実行ファイルの `print` は場の面
   （升ごとに 8 バイト小端）をそのまま出す。十進の文字は出ない。

---

## 2. 作る物の形

```
lattix（一つのネイティブ実行ファイル）
  = front.lx   源のバイト  → 規則の表      （いまの 33_self.lx を全構文へ）
  + run.lx     規則の表    → 最小不動点    （いまの試作 41 行を全意味論へ）
  + show.lx    答え        → 人が読める文字（新規）
```

三つとも **Lattix で書く**。焼くのは `31_gen.lx`（いまの物）である。

**ここが要点**: `31_gen.lx` は「利用者のプログラム」を焼けなくてよい。
**`front.lx` / `run.lx` / `show.lx` の三つが焼ければよい。**
断片を広げる作業は、この三つが通るところで止まる。的が有限で、読めば数えられる。

利用者のプログラムは `run.lx` が**走らせる**（焼かない）。だから利用者は
構成子も集合も `budget` も使える —— 断片の外の物でも、走らせる物が解く。

`31_gen.lx` の「焼く道」は捨てない。**通る形だけの速い道**として残し、
`differ` で「焼いた答え＝走らせた答え」を測り続ける。速さは最適化であって、
意味ではない。

### 2.1 なぜ走らせる物が小さいのか（そしてなぜ速くできるのか）

同じ規則の表から:

| | 行数 |
|---|---|
| x86 を出す（`31_gen.lx`） | 2,687 |
| **答えを出す（走らせる物）** | **41** |

命令を選ぶ仕事は、答えを出す仕事ではなかった。そして速さの出所は命令の符号化
ではない —— `CLAUDE.md`「Lattix が何であるか」にある通り、

```
CALM 定理        単調なら協調が要らない ⇒ バリアは非単調な境目にだけ
Lucid の座標空間  番地も命令番号も時間ではなく **座標** である
LVars の freeze  「もう増えない」と言える瞬間があり、そこで表現を替えてよい
熱帯半環         min/+ ・ max/+ ・ or —— 結合的・可換・**冪等**
BSP superstep    層／SCC がそのまま superstep。中は無秩序でよい
多面体モデル      反復空間 → 書き先の写像。ただし **合法性の証明が要らない**
CRDT             合流は join。複製・分散・増分に手続きが要らない
```

**どのループがどのセルに触るかは、規則の表に宣言されている。** 走らせる物は
それを読むだけでよく、C の最適化器がやる再発見（別名解析・依存解析・
ベクトル化の合法性証明）が一つも要らない。第6段でここを使う。

---

## 3. 段取り

各段に **出口条件**（測って言えること）を付ける。出口条件を満たすまで次へ行かない。
満たせなかったら「満たせなかった」と書いて戻す（半分動く物を置いていかない）。

### 第1段 —— 走らせる物（`run.lx`）を全意味論へ

参照実装の下で育てる（`lattix.py` で走らせて試す）。**x86 には一切触らない。**
一回の試行が秒で回るので、焼く道（一回 5 分）と比べて桁で速い。

| 番号 | 足す物 | 出口条件 |
|---|---|---|
| 1.1 | ガード（`if 列 == 値` / `if not f[C]` / 比較の左右） | ガードを使う例で参照実装と一致 |
| 1.2 | 区間ループ `for (i) in lo .. hi`（上端が場の場合を含む） | 同上 |
| 1.3 | 二次元の場 | 同上 |
| 1.4 | 残りの束: `flat`(⊤) / `sum` / `count` / `and` / `set` / `bag` / `fourv` | 各束の例で一致。⊤ も一致 |
| 1.5 | 層（成層）。否定と比較の境目が superstep | 否定を含む例で一致 |
| 1.6 | 項の残りの演算（`-` `*` `/` `%`、場と場の積） | 一致 |
| 1.7 | 複数の表 | `02_strata` など 16 本で一致 |
| 1.8 | 構成子（内容アドレス）と文字列原子 | `16_lex` / `noema` で一致 |
| 1.9 | `budget`（depth / top / io） | `13_budget` で一致 |
| 1.10 | `source` / `emit`（外界との往復） | `12_world` で一致 |

**第1段の出口**: `examples/` 全 34 本 + `noema.lx` + 撒いた 150 本で、
走らせる物の答えが参照実装と**升まで一致**する。
（このとき表はまだ Python が作っている。それは第2段で置き換える）

### 第2段 —— 前段（`front.lx`）を全構文へ

`33_self.lx` を土台にする。バイト列 → 規則の表。

| 番号 | 足す物 | 出口条件 |
|---|---|---|
| 2.1 | いまの断片構文 | 済 |
| 2.2 | 複数の表 / 表の途中の改行・注釈 / `table X =` の後の改行 | 参照実装の表と一致 |
| 2.3 | 文字列原子（内容アドレスに畳む） | 同上 |
| 2.4 | 構成子の宣言と式 | 同上 |
| 2.5 | `budget` / `source` / `emit` / `component` / `include` | 同上 |
| 2.6 | 式を辺に持つガード（`if a == b + c`） | 同上 |
| 2.7 | 誤りの報告（行・理由・言葉） | 誤ったファイルで行と理由が一致 |

**第2段の出口**: 全 34 本 + `noema.lx` について、`front.lx` が出す表が
Python が出す表と**完全に一致**する。ここで Python は表作りから外れる。

### 第3段 —— 見せる物（`show.lx`）

| 番号 | 足す物 | 出口条件 |
|---|---|---|
| 3.1 | 十進の整数（逆数を掛けて 2^32 で寄せる。`34_decimal.lx` の技） | 数が読める |
| 3.2 | 文字列原子を名前に戻す | `thought[power] = 3` と出る |
| 3.3 | 構成した値を `name(a, b)` に開く | `because_unary(11, power)` と出る |
| 3.4 | ⊥ / ⊤ / true / 集合 の表記 | 参照実装と同じ字面 |

**第3段の出口**: `front + run + show` を参照実装の下で通したとき、
標準出力が `python3 lattix.py f.lx` の出力と **バイト一致**する。

### 第4段 —— 焼く（断片を三つが通るまで広げる）

ここで初めて x86 に触る。**広げる前に数える。**

| 番号 | やること | 出口条件 |
|---|---|---|
| 4.1 | `front/run/show` が断片で書けるか **読んで数える**。足りない形の一覧を出す | 一覧が出ている（作る前に） |
| 4.2 | 既知の穴を先に潰す:<br>・二次元の場を座標に置くと焼いた側が黙って何も書かない<br>・規則を一本足すと E0（C の裏側）と L1（焼いた裏側）が 8 バイトずれる | 最小形の再現が消え、G4d がバイト一致のまま |
| 4.3 | 4.1 の一覧の形だけを `31_gen.lx` に足す。**一つ足すごとに** g4d バイト一致を確認 | 一覧が空になる |
| 4.4 | 三つを畳んで焼く | `./lattix f.lx` が参照実装と同じ答えを出す |

**第4段の出口**: Linux で `./lattix noema.lx` が走り、`python3 lattix.py noema.lx`
と同じ答えを出す。**ここで C も Python も、走らせるのに要らなくなる。**

### 第5段 —— 包む（インストール可能にする）

#### 5.1 木を整える

いまの散らかりを一度だけ直す。試験の道が全部変わるので、**まとめて一度**にやる。

```
lattix/
  bin/lattix                 焼いた実行ファイル（配る物）
  src/                       言語の源（Lattix）
    front.lx  run.lx  show.lx
    gen.lx                   焼く道（速い道）
    lib/                     graph.lx / stratify.lx / tropical.lx …
  examples/                  例（34 本）
  doc/                       README / SPEC / GUIDE / CLAUDE
  test/                      検証（開発用。配らない）
  ref/                       参照実装 lattix.py ほか（開発用。配らない）
  Makefile                   焼き直し・試験・package を一つの入口に
```

#### 5.2 Linux の package

```
lattix-<version>-linux-x86_64.tar.gz
  bin/lattix                     静的 ELF。依存なし（not a dynamic executable）
  share/lattix/examples/…        例
  share/doc/lattix/…             文書
  install.sh                     /usr/local へ置く（--prefix で変えられる）
```

さらに `.deb` を作る（`DEBIAN/control` + 上と同じ配置）。`apt install ./lattix.deb`
で入るところまで。

#### 5.3 Windows の package

`31_gen.lx` は今 **Linux の静的 ELF と生の syscall** しか出さない。
Windows には二つ足す:

1. **容器を PE/COFF にする** —— DOS スタブ + PE ヘッダ + セクション表。
   命令のバイトは同じ（x86-64）。変わるのは容器と番地の付け方だけ。
2. **口を kernel32 にする** —— 生の syscall は Windows では版に依存して壊れる。
   取り込み表（import table）を一つ作り、`CreateFileA` / `ReadFile` /
   `WriteFile` / `ExitProcess` を呼ぶ。前置きと書き出しの十数命令が変わる。

出す物:

```
lattix-<version>-windows-x86_64.zip
  lattix.exe                     依存なし（kernel32 のみ）
  examples\  doc\
```

**危なさ**: 取り込み表は ELF より込み入っている（RVA と thunk）。だから
**第5段の最後**に置く。Linux が完成してから着手する。

#### 5.4 使い方の決まり

```
lattix f.lx                 走らせて答えを出す
lattix f.lx -o out          焼く（速い道。断片の範囲だけ）
lattix --version            版
lattix --help               使い方
終了コード  0 正常 / 6 宣言した広さを超えた / 7 焼けない形 /
            8 源が開けない / 9 出す先が開けない
```

#### 5.5 インストールの試験

まっさらな箱（何も入っていない Linux / Windows）に package を展開し、
`lattix examples/01_shortest.lx` と `lattix noema.lx` が走ることを測る。
**Python も C も入っていない箱で測る。** これが最終の出口条件である。

### 第6段 —— 速さ（インストールできるようになってから）

宣言されている足跡をそのまま使う。**C の最適化を移植しない。**

- **作業リスト** —— どの規則がどのセルを読むかは表にある。セルが動いたとき
  再発火すべき規則実例は、証明せずに引ける
- **層 = superstep** —— バリアは非単調な境目にだけ置く（CALM）
- **幅は束が決める** —— `or` の場は 1 ビット。推論ではなく型
- **分割は座標で** —— どの反復がどのセルに書くかを知っているので owner-computes。
  競合が構造的に起きない
- **freeze** —— もう増えない場は表現を替えてよいし、階数の維持もやめてよい
- **焼く道** —— `gen.lx` が通る形は焼く。`differ` で「焼いた答え＝走らせた答え」を測る

---

## 4. 検証の決まり（全段に効く）

1. **参照実装（`lattix.py`）は定義であり、配らない。** 二つの実装を突き合わせる
   ためだけに残す。これが無いと「同じ源が二つの答えを持つ」を検出できない。
2. **各段の出口条件は「一致」である。** 「動いた」ではない。
3. **撒いて突き合わせる**（`test/differ.py`）を各段で広げる。いまは
   二層・一次元・座標四種。段が進むごとに文法を広げる。
4. **`./check` は何かを変えたら必ず通す。** 42 本。50 分ほど。
5. **半分動く物を置いていかない。** 通らなければ戻し、「戻した」と書く。

---

## 5. やってはいけないこと（私が既にやった失敗）

1. **的を狭めるな。** 断片・`noema.lx`・評価器 —— どれも的ではない。
   的は §0 の一文である。
2. **言い換えを進捗と呼ぶな。** 進捗は「動く物が増えた」ことだけを指す。
   枠組みの説明を三度続けて出したことがある。あれは進捗ではない。
3. **C の最適化を移植するな。** 可変レジスタ（rbp を口の番号に使った）、
   スタック（`push rdx` / `pop rdx` を入れて順序のバグを出した）、
   命令表を手で符号化すること —— どれも「既に持っている情報を捨ててから
   拾い直す」形である。先に問うのは **「宣言されているものを、そのまま
   機械に渡すには？」**（`CLAUDE.md`）。
4. **裏側の制限を言語の仕様に書くな。** `divisor not 2^n` も `mul after add` も
   x86 出力器の性質であって、単調写像の性質ではない。断片の節にだけ書く。
5. **食い違ったら、断る前に定義に照らせ。** 答えは最小不動点が定めている。
   断るのは安全側の通常手であって、正しさではない。
6. **広げる前に数えろ。** 作ってから「足りない」と気づいて戻す、を繰り返さない。
7. **指標を取り違えるな。** 「例が何本焼けるか」ではなく
   「参照実装と同じ答えを出す `.lx` の範囲」。

---

## 6. いまここ（2026-08-23 時点。測った値）

### 6.0 構造 —— 解釈器をやめた

前の走らせる物は **種で場合分けする解釈器**だった。`kd == 3` なら場の読み、
`kd == 4` なら計数器…と分岐し、座標を走りながら計算する。fetch-decode-execute を
宣言で書き直しただけで、**Lattix の構造を一つも使っていない**。全部捨てた。

Lattix のプログラムの実体は「規則・項・ガード」ではない。
**升と升のあいだの寄与の関係**であり、答えはその閉包である。
どのループがどの升に触るかは宣言されているのだから、関係は **数え上げられる**。

前段が渡す表は三つだけである。

```
eg  (辺, 層, 束, 先, 形, 数, a, la, b, w)     先 <- v[a] + v[b] + w
       形 0 値そのもの / 1 Σ源+w / 2 true / 3 {w} / 4 写し / 5 false
       形 6 v[a]*w / 7 v[a]/w / 8 v[a]%w / 9 {v[a]}   （数=2 なら右も升）
       la が **負**なら「源は閉じた面から読む」の合図
       層が **負**（-1-s）なら「層 s の条件のための項」（`ct` 面に住む）
cd  (辺, 層, 種, 束, 升, 演算子, 数, r1, r2, w)
       **条件も辺と同じ形**。左の升と、右の Σ升+w を比べる。
       種 1 比較（閉じた面）/ 11 比較（問いが単調なら生きた面）
       種 2 not / 3 肯定 / 4 ⊒（`is`）/ 5 Belnap の否定
       束 0 は「条件のための面 `ct`」を見る
ix  (指し番号, m, 升, 束, o, 束)
       **升を指す番号**。m=0 なら「そのまま o 番の升」、
       m≠0 なら「m × v[升] + o」＝ **値で決まる升**。束が負なら生きた面。
```

走らせる側には「直接の升」と「値で決まる升」の場合分けが一つも無い ——
`rz[指し番号]` と書くだけである。

### 6.1 `run.lx` —— **配る一枚**

走らせる物がプログラムごとに違うなら、それはコンパイラであって処理系ではない。
形の一覧を **言語の側**から取り、層の上限を宣言して、一枚だけ書いた
（`work/mkrun.py` が作る）。

```
run.lx    2040 行   形 38 / 条件 34 / 束 10 / 層の上限 16
```

プログラムから出るのは表（`eg` / `cd` / `ix`）だけで、末尾に
`include "run.lx"` の一行が付く。**同じ一枚がコーパスの全部を走らせる。**
行数は言語の形の数 × 層の上限であって、プログラムの大きさではない。

### 6.2 内容アドレスを **算術**にした（今回いちばん大きな設計変更）

構成子の名前は内容アドレスである。以前は SHA-256 の先頭 56 ビットだった。
正準ではあったが、**それを計算できるのは SHA-256 を持つ実装だけ**である ——
つまり Lattix 自身には計算できなかった。処理系が自分の言語で書かれることを
目指す以上、これは穴である。

いま、内容アドレスは固定の乗数と法による多項式を **二本**折ったものである。

```
M1 = 2^31 - 1   M2 = 2^31 - 99   K1 = 1000003   K2 = 1000033
各 (M,K):  h ← 折り込み(名前) ;  h ← (h·K + 引数の数+1) mod M
           各引数 a:  h ← (h·K + enc(a)) mod M
番地 = h1·M2 + h2                             （62 ビット）
```

- `*` と `+` と `%` だけ。**掛け算が 31×31 ビットに収まる**ので 64 ビットで足りる。
  128 ビット整数もハッシュ関数も要らない。
- だから **走らせる物が自分で番地を作れる**。構成子は特別な仕掛けではなく、
  ただの辺の連なりになった（`h·K` は形 6、`+enc` は形 1、`mod M` は形 8）。
- 単射性の等級は変わらない（56 ビットのハッシュも当たれば潰れる。いまは 62 ビット）。

同時に、成層器の規則を二つ精密にした。どちらも
**「育つ束の話と、育たない束の話を混ぜていた」** という同じ誤りである。

- `/` と `%` は育つ束の上では単調でないが、**flat / fourv の升の上では単調**
  （⊥ ⊑ v ⊑ ⊤ は育つ鎖ではなく、算術は ⊥ 厳密・⊤ 厳密）。
- **flat の升は座標に使っても層を要求しない**（書き先が後から良くなることが無い）。
  構成子の引数を 'UP' と読み替えていたのは、この一般則の特殊形だった。

### 6.3 動く範囲（実測）

| 入れた物 | 状態 |
|---|---|
| 束 min / max / or / and / flat(⊤) / fourv / set / sum / count / bag | ✓ |
| 表・区間・入れ子のループ、内側の端が外側の束縛で決まる形 | ✓ |
| 反復空間の端が値で決まる形（`for (i) in 0 .. n[]`）、端が ⊥ なら空 | ✓ |
| 座標: 表の列 / 計数器 / 定数 / ずれ / 文字列の原子 / **場の値**（`f[g[i]]`）| ✓ |
| 座標: **構成子の番地**（`_size[node(A, tree[i,j,B], …)]`）| ✓ |
| 座標: 升が二つ以上で決まる式（丸ごと一つの升に作る）| ✓ |
| 値: 定数・足し引き（三項以上は畳む）・写し・true/false・集合（元が升でも）| ✓ |
| 値: `*` `/` `%`（定数とも升とも）・**構成子**（引数が升でも）| ✓ |
| ガード: 比較六種（**両辺が式**・**束が混ざっても**）・`not`・肯定・`is`・Belnap | ✓ |
| `source` / `emit`（外界との往復）| ✓ |
| 層（成層）・束をまたぐ読み | ✓ |

**測った値**（コーパス 50 本 ＝ 手で書いた 17 ＋ `examples/` 33。
`31_gen` と `32_shape` は関係を実体化すると記憶を食い尽くすので外してある）:

| | プログラムごとに組み立てた物 | **固定の `run.lx` 一枚** |
|---|---|---|
| 一致 | **47** | **45** |
| 未対応 | 3 | 5 |
| 食い違い | **0** | **0** |

差の 2 本は「400 秒で切った」＝ **速さ**であって正しさではない。
`./check` の `relation` 段と `onerun` 段が、これを毎回見張る。

通るようになった主なもの: `15_parse`（CYK 構文解析器・構成子）、`16/20/21_lex`
（字句解析器）、`22_parse` / `25_eval` / `33_self`（自己記述の一族）、
`12_world` / `13_budget` / `30_build`（外界との往復）、`35_noema`。

### 6.4 参照実装に見つけて直した穴

突き合わせの度に出る。全部「走らせる物と参照実装が食い違った」から見つかった。

- **⊥ を比べていた**（前線下界の道が `INF >= 3` と答えていた）。
- **表が負の整数を持てなかった**（`(0,-5)` が構文エラー）。表は ℤ を持つ。
- **`certify` / `_sign_sense` / `_fires` がガードを無視していた。**
  起きない寄与の符号・差分・値の形を問うていた。三つとも同じ直し。
- **データの符号を証すすべを入れた。** `x * w` の `w` が literal でなくても、
  その規則が回る束縛の上で負にならないことを **宣言されたデータから**証せる。
- **中間の辺と本体の辺で通し番号が衝突していた**（自分の側の穴。28_asm で露見）。
- **内容アドレスが Lattix に計算できなかった**（上記 6.2）。

### 6.5 上限を **測る**ようにした（CLAUDE.md の節に反していた所）

> 上限を大きく書くのは、上限を測らないことの言い換えである。（CLAUDE.md）

私は場の広さを `1<<62`、`run.lx` の升の上限を `1<<130` と **書いて**いた。
これは節に真っ向から反している。直した ——

- 前段は **二巡する**。一巡目は仮の広さで数え上げ、**一度走らせて**、
  実際に触れた升をすべて数える。二巡目はその測った値をそのまま上端にする。
- `run.lx` の升・辺・指し番号の上限も、コーパス全体を測った値である。
- 宣言された `bound` があればそれを使う（宣言は測定に優先する）。
- 宣言した資源を超える本は **断る**（黙って間違えない）。

測った結果:

| 本 | 前（書いた上限） | 後（測った上限） |
|---|---|---|
| `17_render` | 10^130 | **70,029** |
| `28_asm` | 10^130 | **265** |
| `33_self` | 10^130 | **85,589,044** |
| `35_noema` | 10^130 | **231** |
| `15_parse` | 10^130 | 1.7×10^19 |

`15_parse` だけが大きいままなのは、**内容アドレスを座標にする場**があるからである。
これはこちらが大きく書いたのではなく、content address がそう宣言している ——
焼くときはその場だけが hash 表に落ち、ほかは密配列になる
（`runtime.py` の `HASH_PRELUDE`「密配列が使えないときの一般形」）。

### 6.6 多面体の道を **入れ始めた**（節が言っている答え）

> 多面体モデル: 反復空間 → 書き先の写像。ただし **合法性の証明が要らない**
> 問うべきは「宣言されているものを、そのまま機械に渡すには？」である。（CLAUDE.md）

前段は反復空間を **点に展開して** 辺の表にしていた。宣言は「この規則はこの
ループの巣を回り、この写像で書く」という多面体なのに、点に潰してから渡していた ——
**捨ててから拾い直す**、節がいちばん強く戒めている形である。

表を四つ足して、潰さずに渡せるようにした:

```
dat (行, 列, 値)                 軸の中身。**区間も表と同じ行に置く**
                                 （走らせる側に「列か計数器か」の分岐を作らない）
spc (空間, 軸, 基, 幅, 歩幅)      点 t → x_j = (t / 歩幅_j) mod 幅_j
ssz (空間, 大きさ)
mp  (写像, 定数, a0,l0,k0, …)     値 = 定数 + Σ_s k_s · d[基_{a_s} + x_{a_s}, l_s]
fg  (辺, 空間, 層, 束, 形, 数, 先写像, a写像, b写像, 重み写像)
```

走らせる側は `for (e,…) in fg for (t) in 0 .. SZ[sp] - 1` と書く。
**種で場合分けしない** —— 「表の列か計数器か」は係数 k_s であって分岐ではない。
そして座標の上下は、点に展開しなくても **各列の上下から一次式で出る**
（多面体の良いところ）。

いま通るのは **ガードの無い規則**だけである（`a_min` の `d[j] <- d[i] + w` は
辺 4 本 → **1 本**）。ガードのある規則は点のままなので、`31_gen` はまだ通らない。

**そして「選択」を入れた** —— 小さい空間は点に展開したほうが速い
（多面体は層の数だけ寄せ直すから）。`FAMILY_MIN = 512`。
これは証明ではなく選択であり、節が言っているとおりの形である。

### 6.7 双子が食い違った（`./check` 43/44。**これは成果である**）

`selfparse`（Lattix で書いた解析器と参照実装が同じ層を出すか）が赤い。
原因は特定してある —— **参照実装の判断を一つ精密にしたが、双子に同じことを
教えていない**。

> 一度直した判断は、同じ判断を持つ全員を数えてから閉じる。（CLAUDE.md 気づき34）

判断はこれである:

> 座標に使う読みは、**育つ束のときだけ**非単調である。
> `flat` / `fourv` の升は ⊥ → v → ⊤ としか動かないので、座標に使っても
> 「書き先が後から良くなる」ことが無い —— 寄与が増えるだけである。

参照実装（`classify`）はこれを入れた。入れないと **走らせる物が組めない** ——
升を指す番号 `rz` は flat の場で、あらゆる規則がそれを座標に使う。
双子（`examples/22_parse.lx` の `nonmono` / `lattix.lx` の `tnonm`）はまだ
古い判断（「座標に使う読みは常に非単調」）のままなので、
`20_lex_full` と `21_lex` で層が 7 個・22 個ずれる。

**答えは同じである**（`不一致 0` —— 升は一つ残らず一致している）。ずれているのは
層の *最小性* だけで、双子の方が保守的である＝多めに層を切っている。
だから壊れてはいないが、**二つの実装が同じ言語を述べていない**。

`22_parse.lx` の `rqC`（座標の要求）に `sense != 4 / != 5` を足す形を試したが、
それだけでは合わなかった —— 双子は座標を `rqC` だけで見ていない。
**次にやることの筆頭はこれである**（双子を数え直して閉じる）。

赤いまま置いておくのは規律に反するが、**隠すのはもっと悪い**。
`g4d`（バイト一致自己ホスト）・`selfgen`・`self3`・`fold`・`gen`・`genparse`・
`runtimecheck`・`relation`・`onerun` はすべて緑である。

### 6.8 残っているもの

**1. 双子に同じ判断を教える**（上記 6.7）—— **済み**。
`flat` / `fourv` の升は LVar（⊥ → v で止まる）なので、読みは値でも座標でも
述語でも単調である。単調でなくなる出口は ⊤ ただ一つで、そこは二つ塞いだ ——
**⊤ は「少なくとも真」**（比較が ⊤ で偽に潰れていた）、**⊤ は座標になれない**
（黙って飛ばすと「起きたか起きなかったか」が日程で変わる）。
どちらも `test/discover.py` の乱択反証器が反例を出して決めた ——
座標の読み（`f[g[x]]`）を一度も撒いていなかったので、撒くようにした。

**2. ガードを多面体に載せる** —— **済み**。`fc (辺, 空間, 層, 種, 束, 升写像,
演算子, 数, r1写像, r2写像, 重み写像)`。「全部立った」は (辺, 点) で数える
（`fq[辺]` / `fn[辺,点]` / `fo[辺,点]`）。ついでに二つ入った:

- **閉じた層だけで決まる問いは表に載せない。** 層 s の規則が層 s-1 以下の
  場だけを問うているなら、答えはもう確定している。載せれば走らせる側が
  点ごとに同じことをもう一度数えるだけである。閉じ直すのは **層ごとに一度**、
  点が多いときだけ（`_est(r) >= FAMILY_MIN`）。
- **値で決まる座標も多面体のまま渡せる**（`fp (辺, 空間, 層, 束, 基, a写像)`）。
  `f[g[i]]` の `g[i]` は点ごとに違う値だが、**その値の置き場もまた空間**で
  ある（点 t につき一升）。番地の面 `pa[基 + t]` に一枚写せば、写像は
  「定数 + Σ 係数·軸 + 係数·pa[基 + t]」の一次式のままでいられる。
  入れ子は写しを重ねるだけで届く（`pa` を `pa` から作る）。
  使わない写像は `pa[0] = 0` を読む —— 場合分けを作らないための番兵である。

  測ったこと: `out[i] <- val[idx[i]]`（点 1000）が **辺 0 / 条件 0 /
  多面体の辺 3 / 番地の写し 1**。`32_shape` の 147,456 点の規則は
  49万本の辺に潰れていたのが、潰れなくなった（速さは別の宿題）。

**3. `front.lx`** —— ここが要になった。多面体の道が入ったので、前段の出力は
**プログラムの字数に比例する**（データの大きさではない）。つまり
`examples/32_shape.lx` が出している「規則・項・ガード・座標・ループ」の表から
`spc` / `ssz` / `dat` / `mp` / `fg` / `fc` / `fp` を作るのが前段であり、
実例を数え上げる仕事はもう要らない。

**4. 焼いて包む** —— **配る一枚が焼けた**（`test/baked.py`）。
`run.lx` を一度 C に落とすと 8.6MB の実行ファイルになり、あとは表を
差し替えて何度でも走る。コーパス 52 本のうち **49 本が一致**、
未対応 3（`04_aggregate` と、断られるべき二本）、食い違い 0。

  焼くまで誰も踏まなかった穴が五つ出た（気づき21）:

  - **内容アドレスが 62 ビットに膨れていた。** 番地を座標にする場は番地の幅
    ぶんの帯を一本占めるので、31+31 だと帯四本で 2^64 を超え、升の番号が
    i64 に入らない（⊤ の番兵 INT64_MAX とも衝突する）。**56 ビットに戻した**
    —— SPEC が最初からそう言っていた。升の上限 1.64×10^19 → 2.14×10^17。
  - **集約の「一度だけ撃つ」印が、源の一本目だけで番号を作っていた。**
    表と区間が同じ規則に並ぶと、行ひとつにつき一度しか撃たない ——
    区間の点ぶんの寄与が丸ごと落ちる。手元の 34 本には一本もその形が
    無かったので露見しなかった（気づき34 の九度目）。**寄与元キーを
    束縛の組そのもの**にした（`lattix._prov` と同じ同値）。
  - **`fourv` が焼けなかった。** 値が二つしか無く否定が入れ替えるだけの
    `flat` 束なので、同じ約束で置けた。`~⊥ = ⊥` を「値がゼロでない」で
    読んで撃っていた穴も塞いだ。
  - **密配列の広さを、いま手元にある表で測っていた。** 実行時読込では表が
    あとから差し替わるので、次のデータで箱からはみ出す（気づき44）。
    **宣言した `bound` を優先**する。
  - **集合と袋が焼けなかった。** 要素の域を宣言できない（走らせる物の面に
    入る要素は升の値だから）ので、ビット集合では書けない。**整列した並びを
    arena に置いて番号で指す**ようにした —— 合流は併合、写しは番号の共有。
    袋は重複を残す差し込み。これで **束は十ぜんぶ焼ける**。

**塞げていない穴**（気づき48 —— 塞げなかったものは塞げなかったと書く）:
- x86 の生成器（`31_gen.lx`）は ⊤ の規律を持っていない ——
  比較は ⊤ で偽になり、⊤ の座標は誤りだと言わない。
  解釈実行と C 二種は直したが、x86 はまだである。
- `31_gen` / `32_shape` は前段（`work/flat.py`）が時間で切れる。多面体の道で
  辺は潰れなくなったが、閉じるのに Python の解釈実行を使っているのが遅い。

---

## 7. 旧「いまここ」（第0段の記録）

- 第0段（棚卸しと的の固定）—— この文書
- 第1段 —— **着手済み**。走らせる物の試作 41 行が、min / max / or ・表のループ・
  加算・種の範囲で、四つのプログラムについて参照実装と一致した
- 直近で入った物（どちらも一般の直し、`noema.lx` 専用ではない）:
  - 継続行の判定を「文が閉じていないなら行も閉じていない」に変えた
    （`table X =` の後の改行、括弧を開いたままの改行、`<-`・演算子で終わる行、
    文字列の中の `#`）
  - 構成した値を `because_unary(11, power)` の形で見せるようにした
    （内容アドレスは名前であって、人に見せる物ではない）
- 次にやること —— 第1段 1.1（ガード）から順に


---

## 付録 A —— いま木のどこに何があるか（第5段で整理するまでの地図）

```
lattix/
  PLAN.md          この文書（計画が終わったら消す）
  README.md        使い方          SPEC.md   言語仕様
  GUIDE.md         実装ガイド      CLAUDE.md 行動原理・気づき

  lattix           焼いた実行ファイル（静的 ELF 512KB。断片だけ焼ける）
  lattix.lx        その源（33_self + lib/fold + 31_gen を畳んだ物）
  check            全検証 42 本の入口

  lattix.py        **参照実装**。言語の全部を解く（配らない）
  native.py        C に落とす（データを焼き込む）
  runtime.py       C に落とす（データは実行時に読む）
  attest.py        答えが最小不動点か検査する

  examples/        34 本 + 35_noema.lx
    21_lex.lx      バイト列 → トークン
    22_parse.lx    トークン → 項の DAG
    25_eval.lx     Lattix で書いた Lattix の評価器（深度1まで）
    31_gen.lx      規則の表 → x86（焼く道）
    33_self.lx     源のバイト → 規則の表（前段。断片の構文だけ）
    34_decimal.lx  十進で出す（逆数を掛けて 2^32 で寄せる）
    35_noema.lx    自由に書かれたファイルの例
  lib/             graph / stratify / tropical / fold
  test/            検証 42 本（Python。配らない）
  bench/ demo/     測定と実演（Python。配らない）
  work/            **走らせる物の試作**（下記）
  _gen/            生成物のキャッシュ 451MB（配らない。作り直せる）
```

### work/ の中身（第1段の作業場）

| ファイル | 何をするか |
|---|---|
| `flat.py` | 対象 `.lx` → **寄与の関係**（`eg` / `cd` / `ix` の三つの表）。<br>反復空間を数え上げ、座標を地上で解き、ガードを地上のものと升を読むものに分ける。<br>できない形は **その場で断る**（行と理由を言う）。 |
| `engine2.py` | 関係の形から走らせる物を組み立て、参照実装の答えと突き合わせる。<br>形の一覧を差し替えれば、そのまま **固定の `run.lx`** になる。 |
| `mkrun.py` | コーパス全体から形を集めて、**配る一枚 `run.lx`** を書き出す。 |
| `t/*.lx` | 手で書いた 17 本の小さな種。一つの形につき一本。 |
| `run_all2.sh` / `run_examples2.sh` | 手で回すときの突き合わせ |
| （`test/relation.py`） | プログラムごとに組み立てた物と突き合わせる（`./check`） |
| （`test/onerun.py`） | **固定の `run.lx` 一枚**と突き合わせる（`./check`） |
| `engine.py` / `tab.py` | **旧**（種で場合分けする解釈器）。記録として残してある。 |

## 付録 B —— 走らせる物の中身

`work/t/a_min.lx`（最短路）の全部。表は `cel` / `eg` / `cd` の三つ。

```
field vn0 : min bound 5
field nc0 : count bound 6
field ng0 : count bound 6

nc0[e] <- true   for (e,st,lat,t,vf,n,a,la,b,w) in eg if st == 0
ng0[e] <- true   for (e,st,lat,t,vf,n,a,la,b,w) in eg if st == 0
ng0[e] <- true   for (e,st,k,lat,c,op,n,r1,r2,w) in cd if st == 0

vn0[t] <- w            for … if st == 0 if nc0[e] >= ng0[e] if lat == 1 if vf == 0
vn0[t] <- vn0[a] + w   for … if st == 0 if nc0[e] >= ng0[e] if lat == 1 if vf == 1
                                                            if n == 1 if la == 1
print vn0
```

最短路の全体が **寄与の規則二つ**である。残り三つは「条件が全部立ったか」を
数えているだけで、解釈ではない。座標の計算も、種の場合分けも、一つも無い。

`examples/35_noema.lx`（10 個の場・3 層・`flat` の ⊤・構成子）で 62 行。
形が増えれば行が増えるが、増えるのは **形の数**であって、プログラムの大きさ
ではない。辺 261 本・升 230 個を、同じ 62 行が閉じる。
