# 次の担当への最初の指示（そのまま貼る）

Lattix という言語の開発を引き継いでほしい。木は `lattix/` にある。

**手を動かす前に、必ずこの順で読むこと。読んだと言うだけでなく、実際に読む。**

1. `lattix/CLAUDE.md` —— 行動原理。とくに「Lattix が何であるか（最適化を
   考えるときは必ずここへ戻る）」の節と「やってはいけないこと」。
2. `lattix/PLAN.md` の **§0（目的・言い換え禁止）**、**§2（作る物の形）**、
   **§5（やってはいけないこと）**。
3. `lattix/HANDOFF.md` —— 前任者の引き継ぎ。前任者が外した外し方が書いてある。

的は `PLAN.md` §0 の一文である。言い換えるな:

> 自由に書かれた `.lx` を、Windows / Linux にインストールされた Lattix だけで
> 実行できるようにする。**Python も C も要らない。** 最終形は配れる package である。

## 特に守ってほしいこと

- **gcc で焼くのは道ではない。** 焼くのは `31_gen.lx`（Lattix で書かれた x86
  生成器）である。木の直下の `lattix` は、それが自分で書いた静的 ELF であり、
  `test/g4d.py` がバイト一致を示している。C バックエンド（`native.py` /
  `runtime.py` とそれを使う試験）は **二つ目の実装＝測定器**であって、
  配布物の道ではない。前任者はここを取り違えて時間を溶かした。
- **着手前に「これは §0 のどの部分を前に進めるか」を一行で言えること。**
  言えないなら着手しない。
- **「言い換え」を進捗と呼ばない。** 進捗は「動く物が増えた」ことだけを指す。
- **半分動く物を置いていかない。** 通らなければ戻して「戻した」と書く。
- **処理系が出力しない数値は書かない。** 判定できないなら結論を書かない。

## 検査の回し方（時間を溶かさない）

`./check` は一周 3 時間かかる。**変更ごとに通さない。**
速い部分集合で回すこと:

```
sh work/run_all2.sh          3 分   小さい 19 本
python3 test/nativecheck.py  3 分
python3 test/runtimecheck.py 4 分
python3 test/relation.py     20 分  本ごとのエンジン
```

`./check` は一連の変更をまとめて **最後に一度だけ**。
報告も区切りごとではなく、**実際に一段進んだときだけ**にすること。

## 最初にやること

1. `./check` を通す。**前任者は最後の変更（集合・袋・集約・原子）のあとに
   通していない。** その一つ前は 45/45 だった。落ちた物を直す。
2. そのあとは `PLAN.md` の段取りのまま、**第2段（`front.lx`）** に入る。
   前段（`.lx` → 規則の表）が残っている唯一の Python である。
   多面体の道が入ったので前段の出力は **プログラムの字数に比例する**
   （データの量ではない）—— 実例を数え上げる仕事はもう要らない。
   詳しくは `HANDOFF.md` §4。
