# min の関数を max の場の再帰の中に（14u。焼き手は控えめに断る —— 定義は答える）。
# 育つ二つの値の min は、max の join でも min の join でも追えない（min の場は最初の値に留まり、
# max の場は大きい方を取る）。比べて書けば追えるが、比較は非単調に数えられて層が切れる
table e = (0,1), (1,2), (2,0), (1,3)
field a : max bound 4
a[0] <- 3
a[1] <- 5
a[v] <- min(a[u], 4) + 1   for (u, v) in e
print a
