# Lattix 言語仕様

> プログラムは**単調写像の宣言**であり、答えは定義により**その最小不動点**である。
> だから **答えが実行に依存しない。**

順序・並列・分散・増分・検査は、この一点から導かれる。以下は言語の全体である。

---

## 1. プログラムの構造

プログラムは宣言の並びで、順序に意味は無い。行が単位で、`#` から行末はコメント。

```
table  edges = (0,1,4), (0,2,1), (2,1,2)     地上データ
field  dist : min                            場（束を持つ）
dist[0] <- 0                                 規則
dist[j] <- dist[i] + w   for (i,j,w) in edges
print  dist                                  観測
```

### 宣言

| 宣言 | 意味 |
|---|---|
| `table t = (a,b,...), ...` | 地上データ。行の **集合**（同じ行を二度書いても一度。`sum` / `count` も一度と数える —— 下ろす側は重なった行を種にしない。14t） |
| `field f : 束` | 場。座標から束の値への写像 |
| `f[k1,k2] <- 式 for (...) in t if 条件 ...` | 規則。**寄与**（代入ではない） |
| `for (i) in a .. b` | **座標の空間**を回す。端は値でよい（下記 §4） |
| `print f` | 場を座標つきで印字 |
| `render f` | 場を**座標順に文字コードとして**出す（下記 §7） |
| `source f : 束 from "ch"` | 外界からの単調な観測 |
| `emit "ch" <- 式 for ...` | 外界への出力（集合の成長の差分） |
| `constructor c(a,b) bound N` | 値の構成。名前は**内容アドレス** |
| `component 名(引数) -> 出力 : 束  depth D  io K ... end` | 部品。コスト署名つき |
| `local f : 束` | component 内部だけの場 |
| `include "path"` | 取り込み。行を `# --- included from path ---` とファイルの中身に差し替える（構文解析の前の、源の字の操作。下記） |
| `budget io <= N` | 予算。超過はコンパイルエラー（`depth` / `io` / `top`）|

**文の切れ目は行の終わり**。次の行が前の文の続きになるのは、前の（論理）行が `=` `,` `<-` `+` `-` `*` `/` `%` `(`
で終わるか丸括弧が開いたままのとき、または次の行が `for` / `if` で始まるときだけ。**字下げは意味を持たない**
（部品の中身のように字下げした文も一つの文）。焼く側の前段も同じ規則で切る（14x。前は字下げした行を前の文の
続きと読み、字下げした文を断っていた）。区間は両端を書く（`for (n) in .. 3` は断る —— 14x の前は焼く側だけが
下端を 0 と読んで答えていた）。

**場の次数は一つ**（14s）。一つの場はどこでも同じ数の座標で書き・読む。`n[]`（座標なしの升）と
`n[0]`（座標一つの升）を同じ場に混ぜた本は断る —— 前は別の升として数えていたが、焼く側は `n[]` を
`n[0]` に寄せるので、同じ源が二つの答えを持ちえた。

`seal`（障壁）は**存在しない**。障壁は処理系が最も早い健全な位置に置く。

---

## 2. 値 —— 束（lattice）

値は join 半束の元である。`<-` は代入ではなく **join（寄与）** であり、
情報を壊す操作が言語に無い。だから書き込み競合が構成上存在しない。

| 束 | join | ⊥ | 用途 |
|---|---|---|---|
| `min` | 小さい方 | +∞ | 距離・コスト |
| `max` | 大きい方 | −∞ | 到達時刻・スコア |
| `or` | 論理和 | false | 到達可能性 |
| `and` | 論理積 | true | 全称的な性質 |
| `set` | 和集合 | ∅ | 集まり |
| `flat` | 同じなら其れ、違えば ⊤ | 未定 | 一意な決定・**衝突検出** |
| `fourv` | Belnap 四値 | ⊥ | 否定を通る再帰（下記） |
| `sum` | 寄与元キーつきの和 | 0 | 集約 |
| `count` | 寄与元キーつきの個数 | 0 | 集約 |
| `bag` | 多重集合 | ∅ | 重複を保つ集まり |

- `sum` / `count` は **寄与元（規則 × 束縛）をキーにする**。非冪等な加算を
  冪等・可換に戻すためで、G-Counter の一般化である。同じ寄与が何度届いても一度しか効かない
  —— そして **⊥ が 0 の束では、0 は ⊥ である**。寄与が届いていても観測される値が
  0 なら、束の元としては ⊥ と同じものである（「届いたか」は履歴であって値ではない）。
  だから `s[i] <- i` の i=0 の升は ⊥ で、そこを読む比較は発火しない。7 と -7 が届いて
  打ち消し合った升も ⊥ で、`print` も `render` も書かない（2026-09-24。前は読む側だけが ⊥ と見て、
  書き出す側は店の鍵を見て `= 0` と書いていた —— 同じ判断が二箇所にあった）
- **`or` の元は ⊥ と true しか無い。** `f[i] <- c % 4` のように値を書くのは型の
  誤りで、解釈実行も焼いた符号も断る（終了コード 7・理由 9）。書けるのは `true` と
  **他の `or` の場の読み**だけである（後者なら ⊥ が火を噴かないので両者が一致する）
- `flat` は「二つの異なる値が同じ座標に来たら ⊤」。**曖昧性・矛盾が束の衝突として自動検出される**
  —— ただし **⊤ は誤りではなく束の頂点**である。曖昧性検出では ⊤ が診断だが、
  定数伝播では ⊤ が *答え*（「定数でない」）である。どちらなのかは言語ではなく
  プログラムが言う: `budget top <= 0` と書けば、⊤ に達した時点で契約違反になる
- `fourv` は Belnap 四値（⊥ / T / F / ⊤）。知識の順序で **否定が単調** なので、
  否定を通る再帰が棄却されず全域になる（嘘つき文は深度1で ⊥ を返す）

**⊥ も ⊤ も座標になれない。** キーに未定や矛盾が混ざった寄与は起きない。
同じ理由で **⊥ は集合の要素にもなれない**（未定を集合に入れると観測が単調でなくなる）。

**⊥ は比べられない。** 比較 (`< <= > >= == !=`) の片側が ⊥ の場の読みなら、
その規則実例は **発火しない**（真でも偽でもなく ⊥ である）。「次のトークンは
`=` ではない」を「次のトークンが無いときも真」と読むと、あとで値が入った瞬間に
真が偽に変わる —— 昇っているのに述語が降りる。だから `not` を跨いでも ⊥ のまま
であって、`not (f[x] < 6)` も f が ⊥ のあいだは発火しない。
「無いこと」を言いたいなら、⊥ ではなく **端に置いた定まった値**で言う。

### 整数は ℤ である

> 答えは最小不動点なのだから、**答えが機械の語長に依存してはならない。**

64ビットで環に巻くのは、値の意味を機械に明け渡すことである。だから整数に上限は無い。
処理系は収まる間は機械語で持ち、溢れた瞬間に **内容で名づけられた値** に切り替える ——
同じ数は必ず同じ語になるので、`flat` の等値も `min`/`max` の比較も、
構成子の内容アドレスも、そのまま効く。除算と剰余は **床** に丸める。

`/` と `%` は育つ束（min / max / sum …）の上では単調でないが、
**flat / fourv の升の上では単調**である（⊥ ⊑ v ⊑ ⊤ は育つ鎖ではなく、
算術は ⊥ 厳密・⊤ 厳密だから）。だから内容アドレスは再帰の中で使える。
同じ理由で、**flat / fourv の升は座標に使っても述語に使っても層を要求しない** ——
一度決まったら動かない升（LVar）だからである。二つの但し書きがある。

- `not f[…]` は層を一つ要求する。⊥ が **真**に読まれるからで、これは ⊥ の話である。
- **⊤ は「少なくとも真」である。** 述語が ⊥ ⊑ v ⊑ ⊤ の上で単調であるためには、
  ⊤ で立たなければならない。だから `f[x] < 4` は f が ⊤ の升で **立つ**。
- **⊤ は座標になれない。** 矛盾した升には名前が無いので、そこへは何も入らない。
  座標に使っている場が ⊤ に落ちたら、処理系はそう言って止まる（黙って飛ばさない）。
- **焼いた符号もこの規律を持つ**（2026-09-24。数の場は 2026-09-25）。比較は ⊤ で立ち（⊥ とは
  比べられない）、算術は ⊤ 厳密（読みの一つが ⊤ なら値は ⊤。他の読みが ⊥ なら撃たない）。
  ⊤ は **どの束へも運ぶ**（⊤ はどの束でもいちばん上なので、join は ⊤ を吸収する）:
  - min / max は束の順が数の順そのものなので、⊤ を **数の順の端** に置く —— max は 0x7fff…ffff、
    min は 0x8000…0000。端に置けば join（大きい方 / 小さい方を取る）がそのまま ⊤ を吸収する。
    端は機械の整数の端に取る（32 ビットの即値に置ける所に置くと、印より向こうの値を全部失う）。
  - sum は足すので端では吸収しない。join が升の ⊤ を見て足さず、⊤ の寄与は ⊤ の印を置く。
  - count は値を見ない束なので、⊤ の寄与も一つと数える（解釈実行と同じ）。
  - ⊤ を運ぶ数の場では、正しい値（や和）が端の印とぶつかれば言って止まる（終了コード 4）。
  座標・区間の上端・描画で読まれる場は、⊤ に達した時（数の場は ⊤ を読む前）に止まって言う
  （終了コード 5）。
- **⊤ は述語で見分けられる。** 比較が ⊤ で立つので、`if f[k] < 0 if f[k] >= 0` の両方が立つのは
  ⊤ の升だけである（値ならどちらかが倒れる）。下ろした print はこれで `⊤ CONFLICT` と書く
  （flat の場と、flat の場のある本の min / max / sum の場）——
  新しい原始は要らない。単調であるための規律が、そのまま ⊤ の検出器になっている。

**座標は別である。座標は有限の資源**（`bound N` と同じ）で、機械語に収まらない数を
座標にしようとしたら、黙って壊れずにそう言って止まる。

---

## 3. 式

```
整数リテラル      0  42  -3
真偽              true  false
文字列（原子）     "the"
変数              i  j  w        （for で束縛されたもの）
場の参照          dist[i]  tree[i,j,X]
算術              + - * / %      （零除算は誤りではなく ⊥。式が消えるだけ）
比較              < <= > >= == !=   （両辺は 場の読み / 列 / 計数器 / 定数）
論理              not 式
集合リテラル      {x, y}
上方集合の判定     式 is 定数     （x ⊒ c。下記）
構成               c(a, b)        （constructor で宣言したもの）
```

### `is` 演算子

`x is c` は `x ⊒ c`、つまり **上方集合に入っているか**を問う。

> **`is` はあらゆる束で単調である。** だから層を一つも消費しない。

比較（`<` など）は束の向きによって単調にも非単調にもなるが、`is` は常に安全側で、
処理系が生成できるガードのうち「協調を要求しないもの」を全て尽くす。

---

## 4. 意味論

### 答え

プログラム P の答えは、規則が定める写像 T_P の**最小不動点** lfp(T_P) である。
規則の適用順序・回数・並列度・分散のしかたに答えは依存しない。

### 極性と成層

処理系は各依存を分類する:

- **単調な読み**（重み 0）—— 同じ層で同時に育ってよい
- **非単調な読み**（重み 1）—— 相手が*完成*してから読む。**読む場の動く向きと、読む位置が
  要る向きが合わない読み**である: `not` の下・添字（座標）・集約・向きの合わない比較・
  向きの合わない値（`min` の場に育つ `max` を入れる、育つ場を引き算の右に置く、証の無い
  掛け算・割り算・剰余）。**書き先の側にも向きがある** —— `max` / `or` / `count` の join は
  上へ、`min` の join は下へ動く値を呑み込むが、`flat` / `fourv` / `sum` / `bag` / `set` と
  構成子の引数は呑めない（値が動いてはならない。動かない `flat` の読みだけが通る）
- **`flat` の読みは、`not` の下を除いて、どこに置いても単調である** —— 値でも、比較の辺でも、
  **添字（座標）でも**。升は ⊥ → v で止まる LVar なので、一度決まった値で比べた答えも、
  引いた升も取り消されない。⊤ は矛盾であって答えではなく、処理系がそう言って止まる
  （焼いた側は終了コード 4）。否定だけは別で、`not f[…]` は ⊥ で真に読まれる。
  だから `reach[k] <- nxt[reach[k-1]]`（flat の鎖を自分の再帰の中で座標に読む）は一つの層で解ける。
  焼く側（§12）も同じに数え、座標の読みも階数を継ぐ（証明書は「読んだ升より階数が大きい」を要る）

規則グラフを SCC 分解し、重み付き最長路を取ると **最小の層割当** が決まる。
その層数が **逐次深度** である。

この解析自体が Lattix で書ける（`examples/22_parse.lx`）。辺の表は要らない ——
**場の名前を座標にして**書き手と読み手を繋げば、重み付き最長路は `max` 束の
不動点そのものになる。

- 深度 1 ⟺ プログラムは単調 ⟺ **協調不要**（CALM 定理の境界と厳密に一致）
- 重み 1 の辺が閉路の中にあるプログラムは **成層不能**。これは実装の限界ではなく
  問いが ill-posed であることの証明であり、処理系は修復案（`fourv` 化）を添えて棄却する

### 層は座標である —— 軸に沿った成層（局所成層）

輪に見えても、否定が **同じ場の小さい座標** を読んでいるだけなら病んでいない:

```
lvl[s, j] <- true   for (s) in 1 .. 63 … if not lvl[s-1, j]
```

場の粒度では lvl → lvl の輪だが、s は静的な区間なので、代入して展開した
具体例の集合は元と **同一のグラウンド集合** である。展開後のグラフを
座標で裂いて（書き先の軸座標 c と読みの軸座標 c' が定数で c ≠ c' なら
その辺は無い）成層が立つなら、それが s に沿った **整礎帰納の証明** になる
—— 依存距離が正なら軸を逐次に回してよい、という多面体の合法性と同じ主張。

辺は **読みごと** に引く（14s）: 同じ規則が同じ場を、同じ切り口では単調に（値）、小さい切り口では
非単調に（ガード）読むなら、同じ切り口の書き手からは単調な辺、小さい切り口の書き手からは非単調な辺が出る。
読みの向きを場ごとに一つにまとめると、同じ切り口の単調な読みまで非単調になり、切り口の中の単調な輪
（`a[n, j] <- b[n, j] + 1 … if b[n - 2, 0] >= 3` と `b[n, j] <- a[n, j - 1] + 2`）を病んだと言ってしまう
—— 14r まではそう言っていた。

処理系は棄却の前にこれを試みる。条件は三つで、どれも構造が言う:
目標の座標に **静的な区間の変数** が立っていること（書き先の時計）、
輪の中の読みの軸座標が展開後に **定数** へ畳めること、展開が 65,536
具体例に収まること。満たさなければ従来どおり棄却する（表の上の自己否定や、
座標が場の値で決まる否定読みは、今までと同じく病んでいる）。
検査は `test/axisstrat.py`。

### 供給 —— 表か、空間か

規則実例は表の行から来る。だが **座標の空間そのもの**からも来てよい:

```
for (i) in 2 .. n[]
```

端は値である。上端が育てば規則実例が **増える**。増えるだけなので単調で、
層を一つも消費しない。**答えが自分の宇宙を広げる。**
（下端は下へ、上端は上へ動かねばならない。逆向きなら処理系が非単調と判定する。）

下端の書き方には文法の約束がある。`in` の次が **名前で `..` が直に続かない** なら、それは
表の名である —— `for (j) in lo[0] .. 9` は表 `lo` と読まれて断られる。読みや式の下端は
**丸括弧で包む**（`for (j) in (lo[i]) .. hi[i]`、`for (j) in (i + 1) .. n`）。名前だけの下端
（`for (j) in i .. n`）と、数や記号で始まる式（`0 - 2 .. n`）は包まなくてよい。

von Neumann の普遍構成子は部品の一覧を持っていなかった。持っていたのは
周りの格子と、そこに何を書くかという記述だけである。三つの対象 V・D・C のうち、
座標 C だけが表に運ばれるしかなかったのを、これで直した。

区間の広さには上限がある（`RANGE_CAP`）。**座標は有限の資源**だからで、
超えたら黙って壊れずにそう言って止まる。

### 逐次深度が保証すること

> 逐次深度は **一つのセルが経験するバリアの最大回数**である。
> **大域バリアの回数は、依存グラフが分解する限りそれより少ない（ゼロにもなる）。**

深度は協調の回数を保証する。**実時間は保証しない。**

---

## 5. 値の構成と内容アドレス

```
constructor node(sym, left, right) bound 32
tree[i,k,A] <- node(A, tree[i,j,B], tree[j,k,C])  for ...
```

構成された値の名前は **内容そのもの**である（hash-consing; Ershov 1958）。
符号化は固定の乗数と法による多項式を **二本**折ったもので、**整数の算術だけ**で書ける。

```
M1 = 2^31 - 1     M2 = 2^31 - 99      K1 = 1000003   K2 = 1000033
各 (M,K) について:
  h ← 名前のバイトの折り込み
  h ← (h·K + 引数の数 + 1) mod M
  各引数 a:  h ← (h·K + enc(a)) mod M
    enc(整数 v) = 3(v mod M)   enc(文字列 s) = 3·折り込み(s) + 1   enc(真偽 b) = 3b + 2
番地 = h1 · M2 + h2                       （62 ビット）
```

- 同じ内容は必ず同じ名前になる。**別マシンで作っても同じ**
- だから構成は冪等で、レプリカ合流でも重複しない
- 「新しい」を「いつ作られたか」で定義しないので、**時間に依存しない**
- 掛け算が 31×31 ビットに収まるので **64 ビットで足りる**。128 ビット整数も
  ハッシュ関数も要らない —— つまり **Lattix 自身が計算できる**。
  以前は SHA-256 の先頭 56 ビットだった。正準ではあったが、それを計算できるのは
  SHA-256 を持つ実装だけで、処理系が自分の言語で書かれる道を塞いでいた。

`bound N` は構成の整礎測度で、これを超える深さの構成は停止しない可能性があるため拒否される。

### 変数の正準名 —— 束縛点そのもの

プログラムの正準形では、変数は綴りでも出現順でもなく **束縛点の座標** `b(表, 位置)`
で名づけられる。`for (i,j,w) in edges` の `j` は `b(edges,1)` である。

de Bruijn（1972）は束縛子を *数えて* 指標にした。数えるのは列に住んでいるからで、
**座標に住んでいる言語では数える手続きすら要らない。**
結果として、正準形に α変換の段が存在しない。
（同じ表を二度回す規則だけは束縛点で区別できないので、そこは出現順に戻る。）

---

## 6. 外界

```
source price : min from "market"      入力は「これまでに観測したもの」＝単調に育つ
emit "order" <- {id}  for ...         出力は集合の成長の *差分*
```

効果は「場の成長の差分を実体化すること」なので、再実行・順序変更・レプリカ合流の
どれでも重複しない。**exactly-once が構造から出る**（トランザクションも冪等キーも要らない）。

**io 署名**は「外界との*ブロッキング*往復回数」で、`budget io <= N` で契約にできる。

---

## 7. 出力 —— `render`

```
render out          # out[行, 桁] = 文字コード を座標順に並べて出す
```

文字列型は無い。**列は座標に住んでいて、時間には住んでいない。**
並びを決めるのは実行順序ではなく座標なので、出力は効果ではなく *描画* である。
入力側も同じで、文字列は `(位置, 文字コード)` の表として読む。

`render` は **バイト列**を書く（文字符号化を通さない）。だから描かれるものは
文章に限らない —— 実行形式でもよい。`examples/27_elf.lx` は最小の静的 ELF を
組み立て、その終了コードに最短経路長を置く。**実行形式が答えの実体化になる。**

---

## 8. 合成 —— コスト署名

```
component sssp(edges, src) -> dist : min  depth 1
  dist[src] <- 0
  dist[j]   <- dist[i] + w        for (i,j,w) in edges
end
```

`depth D` と `io K` は**契約**である。宣言と実際が食い違えばコンパイルエラーになる。
型が「形」を守るように、コスト署名は **「遅さ」を守る**。

---

## 9. 答えの証明書

答えは、自分が最小不動点であることを**局所的に**証明できる。

- **(A) 安定性** どの規則実例の寄与も答えに吸収済み ⟹ `lfp ⊑ S`（COMPLETE）
- **(B) 有基性** どのセルも*より小さい階数*のセルだけから導出できる ⟹ `S ⊑ lfp`（SOUND）

両方で `S = lfp`。どちらも**規則実例を1回なめるだけ**で、不動点計算は要らない。
階数（rank）は**信頼しないデータ**でよい —— 正当化が階数について狭義に減少せねばならず、
整数が整列しているので循環支持が組めないからである。

- **階数を比べる読みは、書き先と同じ強連結成分の場の読みだけでよい**（attest.py、2026-09-20）。
  帰納に要るのは「支えの読みが先に示したセルである」ことだけで、書き先に寄りかからない場
  （依存の成分が先の場）の升は、同じ層でも階数を問わずに先に示せる —— 帰納の順を
  （成分の位相順, 階数）の辞書順にとる。層で比べるより弱い要求で、健全さは同じ帰納で言える。
  焼き手の表から読む ir / attest.lx は前段の層で比べる（層は成分をまとめたものなので、より強い要求）。
- **在る升（⊥ でない升）の階数は 1 以上**。どの束でも（集約でも）、階数 0 の在る升は (B) の破れ。
- **階数は符号なしの 32 ビット**（証人の升は 4 バイト）。順序でしかないので、2^31 を越えても順序は保たれる。

### 影の店 —— ⊤ の証人

階数は帰納の半分である（「この座標は `a` **以上**」を下から示す）。だが
`flat` の ⊤ が閉路で押し上げられたとき、証人は最終の答えに残っていない ——
二つ目の確定値が ⊤ に吸収されて消えるからである。

平坦束のセルは**生涯にただ一つの確定値しか持てない**（`⊥ → a → ⊤`）。
だから「⊤ になる前の値」は曖昧さなく決まる。それを集めた **影の店** `S₂` は
最小不動点への途中経過で、有基性を見れば `S₂ ⊑ lfp` が言える。そして影の中では
**同じ座標に相異なる二つの確定値が届いている** —— それが ⊤ の証人である
（余帰納の側。「この座標は `a` より上でしかありえない」）。

影も証明書であり、検査器は処理系を信じない。壊れた影は証人にならない。

**⊤ の支えにも階数の規律が要る**（2026-09-19 に二つの偽物で測って直した）。

- **二つの確定値は、より小さい階数の寄与から数える。** flat の値からのガードと比較は単調
  （⊥ は偽、⊤ は真）なので閉路の中に置ける。`x <- 0`、`x <- w`、`w <- 7 if x` に x = ⊤、
  w = 7 と書いた証明書は、階数を問わずに数えると通った —— x の ⊤ を w の 7 が支え、w の 7 を
  「⊤ は真」が支える。最小不動点は x = 0、w = ⊥ である。
- **閉路でしか支えられない ⊤ は SOUND ではない。** 値が一つと、同じか大きい階数の ⊤ の寄与しか
  無い ⊤ は、最終の答えだけでは本物（`f <- f + 1` で 1 と 2 が届いた ⊤）と偽物（恒等
  `f <- f` で 1 しか来ないのに ⊤ と書いたもの）を見分けられない。前は「破れではない」として
  SOUND に数えていた。影で証人が出たものだけが示せる。
- **影は層ごとに組む。** 規則の層で書かれる flat の場だけを ⊤ の前の値に戻し、下の層は最終の
  答えを読む（非単調な読みは確定した下の層を見る —— 影で下の層まで前の値に戻すと、
  `if not z` が z の前の値 0 を見て立ち、存在しない寄与が証人になった）。影の寄与の階数は
  同じ層の読みを見る（影の値は ⊤ の前の階数、それ以外は最終の階数）。
- **⊤ の階数は、⊤ になった寄与の階数である**（前の確定値の階数ではない）。証人を書く側の約束で、
  C の実行時は前の値の階数のまま残していた（2026-09-19 に直した）。
- **⊤ の前の値は、⊤ でない値の支えにもなる。** `w <- 7 if x` の w は x が ⊤ になる前の値を
  読んで立った —— 最終の答えでは x は ⊤ で階数も w より大きい。影の寄与を同じ階数の規律で
  足してよい（影の値も階数で支えられているので、帰納は一つの整列の上で閉じる）。

**集約の寄与にも階数の規律が要る**（2026-09-20、偽物を数え上げて見つけた）。sum / count は「寄与の
和が答えと等しい」で (A)(B) を兼ねるが、集約が同じ層を単調に読む本（`c <- 1 if c`）では、答えが
寄与を立て、寄与が答えを作る輪が組める（c = 1 と書いた証明書が通った。最小不動点は ⊥）。寄与の
読みが小さい階数から来ない集約は示せない。焼いた本は集約の升の階数を 1 に置く（印が数えるので深さを
持たない）ので、規律に掛かるのは集約が自分の成分を読むとき（`c <- 1 if c`）だけである —— 焼き手の
前段は集約を、読む場より上の層に置く（自分を読むときを除く）。

### attest.lx —— 検査器を .lx で

`attest/` に **Lattix で書いた答えの検査器**がある（Python は組むときにしか現れない —— 組んだ
`attest/front.lx` と `attest/attest.lx` をそのまま置いてある）。焼き手と同じ `lattix` で二枚を焼く:

```bash
./lattix attest/front.lx attest-front         # 源 → 規則の表（焼き手の前段 + 表を語にして描く尻尾）
./lattix attest/attest.lx attest              # [表][証人][出した答え][入力] → 判定
./sssp edges.bin > answer.bin 3> witness.bin  # 出した答え（stdout）と証人（fd 3: 値の面 + 階数の面）
./attest-front examples/01_shortest.lx sssp.tab
cat sssp.tab witness.bin answer.bin edges.bin | ./attest
```

入力は一続きのバイト列 `[表][値の面][階数の面][出した答え][プログラムの入力]` で、表の頭が
場の数と形を言うので、面の長さも出した答えの長さ（場の面をそのまま出す本は値の面と同じ、
`render` の本は描く場の ⊥ でない升の数）も入力の始まりも決まる。**出した答えも確かめる** ——
使う人が見るのは stdout である。場の面を出す本は値の面と一バイトずつ、`render` の本は描く場の
升を座標順に描き直して比べる。attest は答えを計算し直さない —— 規則の実例を
一度なめて (A)(B) を見る。焼いた符号は一行も見ない。判定の見出しは **言えることだけを言う**:

| 見出し | 意味 |
|---|---|
| `ATTESTED` | 答えは源の最小不動点（(A)(B) が通った） |
| `REJECTED -- the answer is NOT the least fixed point` | (A) が破れた —— 答えは前不動点でさえない |
| `REJECTED -- not proven: a value is not grounded` | (B) が破れた —— 余計な値か、違う階数（最終の答えだけでは分からない） |
| `REJECTED -- a rule writes outside its field's bound` | (A)(B) は通ったが、火を噴く実例が広さの外へ書く —— 源に答えが無い |
| `REJECTED -- the output is not the answer that was checked` | 証人は通ったが、出した答えが証人の値と違う（何バイト目かを言う） |
| `UNSUPPORTED` | attest が持たない形。理由を一行ずつ言う |
| `NOT AN ATTEST INPUT` | 表の頭が無い、答えと階数の面が表の言う長さより短い（欠けた升を読めないまま判定しない）、または表の中に attest が持てない語（-2147483646 より下。前段はそんな語を出さない）がある |
| `THE SOURCE CANNOT BE BAKED` | 前段が源を断った（行と理由。答えの面は無くてよい） |

二行目は数を言う: 規則の実例の数、升の数、**プログラムの入力のバイト数**（何を確かめたかを言う ——
連結を間違えれば、別の入力についての判定になる）。

**UNSUPPORTED の理由**（attest の限界。どれも「検査したとは言わない」）: 読みの鎖が四段以上・
⊤ を座標や比較に使う、値が ±2^60 の外（attest は値を四倍して 64 ビットに置く —— 四倍した値は
flat の印 2147483646 / 2147483647 に決して当たらない。溢れる掛け算は、先にビットの長さを足して
見て作らない）、読みやガードが 16 を超える規則、実例が 131,072 を超える・区間が言語の上限より
広い・上端を外の変数で引く区間（13g）・下端が場の区間（13i）、閉路でしか支えられない flat の ⊤、⊤ の前の値が要る値（attest.lx は影を持たない）、階数の小さく
ない読みの寄与に支えられた集約。

**前段は焼き手と同じものを使う。** 前段の読み違いは attest の外にある（前段は読めた語を数え、
読めない語があれば表の頭に断りが載る）。記憶は約 2 GB 使う（実例 131,072 まで）。attest の入力は
焼いた実行ファイルの置き場（983,040 バイト）まで —— 越えれば終了コード 6。
測る: `python3 test/attest_lx.py`（表の意味の参照実装 `attest/ir.py` と判定を突き合わせる）。

### 支持は撤回の前線でもある

階数を書いたとき、処理系は**いまの値を支えている規則実例を一つ選んでいる**。
その実例が読んだセルと使った表の行を控えておくと、事実を消したときに
「消えた事実がこの値の *導出に* 使われたか」が直に引ける。

DRed は「読みうるものを全部消して作り直す」ので、密なグラフでは全体に届く。
支持を辿れば、支えが無事なセルには触らなくてよい —— そのセルの値が変わらないことは、
**導出が生きているという理由で**言える（削除後 `⊑` 削除前 なので等号）。

`記述は二度使われる`: 検査のために作った証人が、そのまま削除の前線になる。

### 答えに理由を尋ねる

証明書のために計算された証人は、そのまま **説明** になる。

```
python3 lattix.py f.lx --why 'dist[5]'
```

- **値がある** → その値に至る *最短の* 導出木（階数が最短を知っている）
- **⊤** → 食い違った二つの寄与を名指し、食い違いの源まで辿る。閉路なら閉路と言う
- **⊥** → 向かった規則実例のうち、**どのガードがなぜ偽だったか**（束縛を当てはめた形で）

三つ目が最も効く。「なぜ空なのか」はデバッグの第一の問いであり、
破壊的更新も print も無いこの言語では、他に訊く方法が無い。

### 領域ごとの完全性

セルの集合 `scope` について、次の三つが言えれば `S|scope = lfp|scope`:

- **(0) 祖先閉性** scope 内への寄与が読むセルは全て scope 内にある
- **(A) 安定性**（scope 内について）
- **(B) 有基性**

(0) が本質である。祖先がまだ育つなら、いま吸収されていることは最小不動点での吸収を意味しない。
**進捗は依存半順序の下方集合であり、下方集合の全体は分配束をなす**（Birkhoff 1937）。
だから別々に進んだ部分実行の証明書は、**和集合＝join で合成する**。

---

## 10. 実行の形（言語の意味ではない）

意味は上で尽きている。以下は処理系がその意味をどう実現するかで、**答えには影響しない**。

- **除算・剰余は単調でない。** 両辺は「値として使えない」扱いになり、層を消費する
- **スケジューラは層ではなく SCC ごとに選ばれる。** 層は協調の *回数* を決めるだけで、
  その中の順序までは決めていない。規則グラフの SCC を凝縮し、位相順に:
  - 再帰デルタが非負なら **ヒープ**（Dijkstra）。順序を決めるのは *値* なので、
    実行しないと分からない。だから優先度キューが要る
  - 再帰読みが **ただ一つの座標で一定符号のずれ**しか持たないなら **座標順の一掃**。
    順序を決めるのは *座標* で、規則の形だけから分かる。だからキューも再発火も要らず、
    一度なめれば最小不動点に達する（Fast Sweeping）。ずれは `f[i] <- g(f[i-1])` のように
    地の座標に沿っていても、`f[t[i]] <- g(f[t[i]-1])` のように **計算された座標**に
    沿っていてもよい —— 基点が同じ式なら同じ値だからである
  - どれも言えないなら FIFO ワークリスト（semi-naive）

  一掃はネイティブでは **融合** として出る: 規則ごとに表を回すのをやめ、
  **行を外側・規則を内側**にする。鎖を規則ごとに回すと掃く回数が入力に比例するが、
  行を外側にすれば一度で済む。
- **領域実行**: セル依存グラフの SCC を凝縮し、深さごとに走らせる。大域バリアは張らない
- **座標の再番号付け**: 座標が不透明（索引位置に裸でしか現れない）なら、
  依存構造から番号を与え直して局所性を作る。答えも仕事量も変わらない

---

## 11. 書けないもの

- 破壊的な状態変更（語彙が無い。書くべきでもない）
- 割り込み・リアルタイム制御・描画ループ（**逐次深度がそう教える**）
- 密な数値カーネル・SIMD（C の領域）
- 事実の削除（撤回）は **書ける** —— 支持は証明書が既に名指しているので、
  消すべきセルは探索ではなく参照で出る（`test/retract.py`）

---

## 12. 自分で焼ける断片 —— `lattix` が受け取る形

ここまでの 1〜11 が **言語**である。以下は、Lattix 自身で書かれた処理系
（`lattix.lx` を焼いた実行ファイル `lattix`）が受け取る **断片** の仕様である。
Python 版（`lattix.py`）はこれより広い言語を受け取るが、**配布物が受け取るのは
ここに書いた形だけ**であり、`lattix.lx` 自身もこの形で書かれている
（だから自分を焼ける ―― それが唯一の終了条件だった）。

```
lattix < prog.lx > prog     # 源のバイトを入れると静的 ELF が出る
chmod +x prog ; ./prog < data
```

### 文の種類

```
table ch = (0,32)                 # 表（口が一つ。実行時に stdin から入る）
field f : max bound 4096 8        # 場の宣言（束と、次元ごとの広さ）
f[0] <- 3                         # **種**（for の無い文）
f[i] <- g[i] + 1  for (i,c) in ch if c >= 97     # 規則
print f / render f
```

- **種は定数だけ**。場も座標も値も定数でなければならない
  （`f[0] <- g[0] + 1` は種ではなく規則である ―― `for (z) in 0 .. 0` を付ける）
- **広さは宣言する**（`bound`）。書かなければ 64。`bound 0` の場は升を持たない（書けば終了コード 6。
  前は ⊥ を敷く輪が一升書いて、場が置き場の末尾にあると SIGSEGV で落ちた）。次元は 3 まで（`bound 3 2 4`。書かない次元の広さは次元0 と同じ）
- **表は一つだけ**で、書いた行は *形* だけを言う（列の数。**15 列まで** —— 行の幅も列のずれも
  符号つきの一バイトで持つので、16 列の行の幅 128 は負に化けた。理由 8）。中身は焼き込まれず、
  実行ファイルが stdin から読む —— **二列なら生バイト**を `(位置, 文字)` に開き、
  **三列以上なら 8バイト小端の列**として読む。**下ろし**は源に書いた表を列ごとの flat の場と種にする
  （`_t<k>c<j>`）—— 負の升（`(1,-3)`）は `-3` の種になる（14h。前は字句の `-` が表の文ごと消え、
  **黙って正の数**になっていた）
- `render f` は一つだけ（無くてもよい。二つ目は理由 8 —— 前は番号の大きい方だけを黙って
  描いていた）。`print` は無視される（名前が宣言に無ければ理由 4）
- 焼き手が一度に扱える数は、**焼き手の源の宣言の `bound` が言う**（14w の時点で 場 2,047・文 16,384・項 16,384・
  ガードの行 16,384・種と規則はそれより手前・座標の所有者 32,768・命令 262,144）。越えれば焼き手自身が広さの検査で
  止まり、**どの箱かを名で言う**（終了コード 6、`field  176 rdo` のように焼き手の場の番号と名）。前は `bound` の写しを
  持つ見張りが五つあり、配列を広げた人に数えられずに四度取り残され、箱に入る本を「箱が足りない」（理由 0）と断った。
  写しの見張りは、正しいときは決して鳴らない —— 箱を越える書きは広さの検査が先に止める —— ので捨てた
- 束は **`min` `max` `or` `flat` `sum` `count` の六つ**。
  `set` / `and` / `fourv` / `bag` は焼けない（**宣言の行で**理由 8 と言う）。
  `fourv` は下ろしが **or の旗二つ**に下ろす（14b）—— Belnap の四値は知識の順序で
  2 × 2 の積の束（T の旗・F の旗。⊥ はどちらも無い、⊤ は両方）で、否定は旗を入れ替える
  だけだから層が要らない。場に次元を一つ足す: `x[e]` → `x[e, 0]`（T）/ `x[e, 1]`（F）。
  下ろす形は白い名簿 —— 書き先の値が `true` / `false` / fourv の読み / `not` + fourv の読み / `or` の場の読み、
  ガードが `if x[e]` / `if not x[e]`（後ろに比較も算術も続かない）、それと `print`。
  ほかの出現（座標・算術・比較・`render` …）が一つでもあれば、その場は `fourv` のまま残り、
  焼き手が宣言の行で理由 8 と言う
- 焼いた場の座標は 0 から始まる（負の座標に書けば終了コード 6）。**下ろしは負の数の座標を書く場の
  次元0 の原点をずらす**（14c）: `ln[-1] <- 1` のように書き先の次元0 に `-k` を書く場は、全部の出現の
  次元0 に k を足し（数一つなら数を書き換え、ほかは持ち上げて ` + k`）、広さも k 増やす。
  全部の出現をずらせない場（集合や fourv を print する・区間の端で読む・`[]` で使う・fourv）はずらさない
  （焼いた本が終了コード 6 で言う）。ずらしても render は同じバイトを出す（座標の順は変わらない）。
  **ずらした場も print できる**（14k）—— print の行はずらした座標で並び、字を書く機械が `座標 - k` を
  書く（負なら `-` を最上位の桁の左に）。動的計画法の表（`d[-1, j]` の縁を持つ編集距離）が印字できる。
  ただし **負の区間の計数器を座標に使う**本（`d[-1, j] <- … for (j) in -1 .. 3`）は、持ち上げた塊の
  場が負の番号を持てないのでまだずらさない（焼いた本が理由 8 で言う）。
  **表の列の負の値も座標になる**（14x）: 書き先の次元0 / 次元1 が表の変数一つで、その列に負の値があれば、
  列のいちばん小さい値の大きさだけずらす（`d[w] <- u for (u, w) in e` で w = -3）。
  **集合の要素も負になれる**（14x）: 要素の次元の原点を k ずらす（k は表の列の最小・区間の数の下端・`{-N}` の
  字面から出す）。print は要素を `座標 - k` で書き、定義と同じ **綴りの順**に並べる —— `-` はどの数字より
  小さいので負の数が先に来て、負の数どうしは大きさの綴りの順（`{-1, -10, -3, 2}`）。非負の数の鍵（十一進四桁）は
  最初の桁が 1 以上で 1331 から上に居るので、その下に負の数の鍵を置けば整列の鎖は一本のまま。k は 127 まで
  （焼き手の座標のずれは符号つき一バイト）、越える集合と、原子と負の数を両方要素にする集合は理由 8 で言う
- **アクセサ**（`c_f[e]` / `_size[e]`）は焼き手の場ではない（解釈実行は項の番地で引く場を作るが、
  番地は 56 ビットで座標にできない）。**下ろしが場所を比べて引く**（14d）: 構成子の出現の塊が
  場所 g ごとに番地を `_sad[g]` に写し、読みは持ち上げた塊
  `_sv[_ag, j] if _sad[_ag] == e for (_ag) in 0 .. _Sz[0] if _sk[_ag] == k` になる。比べるのは flat
  どうしなので単調で、項を作る再帰の中でも層が要らない。仕事は読み × 場所。`_size` は場所の大きさ
  （子の場所も番地を比べて引く —— 場所の数の二乗）。原子を渡す引数のアクセサ・十七文字以上の名前・
  書き先・区間の端は下ろさない（焼き手が理由 4 で言う）
- **`sum` / `count` の規則はループ一つまで**。「一度だけ数える印」を外側の表の行でしか
  引いていないので、二重のループでは内側の実例が同じ印を踏む —— 焼かずに理由 8 と言う
- 一つの場を **一次元と二次元の両方で**使えない（解釈実行は `q[i]` と `q[i,k]` を別の鍵に
  持つが、置き場は場ごとに次数が一つ）。理由 8
- 同じ名前を二度宣言できない（理由 A。解釈実行も断る）
- 種（`for` の無い矢印）の座標は数（式は不可）、値は数か `true` 一つか **`-` 数**（負の種。14h ——
  解釈実行は単項の `-` を `0 - 3` と読む。前は理由 8 で断り、下ろしが表の負の升を種にした日に当たった）。
  座標が広さの外なら理由 B。count の種は 1 と数える（寄与が一つある）。同じ升の種は束で join する（min の
  7 と 9 は 7、flat の 1 と 2 は ⊤、sum は足し、count は数える —— 前は後の種が上書きした）
- 数は 2147483647 まで（理由 C）。割る数・余りの数は二のべきなら 2^62 まで。64 ビットに
  収まらない数の字句はどこに書いても理由 C（前は焼き手そのものが桁を積む所で溢れ、
  終了コード 3 で止まって行を言えなかった）。**場が持てる値の幅は束が決める** —— min の場は
  2147483646 まで、max の場は -2147483646 から、flat の場は ⊥ / ⊤ の印（2147483647 /
  2147483646）を除く 64 ビット。焼いた側は「無い」「⊤」を値で見分けるので、印に届く値
  （min で 2147483647 以上、max で -2147483647 以下、flat で二つの印）は置けない ——
  **規則の値は join の手前で比べて止まり（終了コード 4）、種は焼く前に断る（理由 D）**。
  負の種は -2147483646 まで —— -2147483647 以下は **束に依らず語で見て**断る（理由 D。値にすると
  焼き手自身の max の場が印に届き、焼き手の見張りが行も理由も言えずに止まる）
  印に届かないと示せる値（印でない定数一つ、同じ束の場の素の写し一つ）は見張らない
- **値は 64 ビットに収まる所まで**。掛け算・群の足し引き・sum の join の直後に溢れを見て
  止まる（終了コード 3）。**途中の値も**収まらねばならない（`x * 2 / 4` の `x * 2` が
  溢れれば、答えが収まっても止まる）。解釈実行は多倍長で持つ
- 区間の上端は数か `f[数]` か `f[外の区間の変数]`（依存する区間。入るたびに上端を読み直す）。式の上端（`0 .. n[i] - 1`）は理由 8 —— 下ろしが `_g[i] <- n[i] - 1` に持ち上げて `f[外の変数]` の形にする。下端は数か `(f[数])` か `(f[外の区間の変数])` か **裸の外の区間の変数**（`for (j) in i .. n`。13i）。頭が下端 → 上端 → 広さの順に評価して下端の升に置き、尻尾が計数器をそこから作る（区間は符号つきで比べる）。式の下端は理由 8 —— 下ろしが持ち上げる（丸括弧の中身を `_g` にする。裸の式と表の変数は丸括弧つきの参照 `(_g[…])` にする）。依存する区間（上端が `f[外の変数]`・式）の文の値を塊に持ち上げるとき、塊の区間は **その変数が裸で居る書き先の次元の、宣言した広さ** で包む（14p。塊は式の値を鍵ごとに置くだけなので、広げた鍵は誰も読まない。宣言の無い場は持ち上げない）。場は三次元まで（四次元は理由 8）。三次元の升は `(c0 * 広さ1 + c1) * 広さ2 + c2` に畳む ——
  焼く側は座標を (所有者, 次元) の行で持っていたので、次元を一つ足すのは前段（出現の次元2）と
  証明書の場の行（広さ2。表の印は 20260923 に替えた）だけだった。（13i で証明書のループの行に
  下端の三語を足し、印は 20260924 になった）
  上端・下端の場が ⊥ なら区間は空（解釈実行と同じ。前は min / flat の ⊥ を数として読んで
  二十億回まわり、sum / count の ⊥ の 0 では一度回っていた）。**区間の広さは言語の上限
  （RANGE_CAP = 4,194,304）まで** —— 上端が数なら理由 F、場なら走るときに終了コード 10
- 場の置き場は **面ごとに** 2 GB の手前まで（14）。値の面 F は rbx から、階数の面 K は r10 から符号つき 32 ビットの
  変位で引き、即値で焼く番地（地上・印・生・詰め直し・日程）は F の頭より下に置く —— だから値と階数を合わせれば
  2 GB を越えてよい（前は合わせて 2 GB だった）。どれかが超えれば理由 E
- 値の `true` は 1 である（`x[i] <- true` の max は 1、`true + 1` は 2）。`or` の値は
  `true` か `or` の読み一つ —— 式にすると理由 9

### 頭（書き先）の座標

一段の添字を持つ **二段の読み**まで。次の五つのいずれか、± 定数のずれ（**-128 から +127 まで** ——
ずれは符号つきの一バイトで持つ。前は「255 まで」を通して、`c[i+200]` を c[i-56]、`c[i-200]` を c[i+56] と
読んで答えていた）:

```
f[i]        列（表のループの変数）        f[k]        計数器（区間のループの変数）
f[0]        定数                          f[g[i]]     列で引いた場
f[g[k]]     計数器で引いた場              f[g[i] + 2] 後置のずれ / f[g[i + 2]] 前置のずれ
```

読みの入れ子は段の数を問わない（`f[g[h[i]]]` も書ける —— 内側の読みが自分の座標を
組み立て終えた所で外側が読む。値の読みで八段、書き先で七段まで、ずれる鎖で測った）。
「定数で引いた場」（`f[g[0]]`）は書けない ―― 名前を一つ置く。
二次元の場も座標に置ける（`o[p[r,c]] <- v`）。内側の二次元の読みの次元1 は、畳む前に
**その次元の広さで濾す**（外れれば ⊥。前は次の行の升を読んでいた）。そこに置けるのは
列・計数器・定数（± ずれ）で、**そこにまた読みを置く形**（`h[g[i, k[i]]]`）は理由 8
（内側は畳みかけを r9 一本で持つので、次元1 の読みがそれを潰す）。
一つの添字に **前置と後置のずれを両方は持てない**（`k[k[i+1] - 1]` は理由 8）。

**座標の語はすべて数える**（理由 8）。読みの座標も同じ形で、次元ごとに「添字一つ（数・変数・
読み）と ± 数のずれ一つ」だけを読む。それ以外の語が括弧の中に一つでも残れば焼かない ——
`c[i*2]` / `c[2*i]` / `c[1+i]` / `c[i+i]` / `c[i-1+1]` / `c[i+b[0]]` / `c[b[i]*2]`。前は残りの語を
**黙って読み飛ばし**、`c[i*2]` を c[i]、`c[1+i]` を c[1] と読んで答えていた（2026-09-19 に直した）。

### 値 —— **項は群に分かれる**（掛け算・割り算・剰余が先、足し引きが後）

解釈実行と同じく `*` `/` `%` は `+` `-` より強く結ぶ（`1 + 2 * 3 = 7`）。焼いた符号は
項を **群**（`+` `-` で切れる掛け割りの列）ごとに作り、群の和を場の下の升に溜める。
以前は左から畳んでいて（`(1 + 2) * 3 = 9`）、食い違う形を終了コード 7 で断っていた ——
その断りはもう無い。

**先頭の `-` は最初の項に掛かる**（単項。解釈実行と同じく `*` `/` `%` より強い ——
`- i % 4` は (-i) % 4、`-i / 2` は (-i) / 2）。前は群の符号に置き場が無く、`-5` が 5、
`-b[0] + 10` が 13 になっていた。

**値の式の語はすべて数える**（理由 8）。語は項か、項の手前の演算子でなければならない。
丸括弧（`(0 - b[n])`）、演算子の重なり（`b - -1` / `b * -1`）、終わりの演算子、先頭の `-` 以外の
演算子、演算子の無い項の並び、値の無い規則は焼かない（中間値に名前を付ける）。前は知らない語を
**黙って読み飛ばし**、丸括弧の中身を 0、`b - -1` と `b * -1` を b - 1 にして答えていた。
項の種類:

```
定数 / 表の列 / 計数器 / 場の読み / − 定数 / × 定数 / ÷ 定数 / % 定数
場・列・計数器を引く / 掛ける / 割る / 余りを取る
```

割る・余りは **二のべきの定数のみ**（算術右シフトと and に落ちる。負でも床に落ちる）。
二のべきは **2^62 まで**書ける。2^32 以上の法は shl / shr の二命令に落とす（前は `and` の即値が
符号拡張で -1 になり、`c % 4294967296` が c のまま返った。2^37 以上は「二のべきでない」と
嘘の理由で断っていた）。
二のべきでない正の定数は idiv に落とす（床に寄せる）。0・負・即値に入らない定数は理由 5。
**割る数は場でも列でも計数器でもよい**（場は 14f の種15 / 16、列と計数器は 14j の種17〜20）。
解釈実行と同じく **床**（`//` と `%` —— 余りは割る数の符号に合わせる）で、**割る数が 0 なら ⊥**
（その実例は撃たない）。ただし ⊤ は ⊥ より強い —— flat の書き先で ⊤ を読んだ値を 0 で割れば ⊤。
割る数が -1 なら割らない（idiv は INT64_MIN / -1 で落ちる）: 商は符号を裏返し（INT64_MIN なら
溢れ —— 終了コード 3。解釈実行は 2^63 を持つ）、余りは 0。
前は除数が場でも列でも焼かなかった —— `/` の種が付かず、長く素の「場の読み」に落ちて
**足し算**になっていた（`97 / 98` が 195）のを、理由 8 で断ることで塞いでいた。
下ろした本では、表の列は場の読み（種15）、ガードの算術 `if n % d == 0` は持ち上げた
`_g[n, d] <- n % d`（種20）になる。引く・掛けるは場どうしでも書ける。
焼き手は丸括弧を焼かないが、下ろしが **丸括弧の中身** を塊に持ち上げ、括弧の字を消す: 値の式の丸括弧、
その中の丸括弧（入れ子。14r）、**ガードの辺の丸括弧**（`if (a[i] % 2) == 0`・`if (x % 3) + 1 == 2`。14r）、
構成子の引数の丸括弧（`cons((i + 1) % 3, 0)`。14r）、添字の中の丸括弧（`a[(i + 1) % 5]`。14r）。字を消すのは
**中身がちょうど一つの塊として持ち上がったとき** だけ（持ち上がらなければ括弧は残り、焼き手が理由 8 で言う）。
どのループも使わない中身（`(2)`・`(4 % 7)`）は、親の塊の中なら一点の塊 `_g[0]` にする（親の塊の鍵に
文のループ 0 が無くても読める）。
一つの文の項は 16 個まで（十七個目は焼かない —— 終了コード 7。掛ける数・割る数も一つの項）。
**塊の束は、塊の値がどう動くかで決める**（14u）。書き先へ単調に流れる値の中の塊（深さ 0 の値の丸括弧と、
その内の丸括弧・関数の引数）は **書き先の束**（min / max）にする —— 塊の値は書き先の束の向きに時間とともに
動く（最短路 `d[v] <- (d[u] + w)` の丸括弧は d が下がるたびに下がる）ので、flat の塊は二つ目の値で ⊤ になり、
焼き手は「flat を育つ場から書く」を非単調と読んで輪を断っていた。鍵ごとに実例は一つなので、書き先の束の塊は
最後の値を持つ。向きの合わない読み（`10 - (…)`）は焼き手の極性の解析が層を切る —— 束の選び方は健全さを変えず、
受け取れる本の広さだけを変える。ガードの辺・座標・区間の端・構成子の引数の塊は flat のまま。
**`min(a, b)` / `max(a, b)`**（14u）: 焼き手の項は関数を持たないが、min / max は **束の join そのもの**
である。下ろしは引数を塊に持ち上げ、`(` から `)` までを **関数の束の塊**にして、二つの引数を二つの実例として書く:
`_g[鍵] <- A * _md + B * _mc for (_mc) in 0 .. 1 for (_md) in 0 .. 1 if _mc != _md`。どちらの実例も両方の
引数を読むので、片方が ⊥ ならどちらも撃たない（定義と同じく ⊥ に厳しい）。掛けるのは計数器（0 と 1）なので
単調で、書き先と同じ束の関数は再帰の中に置ける（`d[v] <- min(d[u] + w, 20)`）。書き先と違う束の関数を再帰の
中に置く形（max の場の `min(a[u], 4)`）は焼き手が成層できないと言う —— 育つ二つの値の min は、どちらの束の
join でも追えない（比べて書けば追えるが、比較は非単調に数えられる）。定義は受け取る（控えめな側）。
**広さを書いた集合**（`field s : set bound W`、`s[k] <- {e}`）も下ろす（14u）: 宣言の `set bound W` を
`or bound W E`（E は要素の広さを推したもの）に書き換える。`s[]` に書いた広さ・広さを二つ書いた集合・次元0 を
負へずらす集合は下ろさない（理由 8）。

**向きの合わない読みは、層が一つ増えるだけである。** `min` の場を育っていく
`max` の場から作る、育っていく場を引き算の右に置く、`f[x] * g[y]`、`flat` や
`sum` の場に育つ値を入れる —— どれも書いてよい。焼く側も解釈実行と同じく
そこで層を切り、相手が完成してから読む（`test/strat.py` が二つの層を突き合わせる）。
以前はここに「書かない」と書いてあった —— 焼く側が向きを数えておらず、
**処理系ごとに違う答えが出た**からである（2026-09-19 に直した）。
層を切れないのは、その読みが **閉路の中**にあるときだけで、そのときは成層不能として断る。
**軸に沿った成層も焼ける**（14l〜14o。§4「層は座標である」の焼く側）。解釈実行は輪の中の否定を軸の具体例に
開いて成層する。焼く側は開かずに、**軸の群**を段0 で融合して n を昇る: 切り口 n を作るとき、群が読む
小さい切り口 n - c（c ≥ 1）は前の周回の中で閉じている。群は「段0 が数の区間・書き先の次元0 がその計数器
そのもの」の規則（軸の形）が、軸の形で読まれる場を `g[n - c, …]`（c ≥ 0）で読む繋がりの成分で、中に非単調な
読みがあるときだけ群にする（形だけ見れば `x[n] <- … for (n) in 0 .. 9` はみな軸の形をしている）。二つ以上の
場が軸をまたいで読み合ってよい（14o。グランディ数の mex・ライフゲームの近傍の数）。**同じ切り口 n を読む**
文は、読む場の書き手より後に回す —— 切り口の中の段（同じ切り口の読みに沿った最長路）で群の規則の番号を
並べ直すので、**源の順に依らない**。群の文が源の上で離れていてもよい（群は番号の上で一続きに並ぶ）。
内側にループがあってよい（14n）—— 生成器は群の **段0 だけ**を融合し、内側の段は規則ごとに回す
（`[段0 の頭][内の頭 本体 内の進め]…[段0 の進め]`）。群は **一度だけ回る**（14o）: 群が読む群の外の場
（入口）は群より前の層で閉じ（14m —— 単調な読みでも層を一つ上げる。同じ層で育つ場を読むと一周目に途中の
値を読み、非単調な読みがそれを見て撃った実例は取り消せない）、群の中の読みは周回の入口で見ない。だから
印を持たない二重ループの集約（`nb[t, x, y] <- 1`）も二度数えない。軸の形でない書き手（種・
`s[0, j] <- j for (j) …`）は、群の場を読まなければ **土台**として群より前の層で閉じる（14n）。端の違う規則
（`1 .. 15` と `2 .. 15`）は **下ろし**が群のいちばん広い区間に揃え、元の端をガードにする
（`for (n) in 1 .. 15 if n >= 2`）。下ろしが群の規則の式から持ち上げた塊（`(dir[t - 1] + 1) % 4` の
`_g[t] <- dir[t - 1] + 1`）も群に入るので、塊の段0 も同じ区間に揃える（14r。ラングトンの蟻）。生成器は群が融合しなかったら断る（理由 7 —— 前段だけが許して融合
しなければ、非単調な読みが途中の値を読んで黙って間違える。判断を二箇所に置かない）。**降りる軸**も焼ける
（14p）: 群の読みが全部 `g[n + c, …]`（c ≥ 0）なら段0 を上から下へ回す（後ろから解くゲーム・後ろ向きの動的
計画法）。**切り口の中の単調な輪**（同じ切り口の二つの場が互いを単調に読む。`r[n, j] <- r[n, j - 1] …`
と `q[n, j] <- r[n, j] …` が同じ n で回り合う）も焼ける（14q）: 生成器は段0 の一周（切り口一つ）の中身を、
その周回で何も動かなくなるまで **回し直す**（周回の頭で「動いた」印を退けて、尻で見て戻る。外の周回の印は
退けた升に置いて、切り口を出るときに返す）。切り口の中の輪に **非単調な読み** が混ざる群は群にしない
（回し直すと途中の値を読んだ実例が取り消せない）。回し直す群の中の印を持たない二重ループの集約は二度
数えるので断る（理由 8）。昇る読みと降りる読みが一つの群に混ざる形はまだ焼かない（理由 7）。
**計数器を掛けても向きは保たれる**（14f。`fact[n] <- fact[n-1] * n`）—— 解釈実行は掛ける数が
規則の束縛の上で 0 以上だと数えて示せれば単調と読む（束縛が 20,000 を越えると数えない）。
焼く側は区間の端だけで同じことを言う: 文のループが全部「数一つ .. 数一つ」の区間で、広さの積が
2^14 以下なら、下端が数（0 以上）の計数器は負にならない。表のループがあれば言わない
（**控えめな側にだけ倒す** —— 焼く側だけが単調と読めば、解釈実行が断る本に答えてしまう）。
上端が「数一つ」かは次の語で見るが、ファイルの最後の文には次の語が無いので **最後の語の番号**でも
言う（前は最後の文だけ負になりうると読まれ、成層できないと断った）。
比較の辺と添字の中の **`flat` の読みは単調** に数える（§4。前は比較と添字を一律に非単調と
数えていたので、flat の鎖を自分の再帰の中で座標に読む本は、解釈実行は解けるのに焼く側だけ
成層できなかった）。座標の読みは項の読みと同じく **階数を継ぎ**（cmp / cmovb の二命令）、
周回の入口でも読む場に数える（内側の所有者 `o[p[i, j]]` の `p` も）。

### ガード

```
if f[…]        if not f[…]        if 辺 演算子 辺
```

辺は **場の読み / 表の列 / 計数器 / 定数** のいずれか。辺に算術は書けない
（`if r + 1 == n[0]` は書けない ―― 一つ手前の場を置いて `if r == nz[0]` と書く）。
演算子は `== != >= <= > <`。節は 32 まで。

**ガードの語もすべて数える**（理由 8）。節は三つの形のどれか一つで、比較は節に一つ、比較でない
節の読みは一つ。`if not not x[n]`（前は `not x[n]` と読んで答えが裏返った）、`if b[n] > 2 3`、
`if b[n] = 2`、`if b[n] <> 2`、`if x[n] x[n]`、`if b[n] > 1 > 0` は焼かない。

### ループ

```
for (i,c) in ch          表（**0 番目に置く**。段 1 から先の表のループは理由 8）
for (k) in 0 .. 7        区間（上端が定数。下端は負の数も書ける —— `-3 .. 2`。14k）
for (k) in 0 .. n[0]     区間（上端が場の読み。**上限は数えて言う**）
```

**下端は負の数でもよい**（14k。`for (j) in -3 .. 2` —— 前は `-` を「括弧の外の演算子」と数えて理由 8。
区間は符号つきで比べるので、焼く側は下端を負の即値にするだけである。ただし下端の既定の 0 が max の場に
あり、負の下端が 0 に負けて **黙って 0 から回った** —— 既定は下端が数でないループにだけ置く）。
上端の負の数（`-3 .. -1`）はまだ書けない（理由 8）。
**表のループは段 0 だけ**（14j）。表の行は rcx に置き、列は `[rcx+d8]` で引くので、段 1 から先に
表のループがあると内側が外の計数器を潰す。前は「一番外側に置く」と書き手に頼むだけで見張りが無く、
同じ表を二重に回す本（`for (i,x) in a for (j,y) in a` —— 順位）は **何も書かずに終了コード 0**、
表を内側に書いた本は「広さを超えた」と嘘の理由で止まっていた。下ろした本は表を場にするので焼ける。
入れ子は **8 まで**（計数器は rcx / r13 / r14 の三本と、四本目からは場の下の升）。層は 128 まで。
九重目は焼かない（終了コード 7）—— 前段が for 節を八つまで持つ。
一つの文が束ねる変数（表の列と区間の変数を合わせて）は **10 まで**。十一個目を名指すと焼かない（理由 8）。

**for 節の語もすべて数える**（理由 8）。`for ( 変数, … ) in 表の名`（**束ねる数は表の列の数** ——
違うと解釈実行は断り、焼いた側は束ねた数を行の幅にして行を読み違えていた）か `for ( 変数 ) in 数 .. 上端`
だけで、上端は数か `f[数]` か `f[外の区間の変数]`（添字にずれも二つ目の次元も置けない。表の変数も置けない —— 表の変数は列の値で計数器ではない）、下端は数か `(f[数])` か `(f[外の区間の変数])` か裸の外の区間の変数（`lo[0] ..` は解釈実行が表の名と読む形 —— 焼かない）、表の名は宣言した表の名、
括弧の中は「名前 , 名前 …」。`for (n) in 0 .. 3 4`、`for (n, m) in 0 .. 3`、`in (ch)`、無い表の名、
名の無い `in`、`for` の無い `in` は焼かない。**頭**（矢印の手前）は書き先の出現だけ、**種**の値は
語一つ。前はどれも残りの語を黙って読み飛ばし、解釈実行が構文で断る源に答えていた。

### 守るべき約束

- **⊥ を値として持たない。** 焼いた機械語は「無い」を *値* で見分ける
  （`max` の ⊥ は -2147483647）。⊥ と同じ値を場に置くと、書いたのに「無い」と
  読まれる。⊥ の定数そのものを扱う場（生成器の `fbot`）は、
  2^32 だけ持ち上げて持つ ―― 256 で割った余りも商の余りも 2^32 は素通りする。
  書く側が守れなかったときは、焼いた符号が join の手前で見張って言う（終了コード 4）
- **空の区間は一度も回らない**（頭で一度比べる）。`0 .. n[0]` で `n[0]` が
  負でも、上端が ⊥ の場でも、本体は実行されない —— 解釈実行と同じである

### 断片で書けないもの

`constructor` / `component` / `use` / `include` / `source` / `emit` /
文字列原子 / 集合リテラル / `is` / 四次元以上の場 / 表を二つ以上。
これらは Python 版では動く —— **断片が狭いのであって、言語が狭いのではない**。
（多くは下ろし（lower）が断片の形に書き換える。`component` / `use` はまだ下ろさない。）

**`include` は焼いた本の口が開く**（14y）。取り込みは実行の中の外界の観測ではなく、構文解析の前に源の字を
差し替えることである（定義の `_expand_includes`）。焼いた下ろし・焼き手がファイルに触れる扉は口（前置き）だけ
なので、**`_include` という場を宣言した本**（`_exit` と同じく、名が `_` で始まる場は焼く道のもの）の生バイトの口が、
読んだ字の `include` の行をファイルの中身に替えてから表に開く。焼き手・下ろし・attest の前段（33_self.lx）が
宣言している。データを読む普通の本の入力は変えない。規則は定義と同じ: 行の頭の空白を飛ばして `include ` で
始まる行（後ろが空白だけなら取り込みではない）、パスは残りの空白と両端の `"` を落としたもの。探す順は
**基点/パス → パス → examples/パス → lib/パス**（基点は取り込んだ本の居場所。いちばん外は今の場所 —— 定義の
「lattix.py の居場所」を、焼いた本は **今の場所** と読む。リポジトリの根で焼けば同じファイルを開く）。行は
`# --- included from パス ---` と中身に替わり（中身の最後の改行は一つ落として行で繋ぐ）、字は定義とバイトまで
同じになる（`\r` を落とさないことだけ違う）。入れ子は 32 段まで。開けない・読めない・深すぎる・パスが
2,000 バイトを越える取り込みは標準エラーに言って **終了コード 8**、置き場を越えれば 6。
この口のおかげで **`lattix.lx` は三行の include**（`examples/33_self.lx`・`lib/fold.lx`・`lib/gen.lx`）になり、
`./lattix lattix.lx lattix` だけで焼き手が自分を焼き直す（前は Python が三つを繋いで lattix.lx を書いていた）。

**`budget depth <= N` は焼き手が数える**（14t）。数えるのは利用者の場を書く文の層の数で、下ろしの機械（名が
`_` で始まる場）は数えない。焼き手の層は定義の層より浅くならない（非単調な読みは真に後ろ、単調な読みは同じか
後ろ）ので、焼き手が N 以下なら定義も N 以下。越えたら budget の行で言う（理由 7）—— 定義が受け取っても、焼き手の
層が深ければ断る（控えめな側。下ろしの機械が層を足す本、たとえば構成子の場所の記録を読む本ではそうなる）。
`budget io` と `budget top` は下ろしが扱う: io は source の宣言が無ければ 0 なので文を消し、あれば残して断る。
top は flat の場ごとに ⊤ の升を数える規則と、越えたら **終了コードの升 `_exit`**（sum、一升）に 4 を置く規則に
なる。焼いた本は答えを書いたあと `_exit[0]` の値で終わる（無ければ 0）—— 定義と同じく、答えを出してから 4。
名が `_` で始まる場は下ろしのものとして予約する（`_exit` は終了コードの升）。

**知らない文は焼かずに言う**（理由 8）。断片が知っている文の頭は四つ ——
`table` / `field` / `render` / `print` —— と **矢印のある文**（種と規則）だけで、
それ以外の行に当たったらそこで止まる。前は黙って読み飛ばしていて、
焼けた実行ファイルが segfault するか（`constructor` を含む `.lx`）、
**構成子を無視した別の答え**を出していた。

### 黙って間違えない —— 終了コード

断片の外の書き方は **焼けた実行ファイルが終了コードで言う**（不変条件8）。
`lattix` 自身は常に ELF を出す（形が導けたかは、その ELF が起動時に言う）:

| コード | 意味 |
|---|---|
| 2 | **描く値がバイトでない**（`render f` の升の値が 0..255 の外）。前は下位の八ビットだけを書いていた（300 が 44 に、-1 が 255 に）。解釈実行は 256 以上を文字として持ち、負は飛ばす —— どちらもバイトの口では同じにできないので、止まって言う（場の番号つき） |
| 3 | **値が 64 ビットに収まらない**（掛け算・群の足し引き・sum の join で溢れた。途中の値も含む）。解釈実行は多倍長で持つ。溢れた所で止まって言う（場の番号つき） |
| 4 | **値が印に届いた**（規則の値が min で 2147483647 以上、max で -2147483647 以下、flat で ⊥ の印 2147483647 か ⊤ の印 2147483646）。焼いた符号は「無い」と「⊤」を値で見分け、min の join は ⊥ 以上を、max の join は ⊥ 以下を捨てるので、その値を置けば答えが変わる（前は印を越えた値が黙って消えていた）。join の手前で比べ、届けば止まって言う（場の番号つき）。印に届かないと示せる値 —— 印でない定数一つ、同じ束の場の素の写し一つ（写し元の升も同じ見張りを通っている）—— は見張らない。重なった値が後で join に捨てられる場合（min にもっと小さい値が後から来る）でも止まる —— 来るかどうかは、その時点では分からない |
| 5 | **⊤ を置けない所に ⊤ が来た**。焼いた符号は ⊤ の規律を持つ —— 比較は ⊤ で立つ、flat へ書く算術は ⊤ 厳密、素のガードと flat への素の写しは ⊤ を ⊤ として運ぶ。止まって言うのは二つ（場の番号つき）: ⊤ を **数の場へ読む**（min / max / sum / count の書き先の値に flat の ⊤ が入る —— 読む前に止まる）、⊤ に達した場が **座標・区間の上端・描画**で読まれる（⊤ は座標になれない。⊤ に達した時に止まる） |
| 6 | **宣言した広さを超えた**（座標が `bound` の外に出た。場の番号つき）か、**入力が置き場より大きい**（生バイトなら 983,040 バイト。場の番号は -1。前は黙って切っていた） |
| 7 | **焼けない形があった** —— 項・ガード・座標・ループ・規則・種のどれかが導けない、
宣言の無い場を読んだ、表が二つ以上、あるいは `.lx` ですらない。
上に書いた形から外れたときはここに来る |
| 8 | **入力が読めない** —— `argv[1]` が開けない（黙って stdin に落ちない）、read が失敗した（ディレクトリなど。前は空の入力として答えていた）、語の列が行の途中で切れている（前は 0 で埋めた一行として読んでいた）、口が開く取り込みが開けない・深すぎる・パスが長すぎる（`lattix: cannot open include <パス>` と言う。14y） |
| 9 | **出す先が開けない・書けない** —— `argv[2]` が開けない（黙って stdout に落ちない）、答えの書き込みが失敗した（`> /dev/full` など。前は書けないまま終了コード 0 だった）。証人（fd 3）は頼まれていなければ閉じているので EBADF だけは諦め、ほかの失敗は 9 |
| 10 | **区間が言語の上限より広い**（端が場の区間で、上端 - 下端 が RANGE_CAP = 4,194,304 を超えた）。解釈実行は「座標は有限の資源」と言って断る —— 前は焼いた側だけが回し切って答えていた。入るたびに比べて止まる（上端の場の番号つき） |
| 11 | **周回が上限を越えた**（RUN_CAP = 16,777,216 周回。昇鎖が終わらない本 —— `f[z] <- f[z] + 1` —— には最小不動点が無い）。解釈実行は変化の回数の見張り（400 万回）で断る。周回ごとに少なくとも一升は動くので、周回が上限を越える本は解釈実行も断る。前は焼いた側が回り続けた —— 答えないが、断りもしなかった（周回の番号つき） |

（`if r + 1 == n[0]` のような式の辺、段 1 から先の表のループ、
表が二つ、**17 文字以上の名前**、**成層できない源**（`not` などの非単調な読みが閉路の中にある） ——
どれも 7 で止まる。答えを出してから間違えるより、出さずに言う方がよい。
`test/accept.py` がこの約束を試験している。）

**終了コード 7 は、どこを直すかまで言う。** 焼けなかった実行ファイルは
標準エラーに一行だけ出してから止まる:

```
lattix: cannot bake - line 000003 reason 5: bad divisor
```

| 理由 | 言葉 | 意味 |
|---|---|---|
| 0 | `box too small` | 焼き手の箱（場 2,047・座標の所有者 32,768・規則・種 8,192・命令 262,144・詰め直しの置き場）が足りない。寸法の話なので行は 000000（14j まで言葉が無く、空白が出ていた） |
| 1 | `name too long` | 名前が 17 文字以上 |
| 2 | `nesting too deep` | `for` の入れ子が九重以上 |
| 3 | `too many terms` | 一つの値に項が九つ以上 |
| 4 | `no such field` | 宣言の無い場を読んだ |
| 5 | `bad divisor` | 割る数・余りの数の定数が 0・負・即値に入らない（09-23 まで「二のべきでない除数」—— 二のべきでない正の定数はもう焼ける） |
| 6 | `need one table` | 表が無い、または二つ以上（`.lx` でないときもここ） |
| 7 | `unstratifiable` | 成層できない —— 非単調な読みが閉路の中にある（解釈実行も断る） |
| 8 | `unsupported form` | 断片の外の形（式の辺・段 1 から先の表のループ・比較の節の `not`・定数で引いた書き座標・集約の二重ループ・次元の混在・焼けない束・頭の無い矢印・閉じない括弧など） |
| 9 | `or takes true` | `or` の場に `true` 以外を書いた（`or` の場の読みは可。式は不可） |
| A | `field redeclared` | 同じ名前の場を二度宣言した（解釈実行も断る） |
| B | `bound too small` | 種の座標が宣言の広さの外（種は走るときに濾さないので、焼くときに言う） |
| C | `number too big` | 数が符号つき 32 ビットの即値に収まらない（割る数・余りの数は二のべきなので 2^62 まで）。64 ビットに収まらない数の字句 |
| D | `reserved value` | 種の値が ⊥ / ⊤ の印に等しい（min・flat の 2147483647、max の -2147483647、flat の 2147483646）。負の種は束に依らず -2147483647 以下を断る。規則の値なら走るときに終了コード 4 で言う |
| E | `fields too big` | 値の面か階数の面のどちらかが 2 GB を超える（面ごとに rbx / r10 からの符号つき 32 ビットの変位で引くので、超えると番地が折り返して別の場か地図の外を指した）。即値で焼く小さい置き場が F の頭まで 2 GB を超えるときも（行は 000000） |
| F | `range too wide` | 数の区間の広さ（上端 - 下端）が言語の上限 RANGE_CAP = 4,194,304 を超える（解釈実行も断る） |

行番号は源の行（1 から数える）。構造そのものが立たないとき（理由 6）は
指す行が無いので `000000` になる。**番号は引くもの、言葉は読むもの** ——
番号だけでは表を引きに行かねばならない。人も、AI も。

**空白は語の区切りでしかない**（角括弧のまわりでも）。`f[ i ]`・`f [i]`・`q[0, 1] <- 3`・
`n[ 0 ]` は `f[i]`・`q[0,1] <- 3`・`n[0]` と同じ本である（解釈実行と同じ）。前段は出現の `[` と
添字を「名前の一つ後ろ・二つ後ろ」と数で引いていたので、角括弧の内側の空白を理由 8、名前と `[` の
間の空白を理由 4（宣言の無い場 —— 嘘の理由）で断っていた（2026-09-20 に直した。`test/spaces.py`）。
`<-` と二文字の比較（`>=` `==` `!=` `<=`）は続けて書く（解釈実行もそう読む）。

**名前は 16 文字まで。** 綴りの照合は名前を整数の鍵に畳んで行う
（8 文字 × 7 ビット = 56 ビットが一本。9〜16 文字目は二本目に積む）。
17 文字以上は鍵が衝突して黙って間違うので、焼かない。

**源は 589,824 バイトまで**（焼き手の文字の面の広さ）。源は (位置, 文字) の表に
開かれるので、一バイトにつき 16 バイト要る。超えたら焼き手が終了コード 6 で止まる
—— 黙って切らない。

### 出るもの

### 口 —— 引数と、三つの fd

焼けた実行ファイルは **`プログラム [源] [出す先]`** で呼ぶ。

| 口 | 既定 | 引数で指すと |
|---|---|---|
| 読む | 標準入力 | `argv[1]` を開いて読む（開けなければ・読めなければ **終了コード 8**。置き場より大きい入力は **6**、語の列が行の途中で切れていれば **8** —— 黙って切らない・埋めない） |
| 書く | 標準出力 | `argv[2]` を **0755 で作って**書く（開けなければ **9**） |
| 証人（値の面と階数の面） | **出さない** | **fd 3** が開いていればそこへ書く（`3>witness.bin`）。**値の面（升ごと 8 バイト、or は 1 バイト）に続けて階数の面（升ごと 4 バイト、符号なし）** —— 答えを描く本でも、証人だけで答えの値が揃う（前は階数の面だけで、render の本の答えは確かめられなかった）。階数は 0xFFFFFFFF まで順序を保ち、その次は 0 に戻る（在る升の階数 0 は「導出を与えていない」—— attest は示せないと言う。前は符号つきで溜め、2^31 を越えた階数を読む側が負と見て **小さい階数を黙って書いた**） |
| 知らせ | 標準エラー | 止まった理由の一行（終了コード 2・3・4・5・6・7・10・11 のとき）。2・3・4・5・6・10 は **どの場かをもう一行で言う**（`  field   12 dist` —— 場の番号と宣言の名。入力の表なら `  the input table`。14w。前は場の番号を四バイトの小端で続けていて、人には読めなかった）。11 は周回の番号を四バイトの小端で続ける |

`lattix` 自身も焼けた実行ファイルなので同じで、`./lattix f.lx foo` と
`./lattix < f.lx > foo` は同じバイトを出す（後者は `chmod +x` が要る）。
口の番号は rbp に置く —— **前置きの十五命令と、書き出しの十七命令**が
生成される符号すべてに入る。

証人が既定で出ないのは 不変条件6 の読み替えである ——
**「階数は出せる」であって「常に出す」ではない**。fd 3 が開いていなければ
`write` は EBADF を返し、書き出しの「書けないなら諦める」がそのまま受け止める。

### 出るもの

1,048,576 バイト（1 MB）の静的 ELF（`not a dynamic executable`）。`render` の詰め直しの置き場は
描く場の升数に合わせて取る（前は 1 MB 固定で、それより升の多い場を描くと segfault した）。符号は像の末尾
1024 バイトの手前まで —— 末尾は知らせの置き場で、符号がそこへ届く源は焼き手が
終了コード 6 で断る（前は 640 KB で、届いたかを誰も見ていなかった）。
`render f` があれば **f を座標順にバイトで**（⊥ の升は
飛ばす —— 解釈実行の `render` と同じ意味。値は 0..255 で、外なら終了コード 2）、無ければ場の面をそのまま
（升ごとに 8バイト小端。⊥ は束の ⊥ の値がそのまま出る）。
